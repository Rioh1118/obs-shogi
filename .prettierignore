# 整形しない生成物。
#
# `vp staged`（`.vite-hooks/pre-commit`）が staged の中身を整形するので、
# ここに書かないと **コミットの瞬間に表の桁が揃えられる**。
# 揃った版が入ると、生成器が出す版と一致しなくなり、
# `npm run ratchet:notices` がその後ずっと落ちる。
#
# 揃えない側を採るのは、差分の読みやすさでもある —— 桁を揃えると、
# 依存を1つ足しただけで列幅が動き、触っていない数百行が差分に出る。
THIRD-PARTY-NOTICES.md

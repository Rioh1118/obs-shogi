# 機械化バックログ

`.claude/reviews/` の275本（2026-08-29〜09-07）から、reviewer が
「## lint / hook で強制できるもの」に書いた提案だけを抜いて畳んだもの。
**原文は git 履歴にある**（`git log --diff-filter=D -- .claude/reviews/`）。

**回数は「別々のラウンドで独立に提案された数」。** 入れなかったから同じ所見が再出現した、
という因果が報告書に明記されているものだけを載せる。

## このループが実際にどうなったか

最長は `book-to-main` の**46ラウンド**、次いで `game-engine` 35、`skc` 24。

- **r7〜r19 は13ラウンド連続で「このラウンドで新しく挙がったものは無い」と書かれている。**
  それでも r46 まで続いた
- **r10 以降、所見の多数派は「報告書自身の記述の食い違い」に移った**（名乗る件数・sha・
  深刻度の内訳・採番の綴り）。**コードに批評する材料が尽きて、自分の成果物を批評し始めた**。
  報告書はこの時点で見出しの書式が4通りに割れていて、機械で数え直すこともできなかった
- 機械化は毎回 `#351` / `#386` / `#342` / `#290` へ「送済み」にされ、
  **「案A の方針でこのラウンドでは足さない」が約25ラウンド繰り返された**
- **変異検査を手で回していた**（29通り、次に59通り、それぞれ1件発見）。
  これは `cargo-mutants`（#332、17ラウンド見送り）が自動でやること

### 機械化すると実際に収束した例（唯一の反例）

`410-cap-sfen-spelling` の「doc が指した先が実在するか」は r1〜r7 で毎回出たが、
走査を入れ切った r8 で**0件になった**。r7 の申告:

> doc の腐りは7ラウンド連続で出ているが、**機械化できる形はもう残っていない。**
> パス・識別子・CSS クラスの実在はすべて0件になり、残っているのは「書いてあることが本当か」だけ

**所見の類型は、機械にすれば消える。個別に直しても消えない。**

## 入れた / 既に入っていた（2026-09-08 に棚卸し）

**表の項目は毎回「まだ有効か」を確かめてから取る。** 実際に3件が既に解決済みだった。

| 何を                                                                        | 状態                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 未使用 export の検出（9ラウンド見送り）                                     | **入れた。** `scripts/knip-ratchet.sh` を `npm run verify` へ。0にはできない——`entities/game-session/**` は未マージのエンジン作業が使う薄皮。**基準は数え方で跳ねる**（理由はそのスクリプトの冒頭）                                                                                                                                                                                                                                                     |
| `docs/**/*.md` を verify-gate の対象に（9ラウンド見送り、#251）             | **既に入っていた。** `gate_kinds_for_path` の `docs/*.md\|CONTRIBUTING.md) kinds="$kinds ts"`。`case` の `*` は `/` にも当たるので深さを問わず拾う                                                                                                                                                                                                                                                                                                      |
| doc の識別子の実在検査（5ラウンド見送り）                                   | **既に入っていた。** `scripts/rustdoc-ratchet.sh`（非公開項目も見る）が `verify:rust` に居る                                                                                                                                                                                                                                                                                                                                                            |
| `scripts/*.sh` がゲートのどの分類にも当たらない                             | **今回塞いだ。** ラチェットの基準を緩めるコミットだけが検査を1本も通らない状態だった                                                                                                                                                                                                                                                                                                                                                                    |
| `docsSourcePaths` を `docs/**` へ広げる（4ラウンド見送り）                  | **入れた**（#436）。`spec/screens/` と `decisions/` を足した。`spec/features/` は**予告**を書く場所なので入れない。広げるために腐り3件を直し、他リポジトリのパス2件を外部リンクにした                                                                                                                                                                                                                                                                   |
| SCSS が未定義のトークンを参照すること                                       | **入れた**（#436）。`scssScaleRatchet` に `index.$name` の実在検査。`verify` は SCSS をコンパイルしないので、これが無いと `vite build` まで気づけない                                                                                                                                                                                                                                                                                                   |
| doc の表と現物の突き合わせ（境界の名簿）                                    | **入れた**（#436）。`errorBoundaryInventory`。表の「名乗り」列と `BOUNDARY_LABELS`、鍵の使用回数、ボタンの綴りの写し                                                                                                                                                                                                                                                                                                                                    |
| **JSX を正規表現で読む走査**                                                | **やめた**（#436）。`label="..."` を非貪欲で拾う形は `label={X}` を読み飛ばして次の境界の名乗りを拾う。**定数を読む形へ替えた** —— 綴りの実在は型が、対応は走査が見る                                                                                                                                                                                                                                                                                   |
| 選択を動かす `node_selected` を1箇所へ寄せる（#223 で2箇所漏れた）          | **入れた。** `src/__tests__/kifuOpenTarget.test.ts`。変異3通り（別ファイルから撃つ／provider 内で2箇所／走査対象を空にする）で落ちることを確かめた                                                                                                                                                                                                                                                                                                      |
| 画面へ出る段を組む口が1つに閉じているか（3ラウンド見送り）                  | **入れた。** `src-tauri/tests/state_is_announced_once.rs`。旗・場所の警告・構造体リテラル・別名 `use`・裸の `String` の `Err` を見る。**主張を doc に書いて3回外した**あと走査へ移した                                                                                                                                                                                                                                                                  |
| 画面の文言が仕様書に載っているか                                            | **入れた。** `src/__tests__/screenSpecCoverage.test.ts`。`IndexHealth` で分岐する画面が一覧から漏れることも見る。**見るのは文言そのもの**——`docsSourcePaths` はパス、`ownedIdentifiers` は綴りしか見ない                                                                                                                                                                                                                                                |
| 表と Rust の module doc で同じ節が片方だけ直る（2ラウンド見送り）           | **入れた**（#354）。`src/__tests__/responsibilitySplit.test.ts`。「責任の切れ目」が名指すファイル名の集合を突き合わせる。**文章の言い回しは見ない**——縛ると同じことを2通りに書けなくなる                                                                                                                                                                                                                                                                |
| 同層の横断 import（`entities/A` → `entities/B`）。2件目                     | **入れた**（#113）。`src/__tests__/crossSliceImports.test.ts`。13組を控えに置いて `toEqual`。**束縛を持たない `import "@/…"` も拾う**——最初の版が `from` だけを見ていて、変異を当てたらその形で素通りした                                                                                                                                                                                                                                               |
| 表を指す散文が、その表の行数と合っているか。4件目                           | **入れた**（#113）。`stateTransitionIndex` の `countClaimsIn`。`N状態` / `M事象` を拾って表の行数と比べる。数を書かない自由はあるので、書いたときだけ守らせる。入れた時点で1件捕まえた                                                                                                                                                                                                                                                                  |
| 在庫表の摘要が、表本体の「埋まっていないセル」節と食い違う。2件目           | **入れた**（#113）。`stateTransitionIndex` の `tablesClaimedUnverified` / `unfilledCellCount`。**箇条書きと表の両方を数える**——片方だけだと、書き方の違いを「1件も挙げていない」と読み違える                                                                                                                                                                                                                                                            |
| **TS のコメント**が名指す識別子の実在（6ラウンド見送り）                    | **入れた**（#567）。`src/__tests__/srcCommentIdentifiers.test.ts`。判定は `docsIdentifiers` から借り、決めるのは走査範囲だけ。**corpus はテストを外す**ので、テストにしか在る綴りをコメントが指すと赤くなる（#561 で1件捕まえた —— barrel のコメントが `sliceBarrels` を裸で指していた。パスで書き直した）                                                                                                                                              |
| `vi.mock` のファクトリが実モジュールの型と合っていない（2ラウンド）         | **入れた。** `src/__tests__/viMockShape.test.ts`。`satisfies typeof import("…")` を要求する件数ラチェット。**`-D` にはできない**（入れた時点で既存が多数）。入れた回に1件捕まえた —— 対局の差し替えが実物9本のうち6本しか並べていなかった。変異（印を1つ外す／走査を空にする）で落ちることを確かめた                                                                                                                                                    |
| スクロールする箱の `safe` なし中央寄せ（2ラウンド。散文で2回書かれていた）  | **入れた。** `src/__tests__/safeCentering.test.ts`。SCSS の入れ子は**同じ箱を指す修飾子だけ**親の宣言を連れる（子孫まで連れると、行を縦中央に寄せているだけの宣言まで数えて誤検知が本体より大きくなる）。**全件が実害とは限らない**ので件数ラチェット。変異2件で落ちることを確かめた                                                                                                                                                                    |
| 毎手の判定が根から組み直す形に戻ること                                      | **入れた。** `outcomeJudgeCost.test.ts` と `rulingAdapter.test.ts`。**壁時計ではなく `ShogiRecord.prototype.append` の回数**で見る —— 既存の「400手で20秒以内」は実測 321ms で60倍の余裕があり、2乗のまま1000手まで伸ばしても緑だった。**呼ぶ側にも置く**（判定器の側だけだと、呼び手が組み直す関数へ戻る変更を止められない）                                                                                                                           |
| 画面仕様の節が増えて「未実装」の線が引けなくなる                            | **入れた。** `src/__tests__/specSectionOrder.test.ts`。**端だけを見る**（`## 目的` で始まり `## いま満たしていないこと` → `## これからの要件` で終わる）——真ん中を縛ると書ける形が1つしか無くなって嘘の節が生える。入れる前に数えたら **15枚中6枚が既に規約から外れていた**ので、規約の側を実態へ書き直した                                                                                                                                             |
| `EnginePreset` → `EngineRuntimeConfig` の組み立てが2箇所                    | **入れた。** `src/entities/engine-presets/lib/__tests__/runtimeConfig.ratchet.test.ts`。**型名では見ない**——名乗らないリテラルを1件も拾えない。欄3つが同じ括弧に並び、かつ `preset` を引いている塊を見る。写す形・比べる形は `preset` の条件で外れる                                                                                                                                                                                                    |
| 先後の記号の直書き（走査が `/[☗☖]/` しか見ておらず、`▲△` が素通りしていた） | **広げた。** `turnGlyphLiterals` を `/[☗☖▲△]/` へ。走査器は既に在ったのに**別の文字で書いた4件が全部抜けていた**。`Side` を受ける口が無いことが原因だったので、`sideToColor` を足してから広げた。**既存の走査が「何を見ていないか」を数えること**                                                                                                                                                                                                       |
| エンジンを落とした結果を `let _ =` で捨てる                                 | **入れた。** `src-tauri/tests/kill_result_is_not_discarded.rs`。上限内に終わらなかった回（`TimedOut`）はプロセスが残るので、捨てると残ったことを知る手掛かりが無くなる。**呼び口が2つしか無いから人が見て回れる、が1件通した**（`dispose_late_spawn`）。**許可制・文単位にした**（`_ =`・`select!` の枝・ブロック末尾の式・折り返した `let _ =` が素通りしていた）。空振りは呼び口の控え `KILL_CALL_SITES` と等値で見る                                 |
| 語彙の決定が、その語を使っている散文へ届かない（#362 で12箇所）             | **入れた。** `src/__tests__/abortVocabulary.test.ts`。ADR が語を決めても、**その語を別の意味で使っているコメントは grep の鍵（`#362`）に掛からない**ので一斉に嘘になる。**語そのものを禁じない**——正しい用法を全部巻き込む。打ち切りの綴りを名指したコメントの中だけで断り、句点を跨がない（対比した文を赤くしないため）。**1つの決定につき1本**の形なので、次の語彙の決定でも同じ形が書ける                                                            |
| 欄を持たない enum の写しが TS に届いているか                                | **入れた。** `src/__tests__/gameOverReasonWire.test.ts` と `every_game_over_reason_goes_on_the_wire_as_camel_case`。`the_typescript_copy_has_every_field` は**見本の JSON の object のキー**しか見ないので、値だけの enum を丸ごと落としていた。**責任を2つに割る**——線の綴りが宣言の camelCase であることは serde 本体に問い（`rename` を1つ付ければ落ちる）、写しに届いているかは TS 側で見る（`.rs` も `ts` に分類されるので、どちらを触っても走る） |
| 子プロセスを作る綴りを `tokio::process` しか見ていない                      | **広げた。** `layering.rs` の `CHILD_PROCESS_SPELLINGS`（`process::Command` / `process::Child` / `Command::new` も）と、`process` の下から `Command` / `Child…` を取り込む `use`（別名も）。歩く根も `crates/` まで広げた。`std::process::Command`・`use tokio::{process::Command}`・別名の取り込み・`crates/` の中が素通りしていた                                                                                                                     |
| タスクに子プロセスの持ち主を渡し、捨てても落ちなくなる（2件）               | **`UsiProtocol` の中だけ型で止めた。** `Arc::clone(&self.child)`（読み取りの後始末）と `Arc::new(self.clone())`（`readyok` 待ち）の2件は、`Clone` を外し `EngineChild` を `Arc` に包まないのでコンパイルが通らない。`Arc<UsiProtocol>` をタスクへ渡す形（`game/search.rs` の探索、`analyzer.rs` の無限解析）は止まらず、台帳の `kill_engine` が落とす                                                                                                   |

## 入っていない。繰り返し提案されている

「出典」は所見が出たラウンドと、それを産んだ issue。
**issue が closed でも、そこに書かれた機械化が入ったとは限らない** ——
#332 / #243 / #308 はどれも「エピックへ吸収」で閉じており、走査は1本も入っていない。
**追跡はこの表で行う。** 機械化の置き場は #286（検査の掛かっていない範囲）と
#351（文書と現物のずれ）。

| 回数   | 何を                                                                             | どう                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | 出典                                              |
| ------ | -------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| **18** | `cargo-mutants`                                                                  | 判定関数に絞って掛ける。「腕を消しても全緑」が出る                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | #332 / skc-v040 r5〜v050 r20 / 429 r12            |
| **8**  | `@throws` の**有無**の静的検出                                                   | `throw` を含む関数を呼ぶ export に `@throws` があるか。**内容の正しさは拾えない**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | entities-kifu r5〜r11 / 113 r1                    |
| **8**  | Rust の doc が TS / SCSS の綴りを名指す形 —— **camelCase は入れた。SCSS はまだ** | **2026-09-16 に camelCase を候補へ入れ、干し草にフロントの `src/**` を足した**（`comment_identifiers.rs`）。候補は27件に増え、26件が解け、外部アプリの綴り2つ（`storeYaneuraOuBook`/`enableEngineTimeout`）が `EXEMPT` へ。入れた回に腐りを1件拾っている。**残るのは SCSS の綴り**（`&\_\_seat--turn` のような入れ子の途中形）で、こちらは入れ子を展開したセレクタ集合を作る必要があり、まだ入っていない。大文字始まり（`PositionSearchStatusBar`）も地の文と区別できないので候補外のまま                                                                                                                                                                                                                                             | 410-cap-sfen r1〜r6 / 429 r11 / 585-clock-header  |
| **6**  | `` `X`（path） `` の**対**の突き合わせ                                           | `docsIdentifiers` は綴りがどこかに在れば通り、`docsSourcePaths` はパスだけを見る。**両方が緑のまま対だけが壊れる**。#436 では `AppErrorBoundary`（`pages/AppLayout.tsx`）が2ラウンド生き残った                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 447-position-search r1〜r4 / 436 r4/r5            |
| **6**  | `await` を跨いだ closure 変数を `dispatch`/`setState` の payload に使う形        | 通算9件の変種。**2026-09-09 に測って「入れない」と判断した**（下の節）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | kifu-write-path r3〜r13 / 223 r1                  |
| **3**  | `as TesuuPointer` / `as BranchIndex` を所有ファイル以外で禁止                    | 入れれば `CLAUDE.md` の落とし穴の記述自体が要らなくなる                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | #243                                              |
| **3**  | 束縛なしの空 `catch {}` を UI 層で禁止                                           |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | #308                                              |
| **3**  | barrel の呼び出し元0の export                                                    | `sliceBarrels.test.ts` と同じ形の走査                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | app-shell r1/r2/r3                                |
| **3**  | `model/**` の doc から `lib/**` の識別子を参照することの禁止                     | 前回の修正コミット自身が新しい違反を足した                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | entities-kifu r7/r8/r9                            |
| **2**  | 状態遷移表の ✓ が、踏むテストの無いセルに付く                                    | 2巡とも同じ形で出た（1巡目は V6 / B4 の列、2巡目は開始状態を区別していない8セル）。**表のセルを名乗る規約が `game-session.md` にしか無い**のが根で、`state_transition_cells.rs` は掛かる範囲がそこだけ。範囲を広げるか、表から ✓ を廃してテストの一覧を別に持つかの判断が要る                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 95-book-view r1 / r2（react / robustness）        |
| **2**  | `commentHistory` を Rust の `//!` `///` にも掛ける                               | いま TS 側だけ。同じ述語をそのまま使える                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | 410-cap-sfen r3                                   |
| **2**  | barrel の未使用 export の走査                                                    | `entities/search` だけで28名が未消費。目視では追えない                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | 447-position-search r3/r4                         |
| **1**  | `docsIdentifiers` の走査を `docs/**` へ広げる                                    | **パス側は入れた**（上の表）。識別子側（`docsIdentifiers`）はまだ `state-transitions/` だけ                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 436 r4/r5                                         |
| **2**  | 「`X` は `Y` でしか動かない」型の**断定**が、実装の `case` 集合とずれる          | **入れた**（`stateFieldWriters`）。reducer から書き手を引いて控えと表を突き合わせる。r1 はコード内の doc、r2 は表のセルと**別の綴りで再出現**した。**散文の断定は入れていない**——言い回しに当てると誤検知だらけになった                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 262 r1（comment / react）/ r2（react）            |
| **3**  | 「棋譜が入れ替わったか」の合図を**単独の値**で決める                             | 表裏で2回出た。`loadedAbsPath` 単独だと改名で持ち物が捨てられ（r1）、`boardSeq` 単独だと同じファイルの開き直しまで別物として扱う（r2 → #538）。**走査は入れていない**——どちらが正しい合図かがまだ決まっていない（#538 の設計判断待ち）ので、いま owners を固定すると誤った側を固定する                                                                                                                                                                                                                                                                                                                                                                                                                                                | 262 r1 / r2 / play-view r1（architecture）        |
| **1**  | `expect(after).toBe(before)` で `before` の値を一度も固定していない              | 前後の一致だけを見ると、両方が同じ壊れ方をした退行を素通しする。実測で1129件が緑のまま通った。**1件目なのでテストで固定した**（`kifuStreamFaces.test.tsx`）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 262 r2（robustness）                              |
| **2**  | 画面の `useRef` に、**画面より長生きすべき値**を持つ形                           | 同じ画面で欄が違う2件。#561 は候補手のキャッシュ、#566 はヘッダの経過時間の起点。どちらもビューが作り直された回だけ消え、**エンジンも `state` も何も失っていない**のに画面からだけ消える。**測ったうえで「入れない」と判断した（2026-09-13）** —— `widgets/` `features/` `pages/` の `useRef` はテストを除いて **71箇所**あり、DOM 参照（`inputRef` `listRef` `rootRef`）・タイマーの控え（`scrollTimeoutRef` `rafRef`）・前回値の突き合わせ（`lastSfenRef` `shownKifuRef`）が大半。**当たるのは2箇所だけ**で、「消えてよい」と「消えてはいけない」を字面で分ける鍵が無い。71件に「これは消えてよい」を書かせる検査になり、誤検知の抑えが本体より大きくなる。**再提案するなら、この71件の内訳を覆すこと**                             | #561（実装中）/ #566                              |
| **1**  | **falsy になりうる正常値**が `null` と同じ欄に載る形                             | `Color.Black === 0` を `Color \| null` で返すと `winner ? … :` が先手勝ちを引き分けに潰す。tsc は通す。#354 では返り値を `Side` に替えて外した。**同じ形は `Color` を返す関数すべてに在る**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 354 r1（robustness）                              |
| **1**  | `await act()` が、**本番には無い再描画**を非同期の鎖の途中に挟む                 | 押して始まる非同期の鎖（`onClick` が `void` した鎖）の中で、描画で書かれる欄（`xxxRef.current = state.x`）を読むテスト。`await act()` は React の作業を microtask の合間に流すので鎖の途中で再描画が起き、ref が新しくなって**緑になる**。本番の再描画は scheduler の macrotask なので鎖は一度も描画されずに走り切り、**実機だけが落ちる**。同期の `act()` に替えると本番と同じ順序になる。**走査は入れていない**——`await act(` は実測で **429件 / 40ファイル**あり、大半は「描画を待ちたい」正しい用途。当たるのは「鎖の中で描画待ちの欄を読む」場合だけで、字面で分ける鍵が無い。**再提案するならこの429件の内訳を覆すこと**。1件目なのでテストで固定した（`staleClosure.test.tsx` の「作った棋譜を、作る前に握った口から選べる」） | 対局開始（`fileTreeRef` / `selectNodeByAbsPath`） |
| **1**  | 「1つでもあるか」を `.length === 0` で書く形                                     | #354 では `isUchifudume` がその形で、実測 46.57ms → 0.104ms の差があった。`hasLegalMove` のような早期打ち切りの口がある関数について、`.length` 比較を禁じる                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 354 r1（perf）                                    |
| **2**  | `knip` の計測を本番 entry だけに寄せる                                           | いまはテストからの import も消費に数えるので、**本番から到達しない barrel export をテスト1本で隠せる**（#171 で `EnginePhase` 等4件が実際に隠れて基準が下がった）。**2026-09-11 に測って「入れない」と判断した**（下の節）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | #171 r1 / 113 r1（robustness）                    |
| **1**  | 同期の `#[command]` の中でファイルを開く・読む形                                 | Tauri v2 では async でないコマンドはメインスレッドで動くので、ネットワークボリュームやクラウドへ退避されたファイルを読む間、画面全体が固まる。`scan_ai_root` がエンジン候補の先頭バイトを読むようになって踏んだ。**1件目なので直しただけ**（async＋`spawn_blocking`）。走査にするなら「`#[command]` 付きの非 async `fn` の本文に `fs::` / `File::open` / `read_dir` / `xattr::` が現れない」。**ただしこの形は本文に直接現れるものしか拾えず、記録した当の1件（本文は `scan::index(ai_root)` を呼ぶだけ）は素通りする。** 効かせるなら「非 async の `#[command]` を許可表で持ち、増えたら落とす」                                                                                                                                     | engine-candidates r1                              |

### レビュアーが繰り返す誤った前提（2026-09-13 に実測で否定）

**「`docsIdentifiers` は下線を含む綴りしか拾わないので camelCase を見ない」は誤り。**
#561 のレビューで5人中3人が独立にこう書き、`Ref` で終わる綴りだけ拾う枝を足すよう勧めた。
**実際には `docsIdentifiers` が `lastFileKeyRef` を捕まえた**
（`docs/state-transitions/dock-tabs.md` が消えた ref を指し続けていた1件）。
**機械は既に在る。足すものは無い。**

同じ形の空振りを避けるため: 走査が見ないと主張する前に、その走査を1回走らせること。

## 走査を書くときの既知の罠

**「見つけた件数が0」と「見たセルが0」を必ず区別する。** この repo で最も繰り返し踏まれている形。
走査型のテストには**件数の下限**（`toBeGreaterThan`）を必須にする。無いと、走査が壊れて
0件を返しても緑になる。`state_transition_cells.rs` は `table_source()` が
`game-session.md` 決め打ちで、他の表を1つも見ていないのにずっと緑だった。

**走査器そのものにテストを置く。** `scssScaleRatchet` はリポジトリ全体の件数しか主張しておらず、
合成 SCSS を食わせる検査が無かった。**そのラウンドの robustness 所見は全て、
走査器に文字列を食わせるだけで再現した。**

**既存の警告が0でないものは `-D` で入れない。** 件数ラチェット（増える方向にだけ落とす）なら
片付けを待たずに今日から張れる。`rustdoc-ratchet.sh` と `knip-ratchet.sh` が
どちらもこの形。**基準の現在値をここに書かない**——それぞれの `BASELINE` が唯一の出典で、
写すと写した側が先に腐る。**入れたら変異を当てて落ちることを確かめる**
——`knip` は死んだ `export type` を1つ足し、基準を1つ超えることで確かめた。

**外部コマンドの人間向け出力を正規表現で数えない。** JSON など形の決まった出力を使う。
`knip-ratchet.sh` は最初 `grep | grep | awk` で見出しを数えていて、
knip が見出しを1行も出さない状況（自分が壊れた・指摘が0件）で最初の `grep` が 1 を返し、
`pipefail` + `set -e` が**何のメッセージも出さずに終了**した。CI がそれで落ちた。
**「数えていない」と「基準と違う」を区別できない検査は、この罠そのもの。**

**stderr を stdout へ混ぜない。** `npx` は `npm warn ...` を stderr に出すので、
`2>&1` で受けると JSON の手前に1行載って解析が必ず落ちる。
診断には要るので、捨てずに別へ受けて失敗時に出す。

## 検証の所要時間（2026-09-08 に段ごとに実測）

**`verify` は 6分02秒だった。段ごとに測ったら8割が1段に集まっていた。**

| 段                                  | 実測        |
| ----------------------------------- | ----------- |
| `test:hooks`（門番自身の検査）      | **4分52秒** |
| `typecheck`（`tsc -b`、冷えた状態） | 35.7秒      |
| `test`（vitest 99ファイル）         | 13.6秒      |
| `ratchet:knip`                      | 12.7秒      |
| `lint`                              | 1.2秒       |

`test:hooks` が重いのは**プロセス起動律速**だから。この機械で `git` の起動は
1回あたり約46ms、`bash -c` が約39ms（`/usr/bin/true` は7.7ms）。
判定を1件ずつ当て、`probe_is_readonly` が動詞ごとに repo を複製するので
数千プロセスが立つ。**内部を最適化する話ではない。**

→ **`verify` から外した。** 守っている不変条件は門番を触ったときしか動かないので、
`.claude/hooks/*.sh` を触ったコミットでゲート自身が走らせる（種類 `hooks`）。
CI に控えを置いた。**`verify` は 6分02秒 → 1分08秒。**

### テストのスコープを絞ってはいけない

vitest はファイル単位で並列化するので wall clock は**最長の1ファイル**が決める。
内訳は走査ラチェット26ファイル/10.4秒、振る舞いテスト73ファイル/45.5秒で、
最長は `features/settings/ui/tabs/__tests__/AiLibraryTab.test.tsx` の16.8秒（30テスト）。

**`vitest --changed` / `related` で絞ってはいけない。** 選択は import グラフで行われるが、
`src/__tests__/` のラチェットは走査対象を `fs` で読むので**import 辺が無い**。
絞るとラチェットだけが黙って走らなくなる——「見つけた件数が0」と同じ失敗の形。

**速くしたいなら最長の1本を割る**（`AiLibraryTab.test.tsx`）。
`entities/analysis/model/__tests__/provider.test.tsx` は `advance()` で
**7,750ms を実時間で待って**いるので、そちらは偽タイマーに寄せられる。どちらも未着手。

## 機械では止まらないもの（＝人／LLM のレビューが見るべき唯一の場所）

全トピックで繰り返し「防げない」と申告された類型。ここ以外を人が見るのは無駄。

1. **コメントが主張する条件と、実装している条件式のずれ。** 全275本を通じて最多。
   `commentHistory` は**経緯**の語しか見ないので、**理由の嘘**には効かない。
   → 対処は1つだけ: **「〜だから」と書いてあったら、その条件式がコードのどの行かを指す。**
2. **文脈を前提にしたコメント。** そのコメントだけを読んで意味が通るか。
   読み手は issue も会話も差分も見ていない。
   → **一部は機械化できる**（下の「新しく機械にできるもの」）が、
   「上と同じ理由で」が指す理由が本当にその上にあるかは読まないと分からない。
3. **`await` を跨いだときに何が古くなるか。** 成立条件が広すぎて走査にできない。テストでしか固定できない。
4. **一意性の断言**（「ここだけ」「〜しか」「N箇所」）。実在検査は通るが、数と範囲は見ていない。
5. **1つの題材で測った結果を一般則として doc に書き、別の題材で崩れる形。**
6. **責務の置き場・命名の割れ・段の割り当て。** 構造の判断そのもの。
   ADR-0009 §6 が「**行数では割らない**」と決めているので、サイズでは代替できない。
   合図は「1ファイルに段を跨ぐ責務が同居」「冒頭の `//!` が『と』で繋がって1行で言えない」。

## 新しく機械にできるもの（この見直しで出た）

- **`await` の後の `dispatch` の走査 —— 測ったうえで「入れない」と判断した（2026-09-09）。**
  `src/**` の `async` 本体を数えた。`await` を持つものが **460**、そのうち `await` の後に
  `dispatch` / `set*` を撃つ箇所が **170**。これを0にする形の検査は張れない。

  #223 の形（**新しい要求が古い要求を追い越せる**）に絞って
  「引数を鍵に持つ `async` 関数が、`await` の後にその引数由来の payload を撃つ」を数えると
  **20箇所**（`entities/{app-config,engine-presets,file-tree,game,search,study-positions}` と
  `features/settings` / `widgets/game-board`）。件数ラチェットなら張れる数だが、
  **20のうち競合が起こりうるのがどれかは字面で分からない**（`x,y` を撃つ盤のクリックまで並ぶ）。
  増えるたびに「これは競合しない」を書かせる検査になり、
  `mechanization-backlog` が禁じている「誤検知の抑えが本体より大きい」形になる。

  **今回の2件はどちらも「関門が無い」ではなく「関門を通らない呼び出し元がある」だった。**
  `provider.tsx` の中を走査しても捕まらない。だから代わりに入れたのは
  **口の数を数える走査**（`kifuOpenTarget`）で、こちらは誤検知が原理的に出ない。

  再提案するなら、この20件の内訳を覆すこと。

- **文脈を前提にした指示語の検出 —— 測ったうえで「入れない」と判断した。**
  `commentHistory` は経緯の語しか見ていない。そこで
  「上と同じ / 同様の理由 / 例の / 先ほど / 後述 / 前述 / 上記の理由」を
  `src/` と `src-tauri/src/` で数えた（2026-09-08）。**該当は「上と同じ」の5件のみ、他は0件。**

  うち**3件は同一の doc ブロック内で直前の行を指していて正当**
  （`src-tauri/src/engine/game/session.rs:335`、`src-tauri/src/engine/utils.rs:133`、
  `src/__tests__/sourceText.test.ts:121`）。
  残り2件（`src/entities/file-tree/model/provider.tsx:627`、
  `src/features/kifu-comment-note/ui/__tests__/KifuCommentNote.test.tsx:160`）は
  別の分岐を指していて書き直す価値がある。

  **語で禁じると3件の正当な用法を誤検知する。** 判定に要るのは
  「『上』が同じブロック内か」で、それは走査の主題が語ではなくブロック境界になるということ。
  **本体より誤検知の抑え方のほうが大きい**ので入れない。再提案するなら、この測定を覆すこと。

- **本番 entry だけの knip 計測 —— 測ったうえで「入れない」と判断した（2026-09-11）。**
  #171 r1 が要求した形。`knip --production` は**この構成では何も測らない** ——
  未使用 export 0件・未使用ファイル0件を返し、代わりに 24件の「未使用の依存」を出す
  （`shogi.js` / `lucide-react` まで並ぶので、グラフを解けていない）。

  そこで `entry: ["src/main.tsx"]`、`project` からテストを外した構成を手で書いて測ると
  **163件**（既定の計測は179件）。**少ないほうへ動く。** テストだけが持つファイル
  （`src/__tests__/contrast.ts` など）の export が `project` から外れて丸ごと消えるため。
  しかも #171 が見たかったもの —— 本番から到達しないのにテストの import で隠れている
  `entities/position/lib/positionDraft.ts` / `inspectPosition.ts` の約25の export ——
  は**どちらの一覧にも出てこない**（未使用ファイルとしても未使用 export としても数えない）。

  **測りたいものが数に出ない計測を基準にすると、「見つけた件数が0」と
  「見たセルが0」を区別できない検査になる。** 再提案するなら、この163という数と、
  positionDraft の export が現れない理由を覆すこと。

- **状態の記号の到達可能性の走査 —— 測ったうえで「入れない」と判断した（2026-09-11）。**
  「表の行の記号のうち、他のセルに行き先として現れないものを落とす」。
  `docs/state-transitions/` の17表に掛けると、**`position-editor.md` の1件（`B2`。
  実際に正しい指摘）に対して、他の16表から40件以上**が出る。遷移の行き先を
  太字（`→ **B0**`）で書くのはこの表だけで、他は `→ E0` と裸で書く。

  入れるには先に16表の書き方を揃えることになり、**本体より抑えのほうが大きい。**
  再提案するなら、書き方を揃えたうえで。

- **判定の値段の閾値テスト —— 入れた（#354、2026-09-09）。**
  `src/entities/game/lib/__tests__/outcomeCost.test.ts`。守るのは
  「`judgeGameOutcome` が Rust の `RULING_TIMEOUT`（30秒）の中で返る」だけ。

  入れた理由は、**静的検査では原理的に拾えない故障**だったから——
  打ち歩詰めの検査が相手の応手を数え上げ、その中の歩打ちでまた同じ検査へ入る形で、
  双方が歩を2枚持つ詰み形の実測が **66,593ms**（局面の写し 1,558,624 回）。
  条件の並び順を1行入れ替えるだけで 1ms 未満になる。字面はどちらも同じくらい自然で、
  **速い側と遅い側を静的に見分ける手掛かりが無い。**

  **上限は機械の閾値であって実測値ではない。** 台と負荷で数倍動くので、
  実測に張り付けず、壊れた実装だけが越える桁に置いてある（`EXPECTED_MIN` と同じ流儀）。

  **赤くならず、返ってこない形で落ちることがある。** 数え上げは同期なので
  vitest の打ち切りが効かない（入れ子を戻して実測したところ 200 秒で返らなかった）。
  `npm run test` が止まったら、それがこの検査の赤である。

  **同じ形は他の判定関数にも張れる。** 条件が「入れ子が掛け算になりうる再帰」なので、
  合法手の生成を内側で呼ぶ関数（`isUchifudume` が唯一の既存例）を見ること。

## 判断が要って止まっているもの

- **用語の統一**（`bucket`/`segment` の訳語、`[x]` の意味）。検査より先に決めるほうが安い
- **`docs/spec/screens/*.md` の「埋まっていないセル」節が散文。** `(状態, 事象)` の対で書かない限り
  機械に未踏セルが見えない

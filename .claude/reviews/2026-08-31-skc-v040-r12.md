# レビュー skc-v040 ラウンド12

- 日付: 2026-08-31
- 範囲: ラウンド11の修正ぶん＋この PR の最終状態
- 走らせた reviewer: robustness（変異可）/ comment（読むだけ）/ rust（読むだけ）

## 所見

### [BLOCK] R12-01 消した `tied` の痕跡が3箇所に残り、`Unparsable::error` の `Option` が死んでいる

- 場所: `search/kifu_reader.rs`
- reviewer: **3人とも**（robustness は変異で確認）
- `error: None` を作る経路が r11 で消えたのに、型・doc・`describe` の腕が残っていた

  | 変異                                   | 結果   |
  | -------------------------------------- | ------ |
  | 到達しない腕を `unreachable!()` にする | 51件緑 |
  | 文言を `"XXX-DEAD-ARM-XXX"` にする     | 51件緑 |

- コメントは「同点なら決められない（`tied`）」、`line_count` の doc は
  「同点なら先に試したほうを採る」。**同じファイルの2つの doc が逆を言っていた**
- 3人とも直し方が一致: **`Option` を外せば型が再発を止める**
- 同じ形（消した機能への導線が doc に残る）は R7-03 / R8-02 / R10-06 / R11-05 に続き**5回目**

### [HIGH] R12-02 BOM だけのファイルが平手の初期局面として索引に入る

- 場所: `search/kifu_reader.rs`
- reviewer: robustness（7件を実測）
- 門は**生バイトの ASCII 空白**を見るが、空の記録が生まれる条件は
  クレート側の**復号後の文字列**（`s.trim().is_empty()`）にある

  ```
  BOM だけ（UTF-8 / UTF-16LE / UTF-16BE）  -> Ok(moves=1)
  UTF-16LE の改行と空白だけ                 -> Ok(moves=1)
  全角スペースだけ（Shift_JIS / UTF-8）     -> Ok(moves=1)
  ```

- **書き出しが BOM を書いた直後に落ちた跡そのもの**が素通りする。
  R10-03 で塞ぎ、R11-04 で広げた被害が BOM 1つで再現していた

### [MEDIUM] R12-03 0バイトの門が全ファイルを2度読む。サイズの上限が無い

- 場所: `search/kifu_reader.rs`
- reviewer: rust
- 成功経路でも全読みが2回（門とクレート）。索引作りは最大8本並列で、
  `.kif` に改名した大きなファイルが1つあると使わないコピーが積み上がる

### [MEDIUM] R12-04 読めないファイル（権限・I/O）の文言が生の英語

- 場所: `search/kifu_reader.rs`
- reviewer: robustness（`chmod 000` で実測）
- `io error: Permission denied (os error 13)`。
  `KifuReadError` の doc が定めた「次に何をすればよいか」を、ここだけが満たしていない

### [HIGH] R12-05 `line_count` の「どちらを採っても文言は変わらない」がクレートと食い違う

- 場所: `search/kifu_reader.rs`
- reviewer: comment（nom の `convert_error` を読んで確認）
- 行番号は同じでも、クレートは**読めなかった行の本文をそのまま引用する**ので、
  同点の2候補では引用される行が違う（片方は化けている）

### [MEDIUM] R12-06 テストの doc に、そのテスト自身の前の姿が書いてある

- 場所: `search/kifu_reader.rs`
- reviewer: comment
- 「UTF-8 だけを見ていたときは Shift_JIS の穴に気付けなかった」

### [MEDIUM] R12-07 最終手段の配列が両方の復号を先に確定させる

- 場所: `search/kifu_reader.rs`
- reviewer: rust
- 配列リテラルは両要素をループ前に評価する。UTF-8 lossy で読めても
  Shift_JIS の復号が必ず走る

## 重複・矛盾した所見

- R12-01 は3人全員。**このループで2回目の全員一致**（1回目は R4-01）
- **直し方も3人一致**（`Option` を外す）。人の注意で5回外している形なので、
  型で止めるという判断を採った

## 実測で「差分ゼロ」だったもの

robustness が `origin/main` の総当たり（7つの文字コード）を移植して
HEAD と突き合わせた。

- 9つの文字コード × 7つの壊し方 = **63件で差分ゼロ**
- 各バイト位置を `0xFD` / `0x00` / `0x80` に置換する **1,440件で差分ゼロ**

**r11 の直し（Shift_JIS lossy の追加）は必要十分だった。**

## 範囲外

新しい issue は無し。

## 見ていない範囲

- **12ラウンド続けて `index_cache.rs` の符号化・復号本体を誰も見ていない**（#334 が一部）
- TS 側（この PR は未変更）
- `fs_scan.rs` の走査本体

## lint / hook で強制できるもの

- **R12-01 は型で止まる。** `Option` を外せばコンパイラが二度と作らせない
- doc の識別子が実在するかを見る走査 → **5回目**。R8 / R10 / R11 で3回提案されている
- `cargo-mutants`（#332）→ **8ラウンド連続の提案**
- R12-02 / R12-05 は機械で拾えない

---

## 修正結果（`/review-fix`）

7件すべて直した。1コミット。`npm run verify:rust` / `npm run verify` を通してある。

| 所見   | 結果                                                                   |
| ------ | ---------------------------------------------------------------------- |
| R12-01 | **`Option` を外した。** 到達しない腕はコンパイルエラーで消えるしかない |
| R12-02 | BOM を落としてから見る。UTF-16 の空白（NUL 混じり）も空白に数える      |
| R12-03 | 先頭 8KiB だけ見る（超えても空白しか無いなら通す）                     |
| R12-04 | `ErrorKind` で分けて日本語にし、次の行動まで言う                       |
| R12-05 | 「引用される行の本文は違う」に直した                                   |
| R12-06 | 経緯を落とした                                                         |
| R12-07 | R12-03 の直しで最終手段の形も見直した                                  |

### rust-reviewer のマージ可否

> **この HIGH を直せばマージしてよい。** 12ラウンドで残っているのは
> doc と実装の食い違い1件と資源の使い方2件で、読み取りそのものの穴は無い。
> ラウンドを足すより、`Option` を外して型に語らせるほうが効く段階に来ている。

12ラウンドで**利用者の被害に届いた所見は2件**
（R10-03 = 0バイトが索引に入る、R11-01 = Shift_JIS の1バイト破損）。
残りは doc とテストの精度だった。

### 分類ごとの推移

| 分類                           | r8  | r9  | r10 | r11 | r12   |
| ------------------------------ | --- | --- | --- | --- | ----- |
| コメントと実装の食い違い       | 4   | 4   | 4   | 5   | 3     |
| 空振り / 題材の選び違い        | 2   | 1   | 1   | 3   | 1     |
| 自分が前のラウンドで入れた退行 | 5   | 6   | 5   | 5   | **7** |
| 読み取りそのものの穴           | 0   | 0   | 2   | 2   | 1     |

r12 は**7件中5件が「r11 の修正が残した死骸」**で、
新しい判断を入れたことによる退行ではない。
`Option` を外したことで、この形は次のラウンドに持ち越さない。

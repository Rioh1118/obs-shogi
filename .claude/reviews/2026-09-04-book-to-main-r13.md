# レビュー book-to-main ラウンド13

- 日付: 2026-09-04
- 範囲: r12 の所見9件を直した9コミット（`git diff d286722a HEAD -- . ':!.claude/reviews'`）
- 走らせた reviewer: comment / rust / robustness / oss-hygiene
- 対象コミット: `e1248822`
- 前ラウンド: `.claude/reviews/2026-09-04-book-to-main-r12.md`

所見 **6件**（BLOCK 0 / HIGH 0 / MEDIUM 6）＋範囲外1件。
推移は 22 → 18 → 23 → 10 → 12 → 7 → 5 → 4 → 6 → 8 → 9 → 9 → **6**。

**3ラウンドぶりに減った。** 削って直す方針に変えたぶん、新しい所見面が増えていない。

内訳が変わっている。**AD-03 / AD-04 / AD-05 は報告書自身の記述**で、コードにも doc にも
掛からない。実装や doc に掛かるのは AD-01 / AD-02 / AD-06。

AD-02 は **r11 / r12 が2ラウンド「真偽不明」として残していた命題に、
ようやく反例が取れた**もの。所見が減ったのは、確かめる余力がそちらへ回ったため。

---

## 所見

### AD-01 (MEDIUM, 3人): 「現在値の取り方は `CLAUDE.md`」が、Rust の取り方の無い節を指している

この参照は `5cd52071` が「写す側は `CLAUDE.md` を指すだけにする」として作ったもので、
指し先に数え方があることが前提だった。**`dd5109c5` はその数え方だけを削った。**

辿った先に**Rust 側の取り方が無い**（`dd5109c5` が削った）。残っているのは
「末尾では取れない」「`EXPECTED_MIN` も現在値ではない」の2つの否定。
**TS 側の取り方（`npm run test` の末尾）はそのまま生きている** —— 節ごと空ではない。
**r11 の AB-01 が塞いだ「出典を辿った人が行き止まる」形が、指す側だけ残って再発している。**

- 結果: 対応済み `f52967b1`（参照句の側を削った。`CLAUDE.md` に数え方は書き戻さない ——
  戻すと `awk` の総和 276 とラチェットの走査値 266 が食い違う AC-01 に戻る）

### AD-02 (MEDIUM, 1人が実測): 「競合を抱えていれば検証は必ず落ちる」に反例が取れた

r11 / r12 が2ラウンド「真偽不明」として置いていた命題。reviewer が使い捨てのクローンで実測。

- `docs/IDEAS.md` だけが競合した**実際の rebase 中断状態**（`UU`）で `npm run verify` は
  **exit 0**。ゲート本体に `git commit` を食わせても標準出力0バイト・exit 0（許可）
- `docs/state-transitions/verify-gate-decision.md` だけの競合でも exit 0
- `README.md` だけの競合は種類にすら当たらないので、検証が1本も走らずに exit 0

`.ts` や `.rs` が競合していれば落ちるので「落ちうる」は真だが、**全称は偽。**
この一文は免除の根拠2つのうちの1つで、次にこの免除の範囲を動かす人は
「畳む操作を deny すると必ず行き止まる」を前提に判断する。

- 結果: 対応済み `daef0e7b`（2つ目を削った。**削りすぎで、r14 の AE-02 が戻している** ——
  1つ目だけでは免除しない綴りと区別が付かない）

### AD-03 (MEDIUM, 1人): r12 の「9件中5件」がまた数え落としている

AC-05 / AC-06 が指した2行はどちらも `c8627a11` が新設したもので、これも r11 の直しが直接作っている。
5件ではなく7件。**r12 は同じ段落の AC-09 で「4ラウンド続けて『直した先を数えて名乗る』形を
落としている」と書いていて、その文自体がまた数え落としていた。**

- 結果: 対応済み `3090eb20`（数を書かない形へ）

### AD-04 (MEDIUM, 1人): r12 の「新しい文は1つも書いていない」が偽

純粋な削除で終わっているのは9件中2件だけ。**r12 は「新しい散文が次のラウンドの所見面になる」と
診断したうえでこの宣言を置いた。** 次のラウンドが信じると、そこで新しく書かれた4箇所を
検査対象から外す —— 実際、AD-01 はまさにその組み合わせで生じている。

- 結果: 対応済み `a1321acc`

### AD-05 (MEDIUM, 1人): r1 の報告書のヘッダが、本文の所見数と合っていない

本文の見出しは R-01〜R-22 の22件。「追記した所見」で R-18〜R-22 を足したときにヘッダを
更新していなかった。**r2 以降が引く推移の方が正しく、r1 の自己申告だけが外れている。**

- 結果: 対応済み `e43715eb`

### AD-06 (MEDIUM, 1人): 括弧が二重になっていた

`129020a3` の打鍵ミス。`（（Rust）の行は …` になっていた。

- 結果: 対応済み `10e89d6c`（表の印をコード span で囲んだ）

---

## 範囲外（issue へ送った）

### Q-001 が名指す2ブランチが、どちらも存在しない

`feature/26-Zettelkasten` / `feature/zettelkasten` はどちらも remote にもローカルにも無い。
系統A の `marks.rs` はタグ `archive/annotation-marks-2026-03` にだけ残り、系統B の
`src-tauri/src/meta.rs` は `git log --all` が0件でどこからも辿れない。

Q-001 は `OPERATING-MODEL.md` が「最大のブロッカー」として名指す唯一の項目で、
外から入った人が最初に開く。**「2実装が競合している」という前提を確かめる手段が無い。**

- **#290 へ送った。** 差分の範囲外で、系統B の扱い（畳むか復元するか）の決定が絡む

---

## 重複・矛盾した所見

- **AD-01 は3人が指した。** 残る5件は1人ずつ
- **削って直したぶん、新しい誤りは生まれていない。** r12 で新しく句を書いた4箇所
  （`129020a3` / `b2b74e71` / `da5a99f8` / `05285dfb`）のうち所見になったのは
  打鍵ミス1件（AD-06）だけで、内容の誤りは0
- **AD-01 は「削ったことで別の場所が偽になる」形。** 削る方針の副作用で、
  r9〜r11 の「書き足したことで偽になる」とは逆向き。**削るときは指している側を数えること**
- 所見の水増しは無かった。4人とも「確認して所見が無かったもの」を明記していて、とくに
  **oss-hygiene が「テスト本数の断定」を綴りではなく意味で3通り走査し、追跡済み758ファイルに
  7箇所目が無いことを確認している**（残っている「無い」系の断定5件は全て現物と一致）。
  `CONTRIBUTING.md` の表24行の走らせ方の対応も全行で確認された

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送ったもの（再掲しない前提）
- `src-tauri/src/book/` の振る舞い（doc の主張との突き合わせのみ）
- `user_book1.db` の実測3行とピーク確保の実測
- ShogiHome v1.29.0 側の引用（現物を取っていない）
- 競合状態での `npm run verify:rust`（2分15秒を要するため省いた。ただし AD-02 の反例は
  `ts` のみ／種類なしの経路なので、この省略に依存しない）

## lint / hook で強制できるもの

このラウンドで新しく挙がったものは無い。AD-01（節を出典として名指した文が、その節に実体を
持つか）は機械で拾えそうに見えるが、**指している見出しは今も実在する**（消えたのは中身）ので、
`docsSourcePaths` / `stateTransitionIndex` の粒度では緑のまま通る。#351 / #386 の範囲。

機械で守れないもの: AD-02（全称が実際の挙動を覆っているか）、AD-03 / AD-04 / AD-05
（報告書の自己申告と本文・git 履歴の食い違い）。

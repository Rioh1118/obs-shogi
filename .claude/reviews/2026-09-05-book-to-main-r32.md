# レビュー book-to-main ラウンド32

- 日付: 2026-09-05
- 範囲: r31 の所見7件のうち6件を直した6コミット＋報告書（`git diff 0a07fc31 429987e1`）
- 走らせた reviewer: architecture / oss-hygiene
- 対象コミット: `429987e1`
- 前ラウンド: `.claude/reviews/2026-09-05-book-to-main-r31.md`

所見 **9件**（BLOCK 0 / HIGH 0 / MEDIUM 9）。
推移は 22 → … → 4 → 5 → 7 → **9**。

**8ラウンド続けて HIGH 0。**

**このラウンドで「issue へ送った」が全件実在することを初めて確かめた** ——
`gh` で4 issue の本文と全コメントを読み、報告書が「送った」と書いた所見を
1件ずつ突き合わせた。**捏造は1件も無い。ずれていたのは件数だけ。**

---

## 所見

### AY-01 (MEDIUM, 1人が実測): `.sh` を「歩いた」は固定したが、「`#` を読んだ」が固定されていない

`1e26b597` が足したのは **walk の証明**であって **scan の証明**ではなかった。
`HASH_COMMENT` を当たらない綴りに変える1トークンの変異が、
**708本すべて緑・tsc も緑**で通る。`shells` は2のままなので新しい assert も緑。

**答えが変わることを注入で実証した。**

```
verify-gate.sh:2 に「# 今回の対応でここを足した」を挿す
  原型      → 赤（offenders に出る）
  + C10 変異 → 緑
```

**これは AW-06 が名指しした害そのもの**（「`verify-gate.sh` のコメントに
経緯を書き放題になるのに、赤くなる検査が1本も無い」）で、歩いた本数だけでは塞げていなかった。

- 結果: 対応済み `053ecde0`（読んだ `#` コメントの数に床を置いた。
  `HASH_COMMENT` を潰す変異は新 assert が、`commentsOf` を常に `SLASH_COMMENT` にする変異は
  tsc が TS6133 で落とすことを確認）

### AX-01 (MEDIUM, 1人が実測): AW-07 の「実測」3つのうち2つが、その `expect` が比べる量ではない

| 検査                        | 書いた値 | その `expect` が比べる量                                                      |
| --------------------------- | -------- | ----------------------------------------------------------------------------- |
| `docsIdentifiers.test.ts`   | 195      | **214**（`found.length` はファイル跨ぎの重複込み。195 は綴りの異なり数）      |
| `commentHistory.test.ts`    | 568      | 568（一致）                                                                   |
| `turnGlyphLiterals.test.ts` | 489      | **310**（`tsFiles(SRC, { includeTests: false })`。489 は `sourceFiles(SRC)`） |

**AW-01 が「reviewer の数を写して当て直さなかった」を所見にした、その同じラウンドの中で
同じことをしていた。** しかも**#351 のコメントとして repo の外に出ている**ので、
床を引き上げる人が `turnGlyphLiterals` に 480 前後を置くと**現在値 310 を超えて即座に赤くなる。**

- 結果: 対応済み `37a67e13`。**#351 にも訂正を投稿した**

### AY-02 (MEDIUM, 1人が実測): 「708本」は2箇所あり、`da0f524a` は片方しか直さなかった

r31 の対象は `0a07fc31` で、**その木の本数は707**。AW-05 も AW-06 も同じ木に当てている。

`da0f524a` は「AW-06 の 708 は当てた時点と一致している」と書いたが、
**これは変異を `548a80d5` の後に当てた前提。** `0a07fc31` を戻して実測すると、
変異の有無にかかわらず 707 passed。

**AW-01 が所見にした形を同じ報告書で2箇所やっていて、片方だけ直していた** ——
AU-03 と同じ落とし残しが、その落とし残しを直す側で出た。

- 結果: 対応済み `c122af1d`

### AY-03 (MEDIUM, 1人が実測): 「`docs/` に対がある」の片方が `docs/` に無い

`config_write_failed` は `docs/` に**0件**。`docs/` にあるのは
`naming-and-module-layout.md:134` の `config_write` だけで、
しかもそこが挙げている対は `save_config` → `config_write`。

**現物を指したつもりの記述が実測と合わない形が、
その形を塞ぐために足したテストのコメントに入っていた。**

- 結果: 対応済み `5ff0212d`（どちらがどこにあるかで書き直した。テストの入力は変えない）

### AX-03 (MEDIUM, 1人が実測): 「issue へ送った20件」の 20 が、どの数え方でも出ない

4 issue に入っているこのループ由来の所見は**23件**、
r3 以降にコメントで送ったものだけなら**19件**。
20 に着くには「#342 の R-22 は数えるが #386 本文の3件は数えない」という混ざった基準が要る。

起点の r4 / r5 の「13件」は **r3 の送付ぶんだけ**で r1 の送付を数えていない。
同じ起点で増分を足すと 13 → 15 → 16 → 18 → 19 だが、
書かれているのは 13 → 16 → 17 → 19 → 20。**r6 の時点で +1 ずれ、25ラウンド運ばれていた。**

**送り漏れは無い。ずれているのは数え方だけ。**

- 結果: 対応済み `f96440f2`（AT-01 と同じく**書くのをやめた**。28本すべてから落とした。
  この行の役目は「再掲しない対象がある」ことで、件数はそれに要らない）

### AX-05 (MEDIUM, 1人が実測): AW-02 の落とし残し —— `42a050f2` が偽の前提を2つ持っている

**HEAD にあり `origin/main` に無い**＝この合流が main へ持ち込むメッセージ。

```
- **`git rebase --abort` を deny していた。** 競合を抱えたツリーでは `npm run verify` が
  必ず落ちるので、…
  コミットを1つも作らない綴り（`--abort` / `--quit` / `--skip` / `--edit-todo`）を対象外にした
```

どちらも偽と確定済み（`--skip` は残りを当てるのでコミットを作りうる＝r9 の Z-06、
競合しても検証は必ずは落ちない＝r13 の AD-02 / r15 の AF-01 で3つ実測）。

**`8349177f` は「`verify-gate.sh` の根拠を辿った人が次に開く出典」だから直す、と書いている。
その根拠を `git log` で辿ると着くのが `42a050f2`** で、しかも squash された PR #237 の本文なので
**免除を足した理由を知りたい人が最初に読む1本。**

- 結果: **未対応**（下の「残した所見」を参照）

### AX-06 (MEDIUM, 1人が実測): 「`git log --grep=AP-06` に当たるのは1本」が偽（実際は7本）

`222d9929` はまさに「その綴りで grep すると何に着くか」を主題にした所見なのに、
**その主題の記述自体が現物と違っていた。** 訂正した側のコミットも同じ綴りを持つので当たる。

真なのは「**鎖を自分の主張として述べているのは `6450d325` だけ**」。

- 結果: 対応済み `d12de491`（r30 / r31 の両方を分けて書き直した）

### AX-02 (MEDIUM, 1人が実測): AW-05 の「708本」が、その直しのコミットの「707本」と食い違う

メッセージ側は 707 と正しく書いてある。**残り方が AW-01 と同型** ——
「報告書だけが誤りでメッセージ側が正しい」ので、
`git log` を先に読んだ人と報告書を先に読んだ人で違う数に着く。

- 結果: 対応済み `da0f524a`（ただし片方だけ。もう片方は AY-02 で直した）

### AX-04 (MEDIUM, 1人が実測): 「残る5件は1人ずつ」が、実際は6件

見出しは7件で、`2人が実測` は AW-01 だけ。この欄は
「何人が独立に到達したか」を後のラウンドが重複判定の根拠に引く。

- 結果: 対応済み `0ac56993`

---

## 残した所見

### AX-05 —— **次のラウンドで直す**

このラウンドで書いた `053ecde0` / `c122af1d` / `5ff0212d` の3件を先に入れたため、
`42a050f2` の注記を置く先（`book-foundation-r15/r16` か `verify-gate-decision.md`）の
選択が未着手。**AW-02 と同じ扱い（reword せず、注記を1行）で次のラウンドの冒頭に置く。**

---

## 重複・矛盾した所見

- **9件とも1人ずつ**
- **9件すべてが r31 の直しから出た。** 5ラウンド連続
- **AY-01 は AW-06 の直しが作った。** 「宣言された保証を固定する」ために足したものが、
  **保証の半分（walk）しか固定していなかった** —— scan 側は1トークンで壊れる。
  **注入して答えが変わることまで確かめたのは、このループで初めて。**
  #351 に送った「落とす基準3つ」ではこの型が取りこぼされることも分かった
  （corpus 不変・`missingIn` 無関係・歩いた本数不変で、変わるのは offenders だけ）
- **AX-01 / AX-02 / AY-02 は同じ根**（数の取り違え）。**3件とも r31 が持ち込んだもの**で、
  AX-01 と AY-02 は**AW-01 が所見にしたのと同じ形を、同じ報告書の中で繰り返している**
- **AX-03 / AX-05 は AU-03 / AW-02 と同じ根**（instance だけ直して形の全体に広げない）。
  **AX-03 は25ラウンド運ばれていた**
- **AY-02 は「落とし残しを直す側」で落とし残した。** 二重
- 所見の水増しは無かった。2人とも「確認して所見が無かったもの」を明記していて、とくに:
  - **「issue へ送った」は全件実在した** —— 4 issue の本文と全コメント（#386:1 / #342:4 /
    #290:5 / #351:1）を読み、r1 / r3 / r6 / r13 / r22 / r31 の送付を1件ずつ突き合わせた
  - **`19eed3ed` の3行は3つとも現物のメッセージと一致**し、帰属も正しい
  - **他シリーズに同じ組は残っていない** —— `.claude/reviews/` の211ファイルを `git log` に掛け、
    **初回コミット以降に本文が編集された報告書は `book-foundation` の r15 / r16 だけ**
  - **`missingIn` の単一変異は尽きた**（10通り＋2重4通り。
    正規表現の組み立て・`filter` の向き・`!` の有無・`test`/`exec`・フラグの5軸で、
    答えを変える生存は0）
  - **`548a80d5` / `1e26b597` の新テストはどちらも唯一の殺し手**
  - **`8b377e89`（AW-04）の数はすべて実測と一致**（10行 / 11出現 / 異なり7 / ファイル別の和9）
  - **`a8e3fff8`（AW-01）の数は3つとも再現した**（corpus 1,573,727 → 1,866,272 /
    新たに入る綴り33 / 表が引く異なり195 / `missingIdentifiers` は両方 `[]` /
    `production_unwrap` は corpus に0回）
  - **`hitsIn` の行番号計算の変異は生き残るが、出力が区別できない** ——
    行番号は `offenders` が空でない時にしか現れず、呼び手は `toEqual([])` としか比べない
  - **`stateTransitionIndex.ts` の変異5通りのうち3つは殺される。**
    生存2つは等価変異（`docs/` に `md` で終わって `.md` で終わらないファイルが0本）と並び順

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送ったもの（再掲しない前提）
- `origin/main` が元から持っていた実装と doc の中身
- `docs/spec/screens/` の13面が現物の UI と合っているか
- `user_book1.db` の実測3行とピーク確保の実測
- 3重以上の変異
- `codeOf` の c-like 枝と `stripShellStrings`（r30 が当てたきり）
- `stripLineComments` / `stripShellComments`（r31 が当てたきり）
- **変異の総数（r30 の59通り / r31 の76通り / r32 の44通り）は互いに検算できない** ——
  変異の数え方に依存する量で、当て直しても一致を確認できない。
  **突き合わせたのは結果として書かれた数（corpus 長・214 / 310 / 568 / 33 / `[]`）だけ**
- `.claude/reviews/` の `book-to-main` 以外のシリーズの所見番号の採番全体

## この PR の外で観測したこと

**ゲートが2度メインのチェックアウトを検証した**（#342）。
1度目は `commentHistory.test.ts` / `sourceText.ts` の編集中、
2度目は `index_cache_guard_names`（**このワークツリーに存在しない**）。
どちらも手元は緑で、**あちらが畳むまで待って通した。**

**複合コマンドの `git commit` がゲートに拒否されると、同じ呼び出しの `cat > msg.txt` も
走らない。** その後の単独コミットが古いメッセージで着地したので `--amend` で直した
（`5ff0212d`）。**`git commit` は単独の呼び出しで打つこと** —— r12 が同じことを書いている。

## lint / hook で強制できるもの

- **AY-01 で `#` の枝が塞がった。** ただし**#351 に送った「落とす基準3つ」では
  この型が取りこぼされる** —— corpus も答えも歩いた本数も変わらず、
  変わるのは `commentHistory` の offenders だけ。
  **(d) 各ラチェットに注入した既知の違反が検出され続けること**が要る
- **AX-03 の型（「送った N 件」）は機械で数え直せる** ——
  各コメントの「出どころ」行（`.claude/reviews/<file>` の綴りが定型）を集計する形。
  **今回の +1 ずれは25ラウンド運ばれていて、人の目では止まっていない**
- **AX-06 の型（`git log --grep=<所見番号>` の当たり本数）も数え直せる**
- **AX-01 の型（「実測」と書いた数が、その `expect` が比べる量と違う）も拾える** ——
  #351 に既にある `toBeGreaterThan(<定数>)` の走査と同じ入力で、
  報告書が書いた実測値との突き合わせを足せばよい
- AX-02 / AX-04 / AX-05 / AY-02 / AY-03（数の食い違い・帰属・現物の指し先）は機械で防げない

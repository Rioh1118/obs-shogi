# レビュー book-to-main ラウンド20

- 日付: 2026-09-04
- 範囲: r19 の所見2件を直した分（`git diff d6336676 HEAD`）＋**未確認範囲の掘り起こし**
- 走らせた reviewer: comment / oss-hygiene
- 対象コミット: `3ac79afe`
- 前ラウンド: `.claude/reviews/2026-09-04-book-to-main-r19.md`

所見 **3件**（BLOCK 0 / **HIGH 1** / MEDIUM 2）。
推移は 22 → 18 → 23 → 10 → 12 → 7 → 5 → 4 → 6 → 8 → 9 → 9 → 6 → 7 → 7 → 3 → 3 → 2 → 2 → **3**。

**差分そのものには所見が無かった**（comment は0件、oss-hygiene も焦点1・2 は0件）。
3件はすべて、**このラウンドで初めて見た範囲**から出た ——
`docs/spec/` と `src-tauri/src/book/` の残り4ファイル。

**HIGH は `docs/spec/features/book.md`。** 19ラウンドかけて合流の中身を突き合わせてきたが、
**その合流が何を main へ持ち込むかを書いた仕様だけが、一度も差分に現れていなかった。**

issue へ送るものは無かった。

---

## 所見

### AK-01 (HIGH, 1人): 定跡の仕様が「main に無い」「読める形式は1つも無い」のまま

この合流は `src-tauri/src/book/` を10ファイルまるごと持ち込み、**`yaneuraou_db.rs` を含む。**
`reader.rs` の `support(BookFormat::YaneuraouDb)` は `Some` を返し、
`open.rs` のテストが実際に `.db` を開いて指し手を引いている。

にもかかわらず仕様は3つとも古かった。

- 「main にあるか: **無い。** `src-tauri/src/book/` は存在せず」→ マージした瞬間に偽
- 在庫表が `.db` の reader を**別ブランチ `feat/91-yaneuraou-db-reader`（作業中）**に置いている
  → 現物は `feature/book` にある
- **「読める形式は1つも無い」→ 主語が `feature/book` なので、マージを待たずにいま既に偽**

`docs/spec/README.md` の在庫表も同じ。**次に定跡へ触る人はこの表を見て「main には無い」と
判断し、`open_book` を新規実装しようとする。**

**`docs/spec/` はこの合流の差分に一度も現れておらず、19ラウンド誰も見ていなかった。**
CLAUDE.md は「触った画面の仕様が現物と違うようになったら、同じ PR で直す」と定めている。

- 結果: 対応済み `f07f2f3f`

### AK-02 (MEDIUM, 1人): テストの注記が、その枝がもう返さない種別を名指している

`linked` は空のファイルを作る。`.db` / `.db` なので解決も形式の一致も通り、`support` が
`Some` を返して `yaneuraou_db::load` に入り、**`InvalidContent` で落ちる。**
`UnsupportedFormat` にはならない。

このテストは `err.path()` しか assert しないので、種別が変わっても緑のまま通る。
`UnsupportedFormat` は**実体のパス**を載せる唯一の枝で、「載るのは常に要求時の綴り」という
宣言に対して一番危ない場所。**そこが注記のせいで検査済みに見えていた。**

- 結果: 対応済み `c211e205`

### AK-03 (MEDIUM, 1人): 「検査の順序」の鎖が、未対応形式の判定を落としている

`open_reader` の実際の並びは4段で、鎖に載っていない `support` の段が**大きさの上限より前**にある。
しかもその位置には理由が書いてあり、テストが固定している。
つまり**「検査の順序を語るのはここ1箇所」も成り立っていなかった。**

`api.rs` が読み手をこの鎖へ送るので、`.ybb` の reader を足す人はここから未対応形式の位置を読めない。

- 結果: 対応済み `7c83aa7d`（段を足し、理由は写さず参照だけに）

---

## 重複・矛盾した所見

- **3件とも1人（oss-hygiene）が指した。comment は0件**
- **3件とも「差分の外」から出た。** 19ラウンド、毎回「前ラウンドの直し」を範囲にしてきたので、
  **合流が持ち込むもの自体を書いた doc に届いていなかった**
- **AK-01 は、このループが7ラウンド追ってきた形の最大のもの** ——
  同じ事実（`.db` が読めるか）が実装と仕様の2箇所にあり、実装だけが直っていた
- 所見の水増しは無かった。2人とも「確認して所見が無かったもの」を明記していて、とくに:
  - **comment が r19 の実測値を全部数え直した** —— `main..7ae4f479` = 156 / 非マージ 154、
    対象コミット時点の非マージ 157 本すべてが `<type>: ` に一致、推移19個も全項一致
  - **oss-hygiene が `book-key-failures.md` の全体を `sfen.rs` と初めて突き合わせた** ——
    検査 G0〜G11 の順序、表の `✓` が名指す10本のテストの実在、
    不変条件1の算術（`9+5+90+2+77+11=194` = `LONGEST_VALID_INPUT_CHARS`）まで一致
  - `error.rs` / `session.rs` / `mod.rs` の doc の主張（`MAX_PATH_CHARS` が 34 倍、
    ハンドルは1から、dashmap `6.1.0`、`pub(crate)` の範囲、コマンド6本）も現物と一致

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送ったもの（再掲しない前提）
- **`user_book1.db` の実測3行とピーク確保の実測** —— 現物がリポジトリに無い。
  doc 自身も「機械では守れない」と明示している
- 上流（YaneuraOu / ShogiHome / ScriptCollection）の現物との再照合（r18 で1人が取った）
- `yaneuraou_db.rs` のうち `looks_like_a_move` / `optional_number` / `annotate_line` の各テスト
- reviewer の人数の申告（個別の出力がリポジトリに残らないため検証できない）

## lint / hook で強制できるもの

**AK-01 の型は機械で拾える。** `docs/spec/README.md` の「main にあるか」欄と
`docs/spec/features/*.md` の `main にあるか:` 行が、そこに書かれたパスの実在と食い違って
いないか —— `docsSourcePaths` が既に持っている「パスの実在検査」の裏返しで、
**「無い」と書いてあるのに実在するものを落とす**形。
掛け先は既存の vitest なので、`.claude/reviews/` と違ってゲートに載る。
**このラウンドでは足さない（案A の方針）が、#351 の候補としては今までで一番筋がいい。**

機械で守れないもの: AK-02 / AK-03（コメントが名指す種別・順序と、実際の枝の同一性）。

# レビュー: 改名・移動で盤が巻き戻る（#262）

対象: `fix/262-rename-rewinds-kifu` の `git diff main`（基点 `3ee1551d`）。
観点は react / robustness / comment の3つを並列で当てた。
**所見ファイルはレビュアーが書けない**（Read/Grep/Glob/Bash しか持たない）ので、
返ってきた所見をここへ集約している。

## このラウンドの計画

**根が1つある。** react と robustness が独立に同じものを挙げた（R-01 / B-01）——
`loadedAbsPath` の意味を「盤に載っている棋譜」から「載っている棋譜の**現在の名前**」へ
変えたのに、「パスが変わった＝別の棋譜が載った」と読んでいる箇所を1つしか直していない。
comment の C-01 / C-02 も同じ根から出た（断定が古くなった箇所）。

**これは範囲内。** 同じ PR で書いた `docs/spec/screens/file-tree.md` が
「見ている手数・分岐の選択・盤の向きはそのままで、以後の保存先だけが新しいパスへ移る」と
言い切っているが、書きかけのコメントは黙って消える。**自分で書いた仕様が嘘になっている。**

順序は根から。A を直すと C の一部と D-1 の前提が変わるので、A → B → C → D → E で取る。

| 群  | 何を                                                                     | 所見                      |
| --- | ------------------------------------------------------------------------ | ------------------------- |
| A   | 「盤の中身が入れ替わった」を1つの綴りにし、読み手3つを揃える             | R-01 / B-01               |
| B   | 参照の同一性という**レイヤをまたぐ契約**を、file-tree 側に書いて固定する | R-03 / B-02               |
| C   | この変更で嘘になった断定を直す                                           | C-01 / C-02 / R-02        |
| D   | 表・索引・spec の食い違い                                                | C-03 / C-04 / C-05 / C-09 |
| E   | 残りの doc と、範囲外へ送るもの                                          | C-06 / C-07 / C-08 / B-03 |

## comment-reviewer

### C-01 [BLOCK] `persistIfPossible` の門番の理由が、この変更で嘘になった

`src/entities/game/model/provider.tsx:74-79`

> `loadedAbsPath` は `game_loaded` でしか動かないので、次の棋譜が載るまでこの条件は真であり続ける。

`path_renamed` でも動くようになったので前提が偽。この段落は「E16 のあと保存が全部止まる」の
**根拠**として書かれているため、読み手は「改名で宛先が動いたら門番が通るのでは」と読める。
実際に保証を支えているのは `GameFileTreeBridge` が `loadedJkfDataRef` を**載せられた回にしか
書かない**ことで、その根拠がどこにも無い。

結果: 直した。

### C-02 [BLOCK] `LoadedKifuPathContext` の doc が発火点を2つと言っている

`src/entities/game/model/context.ts:14`

この doc の役目は「滅多に動かないからツリーの全行に購読させてよい」の保証。
3つ目の発火点（＝改名するとツリーの全行が描き直される）が増えたのに2つと書いている。

結果: 直した。

### C-03 [HIGH] 同じパスで載り直したときの扱いが、コメント・表・テストで三者三様

- `useResetOrientationOnKifuChange.ts` の `samePath` に「落とすものは無い」——B2 では偽
- `board-orientation.md` の `(B2, E1)` は「→ B1（`pov` を落とす）」
- 新しいテストは「落とさない」を固定している

絵空事ではない。E16 のあとの復帰導線（前の棋譜をツリーで選び直す）が、まさに
`loadedAbsPath` と同じパスの `game_loaded` を撃つ。

結果: 直した。**振る舞いは変えない**（同じ棋譜なら向きは持ち越す。不変条件1がそう言っている）。
表の側が「棋譜が盤に載る」で別の棋譜と同じパスを混ぜていたので、E1 を「**別の**棋譜が盤に載る」に
狭め、同じパスの載り直しは E6 が引き取る形にした。`(B2, E6)` に ✓ を付ける test も足した。

### C-04 [HIGH] 状態遷移表の索引が「改名・移動のセルが未検証」のまま

`docs/state-transitions/README.md:66`。表本体は「埋まっていないセルは無い」と言っている。
索引だけを見た人は既存のテストを重複して書くか、「未検証だから触ってよい」と判断する。

結果: 直した。

### C-05 [HIGH] `navigation-map.md` の `pov` の説明が改名で偽になる

`docs/spec/navigation-map.md:137`。同じ PR で `app-layout.md:97` は直してあるので、
**同じ事実について2つの spec が食い違う**状態になっていた。

結果: 直した。

### C-06 [MEDIUM] 「載せ直さない」が無条件だが、載せている最中の改名は載せ直しになる

ref に控えるのは `await` の後なので、最初の `loadGame` が飛んでいる間に来た改名は
`loadGame` をもう一度通る。その窓では利用者の編集がまだ入り得ないので落ちるものは無いが、
書いておかないと分からない。

結果: 直した（`loadedJkfDataRef` の doc に1文）。

### C-07 [MEDIUM] `path_renamed` の門番が見る式と、理由が名指しする欄がずれている

理由は `loadedAbsPath` の話をしているのに条件は `state.jkf` を見ている。
等価である根拠（`game_loaded` と `reset_state` が両方を組で動かす）が書かれていない。

結果: 直した。

### C-08 [MEDIUM] `renameLoadedPath` は何も改名しない → **直さない**

「動詞＋目的語の形で、実行しない操作を名乗る」という指摘。`repointLoadedPath` を提案された。

直さない理由: この関数は**載っている棋譜のパスを**張り替えるので、名前は起きることを
そのまま言っている。`entities/game` はファイルシステムに触る口を1つも持たない（`persistence` は
呼び出し側が注入する）ので、「これを呼べばファイルが改名される」と読む余地は型にも無い。
issue #262 が直し方として名指ししている綴りでもある。
ただし指摘の芯（ディスクを触ると読まれうる）には doc で答え、「ディスク上の改名は済んでいる。
ここが動かすのは宛先だけ」を1行足した。

### C-09 [MEDIUM] 「戻すと」が、読み手の知らない過去の実装を前提にしている

`board-orientation.md:143`。`戻す` は「以前はそうだった」を含意する。

結果: 直した。

## react-reviewer

### R-01 [HIGH] `loadedAbsPath` の意味を変えたのに、読み手を1つしか直していない

**B-01 と同じ所見。robustness が独立に同じものを挙げた。** 詳細は下。

### R-02 [MEDIUM] 不変条件を語っている2つのコメントが嘘になった

C-01 / C-02 と同じ。結果: 直した。

### R-03 [MEDIUM] 「同じ参照＝改名」の前提を守る仕掛けが無い

分岐の正しさが別レイヤの実装詳細（解析結果をキャッシュしない）に乗っている。
パスごとに解析をキャッシュする最適化を入れた瞬間、同じ棋譜を開き直しても参照が同じになり、
橋は改名と読んで `loadGame` を撃たない——**盤がディスクの変更を二度と拾わなくなる**。

結果: 直した（B-02 と同じ）。

### 所見にしなかったもの（依頼した観点への回答）

- **`await` を跨ぐ競合**: 無い。`loadGame` の中に `await` が1つも無く、`game_loaded` は
  `run()` を呼んだ同期時点で撃たれる。ref を書く継続は microtask、React が次の
  passive effect を流すのは Scheduler の macrotask なので割り込む隙が無い
- **成功時にだけ控える判断**: 正しい
- **3分岐の穴**: 見つからず
- **再レンダ**: 増えない。改名では `jkf` / `cursor` / `branchPlan` の参照が据え置かれるので
  `cursorView` も `view` も再計算されない。**以前より減る**
- **StrictMode**: 壊れない（このアプリは `main.tsx` で使っていない）

## robustness-reviewer

### B-01 [HIGH] 改名すると、書きかけのコメントが黙って消える

`path_renamed` が `loadedAbsPath` を動かす → `KifuStreamList` の effect が
`setOpenComment(null)` → `KifuCommentNote` の離脱時の保存が撃たれる →
門番（`target.absPath !== loadedAbsPath`）が `"skipped"` → **面がもう外れているので
`showing()` が偽になり、赤い箱も出ない**。打った本文はディスクにもメモリの棋譜にも入らない。

同じ喪失は変更前にもあった（載せ直しでも `loadedAbsPath` が動くため）が、
**そのときは盤ごと巻き戻るという目に見える異常と同時に起きていた。** この PR で他が
全部保たれるようになった結果、改名で失われるのは書きかけのコメントだけになり、しかも
`docs/spec/screens/file-tree.md` は「見ている手数・分岐の選択・盤の向きはそのまま」と書いている。

結果: 直した。合図を `boardSeq` に寄せ、読み手3つを揃えた。

### B-02 [MEDIUM] 参照の同一性という契約が file-tree 側に書かれておらず、テストも無い

橋のテストは `vi.mock("@/entities/file-tree", ...)` でツリーごと差し替えているので、
**実物がこの性質を失っても緑のまま。**

結果: 直した。`entities/file-tree` の `jkfData` と `active_kifu_reconciled` に契約を書き、
`activeKifuReferenceIdentity.test.ts` で両方向（改名では変わらない／開き直しでは変わる）を固定した。

### B-03 [MEDIUM] 保存が飛んでいる最中の改名 → **範囲外。issue へ送った**

`renameNode` は `blockingWrites` を見ないので、保存の往復が着地した時点で
古い名前のファイルが作り直される。**この PR の前から在り、この PR で壊れても直りもしない。**

送り先: #537。`game.md` の ※9 からそこを指すようにした。
直すには「どこで待たせるか」の設計判断が要る（file-tree から game は見えない）。

### 機械にできるもの（`mechanization-backlog.md` 行き）

- **「`X` は `Y` でしか動かない」型の断定が、実装の `case` 集合とずれる。**
  C-01 と C-02 はどちらもこの形。`docsIdentifiers` / `ownedIdentifiers` は綴りの実在しか
  見ないので1つも拾えない。特定の欄を書く `case` の集合は reducer から機械で引ける
- **`docs/state-transitions/README.md` の摘要が、表本体の「埋まっていないセル」節と食い違う。**
  C-04 がこれ。`stateTransitionIndex` は行の存在しか見ていない

---

# r2（r1 の修正後）

観点は react / robustness。**r1 で処理済みのものは再掲していない。**

## react-reviewer（r2）

| #     | 段     | 何                                                            | 結果                                      |
| ----- | ------ | ------------------------------------------------------------- | ----------------------------------------- |
| R2-01 | MEDIUM | `useOverlayLayer` のモックが関数でなく数値を返す              | 直した                                    |
| R2-02 | MEDIUM | reveal の効果のコメントが、依存を替えた結果と食い違う         | 直した                                    |
| R2-03 | MEDIUM | `board-orientation.md` の欄の表から `reset_state` が落ちた    | 直した。**この形は3件目なので走査にした** |
| R2-04 | MEDIUM | `boardSeq: number \| null` の `null` が到達不能かつ意味が反転 | 直した（面を作らない）                    |

R2-01 は「いまは緑だが、分岐メニューを開くテストを1本足した瞬間に
`isTop is not a function` になる」という形。書いた人は Escape の扱いが壊れたと読む。

R2-03 は r1 の C-01 / C-02 と同じ「断定と `case` の集合がずれる」形の**3件目**で、
**2ラウンド続けて別の綴りで再出現した**（コード内の doc → 表のセル）。
two-strikes を超えたので `stateFieldWriters.test.ts` を入れた。
散文の断定は機械が見ていない——言い回しに当てると誤検知だらけになった。

### 所見にしなかったもの

`reset_state` で `boardSeq` を進める判断・`faceKey` の材料・`commitRef` のタイミング・
`boardSeqRef` と `stillHere` の突き合わせは、いずれも穴が見つからなかった。
`PendingDelete` は改名で確認が生き残るようになり、**`absPath` からの実質的な改善**。

## robustness-reviewer（r2）

### B2-01 [HIGH] 改名では消えなくなった代わりに、**同じ棋譜の開き直し**で書きかけが消える

→ **#538 として送った。** この PR では直さない。

合図を `boardSeq` に揃えた結果、面を畳む引き金が「改名」から「開き直し」へ移った。
畳まれると離脱時の保存が `"skipped"` を返し、面がもう外れているので断りも出ない。
踏むのは E16 のあとの復帰導線——**アプリ自身が「その棋譜をツリーでもう一度選んで
ください」と案内している操作**。

**片側だけを見て「悪化した」と読まないこと。** 変更前は、開き直しでも面が畳まれず
下書きが生き残り、門番（パス比較）も一致したので、**読み直したばかりの内容を
古い下書きが上書きしうる**形だった。どちらの側でも黙ることは変わっていない。

**止まること自体は正しい**（下書きは開き直す前の棋譜に対して書かれている）ので、
直すべきは「黙ること」。ただし段・文言・預け方は設計の選択で、少なくとも3通りある
（`notify` で出す／面ごとに預ける／鍵の作り方から変える）。#314 / #315 は
どちらも closed なので、いまこの穴を持つ issue が無かった。だから新しく立てた。

`KifuCommentNote` が指していた closed issue 2つも、そこで #538 に差し替えた。

### B2-02 [MEDIUM] 新しいテストが空振りしていた

「ノートへ渡す識別子は、改名で変わらない」が `before === after` しか見ておらず、
**`boardSeq={null}` に落とす退行（そのノートでコメントが1文字も保存できなくなる）を
1129 件全部が素通しした**。値そのものを言う形に直し、変異で落ちることを確かめた。

**他の新規テストは全て変異で落ちることを reviewer 側でも確認済み**（8通り）。
空振りしていたのはこの1本だけ。

### B2-03 [MEDIUM] 改名する画面の spec が #537 を指していない

`file-tree.md` の「既知の穴」から #262 を消した跡に #537 を足していなかった。
改名の仕様を確認しに来た人が辿り着けない。結果: 直した。

### 所見にしなかったもの

門番は緩んでいない。「パスが違って `boardSeq` が同じ」は `path_renamed` が来たときに限り、
それを撃てるのは橋が**載せられた回にしか控えない**参照と一致したときだけ。
閉じたあとの着地・`PendingDelete`・`renamed` の判定にも穴は無かった。

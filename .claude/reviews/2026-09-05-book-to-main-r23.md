# レビュー book-to-main ラウンド23

- 日付: 2026-09-05
- 範囲: r22 の所見7件を直した6コミット（`git diff 7145ddc7 208d4fe2`）
- 走らせた reviewer: rust / architecture / oss-hygiene
- 対象コミット: `208d4fe2`
- 前ラウンド: `.claude/reviews/2026-09-05-book-to-main-r22.md`

所見 **8件**（BLOCK 0 / **HIGH 2** / MEDIUM 6）。
推移は 22 → … → 3 → 6 → 7 → **8**。

**8件すべてが r22 の直しから出た。** 合流の解き方を直したら、その直し方に穴があった形。

**HIGH 2件は同じ1件の直し（AN-07）が作った。** `G` の衝突を直したつもりが、
**その記述自体が不正確**で、しかも同じ段落の他の主張が偽であることを確かめていなかった。

issue へ送るものは無かった（#290 へ送った根拠に誤りがあったので訂正を送った）。

---

## 所見

### AO-02 / AO-03 (HIGH, 1人): 採番の宣言が、同じ PR が入れた表に全項破られている

**この段落を足したのはこの PR 側で、その反例も同じ PR が持ち込んでいる。**

- 「他の判定表は F を使わない」→ `verify-gate-decision.md` が `F1` / `F2` を使う
- 「`G` は `book-key-failures.md`」→ `game.md`・`game-session.md`・
  `docs/spec/screens/engine-preset-dialog.md` も使う。**4つ**
- `7b3e7271` が足した「`G` は2つの表が使っている」も、**その時点で偽だった**

しかも**最も紛らわしい対が抜けていた** —— `game.md` と `game-session.md` は
`G0` / `G1` / `G2` の3つが完全に重なり、名前も似ていて、`game-session.md` 自身が
冒頭で「別物」と断っている。`book-key-failures.md` だけを否定する注は、
むしろ「他には無い」と読める。

**「「G0」とだけ書かない」も、書いた時点で既に守られていなかった**
（`session.rs` と `events.rs` に複数あり、r24 の AP-04 で数え直した）。

- 結果: 対応済み `72f49466`（**採番を1箇所に集める規約は無い**ので、無い規約を宣言する形をやめ、
  事実と「記号だけで書かない」だけを残した。規則が書いた瞬間に偽にならないよう、3箇所に表名を添えた）

### AO-06 (MEDIUM, 1人が実測): `gate` 種別が `ts` に完全に含まれ、同じ検査が2回走る

`gate` を出す case は1つだけなので `needs_gate=1` は常に `needs_ts=1` を伴い、
`npm run verify` は最後に `test:hooks` を走らせる。
**門番を触る全てのコミットで同じ 23.5 秒のスクリプトが2回走っていた。**

取りこぼしは無いが、`needs_gate` も `for kind` の `gate` 枝も挙動を変えない残骸になり、
足したコメントも「2つが別の検査」と読める。**AN-01 を塞ぐには `ts` だけで足りた**
——それは合流相手が取っていた形でもある。

- 結果: 対応済み `1d8daf22`（種別ごと落とした）

### AO-07 (MEDIUM, 1人が実測): 「シェルのコメントを落とす」が実装より広い

落とすのは**行頭の** `#` だけで、行末は残る。`docsIdentifiers` は保証を名乗っているのに、
**`#` の位置が行頭かどうかで結果が変わる。** 落とせない理由はある（`${kinds# }` / `$#`）。

**いま行末コメントは1つも無いが、その条件は何にも固定されていなかった**
（行末の扱いを変える変異を当てても12本とも緑）。

- 結果: 対応済み `8764e278`（落とせないことを doc に書き、テストで固定した）

### AO-08 (MEDIUM, 1人が実測): 版を固定した文書の免除に、テストが1本も無かった

`5d6b2ca0`（r22 の HIGH を直したもの）を**旧実装へ戻しても 262 本が全て緑のまま**だった。
r22 で HIGH として挙がった箇所そのものに、変異を止めるものが無かった。

- 結果: 対応済み `ce4462f3`（免除する側と免除しない側の両方を固定。
  **旧実装へ戻すと4本落ちることを確認した**）

### AO-01 (MEDIUM, 1人): 下限の説明が、先に読まれる2箇所で `#[test]` と綴ったまま

`017bcec9` は `count_tests` の doc を直したが、**ファイルの1行目と `CONTRIBUTING.md` の
索引はそのまま。** ラチェットに自分のテストが掛かるかを確かめる人が最初に読むのはその2つ。
**AN-04 の穴そのものが「`#[test]` という綴りで語られていた」ことだった。**

- 結果: 対応済み `b84467eb`

### AO-04 / AO-05 (MEDIUM, 1人が実測): r22 の報告書が引いた2つの数

- 「検査を4本から10本へ増やし」の **4本はこちら側の数**。`origin/main` は #385 の直前が1本
- 「この PR は `book.md` を**足す**」が偽。`origin/main` に既にあり、書き換えているだけ。
  **#290 へ送った所見の根拠に使っていた**ので、受け取った側が
  「到達不能な文書が1本増えた」を前提に優先度を判断してしまう

- 結果: 対応済み `89fafa39`（#290 へ訂正を送った）

---

## 重複・矛盾した所見

- **8件とも1人ずつ。** 3人が別々の範囲を見たので重なりが出ていない
- **AO-02 / AO-03 は「直した記述そのものが不正確」という、このループで最も重い形。**
  `G` の衝突を1文字ぶんだけ見て直し、**同じ段落の他の主張を確かめなかった**
- **AO-07 / AO-08 は同じ根**（r22 で直した2つの HIGH に、変異を止めるテストが無かった）。
  r22 は AN-03 / AN-05 についてはテストを足して変異も確かめたが、
  **AN-02 だけ足していなかった**
- 所見の水増しは無かった。3人とも「確認して所見が無かったもの」を明記していて、とくに:
  - **rust が `EXPECTED_MIN = 453` を機械で全部説明した** —— `src/` 379 は
    `unittests src/lib.rs` の実行本数と過不足ゼロ、`tests/` 74 との差 63 は
    `scanning/mod.rs` の 7 本 × `mod scanning;` を書いた 9 クレート、
    残り 10 は `benches/`（`count_all_tests` の外）。**453 は実測ちょうどで余裕0**
  - **architecture が r22 の4綴りを版宣言つきで実測し直し、4つとも拾われるように
    なったこと・上流2綴りが免除のままであることを表で示した**
  - **oss-hygiene が md 299ファイルのリンクを全解決し、この PR が壊したリンクが
    0件であることと、非マージ177コミットすべてが型に沿っていることを確認した**

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送った19件（再掲しない前提）
- `origin/main` が元から持っていた実装と doc の中身
- `docs/spec/screens/` の13面が現物の UI と合っているか
- `user_book1.db` の実測3行とピーク確保の実測

## lint / hook で強制できるもの

- **AO-07 / AO-08 は変異で確かめたテストが入った**（行末 `#` 2本、版宣言の免除 7本）
- AO-02 の型（表の記号の名前空間が衝突しないこと、宣言と実際の割り当てが一致すること）は
  `stateTransitionIndex` に足せば機械化できるが、**r22 で既に #351 の候補として送済み**。
  このラウンドでは足さない
- AO-04 / AO-05（報告書の中の数）は機械で防げない

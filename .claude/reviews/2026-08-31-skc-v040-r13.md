# レビュー skc-v040 ラウンド13

- 日付: 2026-08-31
- 範囲: ラウンド12の修正ぶん＋この PR の最終状態
- 走らせた reviewer: robustness（変異可）/ comment（読むだけ）/ **architecture（r2 以来）**

## 所見

### [BLOCK] R13-01 ラウンド12が「直した」と報告した4件が、コミットに1文字も入っていない

- reviewer: **robustness / comment が別々に `git show` で突き合わせた**。自分でも確認
- `line_count` の「文言は変わらない」/ テストの doc の経緯 /
  最終手段の配列 / 「ほとんど」に濁す — **4件とも未適用**
- 原因は編集の当て方。1本のスクリプトで複数箇所を書き換えようとして、
  **途中の照合に失敗して全体が中断し、1文字も書かれないまま**次へ進んだ
- **R10-07 と同じ形で2回目。今回は同じファイル内なので `git show --stat` でも見えない**
- とくに `line_count` の「どちらを採っても文言は変わらない」は
  **nom が読めなかった行の本文をそのまま引用する**ので偽。
  同点の扱いを単純化する変更がこの doc を根拠に入りうる

### [HIGH] R13-02 全角スペースだけのファイルが索引に入る

- reviewer: robustness（7件を実測）
- 空の記録が生まれる条件はクレートの `s.trim().is_empty()` で、
  `str::trim` は `U+3000` も `U+00A0` も落とす。
  こちらはバイトの集合（ASCII 空白と NUL）で数えていた

  ```
  全角スペース1文字（UTF-8 / Shift_JIS / UTF-16 / BOM 付き）-> Ok(moves=1)
  NBSP 1文字                                                  -> Ok(moves=1)
  ```

- **R10-03 → R11-04 → R12-02 と3回とも「実測した題材の集合」を門にしていた。4回目**

### [MEDIUM] R13-03 ファイルを開けない理由が CSA / JKF で生の英語

- reviewer: robustness（`chmod 000` で実測）/ comment
- r12 で日本語にしたのは `read_bytes` を通る KIF / KI2 だけ。
  CSA / JKF はクレートが自分で開くので `os error 13` がそのまま出る
- **`unreadable` を直接見るテストが1本も無く、変異が2つとも生き残る**

### [MEDIUM] R13-04 「8KiB を超えても後段が弾く」の後段が存在しない

- reviewer: robustness（実測）
- 空白だけの文字列はクレートが `Ok(moves=1)` を返す。
  8192バイトちょうどの空白ファイルが索引に入る

### [MEDIUM] R13-05 BOM の表が同じファイルに2つ手書きされている

- reviewer: architecture
- `strip_bom` と `declared_encoding` が同じ知識を200行離れて持つ。
  片方だけ動かすと片方が黙って古くなる

### [MEDIUM] R13-06 文字コードの機構だけが独立した部品なのに、モジュール境界が無い

- reviewer: architecture
- `Evidence` の「1箇所で作って持ち回る」を強制しているものが何も無い。
  **R6-07 で1度破られた実績がある**
- `search/kifu_encoding.rs` に切れば型検査が強制する

### [MEDIUM] R13-07 「形式 → 綴り手」の対応が3箇所に手書きされている

- reviewer: architecture
- **この PR が同じ編集を3回している**（`to_*_owned` → `try_to_*_owned`）。
  1箇所落とせば `ConvertError` が握り潰されたまま残り、型検査は落ちない
- #322 は `normalize()` の扱いの話で、綴り分けの重複は含んでいない

### [MEDIUM] R13-08 テスト材料の置き場が、中身と範囲で軸が食い違っている

- reviewer: architecture
- `test_support.rs`（クレート直下）と `search/test_kifu.rs`（`search` 配下）を
  「中身」で分けているのに、置き場は「範囲」で分けている

## 重複・矛盾した所見

- R13-01 は robustness と comment が別々に見つけた。**自分でも `grep` で確認した**
- **マージ可否で reviewer が割れた** — robustness は「R13-02 が利用者の被害に届くので
  現状ではマージしない」、architecture は「構造としては純増。マージしてよい」。
  **robustness を採った**（被害に届く側を優先）

## 範囲外

新しい issue は無し。R13-05 〜 R13-08 は architecture の所見で、
**この PR の範囲で直すには大きい**ものが混ざる（R13-06 のモジュール分割、
R13-07 の綴り分けの集約）。下の「修正結果」で扱いを分けた。

## 見ていない範囲

- **13ラウンド続けて `index_cache.rs` の符号化・復号本体を誰も見ていない**（#334 が一部）
- TS 側（この PR は未変更）
- `fs_scan.rs` の走査本体

## lint / hook で強制できるもの

- **`/review-fix` は「消したはずの文字列」を `grep` で確かめる** → R13-01。
  `--stat` では同一ファイル内の取りこぼしを拾えないことが確定した
- `cargo-mutants`（#332）→ R13-03 / R13-04。**9ラウンド連続の提案**
- BOM のリテラルを grep するラチェット → R13-05
- R13-02 は機械で拾えない

---

## 修正結果（`/review-fix`）

R13-01 〜 R13-05 を直した。R13-06 〜 R13-08 は**この PR では直さない**（下記）。
`npm run verify:rust` / `npm run verify` を通してある。テストは52件。

| 所見   | 結果                                                                       |
| ------ | -------------------------------------------------------------------------- |
| R13-01 | 4件とも当てた。**r12 の報告書に訂正を書き足した**                          |
| R13-02 | **門をクレートと同じ判断（復号してから `trim`）に変えた。** 題材13件で固定 |
| R13-03 | 形式ごとの分岐より前に1度だけ開く。4形式で回すテストを足した               |
| R13-04 | doc から「後段が弾く」を落とし、通す理由を正直に書いた                     |
| R13-05 | `Encoding::for_bom` に寄せて手書きの表を1つ消した                          |

### 題材の追いかけっこを終わらせた

R13-02 は**同じ穴の4回目**だった。毎回「実測した題材の集合」を門にしていたので、
題材を1つ足すだけで抜けた。**判断の根拠がクレート側（`s.trim().is_empty()`）にある以上、
こちらも同じ土俵で見るのが唯一ずれない。** 復号してから `trim` する形にした。

### 直さなかったもの

- **R13-06（文字コードの機構をモジュールに切る）** — 効果は認めるが、
  この PR は既に13ラウンド回っており、**構造の変更を今入れると
  ここまでの変異テストの結果が全て無効になる**。#332（`cargo-mutants`）を
  入れてから別 PR で切るほうが安全
- **R13-07（綴り分けの集約）** — #322 の判断（どの経路が正規化するか）と
  同じ場所を触るので、#322 を決めるときに一緒にやるのが筋。#322 に追記済み
- **R13-08（テスト材料の置き場）** — 好みの範囲に近く、R13-06 と一緒に動かすのが自然

### 分類ごとの推移

| 分類                           | r10 | r11 | r12 | r13   |
| ------------------------------ | --- | --- | --- | ----- |
| コメントと実装の食い違い       | 4   | 5   | 3   | 1     |
| 空振り / 題材の選び違い        | 1   | 3   | 1   | 2     |
| 自分が前のラウンドで入れた退行 | 5   | 5   | 7   | 4     |
| 読み取りそのものの穴           | 2   | 2   | 1   | 1     |
| **報告の不正確さ**             | 1   | 0   | 0   | **1** |

r13 は architecture を r2 以来で呼んだので、**構造の所見が4件**出た。
それらは r3〜r12 が1つのファイルを深く掘っている間、誰も見ていなかった層。

# レビュー 434-mixed-screen ラウンド3

- 日付: 2026-09-07
- 範囲: `git diff origin/main...HEAD`。r2 の13件を直した後の状態
- 走らせた reviewer: architecture / react / robustness / comment / oss-hygiene
- 対象コミット: `efd059d1`
- 前ラウンド: [r1](2026-09-07-434-mixed-screen-r1.md) / [r2](2026-09-07-434-mixed-screen-r2.md)

**所見は 24 件（BLOCK 2 / HIGH 2 / MEDIUM 19 / LOW 1）。件数が減っていない**（r1: 21 → r2: 19 → r3: 24）。
**r2 で入れた修正が生んだものが 11 件**（r3-01 / 02 / 03 / 04 / 05 / 06 / 07 / 08 / 09 / 11 / 12）。

## 所見

### BLOCK

#### r3-01 形式がずれた行を1回押すと、盤の棋譜が空になり、次の1手でディスクへ書き戻される（robustness）

**r2-06 で足した3つ目の条件が作った経路。`origin/main` には無い。**

1. `a.kif` → `a.csa` へ改名（Rust は通す。`mv.rs:47` は `is_kifu_file(&dest)` しか見ない）
2. `reconcilePathMutation` は形式を運ばないので `state.kifuFormat` は `"kif"` のまま。
   節の `kifuInfo.format` は `"csa"` → **3つ目の条件が偽になり、押すと開き直す**
3. `openKifuNode` が `parseKifuContentToJKF(KIFの本文, "csa")` を呼ぶ。**投げない**
4. `loadGame` は**成功する**（E16 にならないので断りも出ない）→ 盤が 0 手の空棋譜になる
5. 1手指すと `persistIfPossible` はパスが一致するので通り、**空の棋譜がディスクへ書かれる**

**実測で確認した**（自分でも別に確かめた）——2手の KIF を `csa` として読むと投げずに `maxTesuu = 0`。

さらに、この条件が防ぐと宣言している「`.csa` に KIF が入る」は**1件も防げていない**。
書き込み経路（`saveKifuToFile(jkf, activeKifuPath, kifuFormat)`）は `node.kifuInfo.format` を一度も見ない。
**押さなければ従来どおり壊れ、押すと棋譜を失う。**

- 結果: 対応済み（`8b020bf3` 3つ目の条件を落とし、関門を2条件へ戻した。`isOpenedInTree` の doc に「開き直しで直そうとするな」を書き、形式のずれは **#507** へ送った。**ユーザーの判断**——機構ごと落とす案もあったが、#434 の押し直しと r1-01 の復帰路を残す側を選んだ）

#### r3-02 橋のコメントが、改名で消えた識別子 `isActive` を指したまま（comment / oss-hygiene / react の3人）

`GameFileTreeBridge.tsx:42`。同じ関数の `:59` は `canSkipReopen` で書けている。
`FileNode` に `isActive` は無く、repo に残る `isActive` は全部「選択中の行」の意味の別物。
`dedupeKey` を残す唯一の理由が書いてある行なので、根拠に辿り着けない。

### HIGH

- 結果: 対応済み（`b96a3ba8`）

#### r3-03 `loadFailedAbsPath` に「試行の開始で消す」遷移が無く、古い印が次の要求を先回りして捨てる（architecture / react / robustness の3人）

**r2-04 で入れた印が「水位」であって「縁」ではない。** 落ちるのは `game_loaded` / `reset_state` だけなので、
失敗のあと**何も載らないまま**同じパスへ新しい要求を出すと、印は armed の直後からすでに立っている。

`entities/file-tree` は同じ族の印を `kifu_loading` で必ず落としている（`reducer.ts:8-9`）。こちらだけ落としていない。

react が実際に赤くなる並びを確かめた。**症状は #434 と同じ**——モーダルだけ閉じて何も起きない。
宣言の doc 自身が矛盾している（「載るまで残る」と「もう来ないと判断できる唯一の合図」は両立しない）。

- 結果: 対応済み（`1d9b4133`）。**reviewer 2人の推し（印を試行の開始で落とす）は採らなかった**——落とす形で reproducing test を書いたら赤のままだった。`openKifuNode` がディスクを読み終えるまで `loadGame` に届かないので、その窓で effect が前の回の印を読む。単調増加の `loadFailedSeq` を添える形にし、変異で落ちることを確かめた

#### r3-04 `loadFailedAbsPath` の生成側が1本も固定されていない。2つの変異が 866 本を素通りする（react）

| 変異                                                                   | 結果       |
| ---------------------------------------------------------------------- | ---------- |
| `game_loaded` の `loadFailedAbsPath: null` → `state.loadFailedAbsPath` | **866 緑** |
| `load_failed` を `return state;`                                       | **866 緑** |

r2 の報告書は「解除条件を潰す変異で落ちることを確認」と書いたが、**落ちるのは消費側の分岐だけ**。
消費側の test は `stub.loadFailedAbsPath` を手で置いているので、「載ったら印が落ちる」は誰も見ていない。
**CLAUDE.md の「落ちないなら、そのテストは別の理由で通っている」に当たる。**

### MEDIUM

- 結果: 対応済み（`945cbd1c` 4本足し、**指摘された2つの変異が実際に落ちる**ことを確かめた）

#### r3-05 `selectNodeByAbsPath` が `forceReopen` を守ることを見る test が無い（react）

`!options.forceReopen &&` を落とす変異が **866 緑**。呼び手側（引数を渡すこと）は固定されているが、
受け手側は `vi.fn()` に置き換わっていて誰も見ていない。**#434 の検索経路の直しは2段のうち前段だけが固定。**

- 結果: 対応済み（`f39e78ca` provider を実物で立てる test を新設。`readKifu` が呼ばれるかで見る。同じ変異で落ちる）

#### r3-06 `load_failed` の payload が `null` を取れ、「失敗が無い」と同じ値を書き込む（comment / architecture）

`loadFailedAbsPath: string | null` の `null` は「失敗していない」。`load_failed` が `null` を運べるので、
`loadGame(jkf, null)` が落ちると失敗を記録したつもりで「失敗なし」を書き、前の記録を消す。
現物の呼び手は必ず非 null なので**到達しない引数**。型が不変条件を表していない。

- 結果: 対応済み（`10d57c49` `loadGame` の宛先ごと `string` に狭めた）

#### r3-07 `load_failed` を挿し込んだせいで、`blockingWrites` を持ち越す理由が持ち主を失った（comment / architecture / oss-hygiene の3人）

`reducer.ts:114-126`。先頭2行は `reset_state` のための注意書きなのに、間に `load_failed` が入った。
いま `reset_state` は**理由の無いスプレッド例外**に見え、`{ ...initialGameState }` に畳めるように読める。
畳んだ瞬間、書き込み中にワークスペースを切り替えると `isLoading` が落ちて確認ダイアログが押し直せる事故が戻る。

- 結果: 対応済み（`f7de4cf0`）

#### r3-08 述語の持ち主にした ※7 が、`isOpenedInTree` を切り出す前の対比のまま（oss-hygiene）

`board-orientation.md:107-111`。`9ffa6c75` で「持ち主」に指名し `app-layout.md` から参照を飛ばしたが、
`ba9cc8b4` の切り出しに追随していない。`isOpenedInTree` は **docs 全体で0件**。
**r2-16 が「写経が N 本ある以上、条件が変わったときに全部は直らない」と書いた形が、条件を変えた当のラウンドで起きた。**

- 結果: 対応済み（`72299709` 2つの関門が何を見ているかを表にした。**現物を開いてから書いた**——2度腐らせた箇所なので）

#### r3-09 §4 の補集合が母集合を取り違えている（oss-hygiene）

`failure-surfacing.md:239`。母集合を「§2 の行」にしたので、(a) F-31 / F-32 が「出口が無い」側に入り
6行下の記述と矛盾、(b) 既に issue のある F-12a / F-2 が「issue になっていない」側に入る。
**r1-16 / r2-10 が範囲を補集合に倒したのは数え直しを増やさないためなのに、倒した先が別の数え直しを作った。**

- 結果: 対応済み（`131a7579` 母集合を抽出条件のほうに揃え、issue の出典も名指しした）

#### r3-10 `loadFailedAbsPath` が `game.md` に1度も現れない（oss-hygiene）

E16 行は `error` と `Err` の2つしか書いていない。消す側（E1 / E2）も台帳に無い。
r2 の計画が名指しした退行（落とし忘れ）が、**台帳にもテストにも受け皿を持たない**まま入っている。

- 結果: 対応済み（`689f9334`）

#### r3-11 r2 報告書の結果行3件が見出しを跨いだまま（oss-hygiene）

`r2.md:35-37` / `:60-62` / `:247-249`。**r2-14 の結果行が「所見の本文末尾に置いた」と宣言しているのに、
その書き戻しが自分の報告書では3件外している。** r2 唯一の BLOCK（r2-01）の対応状況が BLOCK の節から消えた。

- 結果: 対応済み（`1d43c289` 19件すべてが自分の節の中に結果を持つ状態にした）

#### r3-12 `app-layout.md` の見出しと本文が、モーダルを数えるかで再び食い違う（oss-hygiene）

`:119` は「ツリーの選択とモーダルだけ」、`:124` は「動くのはツリーの選択だけ」。
**r2-08 と同じ食い違いが向きを変えて残った**（前回は見出しが狭く本文が広い、今回は逆）。

- 結果: 対応済み（`5e3ffb4f` 総括を見出しの1度だけにした）

#### r3-13 `board-orientation.md` の注が ※1 → ※7 → ※2 の並び（oss-hygiene。r2-12 の再掲）

**`app-layout.md` から参照される注になったので、外から辿られる。** 昇順で走査した人は見つけられない。

- 結果: 対応済み（`3f2af58a` 注を昇順へ戻した）

#### r3-14 `game.md` の3箇所が `provider.test.tsx` を「`loadGame` だけ」と書いている（oss-hygiene。r2-13 の再掲）

同じファイルに `hasKifu` の4本があり、1本は `resetGame` を通して E2 を踏む。表の E2 は `✗`。
**`game.md` は `stateTransitionCells.test.ts` の走査対象外**なので、このずれを見ている機械は無い。

- 結果: **r4 へ送る**（計画のとおり）

#### r3-15 `dedupeKey` の採用基準が、唯一の実例とも、同じファイルのもう1つの基準とも合わない（comment。r2-17）

`types.ts:83` の「何十件も出る類にだけ付ける」と `:155` の「引っ込める通知には付ける」の
**どちらにも当てはまらない**のが唯一の実例。次に迷った人はどちらを読んでも「付けない」に着く。

- 結果: **r4 へ送る**（2ラウンド持ち越し）

#### r3-16 「門番が全部止める」の機構説明が4箇所に写経されている（comment）

`provider.tsx:73-78`（持ち主）/ `FileNode.tsx:39-41` / `GameFileTreeBridge.tsx:55-56` / `FileNode.test.tsx:118-122`。
**r2-16 で潰したのと同じ形。** comment は `FileNode` の doc を 11行 → 4行に縮める具体案を出している。

- 結果: **r4 へ送る**（計画のとおり）

#### r3-17 細い context の理由が4箇所にあり、`useGame` には TSDoc が1行も無い（comment。r2-18）

`useGame` は20箇所以上から呼ばれる主口なのに、**「重い／軽い」の案内が軽いほうにしか無い**。

- 結果: **r4 へ送る**（2ラウンド持ち越し）

#### r3-18 橋の `notify({...})` の引数リストに14行のコメントが挟まり、ファイルの半分がコメント（comment。r2-18）

72行中34行。`res.success` の分岐を追いたい読み手が文面の設計を読まされる。
**実際 r3-02（`isActive` の腐り）はこの塊の中で起きた。**

- 結果: **r4 へ送る**（2ラウンド持ち越し）

#### r3-19 `canSkipReopen` と `canSkipOpen` の差が2文字で、実際の差を名前が言っていない（comment）

他所から名指しで参照される（`GameFileTreeBridge` の2箇所、`app-layout.md`、`board-orientation.md` の ※7）のに、
参照のたびに所有者の但し書きが要る。**r3-02 はまさにその但し書きの位置で腐った。**

- 結果: **r4 へ送る**（r3-01 で `isOpenedInTree` の呼び手が1つになったので、r4 で決められる）

#### r3-20 「ツリーと盤を掛け合わせる」規則が、なお2つの層に置かれている（architecture）

`isOpenedInTree` で式の片割れは1箇所になったが、**規則そのもの**は `FileNode`（widget に直書き）と
`usePositionHitNavigation`（反転した真偽値を entity へ渡す）の2箇所。
**同じ根で3ラウンド目**（r1 で4件、r2 で4件、r3 で r3-01 / 02 / 19 / 20）。

- 結果: **r4 へ送る**。**3ラウンド目の機構。** ユーザーが「3つ目の条件だけ戻す」と決めたので機構は残る。r4 で出れば4回目

#### r3-21 対局者名の取り出しが3箇所に手書き（architecture。r1-10 から3ラウンド目）

`後手` を持たない棋譜で、ヘッダは「後手」・盤は「—」。どちらが正かを決めている場所が無い。
**この PR で「どの棋譜から取るか」の決定が2箇所に増えた**（片方だけ戻すと #434 が再発する）。

- 結果: **r4 へ送る**（r1 から3ラウンド持ち越し。**r4 で先に取る**）

#### r3-22 `loadGame` の `Err` が自由文字列のまま誰にも読まれない（architecture。r1-11 から3ラウンド目）

`notify()` の呼び出しは**リポジトリ全体でこの1箇所だけ**。2件目を足す人がこの形を写すと、
段と文言の決め方が呼び出し側ごとに分かれた状態から始まる。

- 結果: **r4 へ送る**（同上）

#### r3-23 barrel の採用基準が2スライスで逆を向き、この PR の追加が両方の側に落ちた（architecture）

`SelectNodeOptions` は**スライスの外から名前で参照されていない**のに barrel に載せた（`file-tree` の基準違反）。
逆に `isOpenedInTree` の引数型 `OpenedKifuFields` は載っていないので**外から名指しできない**。
`entities/game` は `export *` 3本なので、`useLoadedKifuPath` と `loadFailedAbsPath` は判断を通らずに公開面へ出た。

- 結果: **一部対応**（`8b020bf3` で `isOpenedInTree` を barrel から下ろした——呼び手が1つになったため。`SelectNodeOptions` と `entities/game` の `export *` は **r4**）

#### r3-24 [LOW] F-31 の測定印だけ、句点との間に半角空白が入る（oss-hygiene）

他の16件は句点に直付け。ラチェットを書くときに綴りが分かれる。

- 結果: 対応済み（`d963fe45`）

## 重複・矛盾した所見

- **r3-02 は3人、r3-03 は3人、r3-07 は3人**が独立に指摘
- **r3-01 と r3-21 は逆を向いている。** r3-01 は「3つ目の条件を落とせ」、r3-21 は「射影を1箇所に集めろ」。
  どちらも「同じ判断が2箇所にある」ことへの処方だが、**片方は機構を減らし、片方は機構を足す**
- **r3-03 の直し方が2案。** 印を試行の開始で落とす（architecture / react）vs 連番か明示クリア（robustness）。
  **前者を試したが不成立、後者（連番）を採った。** 落とす形で reproducing test を書くと
  赤のまま——`openKifuNode` がディスクを読み終えるまで `loadGame` に届かないので、
  その窓で effect が前の回の印を読む。→ r3-03 の結果
- **r3-08 / r3-09 / r3-11 / r3-12 は r2 の直しの直し残し。** 同じラウンドで直した箇所が、次のラウンドで再び出た

## 見ていない範囲

- **Rust（`src-tauri/`）は3ラウンドとも誰も見ていない。** 差分が無いため
- **SCSS・見た目・実機は3ラウンドとも誰も見ていない。** 通知モーダルの重なり順・フォーカストラップ・
  110文字の本文が `danger` の modal に入ったときの高さ
- `NotificationLayer` が modal を1枚しか描かないことによる待たせ合い
- `failure-surfacing.md` の §2 の表のうち、F-31 / F-32 以外の行と現物の突き合わせ
- 5人のうち `npm run verify` を通したのは react だけ（93 files / 866 緑）

## lint / hook で強制できるもの

- **r3-06 は tsc で閉じる**（`load_failed` の payload を `string` に狭める）
- **r3-02 / r3-08 は走査で拾える。** `src/` と `docs/` のコメント中の `` `X` の `y` `` 形式の識別子が
  実在するかを見る。**`docsIdentifiers.ts` の `IDENTIFIER` は下線を含む綴りしか拾わない**ので、
  `isActive` → `canSkipReopen` のような改名は化石を残したまま緑で通る（今回の実例）
- **注の番号の健全性**（r3-13 / r2-12 / r1-17 と3ラウンド連続）: 昇順・参照と定義の対応を見るラチェット
- **報告書の結果行の位置**（r3-11）: `#### rN-NN` の節が次の見出しまでに `- 結果:` を含むこと。
  oss-hygiene は実際にこの走査で見つけた（r1 は0件、r2 は3件）
- **測定印**（r3-24）: `（YYYY-MM-DD / …）` で終わること。trim してから見ること
- **機械では防げないもの**: r3-01 / 03 / 04 / 05 / 07 / 09 / 10 / 15〜r3-23

## 修正計画（r3 → r4）

- 結果: 対応済み（`d963fe45`）

### 対象そのものを疑った結果（手順4）

**3ラウンド続けて件数が減らず、同じ機構（関門）に BLOCK が出続けた**ので、
手順4 のとおり対象を疑い、**ユーザーに判断を持ち込んだ**（`/implement` 手順7「直し方に判断が要る」）。

決まったこと:

- **r2-06 で足した3つ目の条件（`kifuFormat` / `jkfData`）だけ戻す。** 関門は
  「ツリーと盤の両方がそのパスを指す」の2条件へ。#434 の押し直しも r1-01 の復帰路も残り、
  棋譜を失う経路だけが消える
- **改名で形式がずれる不具合は `origin/main` から在るもの**なので、この PR では直さず issue にする
- **ループは指摘ゼロまで回し切る**

### 束

- **関門**: r3-01 → r3-19 → r3-20。r3-01 で3つ目の条件が消えると `isOpenedInTree` の呼び手が1つになるので、
  r3-19（名前）と r3-23（barrel）の前提が変わる。**r3-01 を先に**
- **`loadFailedAbsPath`**: r3-06（型を狭める）→ r3-03（印を試行の開始で落とす）→ r3-04（生成側の test）。
  型 → 振る舞い → 固定 の順。**r3-04 を先にすると、直す前の振る舞いを固定してしまう**
- **r2 の直し残し**: r3-08 / r3-09 / r3-11 / r3-12。同じ箇所を2度目に触るので、**今度は現物を開いて確かめてから書く**

### このラウンドで直すもの

| 順  | 所見  | なぜこの順か                                           | この直し方で壊しうるもの                                                                                                                                                                                                                  |
| --- | ----- | ------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | r3-01 | **棋譜を失う。** 他の何より先                          | 3つ目の条件が消えるので、**改名で形式がずれた行は押しても開き直らない**（`main` と同じ）。`.csa` に KIF が書かれる不具合はそのまま残る——issue に送る。`FileNode.test.tsx` の形式ずれの1本は**消す対象の振る舞いを固定している**ので落とす |
| 2   | r3-06 | tsc で閉じる。3の前に型を決める                        | `loadGame` の第2引数を `string` に狭めると、`absPath: null` で呼ぶ経路が型で塞がる。**いま呼び手は橋の1つだけで必ず非 null** だが、将来パス無しの読み込みを足す人はここで止まる（それが狙い）                                             |
| 3   | r3-03 | 失敗経路。**r2-04 が作った逆向きの沈黙**               | 印を試行の開始で落とすので、**「前の失敗が残っているうちに読む」形の判定が全部変わる**。`usePositionHitNavigation` の解除条件は「いまの試行が落ちた」と同義になる                                                                         |
| 4   | r3-04 | 2と3で振る舞いが確定してから固定する                   | 無し（test の追加）。ただし**変異を2つとも当てて落ちることを確かめる**こと——r2 でそれを怠った                                                                                                                                             |
| 5   | r3-05 | 4と同じ性質（受け手側が未固定）                        | `entities/file-tree` の provider を実物で立てる test なので、`api` のモックが要る。`workspaceGuard.test.tsx` の組み方に倣う                                                                                                               |
| 6   | r3-02 | BLOCK だが1語の差し替え。3の後だと文面ごと縮むので後ろ | 無し                                                                                                                                                                                                                                      |
| 7   | r3-07 | コメントの持ち主                                       | 無し                                                                                                                                                                                                                                      |
| 8   | r3-08 | ※7 が述語の持ち主。r3-01 で述語が変わるので**1の後**   | `app-layout.md` が ※7 を参照しているので、両方を同時に見て書くこと                                                                                                                                                                        |
| 9   | r3-09 | §4 の母集合                                            | 「§4 の抽出条件を満たす行のうち…」と書くと、**抽出条件そのものが唯一の出典になる**。条件を変えたらここも変わる                                                                                                                            |
| 10  | r3-10 | `game.md` の E16                                       | E16 の行に欄を足すので、`stateTransitionCells.test.ts` の走査に掛かるか確かめる                                                                                                                                                           |
| 11  | r3-11 | 報告書の結果行3件                                      | 無し                                                                                                                                                                                                                                      |
| 12  | r3-12 | 見出しと本文                                           | **総括を2度書かない**——本文側の「動くのは〜だけ」を落とす                                                                                                                                                                                 |
| 13  | r3-13 | ※7 の位置。8の後                                       | ※1 からの参照が前方参照になる（他の表にも前例がある）                                                                                                                                                                                     |
| 14  | r3-24 | 空白2つ                                                | 無し                                                                                                                                                                                                                                      |

### 直さないもの

| 所見                                                            | 行き先 | 理由                                                                                                                      |
| --------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------- |
| r3-14（`game.md` のテスト記述が過小）                           | **r4** | E2 のセルを `✗` → `△` にする判断が要る。r3-10 で同じファイルを触るので、続けて取ると差分が読めない                        |
| r3-15（`dedupeKey` の採用基準）                                 | **r4** | `shared/lib/notification` の doc。2ラウンド持ち越し                                                                       |
| r3-16 / r3-17 / r3-18（コメントの量と重複）                     | **r4** | r3-02 / r3-03 で橋と `FileNode` の文面が動くので、縮めるのはその後                                                        |
| r3-19（`canSkipReopen` / `canSkipOpen`）                        | **r4** | r3-01 で呼び手が1つになってから決める                                                                                     |
| r3-20（規則が2層にある）                                        | **r4** | **3ラウンド目の機構。** ユーザーが「3つ目の条件だけ戻す」と決めたので、機構自体は残る。r4 で改めて出るなら4回目として扱う |
| r3-21（対局者名） / r3-22（`Err` の code 化） / r3-23（barrel） | **r4** | r1 から3ラウンド持ち越し。**r4 では優先する**                                                                             |

**10件を r4 へ送る。** うち r3-21 / r3-22 は r1 から3ラウンド持ち越しなので、r4 で先に取る。

### 次ラウンドの焦点

1. **3つ目の条件を落とした結果**（順1）。改名で形式がずれた行が押しても開き直らないこと、
   `isOpenedInTree` の呼び手が1つになったこと、doc がその形になっているか
2. **`loadGame` の引数を狭めた影響**（順2）。`game_loaded` の payload と `loadedAbsPath` の型
3. **印を試行の開始で落とした結果**（順3）。**正常に載る回で要求が捨てられていないか**（逆向きの退行）
4. **足した test が変異で落ちるか**（順4・5）。r2 で「確認した」と書いて実際には消費側しか見ていなかった
5. **r2 の直し残しを2度目に触った箇所**（順8〜12）。今度こそ現物と合っているか
6. **同じ機構に4ラウンド目の所見が出るか**（r3-20）

### 検証の見積り

14件。TS のみが8件（順1〜7・11）、`docs/state-transitions/` を触るのが4件（順8・10・13・14）で **TS + Rust**、
`docs/spec/` の順12 は TS のみ。**Rust は増分でもベンチに数分**掛かるので、順8以降は1件ずつ走らせる。

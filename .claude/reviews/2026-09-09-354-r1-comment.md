# レビュー: #354 r1（コメント / 命名）

対象: `git diff origin/main...HEAD`（3コミット）
基準: `CONTRIBUTING.md`「コメントの書き方」

---

### [BLOCK] `session.rs` の module doc が、この変更で嘘になった

- 場所: `src-tauri/src/engine/game/session.rs:16-17`
- 根拠:

```rust
//! # 責任の切れ目
//! ...
//! **詰み・千日手・持将棋・最大手数の判定は、まだどちらにも無い** → #354。
//! この層はそれらを持たないので、指した後の局面が終局かどうかを自分では知れない。
```

同じ節の写しである `docs/state-transitions/game-session.md:22-24` は、この PR で
「詰み・千日手・持将棋・最大手数の判定も**フロントにある**」へ書き換わっている。
Rust 側だけが古い断言のまま残った。

- なぜ問題か: この module doc は表（`game-session.md` の「責任の切れ目」）と対になっていて、
  Rust を読む人が最初に当たる面。次に `session.rs` を触る人は「終局判定はまだ誰も持っていない」
  という前提で読み、`AwaitingRuling` を待たずに `go` を出す設計変更（Rust 側で終局を持つ）を
  検討し直すことになる。加えて `→ #354` はこの PR で閉じる issue を指すので、
  開いても「実装されていない」としか書いていない番号が残る。
- 直し方: `game-session.md:22-32` と同じ内容に揃える。すなわち
  「判定はフロント（`src/entities/game/lib/gameOutcome.ts`）にある。この層は持たないので
  終局を自分では知れない」。`→ #354` は落とす（残す理由が無い）。

---

### [HIGH] 「持将棋の判定は `judgeGameOutcome`」が3箇所に書かれているが、実装に無い

- 場所:
  - `src/entities/game-session/index.ts:9-11`
  - `src/entities/game-session/api/rust-types.ts:155-156`
  - `docs/state-transitions/game-session.md:22-24`
  - （波及）`src/entities/game/lib/ruleColor.ts:5-7`
- 根拠:

```ts
// index.ts:9-11
 * 詰み・千日手・持将棋・最大手数の判定は `entities/game` にある
 * （`lib/gameOutcome.ts` の `judgeGameOutcome`）。`moveDecided` を受けたら
 * それに掛けて、`continueGame` と `endGameByRule` のどちらを返すかを決める。
```

```ts
// rust-types.ts:155-156
 * - `rule` — `endGameByRule`。詰み・千日手・持将棋・最大手数・反則。
 *   その判定は `entities/game` の `judgeGameOutcome`（`lib/gameOutcome.ts`）。
```

`gameOutcome.ts` が持つ持将棋は `tryRule` だけで（`gameOutcome.ts:135-137`）、
27点法・24点法は `judgeDeclaration`（`jishogiDeclaration.ts:47`）にある。
同じ PR の `docs/spec/features/game-play.md:91-94` は逆のことを明言している——
「**持将棋の27点法と24点法はここに入らない。**…宣言の可否は `judgeDeclaration` が別に持つ」。
`judgeGameOutcome` は `judgeJishogiDeclaration` を import すらしていない。

- なぜ問題か: 具体的な失敗の筋道がある。`DEFAULT_GAME_RULES.jishogiRule` は
  `"general27"`（`gameRules.ts:39`）。`index.ts` の指示どおりに
  「`moveDecided` → `judgeGameOutcome` → `continueGame` / `endGameByRule`」だけを
  配線した画面を書くと、既定設定の持将棋は**どの経路でも終局しない**。
  `judgeGameOutcome` は `general27` のとき常に `null` を返し、対局は最大手数
  （既定 1000 手）まで続く。書いた人は「持将棋の判定は掛けた」と思っている。
  `ruleColor.ts:6-7` も「千日手と持将棋の点数は tsshogi が持つので（`gameOutcome.ts` の冒頭）」と
  `gameOutcome.ts` を出典に挙げているが、点数を使うのは `jishogiDeclaration.ts` の側。
- 直し方: 3箇所とも「`judgeGameOutcome` が返すのは詰み・手詰まり・千日手・連続王手・
  トライルール・最大手数。27点法／24点法は宣言の規則なので `judgeDeclaration`
  （`lib/jishogiDeclaration.ts`）が別に持ち、利用者の宣言操作から呼ぶ」と書き分ける。
  `ruleColor.ts:6-7` の出典は `jishogiDeclaration.ts` へ差し替える。

---

### [MEDIUM] 「shogi.js 側を選べないのは」の理由が、結論と逆を支えている

- 場所: `src/entities/game/lib/gameOutcome.ts:13-18`、`docs/spec/features/game-play.md:103-107`
- 根拠:

```ts
 * **ルールの持ち主が2つに割れている。** 合法手の生成は shogi.js、千日手・
 * 連続王手・持将棋の点数は tsshogi。どちらか一方では賄えない——tsshogi は
 * 合法手を生成せず、shogi.js は千日手も点数も持たない。
 * shogi.js 側を選べないのは、盤の表示（移動可能マスの強調・成り選択）が既に
 * それを使っていて、重ねると合法手判定が2実装になるため
```

- なぜ問題か: 「盤の表示が既に**それ（= shogi.js）**を使っている」は、shogi.js を
  **捨てられない**理由であって、選べない理由にはならない。「重ねると合法手判定が2実装になる」も、
  合法手生成を tsshogi 側にも置く場合の話なので、支えているのは
  「**tsshogi 一本に寄せる選択ができない**」。書かれているのはその逆で、
  同じ段落の直前（「shogi.js は千日手も点数も持たない」）と読み合わせると、
  読み手は「では何を選んだのか」を決められない。`CONTRIBUTING.md`「〜だから と書いたら、
  その条件式がコードのどの行かを指せること」に照らして、この理由は指す先が無い。
  `session.rs:12-14` と `game-session.md:17-20` にある元の言い回し（「盤の表示にも要るので
  **消せず**、Rust に重ねると合法手判定が2実装になる」）は向きが正しい。
- 直し方: 主語を入れ替える。「tsshogi 側へ一本化できないのは、盤の表示（移動可能マスの強調・
  成り選択）が既に shogi.js を使っていて消せず、合法手生成を重ねると2実装になるため」。
  doc（`game-play.md:106`）も同じ文に揃える。

---

### [MEDIUM] `isOnTrySquare` の「玉がそこへ動けたなら」が、この関数のどの行も指していない

- 場所: `src/entities/game/lib/gameOutcome.ts:80-89`
- 根拠:

```ts
/**
 * トライルールの到達点は相手玉の初期位置。先手なら5一、後手なら5九。
 *
 * **着いた時点で成立する。** 玉がそこへ動けたなら王手はかかっていないので、
 * 「取られない」ことを別に確かめる必要はない
 */
function isOnTrySquare(shogi: Shogi, color: Color): boolean {
  const piece = shogi.get(5, color === Color.Black ? 1 : 9);
  return !!piece && piece.color === color && piece.kind === "OU";
}
```

- なぜ問題か: この関数は「玉がそこへ動けた」ことをどこでも見ていない。見ているのは
  5一 / 5九 に自分の玉が**在る**ことだけで、直前の手が玉の移動だったかは判定に入らない。
  保証を実際に立てているのは `buildRecord`（`gameOutcome.ts:100-105`）で、
  `record.append(move)` が tsshogi の検証（`Position.doMove` → `isValidMove`）を通すため、
  `lastMover` の手番終了時に `lastMover` の玉が王手されている局面は組み立てられない。
  いまの書き方だと、`judgeGameOutcome` を「1手ごと」以外の使い方（任意の `usiMoves` を
  渡して途中局面を裁定する）へ広げた人が、この理由をこの関数の中に探して見つけられない。
- 直し方: 理由の出どころを綴りで指す。「玉が取られない確認は要らない——`buildRecord` が
  `record.append` で合法手だけを積むので、`lastMover` の手番直後に `lastMover` の玉が
  王手されている局面は作れない」。

---

### [MEDIUM] `special-moves.md` の新しい表が、同じ表の中で `JISHOGI` の意味を2通りに使っている

- 場所: `docs/spec/features/special-moves.md:86-96`
- 根拠:

```
| トライルール     | `JISHOGI`                        |
| 最大手数         | **無い**（引き分けの綴りが無い） |
```

`JKFSpecial`（`src/entities/kifu/model/jkf.ts:31-40`）は
`TORYO` / `JISHOGI` / `SENNICHITE` / `TSUMI` / `FUZUMI` / `TIME_UP` / `CHUDAN` / `KACHI` の8つ。

- なぜ問題か: トライルールは `judgeGameOutcome` が `winner` を付けて返す**勝敗の付く**終局
  （`gameOutcome.ts:136`）。それを `JISHOGI` に落とすなら `JISHOGI` は勝敗を表せる綴りという
  読みになるが、同じ表の最下行は「最大手数 = 引き分け」に対して「引き分けの綴りが無い」と書いていて、
  `JISHOGI` を引き分けの綴りとして数えていない。どちらの読みが正かが表から決まらないので、
  この表を頼りに変換を書く人は、トライルールの勝ちを引き分けとして棋譜に残すか、
  逆に最大手数に `JISHOGI` を当てるかのどちらかへ倒れる。
  `KACHI`（宣言勝ち。上の表 `:75` で `DeclareWin` に当てている）が候補として一度も出てこないのも
  同じ穴の表れ。
- 直し方: `JISHOGI` を「引き分けとしての持将棋」と定めるか「持将棋による終局一般」と定めるかを
  1行で書き、その定義でトライルールと最大手数の両方を埋め直す。前者なら
  トライルールは `KACHI` か「無い」になる。

---

### [MEDIUM] 閉じる issue（#354）を、残る穴の追跡番号として使っている

- 場所: `docs/spec/features/game-play.md:74`、`docs/spec/README.md:56`、
  `src-tauri/src/engine/utils.rs:136`
- 根拠:

```
### 終局判定を呼ぶ側が居ない（#354）
```

#354 の題は「対局の終局判定（詰み・千日手・持将棋・最大手数）がどこにも実装されていない」で、
この PR がそれを実装する。同じ PR で `docs/spec/README.md:56` は対局の追跡を
`#354 ほか` → `#532 ほか` へ動かしているが、#532 は
「エンジンの bestmove win を検算していない」で、UI が無いことは扱っていない。
`game-play.md:3` は依然 `#354` を先頭に置いている。

- なぜ問題か: #354 が閉じた後、`game-play.md:74` の節は「閉じた issue が追跡している開いた穴」になる。
  読み手は番号を開いて「実装されていない」という既に解決済みの本文を読むので、
  節の題（呼ぶ側が居ない）と番号の中身が一致しない。`README.md:56` と `game-play.md:3` が
  別の番号を先頭に置いていることで、どちらが対局の入口かも決まらない。
  `utils.rs:136` の `（#354）` も、`detail` の産地が増える条件を指す番号として宙に浮く。
- 直し方: 「呼ぶ側が居ない（UI）」の issue を立てて番号を差し替える（無ければ
  `game-play.md:3` の既存番号のうち UI を扱うものへ寄せる）。`README.md:56` と
  `game-play.md:3` の先頭番号を同じものに揃える。`utils.rs:136` は
  「棋譜ファイル由来の文字列が流れ込む口ができたら」という条件だけを残し、番号を落とす
  （`CONTRIBUTING.md`「番号を付けられないものは TODO ではなく願望」の裏返しで、
  内容の合わない番号は指し先として使えない）。

---

### [MEDIUM] 内部に厚い doc が付き、スライスの公開面には1行も無い

- 場所: `src/entities/game/index.ts:1-3`（対して `src/entities/game/lib/gameOutcome.ts:1-19`、
  `jishogiDeclaration.ts:1-13`、`gameRules.ts:1-9`）
- 根拠:

```ts
export * from "./model/provider";
export * from "./model/useGame";
export * from "./model/types";
```

`judgeGameOutcome` / `judgeDeclaration` / `GameRules` / `DEFAULT_GAME_RULES` は
どれも barrel に載っていない。一方 `entities/game-session/index.ts:9-11` は
外の読み手に対して「`moveDecided` を受けたらそれに掛けて…どちらを返すかを決める」と
呼び方を指示している。

- なぜ問題か: 指示どおりに書こうとした画面（`features/` 側）は
  `@/entities/game/lib/gameOutcome` を深く読むことになる。`sliceBarrels` は
  「barrel が公開しているモジュール」しか見ないので、公開していないこの経路は緑のまま通り、
  スライス境界を通らない依存が最初の呼び出し元と一緒に入る。
  `CONTRIBUTING.md`「TypeScript: 外部から使われる関数・型に TSDoc を」の趣旨とも逆で、
  厚い説明が全部スライスの内側にある。
- 直し方: どちらかに決めて書く。(a) barrel に `judgeGameOutcome` / `judgeDeclaration` /
  `GameRules` / `DEFAULT_GAME_RULES` を載せ、`entities/game/index.ts` の冒頭に
  「ここは棋譜の状態とルールの判定を持つ」と1行置く（`barrelConsumers` は件数ラチェットなので
  最初の呼び出し元と同じコミットで足す）。(b) まだ公開しないなら、
  `game-session/index.ts:9-11` に「公開面にはまだ載っていない。呼ぶ画面と一緒に barrel へ出す」と
  書き足して、読み手が深い import へ倒れないようにする。

---

### [MEDIUM] 同じ「合法手」に2つの綴りがあり、この PR で差が広がった

- 場所: `src/entities/game/lib/moveValidation.ts:216` / `:257` / `:268`、
  `src/entities/game/lib/shogiMoveValidator.ts:35-38`
- 根拠:

```ts
function* generateLegalMoves(shogi: Shogi, color: Color): Generator<ShogiMove>
export function getAllPossibleMoves(shogi: Shogi, color: Color): ShogiMove[] {
  return [...generateLegalMoves(shogi, color)];
}
export function hasLegalMove(shogi: Shogi, color: Color): boolean
```

- なぜ問題か: 同じ集合が `PossibleMoves` と `LegalMoves` の2つの名前で呼ばれていて、
  しかも片方はもう一方の spread でしかない。`getAllPossibleMoves` の doc（`:253-256`）が
  「王手放置チェックは含む」とわざわざ断っているのは、名前が「possible」だから断りが要るという
  ことで、`CONTRIBUTING.md`「名前で表せるものはコメントにしません」に当たる。
  同じ PR で `hasLegalMove` / `generateLegalMoves` が入ったので、いま3つの関数のうち
  1つだけが別語彙になっている。
- 直し方: `legal` を正とする。`getAllPossibleMoves` → `getAllLegalMoves` に改名し、
  `:253-256` の doc から「王手放置チェックは含む」を落とす（名前が言うので）。
  呼び出し元は `shogiMoveValidator.ts:6,38`、`moveValidation.ts:194`、
  `__tests__/moveValidation.test.ts` の3ファイルだけ。
  併せて `jishogiDeclaration.ts` の `judgeDeclaration` / `DeclarationJudgment` /
  `DeclarationFailure` は、同じファイルが import している tsshogi の
  `judgeJishogiDeclaration` と1文字違いで別物になっている（`:19` と `:47`）。
  こちらはモジュール名（`jishogiDeclaration.ts`）が持っている文脈なので現状のままでよいが、
  barrel へ出すなら `judgeJishogiDeclaration` と衝突するので、出す時点で
  `judgeEnteringKingDeclaration` などへ寄せること。

---

## 見ていない範囲

- ロジックの正しさ（詰み・千日手・トライの規則そのもの、`judgeGameOutcome` の判定順が
  規則として妥当か、tsshogi の `repetition` / `perpetualCheck` の意味論）。
  検証したのはコメントの主張と実装・外部ライブラリの型定義の食い違いだけ。
- `src/entities/game/lib/__tests__/zzperf.test.ts` は**追跡外**（`git status` で `??`）なので
  差分に入っていない。中身のコメントは見ていない。コミット前に消すか、
  名前と置き場を決めること（`zz` の接頭辞は他に例が無い）。
- `src-tauri/` のうち `session.rs` / `types.rs` / `utils.rs` / `commands/game.rs` の
  該当行だけを読んだ。それ以外の Rust の doc は見ていない。
- `docs/spec/screens/` 配下と `docs/state-transitions/game-session.md` の表本体
  （状態・遷移のセル）は見ていない。
- SCSS・UI は差分に無いので見ていない。
- `npx vitest run src/__tests__` は 31 ファイル / 328 件が緑（この所見はどれも機械に
  掛かっていない）。`verify:rust` は流していない。

## lint / hook で強制できるもの

- **`session.rs` の腐りは機械化できる。** `docs/state-transitions/game-session.md` の
  「責任の切れ目」節と `src-tauri/src/engine/game/session.rs` の `# 責任の切れ目` module doc は
  互いの写しなのに、突き合わせる検査が1つも無い（`state_transition_cells` はセルの ✓ だけ、
  `search_doc_names` は識別子だけを見る）。最小の形として「表の節に出る
  `src/**` のパス（`gameOutcome.ts` など）が、対応する Rust の module doc にも出ること」を
  数えるだけでも、今回の「片方だけ直した」は落ちる。
- **閉じた issue 番号の参照。** `#N` を含む doc / コメントに対して `gh issue view` で
  state を引き、`CLOSED` を指す `→ #N` / `（#N）` を落とす検査は書ける
  （`TODO(#N)` は `CONTRIBUTING.md` が別枠として認めているので対象外にする）。
  ネットワークに依るので CI 限定になる。
- **barrel に無い綴りを doc / コメントが呼び方として名指すこと。** 今回の
  `game-session/index.ts:9-11` → `judgeGameOutcome` のように、他スライスの
  barrel に載っていない識別子を「呼べ」と書いている箇所は、`docsIdentifiers` の
  親戚として静的に拾える（`ownedIdentifiers` が「`X` の `y`」を見ているのと同じ要領）。
- 上記以外（理由の向き、表の中の意味の揺れ、命名の統一）は人が読むしかない。

# レビュー skc-v040 ラウンド7

- 日付: 2026-08-31
- 範囲: ラウンド6の修正ぶん＋この PR の最終状態
- 走らせた reviewer: comment / robustness / rust
- 前のラウンド: `-r1.md` 〜 `-r6.md`

**r6 で文字コードの推測を消したので、r3〜r6 を占めていた山は消えた。**
残ったのは理由の選び方と、テストの守り。

## 所見

### [HIGH] R7-01 切れた ISO-2022-JP に「切れている」が届かない

- 場所: `search/kifu_reader.rs` の `describe`
- reviewer: robustness / rust（どちらも実測）
- r6 で足した `declared_but_garbled` の腕は `other` の中にあり、
  **クレートが `Kif` を返した時点で到達しない**
- ISO-2022-JP の本文はすべて 0x80 未満なので、クレートの Shift_JIS 復号は
  誤りを出さず必ず `Kif` を返す。つまり**印が ISO-2022-JP のとき、
  r6 で足した欄は構造的に死んでいた**
- 出ていたのは `\x1b$B<j9g3d...` という化けた行。
  **R3-01 で塞いだ故障が、切れたファイルに限って戻っていた**

### [HIGH] R7-02 BOM の無い UTF-16 が行番号ごと捨てられ、能動的に誤った案内が出る

- 場所: `search/kifu_reader.rs` の `can_be_named` / `describe`
- reviewer: robustness（実測）
- r6 は「失うのは文字コード名だけ」と書いたが、**何行目が読めないかも捨てて**
  「文字コードが壊れているか、棋譜ではないファイル」と出していた。
  中身は正しい棋譜で、文字コードも壊れていない

### [BLOCK] R7-03 `can_be_named` の doc に、r6 で消した「NUL の並び」が印として残っている

- 場所: `search/kifu_reader.rs`
- reviewer: comment
- `grep NUL` で消えた機能の生き残りに見える唯一の行。
  読み手は「印は2種類あるはず」と読んで**NUL の判定を書き足す**。
  **消した機能へ戻る導線が doc に1本だけ残っていた**

### [MEDIUM] R7-04 変異が5件生き残る

- 場所: `search/kifu_reader.rs` / `kifu.rs`
- reviewer: robustness / rust（どちらも変異を当てた）
- `declared_but_garbled` を常に真 / エスケープを先頭3バイトだけ /
  `\x1b(J` を削る / `Normalize` の早期 return を削る / lossy を削る
- ISO-2022-JP の題材は**エスケープがバイト0にある**（実物の KIF は ASCII の見出しから始まる）

### [MEDIUM] R7-05 書き込み経路は `.jkf` を空ファイルにしても緑

- 場所: `kifu.rs`
- reviewer: robustness（変異を当てた）
- `assert!(!on_disk.contains("capture"))` だけなので、**空ファイルが一番簡単に満たす**。
  `.csa` と未対応形式は1件も見ていない

### [MEDIUM] R7-06 「3度試して3度とも外した」が経緯の形をしている

- 場所: `search/kifu_reader.rs`
- reviewer: comment
- 中身（3つの反例）は残すべきだが、「3度」は作業回数でリポジトリから辿れない

### [MEDIUM] R7-07 `detect` と `declared` の2語が同じ概念に使われている

- 場所: `search/kifu_reader.rs`
- reviewer: comment
- 日本語 doc は全て「名乗る」で統一され「推測しない」と明記してあるのに、
  識別子だけ `detect`。**6ラウンドかけて出した結論が名前で表せていない**

### [MEDIUM] R7-08 `operations.rs` に処理を言い直すだけのコメントが8箇所

- 場所: `file_system/operations.rs`
- reviewer: comment
- `// ファイル保存` の直後が `write_new_file(...)` など。
  同じファイルにこの PR が入れた「なぜ」のコメントと混在している

## 重複・矛盾した所見

- R7-01 は robustness と rust が別々の入力で同じ結論に達した。
  **rust は「`by_crate` の match より前へ出せ」と具体的な位置まで示し、そのとおりにした**
- robustness の「`describe` の出力9通りのうち次の行動を言うのは2通りだけ」は、
  半分が #327（`ConvertError` の英文）と #325（CSA の文字コード）に既出。
  残る「KIF の語彙に無い行」「知らない手合割」は**このラウンドでは直さない** —
  文言を日本語で前置きする改修は利用者に見える変更で、#327 とまとめて決めるほうがよい

## 範囲外（issue へ送る）

| 内容                                              | 送り先           |
| ------------------------------------------------- | ---------------- |
| 判定関数に `cargo-mutants` を掛ける               | **#332（新規）** |
| 失敗の文言に次の行動が無い（KIF の語彙 / 手合割） | #327 の領域      |

## 見ていない範囲

- 3人とも `index_cache.rs` の符号化・復号本体、`fs_scan.rs`
- TS 側が `promote` / `relative` を埋めて送るかは**まだ測っていない**（#331）
- 実コーパス609件での文言の出方

## lint / hook で強制できるもの

- **`cargo-mutants`** → #332。R7-04 / R7-05 は全て機械で出る。3ラウンド連続の提案
- doc 中の識別子が到達範囲にあるかを見る走査 → R7-03。**4ラウンド連続で同型**
- R7-01 / R7-02 は機械で拾えない

## 次ラウンドの対象

8件すべて直す。範囲外は #332 のみ。

# レビュー 410-cap-sfen-spelling ラウンド3

- 日付: 2026-09-07
- 範囲: `fix/410-cap-sfen-parse-error`（`origin/main` 2273b3a0 との差分、24コミット）
- 走らせた reviewer: rust / robustness / architecture / comment
- 対象コミット: `畳み込み済み`
- 前のラウンド: `-r1.md` / `-r2.md`

**r2 の焦点への答え**:

1. 走査ラチェットは取りこぼしを止めるか → **止めない**（R3-01 / R3-02）
2. `+` の直しで受理集合が変わっていないか → **変わっていない**。rust が
   `(up, promoted)` の全組を `origin/main` 版と突き合わせた
3. 制御文字の置換を消す変異は落ちるか → 落ちる（自分で確認済み）。
   robustness が別に5通り当てて全部落ちることを机上で確認
4. `capped(&format_args!(...))` の保証 → `invalid` と同じ（rust）。
   二重に刈っても省略記号は二重に付かない（内側の `…` は外側で切り落とされる）

## 所見

### [HIGH] R3-01 走査ラチェットが、自分の存在理由に挙げた2件のうち1件を見ていない（rust / architecture / robustness / comment）

**実測で示された。**

- `build.rs:166` の join error を素通しに戻す変異を当てても offenders は空。
  値が `message:` の欄ではなく `vec![...]` にあるため
- `src/search/watcher.rs` を新規に作って `message: format!("watch failed: {e}")` を
  書いても緑（robustness が最小 crate を組んで確認）
- 欄名の**省略形**（`IndexWarnPayload { path, message }`）にすると走査から消える。
  しかも `clippy::redundant_field_names` が `-D warnings` で**省略形を強制する**
- rustfmt が自分で作る改行（`message:` と `.to_string()` が別行）でも外れる
- `EMITTERS` は手書きの3件。`tests/roots/mod.rs:21-23` が
  「一覧はその場で数える。手で並べると、足した人が書き忘れても何も落ちない」と
  **名指しで禁じている作り**だった

### [MEDIUM] R3-02 空振り検査が `use crate::search::message::capped;` の1行で満たされる（rust / comment）

`l.contains("message:")` が `message::capped` の `use` 行に当たる。3ファイルすべてが
それを持っているので、`message:` の欄が1つも無くなっても緑。
**R2-04（`\0` が刈られる位置にあり assert が常に真）と同じ形の故障が、同じ PR で再発した。**

### [HIGH] R3-03 索引を組む段は刈る口を通っていないのに、3つの doc のうち2つが「通る」と書いていた（architecture / comment / rust / robustness）

`index/file_build.rs` の3経路（`:62,70,82`）が `.to_string()` で `String` を作り、
それが `project_manager.rs:363` / `build.rs:194` の `message:` に載る。
`EVT_INDEX_WARN` の6箇所のうち**4箇所が `capped` を通らない**。

`build_report.rs:90-91` はそれを正しく書いていて、この差分が足した
`message.rs:5-7` と `types.rs:304-306` が逆を書いていた。
**どれを読むかで結論が逆になり、実際に走るのは `build_report.rs` の側。**

### [HIGH] R3-04 描画先の表が2行ぶん逆。3箇所とも切られる（comment / rust）

`types.rs` の表は「一致0件の箱・打ち切りの知らせは折り返す」と書いたが、
`overflow-wrap` の指定がどこにも無いので**空白を含まない綴りには折り返す場所が無い**。
列方向のフレックスはアイテムの幅を器に合わせるだけで、語の中に分割点を作らない。
3箇所とも祖先の `overflow: hidden` が切る。

**`Capped::finish` が付ける省略記号は、どの描画先でも画面に出ない。**

r1 の R1-10 が同じ誤り（`flex-wrap` は語を折らない）を指摘していて、
**それを直したコミットが場所を変えて同じ誤りを入れ直していた。**

### [MEDIUM] R3-05 手番・手数・盤の綴りに、駒で直したのと同じ「何も名指ししない文言」が残っていた（robustness）

`capped` は制御文字を**空白**にするので、手数の欄が `"\u{1}"` だと
`invalid ply:  `（末尾は空白1つ）。駒の欄については**この差分の doc 自身が
同じことを問題として名指している**のに、根拠（長さ）だけで2分割していた。

### [MEDIUM] R3-06 `IndexWarnPayload` の doc が、3つの断定とも現物と違う（robustness / comment）

1. 見出し「索引に**入れられなかった**ファイルが1件」→ 読めたが一部を採れなかった
   棋譜は**途中まで入っている**（`BuildWarn` の腕）
2. 「`message` に埋めない」→ **同じ差分が触った** `scan failed: {e}` が根のパスを埋める
3. 「200件まで溜まるので一覧が読めなくなる」→ 描くのは `warns.slice(0, 5)` の**5件**。
   200 は state の保持量で、一覧の読みにくさとは繋がらない

### [MEDIUM] R3-07 コメントに変更の経緯とレビューの経過が入っている（comment）

「人の注意では2回落とした。…揃えたラウンドの次に」「**#410 が通るのはこの腕**」。
`CONTRIBUTING.md`「変更の経緯を書かない」の直接違反。

### [MEDIUM] R3-08 `message` の公開面が crate 全体に開いている（architecture）

`pub(crate) mod` + `pub(crate)` の項目。doc は「`search/` の中」と名乗るのに、
それを守っているのは `engine/` だけを見る走査と doc の文だけ。

### [MEDIUM][差分外] R3-09 警告の件数が200で頭打ちになり、画面が実数として出す（robustness）

## 重複・矛盾した所見

- **R3-01 / R3-02 / R3-03 は同じ根**。「刈る口を通っているか」を**走査で見る**という
  選択そのものが弱い。3人が独立に同じ構造的な答え（型で閉じる）を出した
- **R3-04 は r1 の R1-10 の再発**。同じ誤りを、それを直したコミットで入れ直した

## 見ていない範囲

- アプリを起動した実測は無い（4人とも）。描画の話は TSX / SCSS と CSS の規則からの導出
- `npm run verify`（TS 側）/ `cargo audit` / `npm audit` は未実行
- `NormalizeError::AmbiguousMoveFrom(Vec<Square>)` の要素数（R3-03 の「いまは有界」の根拠は
  バリアントの綴りだけ）
- `benches/search_bench.rs` / `search/cache` / `search/store` の中身

## lint / hook で強制できるもの

- **R3-01 / R3-02 / R3-03 は型で閉じれば機械が要らなくなる**（採用した）
- 「Rust の doc が TS / SCSS の現物を主張する」形は**3ラウンド連続で腐った**
  （R1-10 → R2-08 → R3-04）。`comment_identifiers.rs` は綴りが全大文字か全小文字の
  ものしか候補にしないので `PositionSearchStatusBar` も `wsTab__warnPath` も拾わない。
  **少なくとも「doc が名指した CSS プロパティがその SCSS に在ること」を見る走査は
  two-strikes を超えている**
- レビューの経過がコメントに残る形は、TS 側の `commentHistory` が見ている。
  **Rust の `//!` / `///` は対象外**なので、同じ述語を `src-tauri/` にも掛けられる

## 修正計画（r3 → r4）

### 束

- **刈る口**: R3-01 → R3-02 → R3-03（**走査をやめて型にすると3件とも消える**）
- **画面の記述**: R3-04 → R3-06（どちらも `types.rs` の doc）

### このラウンドで直したもの

| 順  | 所見          | 直し方                                                                                                                                                                                                                |
| --- | ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | R3-01/02/03   | **走査をやめて型で閉じた**（`畳み込み済み`）。payload の欄を `ScreenMessage` にし、`capped` だけが作れるようにした。ラチェットは削除。型を入れた結果 `file_build` の3経路が露わになり、`build_report` の doc と揃えた |
| 2   | R3-04 / R3-06 | 同上のコミットで `types.rs` の doc を書き直した（3箇所とも切られる／省略記号は見えない／描くのは5件／`scan failed` はパスを埋める）                                                                                   |
| 3   | R3-05         | 直した（`畳み込み済み`）。手番・手数・盤の綴りも `{:?}` で出す                                                                                                                                                        |
| 4   | R3-07         | 直した（`畳み込み済み`）。ラチェット削除で1件、`query_service` で1件                                                                                                                                                  |

### 直さないもの（行き先）

| 所見            | 行き先             | 理由                                                                                                                                                                                                                                                 |
| --------------- | ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R3-08           | **見送り（反論）** | `ScreenMessage` は `pub` な payload の欄に出るので、型は `pub` でないと `private_interfaces` が立つ（`-D warnings` で落ちる）。**門番は型ではなく `capped`（`pub(crate)`）と非公開の欄**なので、可視性を締める目的は型ではなく構築子で達成されている |
| R3-09           | **issue #465**     | 1件ごとの文言の長さとは別の層（件数）。`src/` の変更を伴う                                                                                                                                                                                           |
| SCSS の折り返し | **#457**（既出）   | 画面側で折り返させるかは UI の判断                                                                                                                                                                                                                   |

### 次ラウンドの焦点

1. **型で閉じたことで、抜け道が残っていないか。** `ScreenMessage` を作れる経路が
   `capped` 以外に無いか（`Deserialize` を含む）。`.to_string()` で `String` に戻してから
   別の口へ載せる形が生まれていないか
2. **`file_build` を `capped` に通したことで、失われた情報が無いか。**
   索引の警告は1件ごとに300文字で刈られるようになった。`BuildWarn` の
   「どの手順が打ち切られたか」が読める長さに収まるか
3. R3-04 の書き直しが、**今度こそ SCSS の現物から導けるか**（3ラウンド連続で腐っている箇所）
4. `{:?}` を綴り全体に広げたことで、正常な入力の文言が読みにくくなっていないか

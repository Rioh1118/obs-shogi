# レビュー harness-and-research ラウンド4

- 日付: 2026-09-02
- 範囲: `git diff main...HEAD`（8コミット、`85a9646`）
- 走らせた reviewer: `architecture-reviewer`、`oss-hygiene-reviewer`（**r3 で守備範囲に `research/` を足した効果を見る**）
- 前ラウンド: `r1` / `r2` / `r3`

## このラウンドで分かった一番重いこと

**引用の一部が、ShogiHome に存在しない構造だった。**

`research/shogihome/04-position-editing.md` の `<button>` のブロックは、
原文（`PositionEditingDialog.vue:73-113`）の**5本の兄弟要素を互いに入れ子に潰し、
`class` と `<Icon>` を全削除し、i18n キーを日本語の直書きに置換**したもので、
**原文を開いても一行も一致しない**。それを MIT の帰属表示の下に「抜粋」として置いていた。

規約違反というより**上流プロジェクトの実装の誤った提示**で、
外部の人が「ShogiHome はこう書いている」と引用し直すと嘘が伝播する。

同時に、**r3 で「直した」と報告した `calculateLayoutScale` が、まだ改変版のままだった**
（ラムダ引数 `component` → `c` の4行）。**「直した」という報告の誤りが3ラウンド連続。**

## 所見

### HIGH-1 引用規約違反が9箇所。うち1件は原文に存在しない構造（oss-hygiene）

- `04-position-editing.md` の `<button>` ブロック — **捏造**
- `06-tabs-and-layout.md` の `calculateLayoutScale` — r3 で直したはずが改変版のまま
- `02-game.md`（`Player` / `HumanPlayer` / `builder` / `settings/game` の型4本）、
  `05-usi-engine.md`（`USIEngine` / オプションのユニオン / `ExtraBook`）、
  `06`（`UIComponent` の型群 / `dialogPosition`）— 型注釈の削除、注記の挿入、フィールドの脱落
- **直した。**
  - `04` は**原文どおりの抜粋**（先頭2本のボタン）に差し替え、残り3本は表にした
  - `calculateLayoutScale` は原文を `gh api` で取り直し、**`diff` して 0 を確認した**
  - 要約であるものは `**要約。原文ではない**` を直前に置いた（11箇所）
  - `README.md` の規約を「原文どおり／要約」の2種類に分け、
    **「原文どおり」と書くなら実際に `diff` に掛けて 0 を確認する。目視で『近い』を確認しない**を追加

### HIGH-2 `research/README.md` の書き換えで、`findings/` 全編が使う語彙の定義が消えた（oss-hygiene）

- 削除した「証拠の等級」表（`[確定]` / `[推定]` / `[未確認]`）と「失効条件を書く」要件が、
  `findings/` では**全編で使われている**（`[確定]` が281回）
- **片方だけ消した状態がいちばん悪い。** README を入口に L1 を読んだ人は、
  `[確定]` と `[推定]` の差をリポジトリのどこからも引けない
- **直した。** 等級表を戻し、現物で使われている `[反証]` / `[判定不能]` も表に足した

### HIGH-3 `findings/L0` の `stash@{2}` は現物では `stash@{1}`（oss-hygiene）

- L0 が記述した内容（日付・19ファイル・+1190/−530・親コミット）は**すべて `stash@{1}` に一致**。
  `stash@{2}` は無関係な release 設定の WIP
- **手順どおり `git stash branch <name> stash@{2}` を実行すると、別の stash を消費して drop する。**
  目的の1190行は残ったまま、L0 は「保全済み」を謳っているので誰も再確認しない
- 添字は位置であって同一性ではない。L0 執筆後に stash が1本積まれてずれた
- **直した。** 3箇所を stash コミットの SHA `7b95aaf` に置き換え、「添字で書かない」を明記

### HIGH-4 `.claude/agents/` の棲み分けが、r3 の修正で**逆向きに壊れていた**（architecture）

- r3 で「再レンダは perf、実装の重複は react」と書いたが、
  **`perf-reviewer` は所見ごとに「n が何のときに問題になるか」の閾値を要求している**
  （「見積もれないなら、その所見は出さない」）
- 「Provider の value が毎レンダ新しい」に n の閾値は書けない。
  **react は挙げない／perf は出せない、で誰からも出なくなる**
- しかも `react-reviewer.md` の本文4箇所に、perf へ渡したはずの項目が残っていた
- **直した。** 切り分けを**「同一性は react、計算量は perf」**に変更（perf 自身の要求と整合する線）。
  r3 の切り分けは撤回

### HIGH-5 決定3 の `analysis/` の行範囲に、`engine/` に残すはずの5本が含まれる（architecture）

- 「`engine/bridge.rs:445-543` の99行」に入っているコマンドは **13本**で8本ではない。
  残る5本は決定2 が **✓（`engine/` にあって正しい）**と数えたもの
- しかも13本は行番号順に交互に並んでいるので**行範囲では切り出せない**
- r3 HIGH-2（決定2 が ✓ に数えたものを決定3 が ✗ 扱い）と**向きが逆なだけの同じ故障**で、
  r3 の修正で新しく入った
- **直した。** 行範囲を落とし、`engine_*` 5本は残すことを明記

### HIGH-6 L1 / L2 の判定が `docs/PREMISES.md` に一度も反映されていない（oss-hygiene）

- P-002 の根拠3件のうち **2件が誤りで1件は出典不明**（`L1` が `[確定]` で判定）。
  P-004 の「同リポジトリ最多議論」は**2位**、「定跡フォーマット対応のみで閉じられた」は原文と不一致（`L2`）
- `PREMISES.md` は P-006 / P-008 から `findings/L3` を引いているのに、**L1・L2 は配線されていない**。
  355行＋610行が2026-07-27 から誰の判断にも使われていない
- **範囲外 → issue。** ただし `research/README.md` に
  **findings → 前提の逆引き表**を足して、`PREMISES.md` を触る人が辿れるようにした

### MEDIUM 群（直したもの）

| #    | 所見                                                                                                                                                                                                      | 対応                                                       |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| M-1  | `docs/IDEAS.md` が L2 の反証済みの主張をそのまま持っている（「最多議論」）。**同じファイルの26行上をこの PR で直している**                                                                                | 「コメント数は2位、ユニーク参加者2名」に直し `L2` を引いた |
| M-2  | `Q-002` が存在しない `normalizedTree.ts` を「現行を読め」と指示していた。この文書の運用規約では**問いとして成立していない**                                                                               | `main` に実在する `KifuCursor` を対象に直した              |
| M-3  | `OPERATING-MODEL` の「どこに何を書くか」表が `state-transitions/` / `RELEASE.md` / `archive/` / `research/` の4つを落としていた。**r1 の M-9（RELEASE.md の存在に気づかず提案を書いた）はこの欠落の帰結** | 4行を追加                                                  |
| M-4  | §3 の入力が 🔴 限定のままで、r3 が起こした Q-006（🟡）/ Q-007（🟢）が入力に入らない。**孤児を解消したつもりが解消できていなかった**                                                                       | 「🔴 が優先。無ければ 🟡 / 🟢」に直した                    |
| M-5  | `/write-issue` の「直し方に判断が要るもの」に6週間の門が無いのに、6行下の禁止事項は例外なしで禁じていた                                                                                                   | → 次ラウンド（下記）                                       |
| M-6  | `/tidy-commits` の手順5（検証コストの見積り）が手順4 の後ろにあり、**10群に割って20分超に気づくのが全部積み終えた後**になっていた                                                                         | → 次ラウンド                                               |
| M-7  | `file_system` → `tree` の改名が決定3 では確定、未決では未確定として二重に書かれている                                                                                                                     | → 次ラウンド                                               |
| M-8  | `research/shogihome/README.md` の「8本」が表の7本とも引用ブロック39本とも合わない                                                                                                                         | 数を書かず範囲で書いた                                     |
| M-9  | `research/` に週次レビューからの入口が無く、`shogihome/` 7本は inbound 参照ゼロ。**上流が動けば必ず腐るのに引き金が無い**                                                                                 | `OPERATING-MODEL` の表に `research/` を追加                |
| M-10 | ShogiHome の版が `research/` 内に2つ（`shogihome/` は `de27f0c1c352`、`findings/L3` は `f07e934a`。**144コミット離れている**）のに、README は片方を「正」と書いていた                                     | 「版はディレクトリごとに違う」に直した                     |
| M-11 | `L0` のヘッダが「閉じた（判定確定）」だが、本文が判定1 の限界を明記し、人間レーン5件が未了                                                                                                                | → 次ラウンド                                               |

## 重複・矛盾した所見

- 無し。2人の担当範囲が重なっていない

## 見ていない範囲

- **`research/shogihome/` の引用39本のうち、原文と照合したのは11本。約28本が未照合**
- `findings/L2` `L3` `L4` の本文の大部分（約1200行）。出典 URL の生死確認は1本も叩いていない
- `.claude/agents/*.md` 8本の本文（**4ラウンド続けて未読**。今回読んだのは perf / react / ui / architecture の4本）
- `docs/state-transitions/` の9本の中身
- `npm run verify` / `npm run verify:rust`。差分は `.md` のみ

## lint / hook で強制できるもの

**このループで最も価値が高いものが確定した。**

1. **引用ブロックの verbatim 検査（最優先）。** 各 `ts / `html ブロックの各行が、
   指す `path` の原文に部分文字列として存在するか。
   **HIGH-1 の9件が全部これで落ちる。3ラウンド続けて人間の目を素通りした**
2. **skill 内のシェル変数代入の検出**（r2・r3 で2回実害）
3. **数え方コマンドの再実行**（r1・r2・r3・r4 で4回実害）
4. **`stash@{N}` のような位置参照の禁止**（HIGH-3）
5. **語彙の定義の死活チェック**（HIGH-2）
6. **`findings/` の判定と `PREMISES.md` のリンクの突き合わせ**（HIGH-6）

## 次ラウンドの対象

**まだ所見ゼロではない。** M-5 / M-6 / M-7 / M-11 を次ラウンドへ送る（いずれも文書の中の
矛盾で、実害の経路が短い）。加えて**未照合の引用28本**が残っている。

**このループの傾向として記録しておく。** 4ラウンド全てで
「前ラウンドの修正で入った新しい欠陥」が出た。

| ラウンド | 入れた修正              | 次で見つかった欠陥              |
| -------- | ----------------------- | ------------------------------- |
| r1       | `$BASE` を直す          | r2: `$W` で同じ故障が再発       |
| r1       | 表をポインタに置換      | r2: ポインタの先が空            |
| r2       | 決定3-2 を追加          | r3: 丸ごと二重化                |
| r2       | 決定2 の表を追加        | r3: 決定3 と矛盾                |
| r3       | 引用を「直した」        | r4: **まだ改変版**              |
| r3       | perf / react の棲み分け | r4: **逆向きに壊れていた**      |
| r3       | 決定3 に移動を追加      | r4: 行範囲に別の5本が入っていた |

**「直した」と報告する前に、修正後の現物を機械で確かめる**という手順が
4ラウンド通じて欠けている。上の lint 1〜3 はそこを埋めるためのもの。

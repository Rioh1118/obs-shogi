# レビュー book-to-main ラウンド7

- 日付: 2026-09-04
- 範囲: r6 の所見を直した6コミット（`git diff 83001d2d HEAD`、報告書を除いて104行）
- 走らせた reviewer: comment / rust / robustness / oss-hygiene
- 対象コミット: `35a0b95b`
- 前ラウンド: `.claude/reviews/2026-09-04-book-to-main-r6.md`

所見 **5件**（BLOCK 0 / HIGH 0 / MEDIUM 5）。推移は 22 → 18 → 23 → 10 → 12 → 7 → **5**。

**HIGH が0になった。** 5件すべてが「r6 で自分が書いた文が事実でない／範囲がずれている」形で、
新しい機構も新しい検査も1つも増えていない。issue へ送るものも無かった。

---

## 所見

### X-01 (MEDIUM, 3人): 新しく足した経路の出典が、別の経路のセルを指している

`CLAUDE.md` は2つの経路を並べて、括弧の中で1つの出典（`(D, S4)`）を挙げていた。
だが指した先は合流の話だけで、**新規ファイルの経路は形 B。** しかも判定表の B 行は
「S4 へ（**検証**）」のまま、検証が走ると書いていた。

確かめに行った人は、指定されたセルに探している話が無く、自分の経路の行には
正反対のことが書いてあるのを読む。**根拠が無く、表とも食い違う記述**として扱われる。

- 結果: 対応済み `1a0715eb`（判定表の B 行に但し書きを足し、「埋まっていないセル」に
  (B, S4) を1項置いた。**構造ではなく分類の問題なので塞げる**という (D, S4) との違いも書いた）

### X-02 (MEDIUM, 2人): 列挙が3ラウンド連続で不完全で、書き直したときに1つ落とした

- **まだ4つ落ちていた。** `git rm` / `git mv` / `git apply` / `git stash pop` を
  `git commit` の手前に置いた形。ゲートの許可リストは `git add` に限っておらず、
  **任意の git サブコマンド**を通す。reviewer が実測:
  `git rm src-tauri/src/book/mod.rs && git commit -m x` → **exit 0、何も走らない**
- **書き直しで「どの種類にも当たらないファイルだけの変更」を落としていた。**
  このラウンドの6コミットのうち4本がその形で通っている

- 結果: 対応済み `1a0715eb`（**列挙をやめて条件で書いた。**
  素通しするのは (1) どの種類にも当たらないファイルだけ
  (2) 判定した時点のツリーに追跡済みの変更が無いとき、の2つ）

### X-03 (MEDIUM, 2人): 引用の節の見出しが、本文と逆のことを言っている

本文は「義務は `docs/` 全体に掛かる」、見出しは「（この置き場の中だけ）」。
**見出しだけを読んで飛ばす人には、r6 の W-05 で直したはずの誤読がそのまま成立する。**

しかも直下の節が同じ括弧を「規則の適用範囲」の意味で正しく使っているので、
**同じ括弧が片方では正しく、片方では嘘**になっていた。

- 結果: 対応済み `a61a6d57`（限定が掛かる対象（一覧）を見出しに書いた）

### X-04 (MEDIUM, 3人): 「替えるなら」の列挙に、範囲の誤りと落としが4つ

- **「どちらの重複が残ったかを見るテストが無い」が偽。** 走査の枝には2本ある
  （`Some(50)` / `Some(40)` を assert）。無いのは `HashSet` の枝で
- **借用の理由が偽。** 15行下の現物が「`moves` を借りたまま `seen` を作って」から
  `retain` で可変に触って通っている。壁は借りていること自体ではなく、
  **マップを後でも使う周回の中で可変に触ること**。ブロックに閉じる必要も無い
  （reviewer が3通り書いて確かめた）。原因を取り違えたまま読むと、
  直上のコメントが禁じている `String` の確保へ逃げる
- **本家が持っている答えを落としていた。** `None` を何と読むかの答えは、doc が既に引いている
  同じ関数群の中にある —— 欄が欠けていれば `1`（`book.cpp:94`、`misc.cpp:1677-1680`）、
  `none` と書かれていれば `0`
- **同じファイルの中の先勝ちの綴り4箇所（関数名2つを含む）が落ちていた。**
  `cargo test` は走査の枝の2本しか落とさないので、名前とコメントは残る

- 結果: 対応済み `aafd34b4`

### X-05 (MEDIUM, 1人): ワークツリーが巻き戻る引き金を、ゲートだと書いている

`implement/SKILL.md` の「コミットが gate で止まると lint-staged がワークツリーを復元し」。
ゲートは `deny` を返して**呼び出しそのものを実行させない**ので、`git commit` は走らず
pre-commit も走らない。**このセッションでも観測している** —— deny されたコミットは
ゲートの文言だけ、通ったコミットは lint-staged の stash と復元の行が出る。

「gate で止まった＝編集が消えたかもしれない」と読んだ人は、deny のたびに手元を検分し直し、
**本当に消えうる場面（ゲートを通って pre-commit が落ちたとき）を警戒しない。**

- 結果: 対応済み `f474eaeb`。**この PR の6コミットの外**だが、
  ゲートの挙動についての事実の誤りで、放置すると「同じ事実を複数箇所に書いて片方だけ直す」を
  この PR 自身が作るので直した

---

## 重複・矛盾した所見

- **X-01 と X-04 は3人、X-02 と X-03 は2人が指した**
- **X-01 / X-02 / X-03 / X-04 は同じ根**（列挙で網羅を名乗り、写した先の条件を持ち込まない）。
  r1 から7ラウンド続けて出ている形で、**このラウンドは5件中4件**。
  X-02 で**列挙そのものをやめた**のが、この形への最後の手当て
- 所見の水増しは無かった。4人とも「確認して所見が無かったもの」を明記していて、
  **やねうら王 v9.40 の行番号、著作権者3件、`vp check` の説明、PR テンプレート、
  引用の節の本文は、複数の reviewer が現物と突き合わせて正しいことを確認している**

## 見ていない範囲

- #386 / #342 / #290 へ送ったもの（再掲しない前提）
- `src-tauri/src/book/` の振る舞い（doc の主張との突き合わせのみ）
- `user_book1.db` の実測3行とピーク確保の実測

## lint / hook で強制できるもの

このラウンドで新しく挙がったものは無い。既出のものはすべて #386 / #342 / #290 へ送済み。

機械で守れないもの: X-03（見出しと本文の範囲）、X-04（手順が実装と合っているか）、
X-05（因果の取り違え）。いずれも**列挙をやめる**以外に手が無い。

# レビュー search-contracts ラウンド5

- 日付: 2026-09-05
- 範囲: `0e517825~2..HEAD` の2コミット（`c93f6a7d` / `f99a8d2a`）
- 走らせた reviewer: rust / comment（範囲が2コミットなので2本に絞った）
- 前ラウンド: `2026-09-05-search-contracts-r4.md`

**件数は 29 → 16 → 20 → 11 → 8。** 製品のデータを壊す新規欠陥は r3 以降ゼロ。
残っているのは**塞ぎ損ね・届かない診断・doc の誤り・テストが別のものを見ている**の4種類。

## 所見

### BLOCK

#### R5-A テストの byte poke が、コメントの言う欄と別の欄を壊している

comment。`index_cache.rs:1699-1713`。**`c93f6a7d` が書いた。**

`nt_head`（`01 00 00 00 | 01 00 00 00 | 00 00 00 00`）は blob に**2箇所**現れる。
節表の頭と、**桶の出現レコードの `file_id=1 / gen=1 / node_id=0`**。
`encode_all` は節表の後に桶を書くので、`rposition`（最後の一致）が返すのは**出現側**。

**実測で確認した**（probe で一致位置 `[150, 534]`、blob 1226バイト。`rposition` は 534）。

つまりこのテストが壊しているのは「節表の `file_id`」ではなく「出現の `file_id`」。
門番自体は踏むので緑だが、**コメントが宣言した経路（表が別の `file_id` に載り、
元の `file_id` が表を失う）は一度も再現されていない。**

`is_err()` しか見ていないので、別の理由で落ちても緑になる。**それがこの取り違えを隠した。**

### HIGH

#### R5-B R4-K が閉じていない。しかもコミット本文が、していない変更を報告している

rust / comment。`docs/state-transitions/search.md:263` vs `index_cache.rs:603-608`。

表はいまも `| `tesuu`/`fork_off` | **通る。黙って間違った索引になる**（#336） |`。
だが `c93f6a7d` が `fork_off + fork_len > forks_len → Err` を入れたので、
**範囲外なら `restore-ng` → B に落ちる。表と実装が同じ PR の中で食い違っている。**

`git diff 6936b80b..HEAD -- docs/state-transitions/search.md` は `:249-252` の
段落の置換だけで、**表は1行も変わっていない。**

**`f99a8d2a` のコミット本文は「ビット化けの表に、節表の file_id と
fork_off / fork_len の行を足す」と書いている。** していない。

原因は私の作業手順で、`s.replace()` に assert を付けていなかったので
元テキストが一致せずに黙って何もしなかった。**5回目の同型失敗。**

#### R5-C 門番の根拠が、呼び手ゼロの関数を「本番の口」と名指している

comment / rust。`index_cache.rs:664-666,1611` / `index_store.rs:244-251`。

**実測で確認した**: `insert_file_segments` を呼ぶ行は `src-tauri` 全体で0件
（定義と、私が書いた2つのコメントだけ）。

本番で対を入れているのは `insert_many_file_segments`（`build.rs:158,184` /
`project_manager.rs:288`）と `install_restored`（`commands.rs:107`）。

**この一文は、キャッシュ全体を捨てさせる門番の唯一の根拠。** 読み手が確かめようと
開くと、誰にも呼ばれていない関数が出てくる。不変条件自体は成り立つが、
**根拠として指している場所が間違っているので確認できない。**

「入れる口は…**だけ**」も偽（`install_restored` がもう1つ）。**r4-A と同じ形。**

### MEDIUM

#### R5-D 書き側だけ `fork_off` / `fork_len` が無検査。しかも検査の数に3つの説がある

rust。`index_cache.rs:395-417`（書き）vs `:603-608`（読み）。

**R4-B が「`fork_off` は `node_id` と1文字も違わない失敗」と名指した項目が、
読み側だけで閉じられている。** 書き側の門番を置いた理由（壊れたものを書いて
次の起動で `Err`、作り直してまた同じものを書く）がそのまま当てはまる。

`save_checkpoint` の呼び手は `build.rs:222` の1箇所だけで、全件構築の直後にしか
走らないので、**この輪は自力で抜けられない。**

数も揃っていない。

| どこ                      | 何と言っているか                                |
| ------------------------- | ----------------------------------------------- |
| `:421` の見出し           | 「読む側と同じ**3つ**」                         |
| 実装（書き）              | **4つ**（所属 / 並び / 節表の有無 / `node_id`） |
| 実装（読み）              | **5つ**（＋ `fork` の範囲）                     |
| `c93f6a7d` のコミット本文 | 「読み書き対称の**6つ**」                       |

**同じ事実に3つの数が付いている。** R4-K と同じ形。

#### R5-E R4-C を、7つの失敗点のうち1つにしか当てていない

rust。`index_cache.rs:153-159` vs `:130,170,176,180,191,198`。

`log::error!` に上げたのは `encode_all` の1つだけ。同じ関数の中に、
**同じ結末（チェックポイントが書かれない）を `trace!` で報告する箇所が6つ残っている**
（`create_dir_all` / `create tmp` / `write_all` / `flush` / `rename final→bak` /
`rename tmp→final`）。

ディスク満杯の `write_all` 失敗は `encode_all` の失敗より起きやすい。
とくに `rename tmp→final` が失敗すると `final` は退避済みなので、
**`index.v1.zst` が無い状態で終わり、次の起動は `bak` から1世代古い索引を黙って復元する。**

**R4-E が立てた原則（理由が同じなら検査も同じ場所に要る）を、R4-C の側に適用していない。**

なお `rg 'trace!\("[^"]*FAILED'` の実数は **9件**。r4 の報告書は「4件」と書いた（誤り）。

#### R5-F 同じ因果の説明がファイル全体で6回

comment / rust。`index_cache.rs:599-602,659-663,668-670,1613,1856-1858,1959-1961`。

`cursor_lite → None → CursorLite::root() → 0手目` が、`decode_all` の中で3回、
ファイル全体で6回。`:659-670` は**1つのコメント塊の中で同じことを2回**言っていて、
段落が空行なしで連結している。

この因果は `query_service.rs:132-135` に1箇所あるだけで、
**そこを変えたときに直す先が6箇所ある。**

#### R5-G `compact_bucket` の第1引数が、捨てるためだけに残っている

comment / rust。`index_cache.rs:304-308,350` / `:268`。

`debug_assert!` を消した結果、`bucket_idx` は本体で1度も読まれない。
`let _ =` があるので `unused_variables` も clippy も鳴らない。

呼び手は `from_fn(|b| compact_bucket(b, ...))` で毎回渡すのに受け側は捨てる。
**この矛盾は機械では二度と拾えない。**

#### R5-H `BASELINE` の直上のコメントが、数の由来ではなく2行下の `grep` の理由

comment。`scripts/rustdoc-ratchet.sh:15-17`。

R4-I が挙げたのは2点で、(a) 集計行を数えている、(b) **`BASELINE` の由来がどこにも無い**。
(a) は直ったが (b) は直っていない。いまのコメントは `count=` の式に対する説明で、
**数の根拠だと読める位置に、数と無関係な文が座っている。**

## 実測で問題なしと判断したもの

- **6つの門番は正常なキャッシュを1つも弾かない。** 本番が blob を作る形を9通り辿って確認
  （`push_entry` / `push_node` / `bucketize_entries` / `compact_bucket` の k-way マージ /
  `FileBuild::empty` / build 失敗の腕 / join error の腕 / 削除されたファイル /
  `insert_many_file_segments`）。**「節表があって空」も「出現ゼロ」も「削除済み」も弾かれない**
- **`save_checkpoint` の呼び手は `build.rs:222` の1箇所だけで、全件構築の直後にしか走らない。**
  書き側の門番が見るのは毎回「全件構築を終えた直後」という最も素直な形
- **`InvalidHand` から `count` を落とした変更は受理集合を変えていない。**
  `before + n > MAX_HAND_COUNT` は削除した `after > ...` と同値。8通りの綴りで確認
- **鍵の値は変わっていない。** 差分に `position_key.rs` / `zobrist.rs` / `position_apply.rs` /
  `initial_position.rs` が無い
- **ラチェットは `cargo doc` の申告と一致し、両方向で効く**（実走で 11/11、2回目も 11）
- **`HISTORY_WORDS` の2語は Rust のコメントにも効く**（`ROOTS` が `rustRoots()` を含む）
- **`cargo test --lib` 233 passed / `npm run test` 667 passed**

## 見ていない範囲

- `verify:rust` の `cargo fmt` / `clippy`（reviewer は `cargo test --lib` と ratchet のみ実走）
- `decode_all` の `scan` / `path_to_id` 節
- `read/` 配下の本体、`query_service.rs` の実物
- 新しいテスト4本の `is_err()` の**理由**が狙った門番であることの確認（R5-A がその1件）
- TS 側 `features/position-search/`、bench の実走

## lint / hook で強制できるもの

- **doc の表と実装の突き合わせ**（R5-B）: 表に載る欄名について、`decode_all` に
  その識別子を含む `return Err` があるのに表が `→ **B**` を含まない行を落とす。
  **いま `fork_off` の行だけが当たる**
- **コミット本文が主張した変更が差分にあるか**（R5-B）: `verify-gate.sh` に、
  本文が挙げたパスごとに `git diff --numstat` の追加行が0でないことを見る腕
- **呼び手ゼロの `pub fn` を根拠に名指さない**（R5-C）: `src-tauri` 全体の呼び出し数が
  0の `pub fn` を列挙する。**現在 `insert_file_segments` の1件**
- **読み書きの検査の対**（R5-D）: `encode_all` と `decode_all` の `return Err(format!(...))` に
  出る識別子の集合を取り、片側にしか無いものを落とす。**いま `fork_off` / `fork_len`**
- **`Info` 未満で終わる失敗の記録**（R5-E）: `rg 'trace!\("[^"]*FAILED'` が**9件**
- **同じ文の繰り返し**（R5-F）: `cursor_lite` と `CursorLite::root` を同時に含む
  コメント塊が1ファイルに6箇所
- **`let _ = <引数名>;` で消費される引数**（R5-G）: 現在1件
- **機械では防げない**: R5-A（byte poke の意図）/ R5-C の判断部分 / R5-H（置き場所）

## 修正計画（r5 → r6）

### 対象そのものを疑ったか

**収束している。範囲はこのまま。**

|                  | r1  | r2  | r3  | r4  | r5          |
| ---------------- | --- | --- | --- | --- | ----------- |
| 所見             | 29  | 16  | 20  | 11  | **8**       |
| BLOCK            | 1   | 2   | 1   | 0   | 1（テスト） |
| 新規のデータ破壊 | —   | 1   | 0   | 0   | **0**       |

r5 の BLOCK は**テストが別のものを見ている**件で、製品の欠陥ではない。

### 5回目の同型失敗。今度は根本原因が特定できた

| ラウンド | 何が起きたか                                                     |
| -------- | ---------------------------------------------------------------- |
| r1       | M4 が7箇所を名指し、4箇所直して閉じた                            |
| r2       | R2-9 / R2-15 / R2-3 が各5/5/3を名指し、3/3/2 直して閉じた        |
| r3       | 同じものが再掲。**手順（パスの突き合わせ）を入れた**             |
| r4       | R4-B。**パスは見るが項目を見ない**と判明。手順を識別子まで下げた |
| r5       | R5-B。**識別子まで下げても止まらなかった**                       |

**今回の根本原因は手順ではなく、私の編集の仕方。**

`s.replace(old, new)` に `assert old in s` を付けていなかったので、
元テキストが一致せず**黙って何もしなかった**。そのうえで結果表とコミット本文に
「直した」と書いた。

**手順を3つ足す。**

1. **`s.replace()` には必ず `assert old in s` を付ける。** 一致しないなら止まる。
   r5 の R5-B はこれだけで止まっていた
2. **識別子の突き合わせは場所ごとに。** r4 では `fork_len` を全体で数えて `+13` を見たが、
   その13件は全部 `index_cache.rs` で、`docs/` は0件だった。
   **所見が場所を名指しているなら、そのファイルの差分で数える**
3. **コミット本文に「Xを足した」と書いたら、`git diff --stat` にその X が出ることを確かめる。**
   `f99a8d2a` は「表に行を足す」と書いて足していない

### このラウンドで直すもの

| 順  | 所見                          | なぜこの順か                                       | この直し方で壊しうるもの                                                                                                                 |
| --- | ----------------------------- | -------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **R5-B** `search.md` の表     | HIGH。**5回目の実体**。上の手順3つの最初の適用対象 | 表を実装に合わせる。**`assert` を付けて編集し、`git diff -- docs/` で確かめてから閉じる**                                                |
| 2   | **R5-A** テストの byte poke   | BLOCK。テストが別のものを見ている                  | 節表側を壊すには `position`（前の一致）を使い、**一致が2箇所であることを assert してから**位置を採る。`is_err()` でなく文言を見る        |
| 3   | **R5-D** 書き側の `fork` 検査 | MEDIUM だが、読み書きの対が崩れている唯一の欄      | 書き側に足すと、壊れた `NodeTable` を書けなくなる。`an_index_of_a_realistic_size_can_be_read_back` が守る。**見出しの「3つ」も数え直す** |
| 4   | **R5-C** 呼び手ゼロの根拠     | HIGH。門番の唯一の根拠                             | 名前を実物へ。`insert_file_segments` は呼び手ゼロなので**削る**（`pub` を減らすので `layering` に触る可能性）                            |
| 5   | **R5-E** 残り6つの `trace!`   | MEDIUM。R4-C の適用漏れ                            | `error!` に上げる。**ログが増える**が、どれも索引が書けない合図なので妥当                                                                |
| 6   | **R5-F** 6回の繰り返し        | doc のみ                                           | 因果を1箇所に集めて、各所は「この欄は何か」だけに                                                                                        |
| 7   | **R5-G** 捨てる引数           | 引数を落とす。呼び手1箇所                          | 無し                                                                                                                                     |
| 8   | **R5-H** `BASELINE` の由来    | 最後。コメントの置き場                             | 無し                                                                                                                                     |

### 直さないもの

無し。**8件すべてこのラウンドで取る。**

### 次ラウンドの焦点

1. **上の手順3つが効いたか。** r6 で「名指しの取りこぼし」と
   「コミット本文の嘘」が0件なら効いている。**6回目が出たら、
   直し方ではなく私がレビューを読む粒度を疑う**
2. **R5-D の書き側検査が、正常な `NodeTable` を弾いていないか**
3. **R5-E で `error!` に上げたものが、正常な経路で出ていないか**（ログが煩くなる）
4. **R5-A のテストが、狙った門番の文言で落ちるか**
5. r5 で見なかった範囲: `decode_all` の `scan` / `path_to_id` 節

### 検証の見積り

8件 × `verify:rust`（約2分15秒）≈ **18分**。`docs/` と `scripts/` を触る回があるので `verify` も。

## 結果（r5 の修正）

**8件すべて `84241c87` の1コミット。** 同じ関数の同じ検査群を触るため分けられなかった
（`/review-fix` の1所見1コミットからの逸脱。r2 に続いて2回目）。

| 所見                        | 直し方                                                       |
| --------------------------- | ------------------------------------------------------------ |
| **R5-A** byte poke          | 一致の数を assert してから前の一致を採る。`Err` の文言も見る |
| **R5-B** `search.md` の表   | 節表の `file_id` / `fork_off` / `fork_len` の行を足す        |
| **R5-C** 呼び手ゼロの根拠   | `insert_many_file_segments` と `install_restored` へ         |
| **R5-D** 書き側の `fork`    | 足した。見出しの数も落とした                                 |
| **R5-E** 残り6つの `trace!` | `error!` へ。復元側の2件は正規の経路なので据え置き           |
| **R5-F** 6回の繰り返し      | 各所を「この欄は何か」だけに                                 |
| **R5-G** 捨てる引数         | 落とした                                                     |
| **R5-H** `BASELINE` の由来  | 数の直上へ                                                   |

**門番は読み書き5対5。10個すべてに変異を当てて落ちることを確かめた。**

### 手順3つを実際に走らせた

| 手順                                | 結果                                                                                      |
| ----------------------------------- | ----------------------------------------------------------------------------------------- |
| 1. `s.replace()` に `assert`        | **6箇所すべてに付けた。** R5-B の原因はこれで消えた                                       |
| 2. 場所ごとに識別子を数える         | `docs/` の `fork_len` が **1件**（r4 は全体で数えて `index_cache.rs` の13件に隠れていた） |
| 3. コミット本文の主張が差分にあるか | `docs/` は `+24 -22`。「表に行を足す」が実物と合う                                        |

**手順2で偽陽性が1件出た。** `scripts/rustdoc-ratchet.sh` の `BASELINE` が
「未完」と出たが、変更は入っている（`+5 -2`）。**識別子が動かず周りのコメントだけ
変わったため** — r4 で見つけた3つ目の穴。`git diff --numstat` と併せて見れば区別できる。

### 修正で新しく分かったこと

**`trace!("... FAILED")` の実数は9件だった。** r4 の報告書は「4件」と書いていた（誤り）。
7件を `error!` に上げ、復元側の2件は据え置いた。

**書き側の `fork` 検査を足したら、それを留めるテストが無かった。**
読み側の変異は落ちるのに書き側は素通りしたので、テストを1本足した。
**門番を足すたびに、読み側と書き側の両方に変異を当てる**必要がある。

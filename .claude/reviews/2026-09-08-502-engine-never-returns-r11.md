# レビュー 502-engine-never-returns ラウンド11

- 日付: 2026-09-09
- 範囲: `fix/441-stop-analysis-on-unmount...HEAD`（**基底を取り込んだ後**）
- 走らせた reviewer: architecture / react / robustness / comment / oss-hygiene
- 対象コミット: `dbd6f431`
- 前ラウンド: [r1](2026-09-08-502-engine-never-returns-r1.md) 〜 [r10](2026-09-08-502-engine-never-returns-r10.md)

**変異を当てたのは robustness だけ**（r8 から同じ取り決め）。

## このラウンドで分かった一番大きなこと

**マージが、私の変更を4行ぶん黙って差し戻していた。**

git は `failure-surfacing.md` の §2 の表を「衝突」として出したので、私は基底側を土台に
自分の2行を足す形で解いた。**その結果、同じ表の中で私が書き換えていた4行
（F-9 / F-2 / F-19 / F-28）が基底の版に戻った。** 衝突の解決としては通り、
`verify` も緑のまま。oss-hygiene が3点比較（merge-base に無く・マージ前の枝にあり・HEAD に無い行）で
見つけた。**私も同じ比較を全ファイルに掛けて、消えたのがこの4行だけであることを確かめた。**

もう1つ、**r10 で私が出した結論が誤りだった**。「世代の照合3つは潰しても赤くならない」と
書いたが、robustness が**観測できる構えを示した**——中間の commit が畳まれるのは
「1本目が返った時点で2本目が既に終わっている」ときだけで、**起動を2本とも保留にすれば
単独の commit として観測できる**。

## 所見

### HIGH-1 マージが F-9 / F-2 / F-19 / F-28 を基底の版へ差し戻した（oss-hygiene）

- **F-9**「設定タブでのみ見える」——`4211dd83` で「どこにも出ない。読み手は0」に直したのが戻った。
  同じファイルの §1（`engine` の読み手 **0**）と G-6 の欠番説明が、**この行を根拠にしている**
- **F-2** ——「利用者がボタンを押していない枝もある」と「次の一手は ※15 が1つだけ持つ」が消え、
  **`failed` を「起動を待つ」側に括る誤り**が復活した
- **F-19 / F-28** ——「段」の語彙の訂正が戻り、**同じ PR が §3 に足した禁止に違反する2件**が復活

### HIGH-2 `initialize` の世代照合2箇所は観測できる（robustness。実測）

r10 の私の記述（「潰しても赤くならない」）が誤り。潰したときの結末:

- **成功側** → まだ起動中のエンジンについて `isReady` を名乗る。▶ が通り、Rust は
  `NotInitialized` で断るので、**正常に起動している最中に「起こし直してください」と案内する**
- **失敗側** → `failed`（終端）に落ちる。`retriesAfterError` は同じ設定なので偽のまま戻らず、
  **健全なエンジンが起動している最中に解析を打ち切る**——#502 と同じ形

### HIGH-3 `EngineInitializer` の契約の第3条が、実装・呼び手・double のどれとも合わない（comment / architecture / robustness の3本）

r10 で私が書いた「畳み損ねても例外にしない。呼び手は失敗の有無で分岐しない」は
**両方とも偽**。`shutdownEngine()` は裸の `await` で reject しうるし、`restart()` は
そこで切れて `initialize()` に到達しない。**同じ PR のテストがその形に依存している。**

### HIGH-4 ※3 / ※7 の「`finally` で必ず `idle` へ落とす」を、同じコミットが条件付きにした（comment / oss-hygiene）

`34c8c01c` は doc に「必ず」と書きながら、同じ diff で `dispatch` に世代の門を付けている。
※7 はその「必ず」に「将来 `Err` を返すようになっても結論は動かない」という強い主張を載せていた。

### HIGH-5 ※7 が `isRecoverableNotReady` の置き場を旧ファイルのまま指す（comment / architecture）

`8be62413` で移したのに、同じ節の2つの参照のうち1つしか直していない。
**4行上で「どちらも `lib/notReadyReason.ts`」と書いた直後**に、3本目だけ `model/types.ts` を指す。

### HIGH-6 マージが `CONTRIBUTING.md` の索引の表を空行で割った（architecture / oss-hygiene / robustness の3本）

6行が表の外へ出て、GitHub 上では生テキストとして描画される。
**`ratchetIndex` は行単位マッチなので空行を跨いで名前を拾い、緑のまま。**

### MEDIUM-7 〜 MEDIUM-12

- F-40 の本文が比較相手に **F-38**（設定の書き込みの失敗）を指す——採番の付け替え漏れ（comment / oss-hygiene）
- ※15 の `NO_ENGINE_ON_START_MESSAGE` の行が、**私の突き合わせで基底の古い説明に上書きされていた**（comment）
- `notReadyReason.test.tsx` が `provider.test.tsx` に4本とも呑まれ、モジュール doc は
  「ここでしか捕まえられない」と嘘を書き、**もう存在しない `NOT_READY_REFUSALS`** を指す。
  double だけ `satisfies` を持たない（architecture / react）
- G-6 の欠番の理由（G-3 を根拠にする）が成立していない——**G-3 は G-6 より前なので詰めても動かない**（oss-hygiene）
- §3 の「F-32 以外は段が書かれていない」が F-37 を数え落とす（oss-hygiene）
- 「埋まっていないセル」で `initializer.ts` のテストの有無が二重に語られる（oss-hygiene）
- r10 の報告書が**この branch に存在しないコミット**（amend で捨てた `37466d5d`）を指す（oss-hygiene）
- 「`await` の向こうで最新値を読む鏡」の作り方が engine と analysis で割れ、規則がコードに無い（react）

### 差分の外

- **ペインの候補手のキャッシュの鍵が version を含まない**（react）。
  **この PR の断りが案内している操作**（オプションを変えて保存）がちょうどこの窓を踏み、
  解析が断たれた後も**死んだ設定で出した読み筋が盤の下に残る**。`docs/IDEAS.md` の既存項目へ追記
- `EngineProvider` を畳んで張り直すと `engineInitializer.inFlight` だけが残り、
  不変条件1 が破れる（robustness。※2 が名指しする入口では踏めない）

## 修正の結果（`/review-fix`）

| 所見                         | 結果                                                                                      |
| ---------------------------- | ----------------------------------------------------------------------------------------- |
| HIGH-1 / MEDIUM-7 / MEDIUM-8 | `1afc0c50`。4行を復元し、採番と ※15 の行を直した                                          |
| HIGH-2                       | `1afc0c50`。**起動を2本とも保留にする**テスト2本。doc とテストの記述も直した              |
| HIGH-3                       | `1afc0c50`。契約を現物へ（畳みの失敗は投げる。`restart` はそこで切れ、`idle` の枝が拾う） |
| HIGH-4 / HIGH-5              | `1afc0c50`。※7 の根拠を `reducer.ts` へ移し、置き場を直した                               |
| HIGH-6                       | `1afc0c50`。空行を落とし、**`ratchetIndex` に表の分断を見る検査を足した**                 |
| MEDIUM-9 〜 MEDIUM-12        | `1afc0c50`。重複テストを畳み、`equalRuntime` の欄ごとの単体テストを新設                   |

**変異を当てて確かめたもの**（名指しした assert が落ちることまで）:

| 変異                                | 落ちるもの                                         |
| ----------------------------------- | -------------------------------------------------- |
| `initialize` 成功側の世代照合を削る | 新設2本とも                                        |
| `initialize` 失敗側の世代照合を削る | 「終端を名乗らない」                               |
| `CONTRIBUTING.md` に空行を戻す      | `ratchetIndex` の4本                               |
| `equalRuntime` の欄を1つ落とす      | その欄のケース（`enginePath` / `bookFile` で確認） |

**まだ観測できていないのは `finally` の同一性判定2つだけ**（`provider.tsx` / `initializer.ts`）。
robustness も差を作れず、理由（門が開いた先で `initialize` を撃つ者が居ない）を
`engine.md` の「埋まっていないセル」に書いた。

## 見ていない範囲

- `perf` / `ui` / `rust` reviewer は11ラウンドとも走らせていない
- **実プロセスでの確認は11ラウンドを通して1件も無い**
- マージで取り込んだ基底の30コミット分は、接続点以外レビューしていない
- r8 の flake は3ラウンド続けて再現せず。**機構は不明のまま**

## 次ラウンドへ

**マージを含むラウンドでは3点比較を必ず取ること**（merge-base に無く・マージ前の枝にあり・HEAD に無い行）。
git が衝突を出さずに畳んだ回も、衝突を手で解いた回も、これでしか見つからない。

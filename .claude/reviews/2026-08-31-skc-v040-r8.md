# レビュー skc-v040 ラウンド8

- 日付: 2026-08-31
- 範囲: ラウンド7の修正ぶん＋この PR の最終状態
- 走らせた reviewer: comment / robustness / rust
- 前のラウンド: `-r1.md` 〜 `-r7.md`

## 所見

### [HIGH] R8-01 「名乗れない候補」が配列の並び順で決まり、バイト順を取り違えた読み方を出す

- 場所: `search/kifu_reader.rs` の `try_other_encodings`
- reviewer: **robustness 2人が別々に実測**
- BOM の無い UTF-16 は LE と BE のどちらで復号しても誤りが出ないので、
  `had_errors` では区別が付かない。`get_or_insert` は先に当たったものを残すので
  **一覧の先頭にある UTF-16LE が常に勝つ**

  ```
  本文が UTF-16BE（4行目に読めない語）
    出ていた: at line 1 ... 䭢ࡔ牒᫿獞䭢…
    正しい:   at line 4 ...    2 パス
  ```

- r7 が足したテストは LE だけを見ており、**LE が通るのは並び順の偶然**だった
- 同じ根で、バイナリ（`.kif` に改名した JPEG）が
  「棋譜ではないファイル…」の案内を失っていた

### [BLOCK] R8-02 doc が呼んでいる `detect_encoding` / `detected` はもう存在しない

- 場所: `search/kifu_reader.rs`（4箇所）
- reviewer: comment
- r7 の改名の取りこぼし。**消した語彙（`detect` = 推測する）へ戻る導線**が
  doc に4本残っていた

### [HIGH] R8-03 「名乗れなくても理由は捨てない」が、化けた復号については成り立たない

- 場所: `search/kifu_reader.rs` の `can_be_named`
- reviewer: comment
- 化けた試行は `named` にも `anonymous` にも入らず、理由ごと消える。
  doc はそれも含めて「理由は残す」と読める
- **一番大事な理由（化けた復号が指す行番号は実在しない）がどこにも書かれていなかった**

### [MEDIUM] R8-04 ISO-2022-JP の3つの節のうち2つは誰も守っていない

- 場所: `search/kifu_reader.rs`
- reviewer: robustness（変異を当てた）
- r7 が足した「`ESC ( J` の題材」が `\x1b$B` を含んでいたので、
  **`\x1b(J` の節を消しても通る**。`\x1b(B` も同じ

### [MEDIUM] R8-05 `describe` に避けられる `expect` がある

- 場所: `search/kifu_reader.rs`
- reviewer: rust
- ガード節と `expect` に分かれており、腕を1つ足すと panic に変わる。
  非テストコードで `expect` はこの PR が触った5ファイル中ここだけ

### [MEDIUM] R8-06 `KifuReadError` の Display にパスが入り、画面で二重になる

- 場所: `search/kifu_reader.rs`、詰める側は `project_manager` / `api`
- reviewer: rust
- 呼び手は `IndexWarnPayload` の別の欄でも `path` を持ち、画面は両方を並べて描く

### [MEDIUM] R8-07 「クレートは UTF-16 を読めない」が BOM 付きでは成り立たない

- 場所: `search/kifu_reader.rs` の `read_portable` の doc
- reviewer: rust（実測）
- `Encoding::decode` は BOM sniffing をするので、**BOM 付きの UTF-8 / UTF-16 は
  クレート単体で読める**。総当たりが要る根拠の表が、理由ごと間違っていた

### [MEDIUM] R8-08 `describe` の優先順を直接見るテストが1本も無い

- 場所: `search/kifu_reader.rs`
- reviewer: rust
- 壊れ方が3度ともこの順序（R2-03 / R3-01 / R7-01）だったのに、
  確認は全て一時ファイル越しで「その題材が通る腕」しか見ていない

### [MEDIUM] R8-09 `VERSION` の doc に版ごとの履歴と PR 番号が入っている

- 場所: `search/index_cache.rs`
- reviewer: comment
- `CONTRIBUTING.md` の「元は〜だった」「PR #N で対応」。版1の中身はコードが読まない

## 重複・矛盾した所見

- R8-01 は robustness 2人が独立に、別の入力（BOM 無し UTF-16BE / 切れた EUC-JP /
  バイナリ）で同じ結論に達した。**直し方も一致していた**（「改行があること」を見る）
- rust は `describe` の分割について「**不要**。足りないのは分割ではなく
  この関数を直接見るテスト」と、r7 の comment（分割の合図）と逆の判断を示した。
  **rust を採った** — 6段が1関数にあること自体は読みにくくない

## 範囲外

| 内容                                                  | 送り先                   |
| ----------------------------------------------------- | ------------------------ |
| `atomic_write` が rename の前に `sync_all` を呼ばない | #329（この PR は未変更） |

## 見ていない範囲

- 3人とも `index_cache.rs` の符号化・復号本体、`fs_scan.rs`
- TS 側が `promote` / `relative` を埋めて送るか（#331）
- 実コーパス609件での文言の出方

## lint / hook で強制できるもの

- doc の識別子をイントラドキュメントリンクで書き、
  `#![deny(rustdoc::broken_intra_doc_links)]` + `cargo doc` を `verify:rust` に足す
  → **R8-02 は機械で落ちる。** 自前の grep を書くより rustdoc の既存 lint に載せるほうが安い
- `[lints.clippy] expect_used = "deny"` → R8-05
- `cargo-mutants`（#332）→ R8-04
- R8-01 / R8-03 / R8-07 は機械で拾えない

## 次ラウンドの対象

9件すべて直す。

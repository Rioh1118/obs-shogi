# レビュー 441-unmount-session ラウンド9

- 日付: 2026-09-07
- 範囲: `git diff origin/main...HEAD`
- 走らせた reviewer: react / robustness / comment / oss-hygiene
- 対象コミット: `684611f4`
- 前ラウンド: r1〜r8（同じディレクトリ）

## 所見

### [HIGH] 1. 捨てた席の `info` が、捨てる停止が飛んでいる間だけ照合を素通りする

reviewer: react（実測）

`retiredRef` は停止が**解決してから**書くので、その往復の間は「席は空・まだ retired でもない」。
盤を2手続けて動かすと、追い越された席の `info` が採られ、
**前の局面の評価値と読み筋が現在の盤面の解析結果として盤に出る**
——`matches` の doc が名指しで防ぐと書いている失敗そのもの。
`discard` はこの PR が新設した口で、r4-2（`retiredRef`）の直しがこの口だけ穴だった。

### [HIGH] 2. r8 が「未固定」と書いた2件は、テスト1本で両方固定できる

reviewer: robustness（実測。テストの本文まで示した）

「後ろに並んだ返却が飛んでいる間に畳まれても、指さない停止を撃たない」の1本で、
枠の自己照合と `sweepOnUnmount` の並びの**両方**が変異で赤くなる。
**結末は同じにならない**——枠が空くと、指さない停止（席を全部空ける）と
握っている席を返す停止が並列で線へ出る。

### [HIGH] 3. 「再現手段がテストに無い」と書いたセルの再現手段が、同じテストファイルに在る

reviewer: robustness / oss-hygiene（2人）

`(S6/P1, E1)` は #120 の BLOCK そのものと名指ししたセル。
「Rust 側に席を残したままフロントを S6 にする再現手段が無い」と書いてあるが、
`onError` を撃つテストが既にその状態を作っている。
r7-14（`(S1, E11)`）・r8-24（#172 のコメント）に続く**3回目**の「網羅の記録が現物より古い」。

### [BLOCK] 4. `shootQuietly` の「それまで ▶ は Rust に断られ続ける」が、▶ の復帰導線と食い違う

reviewer: comment

▶ は握っている席を返してから頼むので、`take_session` まで進む回は席が空いている。
返却が落ちた回はそこで throw して終わる。**r7-7 が ※1 で潰した嘘が、コード側に残っていた。**

### [HIGH] 5. 「指さない理由は ※12 に1つだけ」が、また3箇所に写された

reviewer: comment

※12・`useEngineSeat`・テストの3箇所に、3文まるごとの複製。
r3-16 → r4-17 → r8-13 に続く同じ形。

### [HIGH] 6. E6 の列で「→ S1 のまま」が S2〜S5 にも写っている

reviewer: oss-hygiene

`if (!isReady) return` は `syncWaitRef` にも `debounceTimerRef` にも触らずに抜けるので、
S2 / S3 で踏むと**その状態のまま**止まる（欄が非 null で残る）。r6-8 の直しで全行に写った。

## MEDIUM

7. **`provider.tsx` が540行・17 ref・8 effect で4つの機械を持ったまま**（react）。
   9ラウンドの振る舞い修正が全部同じ3関数に積まれた。→ **issue #489**
8. `reducer.ts` の新しいコメントが、消した action の説明になっている（comment）。
9. 画面仕様の「■ の停止が失敗 → F-6」が、F-6 自身の「■ の失敗は F-7」と逆（comment / oss-hygiene）。
10. 「席の判定はフックの中に1つだけ」が、唯一の呼び手側の門（`no-position`）と食い違う（comment）。
11. `releasingRef` の理由が同じファイルの中で3回書かれている（comment）。
12. `quietly` の呼び手が1つで、同じ関数の隣の枝は `shootQuietly` を直に呼んでいる（comment）。
13. `failure-surfacing.md` の「`clearError` は6スライスすべて」が、この PR で4になった（comment）。
    同じファイルが3行上で「呼び出し箇所の数は書かない（数えると嘘になる）」と書いている。
14. `releasingRef` の説明の二分（待てる側／待てない側）に `discard` が入っていない（robustness）。
15. ※13 が引き金を2つしか書いていないのに、E14 の3セルがそこを指している（oss-hygiene）。
16. `app.md` の「棋譜を閉じる」が A3（`activeKifuPath !== null`）へ落ちている（oss-hygiene）。
    **E14 の委譲元**なので、入口が壊れていると降りてこられない。`main` から在る。

## 重複・矛盾した所見

- 所見3 は robustness と oss-hygiene が独立に同じ行を指した。
- 所見9 は comment と oss-hygiene が同じ2行を指した。
- 矛盾は無し。

## 見ていない範囲

- `analyzer.rs` / `protocol.rs` の内部と #463 の窓の幅。**9ラウンド続けて未見。**
- `features/engine-position-sync` の実装。**9ラウンド続けて未見。**
- 実プロセスを使った検証は誰もしていない。

## lint / hook で強制できるもの

- **`SeatReleasePoint` を撃つ口が、`matches` の除外にも登録されているか**（所見1）。
- **同じ文が同一ファイル内で2度現れること**（所見5・11）。r8 の案は「2ファイル以上」だったので
  ファイル内の3重複を素通りする。
- **報告書が「未固定」と書いた行を、次ラウンドの焦点へ機械的に転記する**（所見2）。
- 所見3・4・6〜10・12〜16 は機械では止まらない。

## 修正の結果

| 所見              | 結果                                                             | コミット / 送り先 |
| ----------------- | ---------------------------------------------------------------- | ----------------- |
| 1 / 2 / 3         | 直した。捨てる時点で `info` を落とす／テスト3本（変異で確認）   | `223f91e0`        |
| 4 / 5 / 6 / 8 〜 16 | 直した                                                         | 直後のコミット    |
| 7                 | **issue #489** へ。自動再開の機械の切り出しは #441 の範囲外     | —                 |

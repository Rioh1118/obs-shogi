# レビュー book-to-main ラウンド27

- 日付: 2026-09-05
- 範囲: r26 の所見4件を直した3コミット（`git diff e55cee20 c7724cf2`）
- 走らせた reviewer: architecture / oss-hygiene（Rust の差分が無いので2人）
- 対象コミット: `c7724cf2`
- 前ラウンド: `.claude/reviews/2026-09-05-book-to-main-r26.md`

所見 **3件**（BLOCK 0 / HIGH 0 / MEDIUM 3）。
推移は 22 → … → 6 → 4 → 4 → **3**。

**3ラウンド続けて HIGH 0。**
3件のうち1件は例外の条件が名指しした片方で成り立たないもの、2件は数と帰属。

issue へ送るものは無かった。

---

## 所見

### AS-01 (MEDIUM, 1人が実測): 例外の条件が、名指しした `session.rs` で成り立たない

`session.rs` は `A` / `E` / `G` のほかに **`F-27` を6箇所**使っている。
これは `failure-surfacing.md` 自身の採番で、module doc は3種しか挙げていない。
しかも6箇所のうち2箇所だけ「台帳の F-27」と添えてあり、
**同じファイルの中で扱いが割れていた。**

同じ doc の中で `F-N` の位置づけも2通りに読めた ——「ここが採番するのは `F-N` だけ」
（＝一意なので表名は要らない）と「`F-1` と `F1` はハイフンの有無しか違わない」（＝衝突の例）。
**どちらを先に読んだかで「`session.rs` は例外に当たるか」の結論が変わる。**
当たらない側に転ぶと、この例外が救おうとした22行がまるごと再浮上する。

- 結果: 対応済み `d2b1482c`（`F-N` を例外の数から明示的に外し、module doc にも別枠だと書いた）

### AS-03 (MEDIUM, 1人が実測): `\n` 枝が落とす行数が、実測と26ずれている

hooks の行頭コメント224行のうち、`\n` 枝が落とすのは **198行**。
残り26行は字下げがあるので `[ \t]` 枝の担当。「224行全部がこの枝」と読める形で書いていた。
**r26 が AR-03 / AR-04 で「報告書の数を実測と一致させる」を所見にした、その同じ形。**

- 結果: 対応済み `a34b95a1`。併せて**最後まで生き残っていた変異**（`(?!!)` を外す。
  704本すべて緑のままだった唯一のもの）を塞いだ ——
  実装が「shebang は残す」と宣言している以上は固定する

### AS-02 (MEDIUM, 1人が実測): r26 が2つの module doc を「同じラウンドの直し」と括っていた

`session.rs` は規則と**同一コミット**（r24 の AP-04 対応）、`yaneuraou_db.rs` は
**1ラウンド後**（r25 の AQ-04 対応）。AR-02 の核は正しいが、経緯の粒度が違う。

報告書はラウンドの推移を追う唯一の記録で、毎回「前のラウンドの直しから出たか」を数えている。
**寄せると r25 の AQ-04 が生んだ差分が r24 の帳簿に付け替わる。**

- 結果: 対応済み `7a253d7a`

---

## 重複・矛盾した所見

- **3件とも1人ずつ**
- **AS-01 は AR-02 の直しが作った。** 例外を書いたが、**その例外が名指しした2ファイルの
  片方で成り立っていなかった** —— 規則を直すたびに、規則が名指しした現物を数え直していない
- **AS-03 / AS-02 は AR-03 / AR-04 と同じ根**（報告書と注記の数・帰属）。4ラウンド連続
- **shell の枝は、これで変異が全て塞がった。** architecture が8通りの変異を1つずつ当て、
  生き残ったのは `(?!!)`（宣言された意図）と、出力が区別できない2つ（置換文字列の違い）だけ。
  前者はこのラウンドで塞ぎ、後者は**唯一の利用者にとって判定が変わらない**ことが実測された
- 所見の水増しは無かった。2人とも「確認して所見が無かったもの」を明記していて、とくに:
  - **例外にも当てはまらず表名も無い箇所は、`src/` と `src-tauri/src/` に残っていない**
    （両ツリーを `\b[A-Z]-?[0-9]+\b` で全走査。残りは SFEN 文字列 / 棋譜形式のコード /
    CPU キャッシュ / 算術など、表の記号でないもの）
  - **module doc へ委ねる形にすべきファイルは他に無い**（表の記号が複数行出るのは
    `session.rs` 22行と `yaneuraou_db.rs` 14行だけ。次に多いのが1〜3行で全て表名つき）
  - **AR-03 の言い換え（8 / 10 は定義している文書の総数）は正しい**
  - **AR-04 の直しで、r24 と r25 のヘッダが同じ数え方（insertions）になった**
  - **`581637db` の2つの assert はどちらも load-bearing** —— 片方だけを殺す変異が
    両方向に存在することが実測で示された

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送った19件（再掲しない前提）
- `origin/main` が元から持っていた実装と doc の中身
- `docs/spec/screens/` の13面が現物の UI と合っているか
- `user_book1.db` の実測3行とピーク確保の実測
- `codeOf` の c-like 枝の分岐網羅
- **`F-N` を裸で書いている他のファイル**（`registry.rs` / `types.rs` / `protocol.rs` /
  `manager.rs` / `commands/game.rs` / `lib.rs`）。`origin/main` から動いていないので
  この PR が持ち込んだものではない

## この PR の外で観測したこと

**ゲートがまたメインのチェックアウトを検証した**（#342）。今回は
`src/search/cache/index_cache.rs` がコンパイルできない状態で、
**このワークツリーにそのディレクトリは存在しない。**

## lint / hook で強制できるもの

- **AS-03 で shell の枝の変異が全て塞がった**（8通り中、意味のある変異はすべてテストが殺す）
- AS-01 の型（注記の記号が module doc の宣言か同じ行の表名で説明されること）は
  機械化できるが、**`F-N` を数から外すかがまさに争点だった** ——
  条件を一意にしたのがこのラウンドなので、検査を書くならこの後。#351 の範囲
- AS-02（報告書の経緯の帰属）は機械で防げない

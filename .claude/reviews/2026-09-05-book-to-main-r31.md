# レビュー book-to-main ラウンド31

- 日付: 2026-09-05
- 範囲: r30 の所見5件を直した5コミット＋報告書（`git diff f1a569e6 0a07fc31`）
- 走らせた reviewer: architecture / oss-hygiene
- 対象コミット: `0a07fc31`
- 前ラウンド: `.claude/reviews/2026-09-05-book-to-main-r30.md`

所見 **7件**（BLOCK 0 / HIGH 0 / MEDIUM 7）。
推移は 22 → … → 4 → 4 → 5 → **7**。

**7ラウンド続けて HIGH 0。** 6件を直し、**1件は #351 へ送った**。

増えたのは、変異を**76通り**当てて `walk.ts` と `docsIdentifiers.ts` 側まで
踏んだため（r29 / r30 の両方が「見ていない範囲」に置いたままだった）。

---

## 所見

### AW-01 (MEDIUM, 2人が実測): r30 の AV-04 が書いた3つの数が、3つとも現物と合っていない

**reviewer の数をそのまま写して、自分で当て直していなかった。**

| 書いた値                            | 実測                                                                                                         |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| 707本すべて緑                       | 変異を当てた木は `f1a569e6` で **706**。707 は直した後の値                                                   |
| 145件のうち33件が「実在する」へ反転 | 検査が見るのは `docs/state-transitions/` の**195件**。原型でも `g` 除去でも `missingIdentifiers` は **`[]`** |
| `production_unwrap` が反転する      | corpus に**0回**しか現れない（唯一の実体が `src/__tests__/` で `includeTests: false` が外す）                |

**壊れる向きが corpus を太らせる側なので、`[]` は `[]` のままにしかならない。**
33 は反転数ではなく、corpus に新たに入り込む識別子形の綴りの異なり数
（corpus は 1,573,727 → 1,866,272 バイト）。

**「707本」は、r30 自身が40行下の AV-05 で r29 に対して立てた基準を破っている。**
しかも今回は**報告書だけが誤りでコミットメッセージ側が正しい**ので、
`git log` を先に読んだ人と報告書を先に読んだ人で違う数に着く。

**塞いだこと自体は正しい** —— `stripLineComments` の宣言（「行コメントを落とす」）を
固定するものが1本も無く、`docs/` が33件のどれかを引いた瞬間に
壊れた側だけが黙って緑になる。

- 結果: 対応済み `a8e3fff8`（理由を「いま赤くなる」から「宣言を固定するものが無い」へ書き直した）

### AW-05 (MEDIUM, 1人が実測): `missingIn` の語境界が片側しか固定されていない

`` `\b${name}\b` `` の**末尾の `\b` を落とす変異が、当てた時点の707本すべてを緑のまま通す。**
唯一のテストが与えるのは `STOP_WRITE_TIMEOUT` —— **接頭辞**を足した形で、
`WRITE` の直前は `_` なので末尾の `\b` を落としても当たらない。

doc は「接尾辞や接頭辞を足す改名は最も普通の形なので、そこが抜けると
検査の意味が大きく減る」と**両方向を宣言している。**
テストのコメントも「接尾辞を足す改名」と書きながら接頭辞を与えていた。

`docs/` には現に `config_write` → `config_write_failed` の対がある。

- 結果: 対応済み `548a80d5`（現物の綴りで1本足し、**この1本だけが落ちることを確認**）

### AW-06 (MEDIUM, 1人が実測): `.sh` を歩けなくなっても緑になる

`sourceFiles` の拡張子から `sh` を落とす変異が、当てた時点の707本すべてを緑のまま通す。

`walk.ts` は「**`.sh` も含む。** 拡張子で外すと、機械の doc にだけ規約が
掛からない場所ができる」と設計の理由を名指ししているのに、その保証が無テスト。

空振り止めが効かないのは、床が4つの根の**合計**に置いてあるため ——
`SRC 489 / RUST_SRC 64 / src-tauri/tests 13 / .claude/hooks 2` の568に対して床は150。
`.sh` を拾う根が丸ごと落ちても566で、床の3.8倍あるので素通りする。

**`verify-gate.sh` のコメントに経緯を書き放題になるのに、赤くなる検査が1本も無い。**

- 結果: 対応済み `1e26b597`（根ごとに見る assert を足し、変異で落ちることを確認）

### AW-02 (MEDIUM, 1人が実測): 訂正済みの記述がメッセージに残る組は他に3つあった

`be6bda97` は「このループで初めて出た形」として1組にだけ注記を置いたが、
`git log` を走査すると同じ組が3つある。

- `d6336676`（r18）「148コミットの型」 —— r19 の AJ-01 で数を落とした
- `e1248822`（r12）「9件すべて削る…新しい文は1つも書いていない」 ——
  前半は r14 の AE-06、後半は r13 の AD-04 が偽と確定させた
- `1110e34b`（r27）「shell の枝は変異が全て塞がった」 ——
  当て漏らしが r28 / r29 / r30 で**3度**見つかっている

**r29 の AU-03 が「直近の4本だけ消すと境界に根拠が無い」として全11本に広げたのと
同じ落とし残しが、今度は訂正注記の側で起きていた。**

- 結果: 対応済み `19eed3ed`（3組それぞれに1行置き、r30 の「初めて」を落とした）

### AW-03 (MEDIUM, 1人が実測): 鉤括弧で2つのメッセージに掛けた鎖が、片方にしか無い

`e157700c` のメッセージは「同じ場所が4ラウンド連続で」で、**所見番号を1つも書いていない。**
**鎖を自分の主張として述べているのは `6450d325` だけ**（`git log --grep=AP-06` 自体は、
訂正側のコミットを含めて7本に当たる）。

- 結果: 対応済み `222d9929`（残り方が違うことを分けて書いた。
  「4ラウンド連続」が訂正前の値であること自体はどちらも真なので、r28 の注記は成り立っている）

### AW-04 (MEDIUM, 1人が実測): 「綴りの異なり数では9」が、直前に並べた綴りの数（7）と合わない

集合は `{F-02, F-04, F-05, F-06, F-10, F-12, F-15}` の**7**。
9 になるのは**ファイルごとに数えて足した**とき（r2 が5、r5/r6/r7/r8 が各1）。

**AV-02 の直しは3つの数え方を並べる形だったのに、
3つ目だけが他の2つ（どちらも全体の合計）と違う集計になっていた。**

- 結果: 対応済み `8b377e89`

### AW-07 (MEDIUM, 1人が実測): 「0件を見て緑」の床が実測の 1/19 → **#351 へ送る**

`docsIdentifiers.test.ts` の `toBeGreaterThan(10)` に対して、
**その `expect` が比べる `found.length` の実測は 214**（ファイル跨ぎの重複を含む延べ。
綴りの異なり数は195）。1文字の変異で範囲が **93%** 消えても緑になる。

- ``[^`\n]``→``[^`]``（否定クラスから `\n` を落とす）: 拾い数 195 → **157**
- `[...found].sort().slice(0, 1)`: 拾い数 195 → **13**（床10を上回るので緑）

同じ形が `commentHistory.test.ts`（150 対 568）と
`turnGlyphLiterals.test.ts`（100 対 **310**）にもある。
**後者が数えるのは `tsFiles(SRC, { includeTests: false })`** で、
`sourceFiles(SRC)` の489ではない。

**送る理由。** コメントが宣言しているのは「0件を見て緑になる形を止める」で、
床はその宣言を literally 満たしている。**書いた事が偽ではないので、案A では
この PR の範囲外**（r3 で決めた「事実の誤りだけを直し、締められる余地は issue へ」）。
`EXPECTED_MIN` と同じラチェットに変える判断は3ファイルに跨るので、まとめて #351 で扱う。

- 結果: **#351 へ**（この PR では直さない）

---

## 重複・矛盾した所見

- **AW-01 は2人が別々に到達した**（このループでは久しぶり）。残る6件は1人ずつ
- **AW-01 が最も重い。** 「変異を当てて塞いだ」の記述が5度目に外れたが、
  **今回は取りこぼしではなく、書いた数そのものが検算されていなかった。**
  reviewer が出した数を写して、実際の検査に当て直していない ——
  **このループが AC / AQ / AR / AT / AV で繰り返し所見にしてきた形を、
  変異の結果を書く側でやった**
- **AW-05 / AW-06 は同じ根**（doc が宣言した保証にテストが無い）。
  r27 の AS-03（`(?!!)`）で確立した扱いをそのまま当てた
- **AW-02 は AU-03 と同じ根**（直した instance だけに当て、形の全体に広げない）。
  **AU-03 でその教訓を書いた次のラウンドで、訂正注記の側で再発した**
- **AW-03 / AW-04 は数え方と引用の精度**。7ラウンド連続
- 所見の水増しは無かった。2人とも「確認して所見が無かったもの」を明記していて、とくに:
  - **`298a5509` の新テストは `g` 変異の唯一の殺し手**（落ちるのはこの1本だけ）
  - **r30:28-32 の3行（入力 / `orig` / `g` 除去）は現物と完全に一致**
  - **`stripLineComments` / `stripShellComments` の単一変異21通りは尽きた。**
    生存9件のうち3件は**出力が1バイトも違わない等価変異**、1件は実 corpus で出力完全一致
  - **2重変異13通りに、答えを変える生存は1件も無い**（枝を跨ぐ組を含む）
  - **`g` 除去は `codeOf` を使う他のラチェットの答えを1件も変えない。**
    呼び手は grep で4つ。**r30 が挙げた `commentHistory` / `asyncResultUse` /
    `escapeReceivers` は `codeOf` を import していない**（自前の正規表現と AST を使う）
  - **AV-01 の鎖は3組とも正しい**（AQ-03 が r25、AR-04 が r26、AT-01 が r28）
  - **r28 に索引が揃っていない箇所は残っていない**
  - **AV-02 の「10行」「11出現」は実測と一致**
  - **AV-05 の直しは正しい**（`e157700c` = 705、`88dd1666` は1本だけ足す）
  - **「全11本」は一致**（`6450d325` が4本、`83d0f1b8` が7本）
  - **`walk.ts` の `includeTests` の既定は3通りで確実に殺される**
  - **`IDENTIFIER` の下線要求は両側とも殺される**。`EXEMPT` は7件中6件が殺される

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送ったもの（AW-07 を含む。再掲しない前提）
- `origin/main` が元から持っていた実装と doc の中身
- `docs/spec/screens/` の13面が現物の UI と合っているか
- `user_book1.db` の実測3行とピーク確保の実測
- 3重以上の変異
- `hitsIn` の行番号計算そのものの変異
- `stateTransitionIndex.ts`（`markdownFiles` / `docsPath`）の変異
- `codeOf` の c-like 枝は r30 が22通り当てたきりで、このラウンドは当て直していない
- `stripShellStrings` は r30 が単一15＋2重10で尽くしたきり
- `.claude/reviews/` の `book-to-main` 以外のシリーズで、
  「本体を直したがメッセージが訂正前」の組が残っていないか
  （走査したのは book-to-main の r1〜r30 と book-foundation のみ）

## lint / hook で強制できるもの

- **AW-05 / AW-06 は変異で確かめたテストが入った**（語境界の接尾辞側1本、`.sh` の根1本）
- **AW-07 は #351 へ送った**（3ファイルに跨る床のラチェット化）
- **#351 で回す定型変異の落とす基準は3つ要ることが分かった。**
  「生存**かつ** `sourceCorpus()` が変わる」だけでは取りこぼす ——
  `missingIn` の末尾 `\b`（AW-05）は corpus が1バイトも変わらず**答え**だけが変わり、
  `sh` 落とし（AW-06）は corpus も答えも変わらず**歩いた本数**だけが変わる。
  (a) corpus のハッシュ / (b) `missingIn` を広い集合に掛けた結果 /
  (c) 各ラチェットが歩いたファイル数、の3つ
- AW-01 / AW-02 / AW-03 / AW-04（数え方・帰属・訂正注記）は機械で防げない

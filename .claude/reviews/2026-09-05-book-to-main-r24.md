# レビュー book-to-main ラウンド24

- 日付: 2026-09-05
- 範囲: r23 の所見8件を直した7コミット（`git diff 208d4fe2 34d5840e`）
- 走らせた reviewer: architecture / oss-hygiene（Rust の差分が無いので2人）
- 対象コミット: `34d5840e`
- 前ラウンド: `.claude/reviews/2026-09-05-book-to-main-r23.md`

所見 **6件**（BLOCK 0 / **HIGH 2** / MEDIUM 4）。
推移は 22 → … → 6 → 7 → 8 → **6**。

**6件すべてが r23 の直しから出た。** 2ラウンド続けて「直した記述そのものが不正確」。

**HIGH 2件は、r23 の HIGH（採番の宣言）を直したその文が、また数え落としていた**もの。
`G` の4つを列挙したが、**同格かそれ以上の衝突を4つ落としていた**。

issue へ送るものは無かった。

---

## 所見

### AP-03 / AP-04 (HIGH, 1人): 記号の衝突を数え上げる形が、また数え落とした

`72f49466` は `G` の4つを列挙したが、落としていたもの:

- **`A` は `app.md` と `game-session.md` で A0〜A4 の5つが完全に重なる。**
  しかも `game-session.md` は「上位は `app.md`」と書いて明示的にそちらへ送っている
  ——**親子を宣言した2表の間**で、`G` の3つより多い
- `P` は4表（`board.md` / `analysis.md` / `engine.md` / `game.md`）
- `L` は `failure-surfacing.md` 自身の L0〜L2（層）と `app-layout.md` の画面状態
- `T` は `file-tree.md` の状態と、`PREMISES.md` ほかの段階（tier）

`S` / `E` の「ほぼ全ての表が使う」も外れていた（記号を持つ26表のうち8本と9本）。

**列挙は表が増えるたびに腐るし、数え落とすと「他には無い」と読まれる。**
r23 の AO-02 が HIGH とした理由がそのまま再発していた。

併せて規則の未達（AP-04）—— 「記号だけで書かない」は `G` が6件中3件しか直っておらず、
`A` は12件、`E` も `src/` に3件あった。**規則は書いた瞬間にまた偽になっていた。**

- 結果: 対応済み `0165bc18`（数え上げをやめ、「記号は表ごとに閉じている。どの表かを必ず添える」
  だけを残した。件数が多い `session.rs` は module doc に出典を1行置き、個別の注記はそちらへ委ねた）

### AP-01 / AP-02 (MEDIUM, 1人が実測): 「行末の `#` は落とせない」が偽

`8764e278` は不可能性を主張したが、シェル本来の規則（`#` が語頭にあるときだけコメント）で
区別できる。挙げた根拠2つも実例になっていない —— `$#` は hooks に0箇所、
`${kinds# }` は二重引用符の中なのでどのみち文字列として潰れる。

**不可能と書くと、次に塞ごうとした人が引き返す。** 穴は現に開いたままだった。

さらに**足した2本目の assert が空虚だった**（AP-02）。入力を二重引用符で書いたので
期待値の `kinds` は左辺から必ず取れる。**このコミットが直そうとした形
（テストが前提を固定していない）が、足した2行のうち1行でそのまま再現していた。**

- 結果: 対応済み `55dc0608`（語頭の `#` を切る形にして塞いだ。**現物への影響は0行**。
  文字列を先に潰す順へ。空虚な assert も書き直した）

### AP-05 (MEDIUM, 1人が実測): 免除の4条件のうち `LAYER` の枝を突いていなかった

`ce4462f3` の7本は、旧実装へ戻すと4本落ちるが、**`&& !LAYER.test(bare)` だけを消すと
36本すべて緑のまま**だった。枝が効くのは「レイヤ名で始まるのに実在しないパス」だけで、
非免除の5件はどれもその枝を通らない。

誰かが「冗長」と読んで落とすと、**こちら側の腐ったレイヤ相対パスの行番号が黙って免除される**
——AN-02 で HIGH として挙がった形の再発。

- 結果: 対応済み `352a5364`（1行足し、枝を落とすと落ちることを確認）

### AP-06 (MEDIUM, 1人): r23 のヘッダが、レビュー対象でない木を「対象コミット」に据えていた

`89fafa39^` は `ce4462f3`（r23 自身の修正）で、**そのラウンドで見つけた所見が既に直っている木。**
他のラウンドは全て「レビュアーが見た木」をハッシュ1つで書いていて、r23 だけ規約が崩れていた。
コミット数も6が正しい。

- 結果: 対応済み `da74e043`

---

## 重複・矛盾した所見

- **6件とも1人ずつ。** 2人が別々の範囲を見たので重なりが出ていない
- **2ラウンド続けて「直した記述そのものが不正確」。** r23 の AO-02 も r24 の AP-03 も、
  **列挙で網羅を名乗る**形。r7 の X-02 で「列挙をやめる」と決めた判断を、
  17ラウンド後に同じ場所でやり直したことになる
- **AP-02 / AP-05 は同じ根**（足したテストが名指しした変異を止めない）。
  r23 で「テストが無い」を直したのに、**足したテストの一部が空虚だった**
- 所見の水増しは無かった。2人とも「確認して所見が無かったもの」を明記していて、とくに:
  - **oss-hygiene が `G` の4つ・`F1` / `F2` の帰属・`H` が1表だけであることを
    `docs/` 全体の grep で確かめ**、そのうえで落としていた4つを挙げた
  - **architecture が語頭規則を `.claude/hooks/*.sh` 2ファイルに当てて
    差分0行を実測し**、「落とせない」が偽であることを示した
  - `gate` 種別の落とし残しは無く（`rg --hidden` で残骸0）、
    `.claude/hooks/*.sh` → `ts` → `npm run verify` → `test:hooks` の連鎖も実測で確認された

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送った19件（再掲しない前提）
- `origin/main` が元から持っていた実装と doc の中身
- `docs/spec/screens/` の13面が現物の UI と合っているか
- `user_book1.db` の実測3行とピーク確保の実測
- **`session.rs` の `A` 12件・`E` 4件の個別の注記**（module doc に出典を置いて委ねた。
  個別に表名を書く形へ戻すかは、次に触る人の判断）

## この PR の外で観測したこと

**ゲートが再びメインのチェックアウトを検証した**（#342）。
`src/search/position/sfen_position.rs` の3本が落ちたが、**このワークツリーに
そのディレクトリは存在しない**。こちらの `verify:rust` は green。

## lint / hook で強制できるもの

- **AP-01 / AP-05 は変異で確かめたテストが入った**（語頭の `#` 4本、`LAYER` の枝1本）
- AP-03 の型（表の記号の名前空間が衝突しないこと）は `stateTransitionIndex` に
  足せば機械化できるが、**r22 / r23 で既に #351 の候補として送済み**
- AP-06（報告書ヘッダのハッシュ）は機械で防げない

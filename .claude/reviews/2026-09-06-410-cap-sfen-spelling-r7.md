# レビュー 410-cap-sfen-spelling ラウンド7

- 日付: 2026-09-07
- 範囲: `fix/410-cap-sfen-parse-error`（`origin/main` 2273b3a0 との差分、43コミット）
- 走らせた reviewer: rust / robustness / comment
  （architecture は r6 で「焦点に新しい所見は無い」と答え、残りは `/tidy-commits` の
  下ごしらえだったので今回は回していない）
- 対象コミット: `畳み込み済み`

**r6 の焦点への答え**:

1. `describe` の3腕と `unreadable_record` の7腕の語順 → **揃っていた**（rust / comment）
2. 実在しない綴り → **0件**。rust がバッククォート107件、comment が625件を機械的に引いた
3. テストの番人の18文字の窓 → **rust が場合分けで塞がっていることを示した。所見に上げず**

## 所見

### [MEDIUM] R7-01 join error の3本だけ、基準から外れたまま残っていた（robustness）

`spawn_blocking join error: {e}` が3箇所（索引側2つ・検索側1つ）。刈る規約には入れたが
**言葉は内部の語彙のまま**で、そのファイルの局面が1件も索引に入らないことも、
次に何をすればよいかも言っていなかった。

**同じ差分が、まったく同じ欠陥をもう1本（`scan failed`）について直している。**
兄弟が直って、これだけ残った。

### [MEDIUM] R7-02 `parse_failed` の doc に「組みながら」が残っていた（comment）

r6 で3行上を直したのに、同じ doc ブロックの下が残っていた。`parse_failed` の呼び手は
5つで**全部が組み上がった `String` を渡している**ので、この主張は誰も満たしていない。

### [MEDIUM] R7-03 語順の規約が `describe` の doc から辿れない（comment）

規約は `unreadable_record` の doc にしか無く、主語が自分の腕に閉じていた。
**r6 で `describe` の腕が外れていたのは、読める場所に規約が無かったから。**

### [MEDIUM] R7-04 状態遷移表の1行目と4行目が、鍵だけでは区別できない（comment）

`Err` → `Ok` に直した副作用。1行目の鍵（`Ok`、`warns` 空）は4行目を文字通り含むのに、
局面の欄は「入る」と「入らない」で正反対。**この表が自分の存在理由として書いている
用途（doc と issue が2回続けて逆を書いた）で誤読させる。**

### [MEDIUM] R7-05 `MESSAGE_LIMIT` の名前を doc が段落で打ち消していた（comment）

見出しと第2段落が、名前が言っていることを取り消すためだけに存在していた。
R4-08 で `capped` に適用した基準（`CONTRIBUTING.md`「名前で表せるものはコメントにしない」）が
同じファイルの定数に掛かっていなかった。

### [MEDIUM] R7-06 `IndexWarnPayload::message` の doc が「先頭＝最古」を書いていない（comment）

「先頭5件だけ」と事実は書いたが、そこから出る結論（**後から出た警告は描かれない**）と
#465 への参照が無かった。

## 所見なし の申告

- **rust: 所見なし。** 語順・綴り・テストの番人・Rust としての問題を全部当たって0件。
  `expect` 4件の根拠、`let _ = write!` の理由、確保の頭打ちが実際に効くこと、
  ロックが `await` を跨がないことまで確認している

## 重複・矛盾した所見

- 無し。3人の所見が重ならなかったのはこのラウンドが初めて

## 見ていない範囲

- アプリを起動した実測は無い（3人とも）
- `npm run verify`（TS 側）/ `cargo audit` / `npm audit` は未実行
- `benches/search_bench.rs` / `search/cache` / `search/store` の中身
- R7-01 の到達頻度（棋譜クレートの中で panic する題材を作っていない）

## robustness がまとめた棚卸し

#410 について、利用者から見て何が直り何が残っているかを17行の表にしている
（この報告書の本文には写さない。原文は `/private/tmp/.../tasks/` の記録に残る）。要約:

- **#410 の本体3件**（20万文字で潰れる / どの字が悪いか消える / `+9` を `9` と誤って名指す）は閉じた
- **その修正が途中で壊した2件**（失うものを言う一文が消える / 案内が引用に押し出される）も閉じた
- 残りは全部 `src/` 側か別の関心事で、**R7-01 を除いて issue に載っている**

## lint / hook で強制できるもの

- **doc の腐りは7ラウンド連続で出ているが、機械化できる形はもう残っていない。**
  パス・識別子・CSS クラスの実在はすべて0件になり、残っているのは「書いてあることが本当か」だけ
- R7-01 は型では閉じられない（`BuildWarn` のように一文が本文へ織り込まれている腕がある）。
  テストで持つのが妥当

## 修正計画（r7 → r8）

### このラウンドで直したもの

| 所見           | 直し方                                                         |
| -------------- | -------------------------------------------------------------- |
| R7-01          | 3本とも利用者の言葉にし、失うものを付けた（`畳み込み済み`）    |
| R7-02 〜 R7-04 | 同上のコミットで doc と表を現物に合わせた                      |
| R7-05          | `EMBEDDED_VALUE_LIMIT` に改名し、doc の第2段落を1行に縮めた    |
| R7-06          | 「先頭＝最古なので後から出た警告は描かれない」と #465 を書いた |

### 次ラウンドの焦点

1. **R7-01 の3本が、`IndexWarnPayload::message` の doc の基準を本当に満たしているか**
   （何が起きて・何を失い・次に何をするか。300文字に収まるか）
2. 改名（`EMBEDDED_VALUE_LIMIT`）の残骸が無いか
3. **所見が0なら0と書くこと。** r7 は rust が0、comment が5（全部 doc）、robustness が1。
   振る舞いを変えたのは R7-01 の1件だけ

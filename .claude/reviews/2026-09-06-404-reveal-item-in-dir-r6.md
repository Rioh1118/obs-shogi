# レビュー 404-reveal-item-in-dir ラウンド6

- 日付: 2026-09-07
- 範囲: `fix/404-reveal-item-in-dir`（HEAD `畳み込み済み`、`origin/main` = `8d53ea45`）
- 走らせた reviewer: architecture / react / robustness / comment / oss-hygiene
- 前ラウンド: `2026-09-06-404-reveal-item-in-dir-r5.md`

所見は **14件**（r1: 18 → r2: 21 → r3: 24 → r4: 20 → r5: 13 → r6: 14）。
うち**実装に効くのは6件**で、残りは doc の主張と、報告書・履歴の作法。

## 所見

### R6-01 [MEDIUM] `sliceBarrels` の「スライスの中」の判定が前方一致（architecture、実測）

`startsWith("src/entities/engine")` は `src/entities/engine-presets/...` にも一致するので、
隣のスライスからの深い import が素通りする（現に2件ある）。

- 結果: 見送り（issue #476）検査そのものの穴で、#404 の前から在る。
  この PR は `entities/engine` の barrel への追加を取りやめたので、増やしてもいない

### R6-02 [MEDIUM] barrel に `classifyEnginesDir` を載せたが、引数の `DirInfo` は非公開（architecture）

呼び出し元は結局 `api/aiLibrary` を深く読むので、同じスライスに入口が2つできる。

- 結果: 対応済み（畳み込み済み）barrel に載せず、`features/settings` が既に使っている
  深い import の形へ揃えた

### R6-03 [MEDIUM] 「呼び出し元の無い許可が残っていないか」を見ているのは `open_path` だけ（architecture / comment / oss-hygiene の3名、実測）

`opener:allow-open-url` や `opener:default` に置き換えても緑のまま。
このブランチが落とした「誰も呼ばない許可」がそのまま戻せる。

- 結果: 対応済み（畳み込み済み）識別子の集合で一般化した。ACL の解決は模していない

### R6-04 [MEDIUM] `Record` を引く手前に三項が残り、`unknown` の行が到達しない（architecture）

書き換えても画面が変わらず、tsc もテストも言わない。R5-04 を直した直後のコミットが、
その三項を表の手前に残していた。

- 結果: 対応済み（畳み込み済み）三項を落とし、`enginesDir` の不変条件に任せた

### R6-05 [HIGH] `currentRootRef` をレンダで書くので、切り替え直後の窓で関門が素通りする（react、実測）

`setLocalAiRoot` の直後に返る `await` の継続は再レンダより先に走る。
実測で `scanAiRoot` の呼ばれ方が `[A, B, A]` になり、**選んだルートの走査結果が捨てられて
「読めていません」で固定される**。

- 結果: 対応済み（畳み込み済み）ルートが変わる場所（`onPick` と同期の effect）で書く

### R6-06 [MEDIUM] `onEnsureEngines` の**正常な成功**を固定するテストが1本も無い（react、変異で確認）

作成後の読み直しを丸ごと殺しても 11 passed だった。その変異が入ると、押した利用者の画面は
「診断中…」のまま止まる。

- 結果: 対応済み（畳み込み済み）作成 → 読み直し → 開けるようになる、を1本足した

### R6-07 [MEDIUM] `unknown` の回に、隣の2枚のカードが「0 件」と断言する（react / robustness）

`engines` のカードだけ `—` にしても、隣が数を言い切るのでそちらが読まれる。
「下のフォームから作成できます」が指すフォームも、その回は画面に無い。

- 結果: 対応済み（畳み込み済み）索引を読めたかどうかを渡し、3枚を同じ分岐に通した

### R6-08 [HIGH] 「engines/ を開いて実行ファイルを配置してから」の案内が、実挙動と噛み合わない（robustness）

`reveal_item_in_dir` が開くのは**親フォルダ**（3つの OS とも）。案内どおりに操作すると
実行ファイルは `ai_root` 直下に置かれ、スキャンしても0件のまま同じ案内が出る。

- 結果: 対応済み（畳み込み済み）「場所を表示するので、その中に置いてから」に直した。
  ボタンの文言（「開く」）は据え置き——利用者が決めた判断

### R6-09 [MEDIUM] 「下のフォームから作成できます」の指す先が、その状態では描かれていない（robustness）

- 結果: 対応済み（畳み込み済み）で文言は出し分けたが、**条件が1箇所に寄っていない**ので issue #475

### R6-10 [MEDIUM] `unknown` を塞ぐ理由が、実装している条件より狭い（comment）

「作成はルートの存在を確かめるので同じ失敗が返る」が成り立つのは3経路のうち1つだけ。

- 結果: 対応済み（畳み込み済み）根拠を「`other` と区別が付かない」に直した。
  `other` 側の「必ず失敗する」も、リンクなら通る（#469）ので断定を外した

### R6-11 [MEDIUM] `revealFailureNotice` の「文面の決まりはここに閉じる」が事実でない（comment）

同じ決まりが spec にほぼ逐語で並び、台帳はそちらを正典として指している。
しかも中身は既に片寄っていた。

- 結果: 対応済み（畳み込み済み）画面の仕様は `docs/spec` が持つ、という流儀に寄せた

### R6-12 [MEDIUM] 検査の冒頭 doc が定数に付いていて、範囲も実際より広い（comment）

- 結果: 対応済み（畳み込み済み）空行で独立させ、範囲は R6-03 の一般化で doc に追い付かせた

### R6-13 [MEDIUM] r1〜r3 の報告書が指すコミットが41件解決しない（oss-hygiene、実測）

r5 は `git cat-file -e` で確かめたが、**宙ぶらりんのコミットでも成功する**ので判定が誤っていた。
正しくは `git merge-base --is-ancestor <hash> HEAD`。rebase 前のハッシュなので、
clone した人には最初から引けない。

- 結果: **`/tidy-commits` の後に一度だけ差し替える**（tidy でまた無効になるため）。
  この報告書の下に手順を残す

### R6-14 [MEDIUM] 台帳の出典（ブランチ名・ハッシュ）が squash マージの後は検算できない（oss-hygiene、実測）

名指しされた5つのブランチは `git ls-remote` に1つも無く、`main` `畳み込み済み` も
`origin/main` の履歴に無い（squash 済み）。

- 結果: 見送り（issue #474）規約そのものを PR 番号へ変える話。この PR の3箇所も
  同じ形式で書いてあるので、規約が変わったときに一緒に移す

### R6-15 [MEDIUM] 履歴に赤いコミットが2つ、tidy で消えるハッシュを指す subject が2つ（oss-hygiene）

`畳み込み済み`（`scanButton` の定義漏れ）と `畳み込み済み`（型検査が通らない）で `npm run verify` が赤い。
直した側の subject が枝の中の短縮ハッシュを指している。
`openerCapability` は 0 → 402行 → 169行 → 230行と往復していて、16コミットのうち約8が差し引き0。

- 結果: **`/tidy-commits` で畳む**（PR の直前）。畳めば赤い中間点も subject 内のハッシュも同時に消える

### R6-16 [MEDIUM] `settings.md` の「対象」が存在しないファイルを名指ししている（oss-hygiene）

`src-tauri/src/ai_library.rs` は `7a4f8872` の分割で消えている。
この PR がいちばん触った判定（`dir.rs` / `scan.rs`）へ、仕様書の1行目から辿れない。

- 結果: 対応済み（畳み込み済み）`src-tauri/src/ai_library/` に直した

## 重複・矛盾した所見

- **R6-03 は3名、R6-07 は2名**が独立に挙げた
- **R6-04 / R6-07 は同じ根**——`Record` に寄せたのに、その手前や隣に畳む口が残っていた
- **R6-13 / R6-14 / R6-15 も同じ根**——**この repo では短縮ハッシュとブランチ名が生き残らない**
  （squash マージ・rebase・`/tidy-commits`）。報告書・台帳・commit message の3箇所で同じ形で腐る

## 見ていない範囲

- **実機の挙動は6ラウンドとも誰も見ていない**
- Rust 側（この差分は `capabilities/default.json` 以外触っていない）
- SCSS と実描画、支援技術での読み上げ
- issue 送りのもの（#448 #450 #451 #452 #453 #454 #455 #456 #461 #462 #464 #468 #469 #474 #475 #476）

## PR の直前にやること

1. `/tidy-commits`——`畳み込み済み`＋`畳み込み済み`、`畳み込み済み`＋`畳み込み済み` を畳み、
   `openerCapability` の往復を潰す
2. **その後で** `.claude/reviews/` の全ハッシュを、`git merge-base --is-ancestor` で
   検算しながら subject で引き直す（tidy の前にやると二度手間になる）
3. PR の説明に書くのは**残ったもの**——(a) #404 の症状と直し方、(b) 通知の土台を通る
   グローバルの1件目（F-13）、(c) `engines` の4状態、(d) 検査は識別子の集合しか見ない（#462）。
   **「ACL を検査で担保した」とは書かない**

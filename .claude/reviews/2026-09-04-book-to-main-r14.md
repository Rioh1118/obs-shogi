# レビュー book-to-main ラウンド14

- 日付: 2026-09-04
- 範囲: r13 の所見6件を直した6コミット（`git diff e1248822 HEAD -- . ':!.claude/reviews'`）
- 走らせた reviewer: comment / robustness / oss-hygiene（Rust の差分が無いので3人）
- 対象コミット: `cfaa8251`
- 前ラウンド: `.claude/reviews/2026-09-04-book-to-main-r13.md`

所見 **7件**（BLOCK 0 / HIGH 0 / MEDIUM 7）。
推移は 22 → 18 → 23 → 10 → 12 → 7 → 5 → 4 → 6 → 8 → 9 → 9 → 6 → **7**。

**7件のうち5件は報告書自身の記述**（AE-03 〜 AE-07）で、コードにも doc にも掛からない。
実装と doc に掛かるのは AE-01 / AE-02 の2件で、どちらも**同じ削除の取り残しと削りすぎ。**

**このラウンドの根は1つ。** r13 で「削って直す」に方針を変えたが、
**削るときに、その記述に寄りかかっている側を数えていない。**
r13 の AD-01（削ったら参照が空を指した）と同じ形が、今度は3箇所で出た ——
テスト側のコメント（AE-01）、免除を切り出す条件（AE-02）、報告書の内訳（AE-05）。

issue へ送るものは無かった。

---

## 所見

### AE-01 (MEDIUM, 3人が実測): 削った偽の全称が、テスト側の説明に残っていた

`daef0e7b` は `verify-gate.sh` と判定表から「競合を抱えたツリー＝検証が必ず落ちる状態」を
削ったが、**同じ命題を括弧の中で定義の形にしたものが `verify-gate.test.sh` に残っていた。**

reviewer が実測 —— `README.md` だけが `UU` のツリーで免除を潰した版に `git rebase --abort` を
食わせても標準出力0バイト・rc=0（`gate_kinds_for_path` が `README.md` をどの種類にも当てない）。

免除の範囲を動かす人が最初に開くのは実装のコメントか `expect_teardown` の並び。
**実装側だけ直したので、潰した判断材料がテスト側から次のラウンドへ渡る。**

- 結果: 対応済み `1c187579`（根拠は `gate_is_teardown` の上に一本化し、ここには参照だけ）

### AE-02 (MEDIUM, 1人が実測): 残った根拠1つでは、免除しない綴りと区別が付かない

`daef0e7b` は偽の全称を削ったが、**削りすぎていた。** 残した「中身が手元のツリーではない」は、
免除しない綴りにも同じだけ当たる —— reviewer が実測 —— `git merge --no-edit topic` も
`git cherry-pick <sha>` も、手元の未ステージの変更を取り込まないコミットを作る。
どちらも検証する側に置いてある。**額面どおり適用すると、免除の集合が書かれた根拠より広く動く。**

さらに判定表は「ここでは『余分な検証が走るだけ』が成り立たない」を残したまま、
それを支える理由だけを失っていた。結論は真だが、書かれている前提からは導けない。

- 結果: 対応済み `df60dc22`。**その条件も偽だった** —— 種類に当たっても落ちない組み合わせが
  実在する（r15 の AF-01 で、種類と検査の対応を主張しない形へ縮めた）

### AE-03 (MEDIUM, 2人が実測): r13 の「対象コミット」が存在しないオブジェクトを指している

`git cat-file -t 9c1eeb5c` は `Not a valid object name`。正しくは `e1248822`。
**このラウンドの reviewer 3人が実際に範囲指定で落ちて、自力で引き直した。**
r13 が AD-05 で「報告書の自己申告が現物と合っていない」を潰した直後に、同じ形が自身のヘッダに入っていた。

- 結果: 対応済み `f76f6553`

### AE-04 (MEDIUM, 2人): 数え落としを直した文が、入れ替えた先で別の数を外していた

`c8627a11` が索引に新設したのは3行。4 が当たるのは「Rust の行を1から4に増やした」という
**合計**の方。`3090eb20` は「数を書かない形へ」と宣言して直した同じ段落に、新しい数を入れて外していた。

- 結果: 対応済み `c1400c5c`（件数を削った）

### AE-05 (MEDIUM, 1人): r13 の内訳が AD-06 をどちらの箱からも落としている

3 + 2 = 5 で6件目が漏れていた。AD-06 の直しは `CONTRIBUTING.md` の1ファイルで、
**報告書ではなく doc。** この段落は「実物に掛かるものはもっと少ない」という診断の根拠になっている。

- 結果: 対応済み `37e954fb`（数を書かず、所見の番号だけを並べる形へ）

### AE-06 (MEDIUM, 1人): 「9件すべてが削る／縮める方向」が `da5a99f8` で破れる

`da5a99f8` の差分は1行の置換で、**削除も限定もしていない純粋な追記**（元の記述は誤りではなく不足）。
`a1321acc` は「新しい文は1つも書いていない」という偽の全称を落としたが、
**同じ文に残った「すべて」という全称は落とさなかった。**

- 結果: 対応済み `43bf9ea2`

### AE-07 (MEDIUM, 1人): AD-01 の根拠が、指し先の節を空だと書いている

指し先の `CLAUDE.md` の節には **TS 側の取り方が肯定形で残っている。** `dd5109c5` が削ったのは
Rust 側の1行だけ。「空だ」を信じて節を整理すると、`vite.config.ts` がいまも前提にしている
生きた出典を落とす。「3つとも Rust の文脈」も外れていた。

- 結果: 対応済み `1168313a`

---

## 重複・矛盾した所見

- **AE-01 は3人、AE-03 / AE-04 は2人が指した**
- **AE-01 / AE-02 / AE-05 は同じ根**（削るときに、その記述に寄りかかっている側を数えない）。
  r13 の AD-01 と同じ形で、**削る方針に変えてから2ラウンド続けて出ている**
- **2人の判断が割れた1件があった。** `--continue` を外す理由に残る
  「deny されても `--abort` で降りられる」を、comment は「削った根拠に依存するので消せ」、
  oss-hygiene は「別の事実なので誤りではない」とした。ここでは残す判断をしたが、
  **r15 の AF-03 で comment の側が正しかったと分かった** —— この基準を当てると、
  免除している10通り（`--quit` / `--skip` / `--edit-todo`）も同じく `--abort` で降りられる
- 所見の水増しは無かった。3人とも「確認して所見が無かったもの」を明記していて、とくに
  **robustness が免除の20通り（5動詞 × 4オプション）を実測し、`--skip` 系4つが
  ステージ済みの手元の変更を hard reset で捨ててから残りを当てることを確認している。**
  `f52967b1` が削った参照を他から指している箇所が1つも無いことも、追跡済み全ファイルで確認された

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送った17件（再掲しない前提）
- `src-tauri/src/book/` の振る舞い（このラウンドは Rust の差分が無い）
- `user_book1.db` の実測3行とピーク確保の実測
- ShogiHome v1.29.0 側の引用（現物を取っていない）
- `docs/IDEAS.md` が競合した状態での `npm run verify` の再実測（AE-01 は
  `README.md` の経路だけに依存するので、この省略に依存しない）

## lint / hook で強制できるもの

**AE-03（報告書が名乗る短縮 sha が解決できるか）は機械で拾える** ——
`.claude/reviews/*.md` の `対象コミット` を `git cat-file -t` に通すだけ。
ただし `.claude/reviews/` は `gate_kinds_for_path` が種類 `""` を返すので、
報告書だけのコミットではゲートが1本も走らない。**掛けるなら CI 側。#351 へ送る候補**だが、
このラウンドでは足さない（案A の方針）。

機械で守れないもの: AE-01 / AE-02（コメントの根拠が実際の分岐と一致しているか）、
AE-04 〜 AE-07（報告書の自己申告と本文・git 履歴の食い違い）。

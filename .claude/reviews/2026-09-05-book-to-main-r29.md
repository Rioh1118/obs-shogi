# レビュー book-to-main ラウンド29

- 日付: 2026-09-05
- 範囲: r28 の所見4件を直した3コミット＋報告書（`git diff 1110e34b e157700c`）
- 走らせた reviewer: architecture / oss-hygiene
- 対象コミット: `e157700c`
- 前ラウンド: `.claude/reviews/2026-09-05-book-to-main-r28.md`

所見 **4件**（BLOCK 0 / HIGH 0 / MEDIUM 4）。
推移は 22 → … → 4 → 3 → 4 → **4**。

**5ラウンド続けて HIGH 0。**

**4件すべてが r28 の直しから出た。** うち3件は「直した記述が、直しきれていない・数え違っている」形。

issue へ送るものは無かった。

---

## 所見

### AU-04 (MEDIUM, 1人が実測): 空の引用符を食わない変異が、まだ生き残っていた

`stripShellStrings` の `"[^"\n]*"` の `*` を `+` にすると**空の `""` に当たらなくなる。**
706本すべて緑のままだった。

**引用の組が1つずれて、隣の中身がまるごと残る。**

```
orig : expect_dir "" "" ""
plus : expect_dir """GIT_DIR=$target/.git GIT_WORK_TREE=$target git commit -m x""$here"
```

戻る場所は現物の1行（`.claude/hooks/verify-gate.test.sh:210`）で、
`GIT_DIR` / `GIT_WORK_TREE` / `GIT_INDEX_FILE` が「ソースに無い」から「実在する」へ戻る。
**壊れる向きは AT-04 と同じ「消し足りない」側。**

**r27 が「8通り当てて全部塞いだ」、r28 が「`g` の2つ」で、これが3度目。**
今回は29変異を全て `npm test` に掛け、生存9件のうち
`missingIdentifiers` の答えを変えるのがこの1件だけであることまで確かめた。

- 結果: 対応済み `88dd1666`（**この1本だけが落ちることを確認**）

### AU-02 (MEDIUM, 1人が実測): 「引くときはラウンドを添えること」の反例が、この PR の中に10箇所ある

`c6e95716` が `failure-surfacing.md` に書いた規則が、
**同じ PR が持ち込む報告書で守られていない。**

`book-foundation` の r2（5箇所）/ r5 / r6 / r7 / r8 が、r1 の
`F-02` / `F-04` / `F-05` / `F-06` / `F-10` / `F-12` / `F-15` を**裸で**引いている。
`F-10` と `F-12` は**台帳と綴りが完全に一致する**（台帳の `F-10` は研究局面の削除失敗、
`F-12a/b` は棋譜の保存・操作失敗）。同じファイルが正しい形も持っていて
（`r3:51` の「R1 の F-12」、`r5:65` の「R1 F-06」）、**混在している。**

**規則を書いた瞬間に偽になる形は AP-04 / AQ-04 と同じで、これが3度目。**

- 結果: 対応済み `33b38cac`（**反例10箇所を直す側は採らなかった。**
  この台帳が持つのは失敗の現在地であって、報告書どうしの引き方ではない。
  範囲外に規約を伸ばしたのが誤りなので、事実だけを残した）

### AU-01 (MEDIUM, 1人が実測): r28 が数えた「4ラウンド連続」の先頭が、別の欄の所見だった

**AP-06 は括弧書きの所見ではない。** r24 の見出しは
「r23 のヘッダが、レビュー対象でない木を『対象コミット』に据えていた」で、
直しの `da74e043` が触ったのは r23 の `範囲` と `対象コミット` の2行。
r24 自身が「AP-06（報告書ヘッダのハッシュ）」と分類している。
**r23 のヘッダにはそもそも括弧書きが無い。**

r27 にもこの欄の所見は無い（AS-01 / AS-02 / AS-03 の3件）。
実測は **AQ-03 → AR-04 → AT-01 の3度で、r27 を挟まないので連続でもない。**

**two-strikes は3度でも超えているので、欄をやめた判断そのものは変えていない。**
直したのは根拠の記述だけ。

- 結果: 対応済み `58ad1581`

### AU-03 (MEDIUM, 1人が実測): 「括弧書きごとやめた」が7本に残っていた

`6450d325` が落としたのは r24〜r27 の4本だけで、**同じ形が r14〜r20 に残っていた。**

残った7本の中身は実測と合っている（r14〜r16 の範囲に `.rs` の差分ファイルは0、
r17〜r20 の差分は `.claude/reviews/` のみ）。
**それでも消す境界に根拠が無い** —— 「間違っていた欄」ではなく「直近の4本」で切ったので、
シリーズを読む人には「r20 には理由があり r24 には無い」という差だけが残り、
**なぜ消えたのかを記録した場所がどこにも無い。**

- 結果: 対応済み `83d0f1b8`（全11本から落とし、r28 に経緯を1行書いた）

---

## 重複・矛盾した所見

- **4件とも1人ずつ**
- **4件すべてが r28 の直しから出た。** r23 / r24 に続いて3度目
- **AU-04 は AT-04 の直しが作った。**「変異を当てて全部塞いだ」が**3ラウンド続けて外れている**
  （r27 が8通り → r28 が `g` の2つ → r29 が `*`→`+`）。
  数え上げで網羅を名乗る形が、変異テストの側で3度出た。
  今回は29変異を全数当てて生存9件の内訳まで出したので、
  **「全部塞いだ」ではなく「当てた変異と、生き残ったが答えを変えないもの」を並べる形にした**
- **AU-02 は AP-04 / AQ-04 と同じ根**（規則を書き、同じ PR に反例を残す）。3度目。
  **今回は反例を直す側でなく、規則を引っ込める側を採った** ——
  規則の範囲（`failure-surfacing.md` は失敗の台帳）が届かない場所を規定していた
- **AU-01 / AU-03 は AT-01 の直しから出た。** 欄をやめる判断は正しかったが、
  **その根拠の数え上げと、やめ方の適用範囲が両方とも足りていなかった**
- 所見の水増しは無かった。2人とも「確認して所見が無かったもの」を明記していて、とくに:
  - **`c6e95716` が絞った量化子の前半は正しい** —— `docs/` を全走査し、`F-N` を**定義**して
    いるのが `failure-surfacing.md` だけであること（`0004-notification-taxonomy.md` の
    `F-1`〜`F-18` は台帳の真部分集合で記述も一致）、`src/` `src-tauri/src/` の23箇所が
    全て参照であることを確認した
  - **`book-key-failures.md` の直しは正しい** —— r1 の `F-04` の中身と、
    書き換えた文の主張が一致する。台帳の `F-4`（購読失敗）とは無関係、も正しい
  - **AT-03 の帰属の直しは正しい** —— r24 の見出しは合体形で、`0165bc18` の
    コミットメッセージも「AP-03 / AP-04 に対応」と両方を含む
  - **AT-01 の事実そのものは正しい** —— r27 の範囲に `yaneuraou_db.rs` が
    1 deletion / 2 insertions で入っている
  - **`g` の2変異は両方落ちる**（実測）。`not.toContain("OTHER_NAME")` は
    コメントの `g` 除去の**唯一の殺し手**
  - **`not.toContain("gate_z")` は冗長だが空虚ではない** —— 同じ変異で `toContain("tail")`
    も必ず落ちる（残った ` #` をコメントとして切るため構造上こうなる）
  - **r28 の `orig` / `noG` / `sqNoG` の3行は現物と一致**（変異体ごとに corpus を組み直して照合）

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送った19件（再掲しない前提）
- `origin/main` が元から持っていた実装と doc の中身
- `docs/spec/screens/` の13面が現物の UI と合っているか
- `user_book1.db` の実測3行とピーク確保の実測
- `codeOf` の c-like 枝（`openIndex` / `indexOf("*/")` / while ループ）の変異
- `docsIdentifiers.ts` の `IDENTIFIER` / `EXEMPT` / `missingIn` 側の変異
- 2重変異（今回当てたのは単一変異29通り）
- `.claude/reviews/` 208本の所見番号の採番を全シリーズでは網羅していない
  （`game-engine` 系は `R6-M4` 形式なので綴りが衝突しない）

## この PR の外で観測したこと

**ゲートがまたメインのチェックアウトを検証した**（#342）。今回は別セッションが
`src/__tests__/commentHistory.test.ts` と `sourceText.ts` を編集中で、
**そちらの `sourceText.ts:22` が `commentHistory` に引っ掛かっていた。**
このワークツリーの同じ検査は緑（706 passed）。**あちらが畳むまで待って通した。**

## lint / hook で強制できるもの

- **AU-04 で `*`→`+` の変異が塞がった。** ただし**当て漏らしは3度続いていて、
  人の注意では防げない** —— `sourceText.ts` に対する定型変異
  （各代替枝・各 replace の除去、量指定子 `*`→`+`、`g` フラグ除去、置換文字列の空化、
  合成順の入れ替え）を回して「生き残り**かつ** `sourceCorpus()` が変わる」ものを落とす形なら
  機械で拾える。今回それを29変異ぶん実際に回して1件見つけた。#351 の範囲
- AU-02 の型（`.claude/reviews/` の裸の引用）は機械化できるが、
  **規則そのものを引っ込めたので対象から外れた**
- AU-01 / AU-03（報告書の帰属と落とし残し）は機械で防げない

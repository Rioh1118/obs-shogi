# レビュー 434-mixed-screen ラウンド2

- 日付: 2026-09-07
- 範囲: `git diff origin/main...HEAD`（29ファイル / +1375 -167）。r1 の16件を直した後の状態
- 走らせた reviewer: architecture / react / robustness / comment / oss-hygiene（perf は外した——r1-04 で購読を外したので、新しく熱い経路に触っていない）
- 対象コミット: `36945656`
- 前ラウンド: [r1](2026-09-07-434-mixed-screen-r1.md)。**r1 の BLOCK と HIGH 3件は全て閉じたことを robustness と react が確認した**

## 所見

### BLOCK

#### r2-01 `isActive` の doc の2つの箇条書きが、条件と症状を取り違えている（comment）

`FileNode.tsx:37-40`。**r1-01 で自分が書いた doc。**

```
 * - 盤だけを見ると、載せられなかった棋譜が「載っている」ことになって押し直せない
 * - ツリーだけを見ると、**その前に開いていた棋譜へ戻れない**。…
```

E16 のあと（`activeKifuPath = こわれた.kif` / `loadedAbsPath = a.kif`）で両方とも逆:

| 片側だけにすると                        | 実際に効かなくなるのは                               |
| --------------------------------------- | ---------------------------------------------------- |
| 盤だけ（`loadedAbsPath === path`）      | **`a.kif` へ戻る操作**（＝ r1-01 の BLOCK）          |
| ツリーだけ（`activeKifuPath === path`） | **載せられなかった棋譜の押し直し**（＝ #434 の症状） |

正しい向きは repo 内の3箇所（`FileNode.test.tsx:81` と `:96`、`app-layout.md`）に書かれていて、
**この doc だけが逆**。次に条件を1つに削ろうとした人は、消してよい側を取り違える。

`CLAUDE.md` が「4ラウンド連続で出た」と名指ししている故障（コメントに書いた理由が、
実装している条件と違う）を、**その注意書きを読んだうえで踏んだ**。

### HIGH

- 結果: **解消済み**（`ba9cc8b4` r2-06 で述語を切り出したときに doc を書き直し、そのとき2つの箇条書きの向きも正した。単独のコミットは作っていない）

#### r2-02 mock を「毎回新しい参照」にした理由が成り立たない（comment）

`gameFileTreeBridge.test.tsx:26-28`。reviewer が実際に確かめた——`useFileTree: () => tree`
（同じ参照）に戻しても **9本とも緑**。橋は3つのフィールドに分解していて effect の依存もその3つなので、
包み側の同一性は React に何も伝えない。切り替えを見せているのは `tree` の書き換え＋`rerender` のほう。

**効いているのは隣の「毎回新しい要素を作る」だけ**（そちらは同じ要素を渡す版に戻すと実際に1本落ちる）。
効いていない行に強調付きの理由が付いているので、読み手はどちらが荷重されているか判断できず、
スプレッドを「切り替えテストの作法」として他所へ写す。

- 結果: 対応済み（`7d9e06a3` 固定した参照に戻しても10本とも緑になることを自分でも確かめ、スプレッドごと落とした。効いているほう（要素の作り直し）に理由を移した）

#### r2-03 F-31 の行に、r1-03 で否定した「StrictMode」の理由が残っている（oss-hygiene）

`failure-surfacing.md:155`。**この行はこの PR が書いた。** コードのコメントは `f79f5409` で
押し直しの経路へ書き直したのに、**同じ事実の台帳側だけが古い理由を持ったまま**。
この行は `dedupeKey` の存在理由を説明する唯一の doc なので、読んだ人は「鍵は要らない」と結論する。

台帳が自分で「直したのに行が古いままなら嘘をつく」（同ファイル `:27-28`）と書いている条件そのもの。
あわせて `426b6558` の追記が前文と句点無しで繋がっている。

### MEDIUM

- 結果: 対応済み（`3a9e429c`）

#### r2-04 盤に載せられなかった回だけ、検索ヒットの要求が `pendingRef` に残り続ける（architecture / react / robustness の3人）

`usePositionHitNavigation.ts:98-121`。解除条件は2つ（別の棋譜を選んだ／`kifuError`）で、
**この PR が出口を作った当の失敗が3つ目**なのに数えられていない。
`loadGame` の失敗は `kifuError` を立てず、`selectedNode` もその棋譜のまま。

筋道: 検索ヒットで壊れた棋譜へ飛ぶ → 断りが出る → **要求は armed のまま残る** →
利用者が外でファイルを直し、ツリーで同じ行を押し直す（**r1-01 でできるようにした操作**）→
`loadedAbsPath` がその棋譜になった瞬間に `applyCursor` が発火し、
**ずっと前に押した検索ヒットの局面へ盤が飛ぶ**。

`:102-105` のコメントが「防ぐ」と宣言している事象そのもの。3人とも同じ直し方を出した——
失敗をパス付きで state に残す（`loadFailedAbsPath`）。
**3人とも「`activeKifuPath === p.absPath && loadedAbsPath !== p.absPath` で代用するな」と書いている**
（正常に載っている最中の1レンダぶんにも真になり、成功する要求まで捨てる）。

- 結果: 対応済み（`244be1ca` `loadFailedAbsPath` を足し、解除条件を3つ目として置いた。**「載っていないだけの回では捨てない」も同じ test で見ている**。解除条件を潰す変異で落ちることを確認）

#### r2-05 `forceReopen` が任意で、省略時の既定が r1-02 で壊れていた側の挙動になる（architecture / robustness）

`types.ts:189,241`。`?` が2つ重なっているので、**書き落とした呼び出しの既定値が
「モーダルだけ閉じて何も起きない」**。doc は「呼び出し側**だけ**が判断できる」と言っているのに、
型は判断しないことを許している。判断材料（`loadedAbsPath`）は `entities/file-tree` から見えないので、
既定値で埋めることが原理的にできない場所——だから「決めさせる」しか手が無い。

**tsc で完全に防げる。** 呼び手は1箇所なので変更は1行。

- 結果: 対応済み（`ab8f586d` `options` と `forceReopen` の `?` を両方外した）

#### r2-06 2つの関門がなお別の式で、widget 側だけ `jkfData` / `kifuFormat` を見ない（architecture）

`FileNode.tsx:42` は2条件、`provider.tsx:683-687` は `activeKifuPath` ＋ `jkfData` ＋ `kifuFormat`。
r1 の計画は「r1-01 と r1-02 は同じ根、別々に直すと必ずまた割れる」と書いたのに、
**結局2つの別々の式として直した**ので差が1つ増えた。

観測できる帰結（`origin/main` から在る。**この PR の退行ではない**）:
`a.kif` → `a.csa` へ改名すると `reconcilePathMutation` が `format` を運ばず `state.kifuFormat` は `"kif"` のまま。
盤には載るので関門が真になり、押しても開き直らない → `kifuFormat` は一生更新されない →
`GamePersistenceGate` が `saveKifuToFile(jkf, "/ws/a.csa", "kif")` を組み、
**`.csa` に KIF のテキストが書かれる**。

`selectNodeByAbsPath` へ寄せる案は `revealNodeInCurrentTree` の副作用（行のスクロール）が付くので採らない。

- 結果: 対応済み（`ba9cc8b4` `isOpenedInTree` に切り出して両方から呼ぶ。**改名で形式がずれた回に開き直す**ことを test で固定した。`state` を丸ごと渡すと `react-hooks/exhaustive-deps` が落とすので、3つのフィールドに絞って渡している——**このルールは有効になっている**（r1・r2 で reviewer が「未確認」としていた点））

#### r2-07 `app-layout.md` が関門を「盤だけを見る」と書いており、r1-01 の BLOCK を再現させる（oss-hygiene）

`:132-134`。`423cc3c1`（r1-15）で書き直した後も片方しか書いていない。
CLAUDE.md は「画面に触る前に `docs/spec/` を読む」と指示しているので、
ここを唯一の仕様として読んだ人が素直に実装へ戻すと **r1-01 の BLOCK が復活する**。
`board-orientation.md` の ※7 は2条件で書けているので、2つの doc が同じ述語を別に説明している。

- 結果: 対応済み（`9ffa6c75` 述語の持ち主を `board-orientation.md` の ※7 にし、こちらは参照に倒した）

#### r2-08 節の見出し「画面は動かず」が、2行下の本文と衝突している（oss-hygiene）

`app-layout.md:119` と `:124` / `:129`。r1-15 は本文だけを直し、見出しに同じ食い違いが残った。
見出しは自身の「モーダルが出る」とも矛盾している。r1-20（IDEAS の見出し）と同型。

- 結果: 対応済み（`984bb2fc`）

#### r2-09 §4 の「F-19 以降は出口が1つも無い」が、この PR で偽になった（oss-hygiene）

`failure-surfacing.md:245-247`。F-31 に出口を作り、**同じ節の issue 表から F-31 の行を落としながら、
総括の一文だけを残した**。同ファイル `:45-47` は「F-31 が1件目」と正しく書いている。

- 結果: 対応済み（`07243cc4`）

#### r2-10 §4 の未 issue の列挙が範囲のままで、F-33〜F-36 を落としている（oss-hygiene）

`failure-surfacing.md:239-243`。r1-16 の直しは §2 前書きと §3 に入ったが、
**実際に漏れが起きた §4 の列挙そのものは範囲のまま**。同ファイル `:197-198` が
「ここでも番号の範囲を書かない」と名指しで禁じた綴りが、4行下に残っている。

- 結果: 対応済み（`f6a9c0c8` issue 表との補集合に倒した）

#### r2-11 書き換えた F-31 の行から、測定日とブランチの印が消えた（oss-hygiene）

`failure-surfacing.md:155`。同ファイル `:30-31` が「**行を足したときも既存の行を書き換えたときも**」
印を添えると決めている。隣の F-32 は印を保っている。台帳の唯一の鮮度表示。

- 結果: 対応済み（`46c5a626`）

#### r2-12 `board-orientation.md` の注が ※1 → ※7 → ※2 の並びになった（oss-hygiene）

`:98` / `:107` / `:113`。番号順でも初出順でもない。この repo の他の表（`game.md` の ※1〜※8、
`file-tree.md` の ※1〜※4）は全て昇順なので、読み手はその並びで走査する。

- 結果: **r3 へ送る**（計画のとおり）

#### r2-13 `game.md` が `provider.test.tsx` を「`loadGame` だけ」と書いているが、`resetGame` まで踏んでいる（oss-hygiene）

`:220` / `:222-223` / `:342`。同じファイルに `hasKifu` の4本があり、うち1本は `resetGame` を通して
G1→G0（E2）を `hasKifu` の側から見ている。表の E2 は `✗` なので、
**E2 のテストを書く人は既に在る1本を知らずに重ねる**。
テストの厚さを誇張しない規約は、過小に書いてよいという意味ではない。

- 結果: **r3 へ送る**（計画のとおり）

#### r2-14 r1 報告書の「結果」が所見から切り離れ、r1-06 だけ結果が無い（oss-hygiene）

`r1.md:27-29` / `:65-67` / `:300-302`。各深刻度の**最後の所見**の結果が、次の見出しを跨いだ位置に落ちた。
r1-01（唯一の BLOCK）の対応状況が BLOCK の節から消えている。
r1-06 は16件中で唯一「計画表にあるのに結果が無い」ので、未対応と読める（実際は `426b6558` で対応済み）。

- 結果: 対応済み（この書き戻しで、結果行を所見の本文の末尾——次の見出しの手前——に置いた。r1 側の3件の位置と、r1-06 に欠けていた結果行も同時に直す）

#### r2-15 `isActive` / `isAlreadyActive` が、どちらも「省いてよいか」なのに状態を名乗っている（comment。r1-08 の再評価）

r1-08 の時点より悪化している。`isAlreadyActive` は `forceReopen` を折り込んだので
**「もう active である」が真でも false になりうる**——名前が式を説明しなくなった。
`isActive` の意味を埋めるために12行の doc が関数本文の中に立っている。

- 結果: **一部対応**（`ba9cc8b4` で `isActive` → `canSkipReopen`、`isAlreadyActive` → `canSkipOpen` へ改名。r2-06 の直し方に畳まれた。**残りは r3**——名前が揃ったので doc をどこまで縮められるか）

#### r2-16 差の説明が src の10箇所に写経されたまま、宣言には無い（comment。r1-09 の再評価）

宣言は `types.ts:37-38`「現在ロードしている棋譜ファイル」と `file-tree/model/types.ts:96-97`
「現在開いている棋譜」で、**言い回しで区別が付かない**。差の説明は src だけで**10箇所**（docs にさらに2箇所）。
**写経が10本ある以上、条件が変わったときに全部は直らない——今回すでに1本が逆を書いた（r2-01）。**

- 結果: 対応済み（`a0fe1627` 不変条件を2つの宣言に置き、写しを4箇所縮めた）

#### r2-17 `dedupeKey` の宣言が示す採用基準が、唯一の呼び出し側と合っていない（comment）

`shared/lib/notification/types.ts:79-85` は「棋譜を1つ開くたびに何十件も出る類の失敗にだけ付ける」。
唯一の実例（`GameFileTreeBridge`）は**押し直しのたびに1件ずつ積み上がる**失敗なので、基準違反に見える。
次に迷った人は「何十件出るわけではないから付けない」と判断する（r1-03 で決めた側と逆）。

- 結果: **r3 へ送る**（計画のとおり）

#### r2-18 橋の effect 本文に14行の説明が入り、細い context の理由が4箇所に重複（comment）

`GameFileTreeBridge.tsx` は66行のうち26行がコメントで、`notify({...})` の引数リストの中に
`dedupeKey` の理由4行と `body` の理由10行が挟まっている。
細い context の理由は `context.ts` / `useGame.ts` / `loadedKifuPathContext.test.tsx` / `FileNode.tsx` の4箇所。
あわせて `useGame` には TSDoc が1行も無く、**「どちらを使うか」の案内が細いほうにしか無い**。

- 結果: **r3 へ送る**（計画のとおり）

#### r2-19 断りが「盤は表示だけになる」と告げながら、その状態を解く操作を書いていない（robustness）

`GameFileTreeBridge.tsx:56-58`。実際の復帰は**ツリーで前の棋譜をもう一度押すこと**で、
**r1-01 で初めて効くようになった操作**なのに、本文の「別の棋譜を選んでください」からは
「諦めて他を開け」としか読めない。r1-05 とは別の点——**復帰導線が1つ増えたのに本文が追いついていない**。

## 重複・矛盾した所見

- **r2-04 は3人が独立に指摘**（architecture / react / robustness）。直し方も3人とも同じで、
  **同じ代替案（`activeKifuPath` で推測する）を3人とも名指しで否定している**。採用に迷いは無い
- **r2-05 は2人**。tsc で閉じるので、手順3-1（機械で強制できるものを先に）の筆頭
- **r2-01 / r2-07 / r2-16 は同じ根**——「2つのパスの差」を書いた散文が12箇所にあり、うち2箇所が誤っている。
  r2-16 を先に入れて写経を減らさないと、r2-01 と r2-07 を直しても3度目が出る
- **r2-06 と r2-15 は同じ場所を触る。** 述語を1つに切り出せば名前も1つで済むので、r2-06 の直し方に r2-15 を畳める
  （**コミットは分ける**。束は順序の単位）
- **r2-09 / r2-10 / r2-11 は r1-16 と r1-15 の直し残し。** 同じファイルの同じ節なので続けて取る

## 見ていない範囲

- Rust（`src-tauri/`）。差分が1行も無いので全員が見ていない
- SCSS・見た目。通知モーダルと `KifuReadErrorDialog` / `PositionSearchModal` の重なり順とフォーカストラップは、
  **r1 でも r2 でも誰も確かめていない**
- `NotificationLayer` が modal を1枚しか描かないことで、先行するモーダル通知の裏に F-31 が待たされる経路
- 通知の見出しに極端に長いファイル名が入ったときの見え方（`HandHeader` は15文字で丸めるが、通知側に丸めは無い）
- 実機（Tauri の WebView）での挙動。誰も動かしていない
- comment / robustness / architecture / oss-hygiene は `npm run verify` を走らせていない（react だけが走らせ、93 files / 862 緑を確認）
- r1 から r2 へ送った r1-10（対局者名の取り出し3箇所）と r1-11（`Err` を code の union に）は、
  **現物が r1 の記述どおり残っていることを architecture が確認した**が、再掲はしていない

## lint / hook で強制できるもの

- **r2-05 は tsc だけで閉じる。** `?` を2つ外す。ラチェットも規約も要らない
- **注の番号の健全性**（r2-12 / r1-17）: `docs/state-transitions/*.md` で行頭 `※N` の定義が昇順であること・
  参照に定義があること・定義が参照されることを見るラチェット。`stateTransitionCells.test.ts` と同じ形。
  **今回の3件が全部落ちる**
- **§2 の測定印**（r2-11）: `| **F-N**` 行のうち F-19 以降で「いま起きること」が `（YYYY-MM-DD / …）` で
  終わることを見る。`:30-35` の規約がそのまま述語になる
- **番号の範囲の書き方**（r2-10）: 同ファイルで `F-\d+〜F-\d+` の出現を禁じる。
  「書かない」と宣言している綴りなので、例示の2箇所だけ許可リストに置く
- **`StrictMode` を理由に挙げる記述**（r2-03）: r1 が src 向けに挙げたラチェットの対象に `docs/` を含める
- **報告書の形**（r2-14）: `#### rN-NN` の節が次の見出しまでに `- 結果:` を含むこと。`/review-fix` 側で見られる
- **機械では防げないもの**: r2-01 / r2-04 / r2-06 / r2-07 / r2-08 / r2-09 / r2-13 / r2-15〜r2-19

## 修正計画（r2 → r3）

- 結果: 対応済み（`0ae8702c` 解き方を本文に足し、test で固定した）

### 束（同じ根から出ている所見）

- **「2つのパスの差」を書いた散文が12箇所**: r2-16 → r2-01 → r2-07。
  **r2-16（宣言に不変条件を置き、写経を削る）を先に入れないと、r2-01 と r2-07 を直しても3度目が出る。**
  実際 r2-01 は r1-01 で自分が書いた写経が逆を向いた事故で、r2-07 は同じ事実の doc 側
- **関門の述語**: r2-06 → r2-15。述語を1つに切り出せば名前も1つで済むので、r2-15 は r2-06 の後でないと決まらない
- **`failure-surfacing.md` §2/§4 の直し残し**: r2-03 → r2-11（同じ行）→ r2-09 → r2-10。
  r1-15 と r1-16 が届かなかった範囲。同じファイルの同じ節なので続けて取る
- **r2-14 は書き戻しの作法**なので、この計画の結果を r1/r2 の報告書へ書くときに一緒に直す。単独のコミットにしない

### このラウンドで直すもの

| 順  | 所見  | なぜこの順か                                                         | この直し方で壊しうるもの                                                                                                                                                                                                                                             |
| --- | ----- | -------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | r2-05 | **tsc だけで閉じる**（手順3-1）。呼び手は1箇所なので変更は1行        | `SelectNodeOptions` を必須にすると、**テストの呼び出しも全部書き直しになる**。`usePositionHitNavigation.test.tsx` の既存アサーション（`toHaveBeenCalledWith(path, {forceReopen})`）は既にオブジェクトを渡しているので影響しないはずだが、そこが前提                  |
| 2   | r2-04 | **失敗経路と門番**（手順3-3）。3人が独立に出した唯一の振る舞いの欠陥 | `GameContextState` に欄が1つ増える。`gameReducer` の `game_loaded` / `reset_state` で落とし忘れると、**一度失敗したパスの要求が永久に捨てられる**（今度は逆に、正しい要求まで届かない）。`reducer.test.ts` の identity の短絡（同じ state を返す枝）に新しい欄が絡む |
| 3   | r2-06 | 門番の向き。r2-15 の前提                                             | 述語に `jkfData` / `kifuFormat` が入るので、**ツリーのクリックで開き直る回が増える**。増えるのは「盤には載っているが format か jkfData がずれている」回で、そこは今まで開き直せずに壊れた保存をしていた側。カーソルが 0 手目へ戻る回が増えるのは事実                 |
| 4   | r2-16 | **写経を減らす束の先頭。** 5と6の前に入れないと3度目が出る           | 呼び出し側10箇所から差の説明を削るので、**削りすぎると各所の固有の理由（向きを落とす合図／門番が止める／見出しの出どころ）まで消える**。残すのは「その場所固有の帰結」だけ                                                                                           |
| 5   | r2-01 | BLOCK。ただし r2-16 で写経を削った後だと、直す対象そのものが縮む     | 無し（doc のみ）。ただし**縮めた結果、逆に書いてあった内容が消えるだけで終わる可能性がある**——その場合は「解消済み」として記録する                                                                                                                                   |
| 6   | r2-07 | r2-01 と同じ事実の spec 側                                           | `board-orientation.md` の ※7 と二重に持つと、次に片方だけ直る。**参照に倒すか、どちらが持ち主かを決める**                                                                                                                                                            |
| 7   | r2-02 | テストの理由。実装に触らない                                         | mock を `() => tree` に戻すと、**橋がオブジェクトごと依存に入れる形へ変わったときにテストが先に気付けなくなる**。戻すか、理由を実際のものに書き換えるかの判断が要る                                                                                                  |
| 8   | r2-03 | 台帳の嘘。r2-11 と同じ行                                             | 無し                                                                                                                                                                                                                                                                 |
| 9   | r2-11 | r2-03 と同じ行なので続けて                                           | 無し                                                                                                                                                                                                                                                                 |
| 10  | r2-19 | 本文。r2-06 で関門が変わった後でないと、案内する操作が確定しない     | 本文が長くなる。`gameFileTreeBridge.test.tsx` の本文アサーション3本と噛み合わせる                                                                                                                                                                                    |
| 11  | r2-08 | 見出し                                                               | 無し                                                                                                                                                                                                                                                                 |
| 12  | r2-09 | §4 の総括                                                            | 「F-19 以降で出口があるのは F-31 と F-32 だけ」と書くと、**次に出口を作った行を書き足す義務**がこの文に移る                                                                                                                                                          |
| 13  | r2-10 | §4 の列挙。9と同じ節                                                 | 補集合に倒すと、issue 表の5件が唯一の名指しになる。**表から行を落とすときに列挙も連動する**（今回それを踏んだ）                                                                                                                                                      |

### 直さないもの

| 所見                                  | 行き先                             | 理由                                                                                                                                      |
| ------------------------------------- | ---------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| r2-12（※7 の並び）                    | **r3**                             | 見た目の並びで、誤読の実害が小さい。r2 は13件で手順7 の上限に近い                                                                         |
| r2-13（`game.md` のテスト記述が過小） | **r3**                             | 同上。E2 のセルを書き分ける判断が要るので、単独で落ち着いて取る                                                                           |
| r2-14（報告書の書き戻しの位置）       | **この計画の結果を書くときに直す** | 単独のコミットにしない。r1-06 の結果行も同時に足す                                                                                        |
| r2-15（`isActive` の改名）            | **r3**                             | r2-06 で述語が確定してから。**r1-08 から2ラウンド持ち越している**ので、r3 では必ず取る                                                    |
| r2-17（`dedupeKey` の採用基準）       | **r3**                             | `shared/lib/notification` の doc に触るので、この PR の範囲の外側に近い。ただし唯一の実例を作ったのはこの PR なので、送るだけで落とさない |
| r2-18（橋のコメント量と重複）         | **r3**                             | r2-19 で本文を触るので、切り出しはその後のほうが差分が読める                                                                              |
| r1-10（対局者名の取り出し3箇所）      | **r3**                             | r1 から持ち越し。`GameView` に射影を足す変更で、`cursorView` の memo に触る                                                               |
| r1-11（`Err` を code の union に）    | **r3**                             | r1 から持ち越し。`TODO(#202)` の判断に踏み込む                                                                                            |

**8件を r3 へ送る。** うち r2-15 と r1-10 / r1-11 は**2ラウンド持ち越し**なので、r3 で優先する。

### 対象そのものを疑ったか

**所見は3つの機構に集まり続けている。r1 と同じ分布。**

| 機構                        | r1              | r2      | 内訳（r2）                    |
| --------------------------- | --------------- | ------- | ----------------------------- |
| 「もう開いている」の関門    | 4件             | **4件** | r2-01 / r2-06 / r2-07 / r2-15 |
| 通知の呼び出し              | 4件             | **4件** | r2-03 / r2-17 / r2-18 / r2-19 |
| 2つのパスの差を説明する散文 | （r1-09 の1件） | **3件** | r2-01 / r2-07 / r2-16         |

**減っていない。** 手順4 は「所見が減らないラウンドが3回続いたら、直し方ではなく対象を疑う」と言う。
r1 の計画は既に「C（関門）を落とす案」を1行載せて残す判断をした。**r2 でも同じ機構に4件出たので、
r3 でも出るなら3回目になる。** そのときは C を落として issue に出す判断を、
ユーザーに選ばせる形で持ち込むこと（`/implement` 手順7 の「直し方に判断が要る」）。

ただし r2 の4件は**どれも「散文が実装とずれた」**であって、関門の設計そのものへの異議ではない。
r2-16（写経を1箇所に集める）が効けば、この機構の所見は次から出なくなるはず——**それが r3 の観測点。**

### 次ラウンドの焦点

1. **`loadFailedAbsPath` の落とし忘れ**（順2）。`game_loaded` / `reset_state` で消えるか。
   **消えないと、一度失敗したパスへの正しい要求まで捨てられる**
2. **関門に `jkfData` / `kifuFormat` が入ったことで、開き直る回が増えた**（順3）。
   カーソルが 0 手目へ戻る回が増えていないか。改名の直後に踏めるか
3. **写経を削りすぎていないか**（順4）。各所の固有の理由が消えていないか
4. **`app-layout.md` と `board-orientation.md` のどちらが関門の持ち主か**（順6）。二重に持っていないか
5. **本文が長くなったこと**（順10）。`danger` の modal に入る文量として読めるか
6. **§4 を補集合に倒したことで、issue 表と列挙が連動するようになったか**（順12・13）
7. **同じ機構に所見が出続けるか**（上の「対象そのものを疑ったか」）。出るなら3回目

### 検証の見積り

13件 × 1コミット。TS のみが 7件（順1〜7）、`docs/state-transitions/` を触るのが 4件（順8・9・12・13）で
**TS + Rust**、`docs/spec/` の順6・11 は TS のみ。

**Rust は増分でもベンチに数分かかる**（`bench_*` が10本ある）ので、順8以降をまとめて走らせないこと。
**時間は書かない**（`CLAUDE.md`）。上限に触れたら順11以降を r3 へ送り、この節に追記する。

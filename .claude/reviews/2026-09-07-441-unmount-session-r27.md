# レビュー 441-unmount-session ラウンド27

- 日付: 2026-09-09
- 範囲: `git diff origin/main...HEAD`（`fix/441-stop-analysis-on-unmount`）
- 走らせた reviewer: react / robustness / comment / oss-hygiene / architecture（5人とも走り切った）
- 対象コミット: `236ee79f`
- 前ラウンド: [r25](2026-09-07-441-unmount-session-r25.md) / [r26](2026-09-07-441-unmount-session-r26.md)

## 前ラウンドからの持ち越し

**r23 の所見16 が「→ r24 で再掲」と書かれたまま、3ラウンド誰にも再掲されなかった**（react が発見）。
`自動再開` で r24 / r25 / r26 を grep しても再掲は0件。**「直さない」と判断したのでも
「直った」のでもなく、黙って消えていた。** 下の所見7 で決める。

## 所見

### [HIGH] 1. `dropPending` の doc が、r26 で実証した窓を「守っていない」と宣言している

reviewer: comment

`useResultFlush.ts:31-33` は r25 以前の誤った断定を現在形で残している。

```ts
* **これは掃除であって、門ではない。** 捨てた席の結果を画面に出さない保証は
* `commitLatest` の席の門が持つ
```

r26 で書き直したのは**呼び手**（`provider.tsx`）だけ。**定義側が逆のことを言っている。**
`useResultFlush` を触る人は定義側だけを読むので、`dropPendingForLostSeat` の呼びを
「無くても席の門が守る」と判断して外せる——外した瞬間に r26 が閉じた窓が開く。

### [HIGH] 2. r26 の直しが、同じ矛盾を新しい形で作り直した（**こちらの退行**）

reviewer: comment

`1c18739b` は「`waits.ts` に『本番から呼ばないこと』が書かれていない」ことを番人の
存在理由として書きつつ、**同じコミットでその一文を `waits.ts` に書いた**。

```ts
// testWaits.test.ts:15-17
* **書いていないのは「本番から呼ばないこと」のほう**——気づいた人が止まる理由がそこに無い。

// waits.ts:1-3（同じコミットが書き加えた）
* **呼んでよいのはテストだけ**で、`src/__tests__/testWaits.test.ts` が綴りで止める
```

**r26 の所見6 が指摘した失敗が、直した形のまま再現している。**

### [MEDIUM] 3. 変更の経緯がコメントに残っている（うち1件は**こちらの退行**）

reviewer: comment

- `waits.ts:52`「実際に 200ms まで縮めて落とした」——`6e32655d` でこちらが足した作業の記録。
  直前の節が同じことを現在形で言えている
- `sourceText.test.ts:155-156`「r21 で見つけた腐りは」——レビュー回次。
  `commentHistory` の `REVIEW_TAG` は矢印か括弧を要求するので `r21 で` が**素通りしている**

### [MEDIUM] 4. 「散文の寸法を全部 `waits()` から導いた」は `docs/` で成立していない

reviewer: oss-hygiene / comment（独立に）

`analysis.md:39` / `:78` / `:113` / `:288` の4行が数字のまま。**4行ともこのブランチが足した行。**
とくに `:113` は `waitUntil(syncedSfen === currentSfen, 2000)` と、**リテラルが渡っている姿**を
書いていて現物と形からして違う。r26 の表の「全部」は誤り。

### [MEDIUM] 5. 「数字も比も写さない」と書いた2行下で、同じファイルが比を写している（**こちらの退行**）

reviewer: oss-hygiene

`provider.test.tsx:78-80` で規則を新設し、`:133` と `:1153` で `1/4` を写している。
`waits.ts:50-52` は「これ以上大きくしない」としか書いておらず**下げるのは禁じていない**ので、
3 や 2 に下げた人のところで黙って嘘になる。**規則の新設者が最初の違反者。**

### [MEDIUM] 6. `bookIfRestarting()` の門を落としても、テストが1本も落ちない

reviewer: react（実測）

```
# 現物          starts=2 refused=[] analyzedSfen=P3 analyzing=true
# 門を落とす    starts=4 refused=[3,4] analyzedSfen=P1 analyzing=false
#               error=解析を再開できませんでした。…
```

筋は特別ではない——**解析中に盤を2手進め、1手目の自動再開がまだ往復の途中**。
外すと `swapSeatAndGo` が2本並び、Rust が2本とも断って**解析が止まり断りが出る**。
同じ窓を見ている既存テストは ▶ の口（`awaitInFlightRestart`）を通るものだけで、
**タイマーの口はどこも通っていない。**

### [MEDIUM] 7. r23 の所見16 の持ち越しが落ちた（**こちらの手落ち**）

reviewer: react

自動再開が持つ ref は8本（`sentSfenRef` / `restartInFlightRef` / `desiredSfenRef` /
`debounceTimerRef` / `restartSeqRef` / `syncWaitRef` / `pendingAfterRef` / `runRestartRef`）で、
r23 から1つも動いていない。**この8本を一緒に読まないと所見6 のような門は判断できない**
——現に27ラウンド気づかれなかった。

### [MEDIUM] 8. 横断の走査器をスライスへ置き `.ratchet` を名乗らなければ、索引の義務から外れる

reviewer: architecture（実測）

r26 の所見5 で塞いだのは片側だけ。**名乗る義務そのものは誰も見ていない。**
`CONTRIBUTING.md:367-369` と `testsLayerBoundary.test.ts:148-149` は分担を宣言しているが、
**どちらも嘘**。加えて `CONTRIBUTING.md:366` の綴りが `.ts` のままで、機械は `.tsx` を受ける。

### [MEDIUM] 9. `src/` のコメントが書くソースのパスを、誰も検査していない

reviewer: architecture（実測）

`docsIdentifiers.ts:44-46` は「検査を指したいならパスで書く」と**明示的に誘導している**のに、
**その指し方だけが無検査**。同じ形の参照は `src/` に20箇所。

### [MEDIUM] 10. `(S0/P0, E6)` を埋めた結果、同じコードを通る `(S6/P0, E6)` だけが `—` のまま

reviewer: architecture

S6 から ▶ を押している間も状態は S6 のままなので、`(S0/P0, E6)` と1行も違わない経路を通る。
`—` は「起きない」の印なので嘘。`(S6/P0, E9)` / `(S6/P1, E9)` にも同じ形がある。

### [MEDIUM] 11. 凍結の手前に置いてよいものが、3箇所で2通りになっている

reviewer: architecture

機械（`seatSlotShape`）は `useRef` と `useCallback` を許すのに、`useEngineSeat.ts:230` と
検査自身のヘッダは「`useRef` 以外を置かない」と**機械より狭い規約**を名乗っている。
`useResultFlush.ts` は凍結行の上に `useCallback` を4本置いたまま緑。

### [MEDIUM] 12. 断りが案内する「設定で…」の導線が、エンジン管理タブに着かない

reviewer: robustness（実測）

`openModal("settings", { tab: "general" })` の `"general"` は**存在しないタブ名**で、
`isTabKey` が落として「ワークスペース」が開く。**このブランチが足した断り5本**は
復帰手段としてエンジン管理タブでの操作を指しているので、#277 が出口を作った瞬間に
「案内どおりに押したのに別の画面」になる。

**`origin/main` から在る欠陥**（`9345ff1f`）で、この変更とは独立に直せる。

### [MEDIUM] 13. `dropPendingForLostSeat` の `!seat.isHeld()` は現物では常に偽

reviewer: robustness（実測）

```
# 「握っているのに呼ばれたら投げる」を入れて全件 → 1080/1080 緑（一度も踏まない）
# 条件を落として無条件に dropPending           → 64/64 緑
```

**r26 で確定したのは「本体が要る」ことで、「条件が要る」ことではなかった。**
しかも doc が挙げた理由（「最初の `info` まで消える」）は、この関数を**呼ばない**
`landed === "held"` の枝が持っている。将来 席を返さずに呼ぶ口が1本増えると、
この条件は**書き戻された席を根拠に反映待ちを残す**——r26 が閉じた窓がそのまま開く。

### [MEDIUM] 14. 同じ worktree で reviewer を並走させると、片方の変異が他方の実測を汚す

reviewer: oss-hygiene（実測）

汚染の最中に走らせた `npm test` はフルで 3/5 が赤になった。
**「実時計の待ちを縮めたせいの flaky」と読めば BLOCK を1件でっち上げていた。**
隔離コピーで測り直すと 1080 passed で flaky は無い。
**r26 と r27 で2回起きている**（r26 では comment が、r27 では3人が dirty な tree を踏んだ）。

## 確かめて問題が無かったもの

- **r26 で比に書き換えた `advance()` は検出源を殺していない**（react が13個の変異を
  HEAD と r26 前の木の両方に当て、**倒れたテスト名まで完全に一致**）
- **`want` への改名は閉じ込める値を取り違えていない**（react が実測）
- **r26 のテストは `late-restart` 以外を巻き込んでいない**（react が `stopCore` を計装して確認）
- **`keepOrForget` が席を書き戻せるのは `discard` だけ**（robustness が他の解放点を全部辿った）
- **`(S0/P0, E6)` に書いた「上限を待たずに降りる」は現物と合い、機械も付いている**（robustness）
- **r26 が未検証と記録した5行は、5行とも未検証のまま**（robustness が5つとも変異で確認）
- **`.ts` / `.tsx` の両方で索引の義務が掛かる**（architecture が使い捨てで実測）
- **リンクとソースパスは全部解決する**（oss-hygiene が `.md` 400 本を機械で辿った）
- **依存の方向の違反0件、Tauri コマンドの登録漏れ0件**（architecture）
- **`bridge.rs` の152行は TS 側の席の扱いと矛盾しない**（architecture。**27ラウンドで初めて読んだ**）

## 見ていない範囲

- `analyzer.rs` / `protocol.rs` の内部と #463 の窓。**27ラウンド続けて未見。**
- 実プロセスでの検証。誰もしていない。

## 修正計画

順は「振る舞い → こちらの退行 → doc の矛盾 → 機械 → 進め方」。

1. **所見6 → 門を守るテストを1本置く。** 変異で赤くなることを確かめる
2. **所見13 → 条件を落として無条件にし、doc を現物に合わせる。**
   r26 で確定したのは本体であって条件ではない、を書き残す
3. **所見12 → `tab: "engine"` に直す**（別コミット。`origin/main` から在る欠陥）
4. **所見2・3・5 → こちらの退行を戻す**
5. **所見1・4・10・11 → doc の矛盾**
6. **所見8・9 → 機械の穴を2つ塞ぐ**（どちらも既存の道具を借りるだけ）
7. **所見7・14 → 進め方。** 持ち越しの落ちと、並走の汚染

**壊しうるもの。** 2 は r26 で閉じたばかりの窓に触るので、**同じ変異をもう一度当てて
赤くなることを確かめてから**でないと動かさない。6 の機械は既存2本が満たしているはずだが、
満たしていなければ足す前に直す。

### 次ラウンドの焦点

- 2 の後も、r26 のテストが `dropPendingForLostSeat` の本体を落として赤くなるか
- 1 のテストが、**タイマーの口**を本当に通っているか（▶ の口で通ると所見6 を守れない）
- 6 で足した2本が、**逃げ道**を残していないか

## 修正の結果

| 所見 | 結果 | コミット |
| ---- | ---- | -------- |
| | | |

# レビュー search-contracts ラウンド3

- 日付: 2026-09-05
- 範囲: `1e2608fa..HEAD`。r2 の修正8コミット（`4c6e3c1a..4f5ac2ec`）が新しく入っている
- 対象コミット: `4f5ac2ec`
- 走らせた reviewer: architecture / rust / comment / robustness
- 前ラウンド: `2026-09-05-search-contracts-r2.md`（その前は r1）

**所見が減っていない。** r1 29件 → r2 16件 → r3 20件（＋積み残し9件）。
`/review-plan` 手順4 が「所見が減らないラウンドが3回続いたら、直し方ではなく対象を疑う」と
定めている状態に入った。**下の「対象そのものを疑ったか」で範囲そのものを畳む。**

## 所見

### BLOCK

#### R3-A 同じラウンドの中で、doc と実装が食い違った

comment。`position_key.rs:256-259`（`7f5d2ca7`）vs `index_cache.rs:598-604`（`041e260d`）。

`7f5d2ca7` のテスト doc は「版検査を通り、並び替えずに `Segment` へ渡って**検索が黙って0件になる**」と書いた。
その **4コミット後の `041e260d` が、まさにその門番を足した**。いま並びが崩れたキャッシュは
`decode_all` が `Err` を返し、全件作り直しへ落ちる。

**1つのファイルの中に同じ状況についての2つの主張がある**（`:39-40` は「読めなくなる」＝現在の実装どおり）。
並び順を触る人が `:256-259` を信じると、既にある門番を前提から外して判断する。

### HIGH

#### R3-B `InvalidHand` の `kind` が桁の検査では常に `Pawn`。利用者が書いていない駒を名指す

**4人全員。** `sfen_position.rs:219-224`（`4c6e3c1a` が新設）。実測:

| 綴り                            | 出る文言                                     |
| ------------------------------- | -------------------------------------------- |
| `... b 19R 1`                   | `too many pieces in hand: Pawn x19 (max 18)` |
| `... b 20L 1`                   | `too many pieces in hand: Pawn x20 (max 18)` |
| `... b 99999999999999999999P 1` | `too many pieces in hand: Pawn x99 (max 18)` |

この枝は**駒の文字を読む前**にある。`count` も嘘（20桁書いて `x99`）。
文言は `query_service.rs:179` からそのまま `EVT_SEARCH_ERROR` に載り、
`PositionSearchStatusBar` に素で描かれる。

**r1 の `InvalidHand(String)`（綴りを丸ごと）は少なくとも嘘ではなかった。**
情報量を減らしたのではなく、誤情報に置き換えた。

同じ欄の `add_n` 側（`:264-267`）は `pk` も累計も正しいので、**1つの欄が正しい値と作り話の両方を運ぶ**。

#### R3-C `node_id` / `fork_off` / `fork_len` が無検査。R2-3 が名指しした3つのうち2つしか入れなかった

rust / robustness / architecture。`index_cache.rs:594` / `node_table.rs:19-24` / `query_service.rs:131-134`。

**R2-3 の表題は「桶の位置・鍵の並び・`node_id` を検査しない」だった。**
`041e260d` が入れたのは前2つだけで、**結果表は「変異2つで確認」として閉じた**ので
残った1つが記録から消えた。

範囲外の `node_id` → `cursor_lite` が `None` → `CursorLite::root()` →
**その局面のヒットが「そのファイルの0手目」として画面に並ぶ**。押すと開始局面へ跳ぶ。
範囲内だが別の節を指す場合（1バイト化ければ大半がこちら）は `None` にすらならず、
**手数も分岐も違う場所へ飛ぶ**。

`gen` も無検査で、こちらは `is_occ_alive` が偽になって**ヒットが黙って消える**。

#### R3-D 桁の検査に付けた理由が、同じコミットが入れた累計検査で無効になっている

comment / architecture。`sfen_position.rs:215-217`。

コメントは「駒を置くのは1枚ずつのループなので、読み切ってから弾くと `999...9P` でその回数だけ回る」。
だが `add_n:263` は**ループの前**に累計で弾くので、1回も回らない。**書いた理由は既に偽。**

実際に効いているのは (a) `num * 10` の `u32` 溢れ、(b) 駒の文字が来ない綴り
（`"9999999999999999"`）は `add_n` に**到達しない**ので累計検査が門番にならない、の2つ。
どちらも1行も書いていない。

読み手が書いてある理由を信じると「累計検査があるから冗長」と判断して削り、(a)(b) を踏む。

#### R3-E `docs/state-transitions/search.md` が `041e260d` を知らない。載っている実測値も古い

robustness。`search.md:48` / `:250-258`。`git show --stat 041e260d` が触ったのは `.rs` 1ファイルだけ。

- `:48` の `restore-ng` の理由の列挙に、新しい2つ（桶の所属・並び）が**無い**
- `:258` の「`z0` / `z1` の化けは**通る**（黙って間違った索引になる）」が**偽**
- `:251` の「**672通り中 513件が通る**」は検査を足す前の測定値。
  **r2 の規律2（数を書くなら出典を併記）で生き残らせた数が、実装を変えたことで嘘になった**

**振る舞いを変えたら、その振る舞いを記録している doc を探す**という手順が無かった。
R3-A と同じ根。

### MEDIUM

#### R3-F `bucket.rs` の桶の説明が、3ラウンド連続で誤り

comment / rust / architecture。`bucket.rs:3-8`。

r1: 「2^128 通りあるので二分探索が長くなる」→ 誤り。
r2: 「1ファイルにつき1つのセグメント」「舐める本数が 1/256」→ 誤り。
r3: 「**セグメントの本数は減らない**」→ 誤り。

- `install_restored`（`index_store.rs:204-211`）は**復元直後に桶あたりちょうど1本**にする
- `COMPACT_THRESHOLD` を超えた桶は65本→1本に畳む（`:279-287`）— **2行上で自分が名指しした反例**
- 畳む前でも約 `0.357N`（r1 の実測「1ファイルが触る桶は平均91.5個」から）

そして**一番大きな効き目が書かれていない** — `search_occurrences_by_key`（`:80-82`）は
`key.bucket()` の桶しか開かず、他の255個のセグメント列を1本も見ない。

「得ているのは2つ」と数え切ったうえで「本数は減らない」で締めるので、
**読み手は「桶は舐める量に効かない」と読み、検索経路から桶を外す判断を許す**。

#### R3-G `pub(super) HAND_COUNT_SLOTS` が `- 1` の規約を `zobrist.rs` の外へ出した

rust / architecture。`zobrist.rs:7-12,56`（`4c6e3c1a`）/ `sfen_position.rs:50-54,430-436`。
**r1 の M8 の悪化。**

出たのは定数ではなく **`- 1`** の方。「表現できる最大の枚数 = 枠数 - 1」は
`hand_count:156` の頭打ちが決めている規約で、それが `sfen_position` の散文とテストに写った。
頭打ちを「末尾の枠は溢れ専用」のような形に変えると、`- 1` は黙って1枚ぶんずれるのに、
`the_accepted_hand_count_matches_the_key_slots` は**両辺が同じ定数を見ているだけ**なので通る。

`zobrist.rs:7-8` が禁じている「同じ規約が2箇所」そのもの。doc は `:7` のまま追随していない。

併せて、正しさに要るのは `MAX_HAND_COUNT <= HAND_COUNT_SLOTS - 1` の片側だけなのに
等号で留めているので、鍵の枠だけを広げる無害な変更でテストが落ちる。

#### R3-H R2-15（訳語）が名指しの2箇所を取りこぼしたまま、結果表で閉じた

comment / rust。`index_store.rs:17,313`。`git diff 4d7df083..HEAD --stat` に `index_store.rs` が無い。

r2 が名指ししたのは5箇所で、`87eae715` が直したのは3つ。`:313` の1行には
`bucket` と `segment` の**両方**が素で出ている。

#### R3-I `docs/state-transitions/search.md:211,213,214` に `` `api` `` が3箇所

architecture。**同じ語が3ラウンド生き残っている** — r1 の M4 が4箇所直して3箇所残す →
r2 の R2-9 が指摘 → `87eae715` が Rust 側だけ直して docs を残す。

`87eae715` が直した `file_build.rs:3` / `csa.rs:205` / `kifu_reader.rs:41` は、
この doc と**同じ内容を書いている場所**。片方だけ現物に合ったので、
同じ事実を語る2種類の名前が同居している。

#### R3-J `add_n` の `unwrap_or(0)` と `expect` が同じ前提を2通りに扱う

robustness。`sfen_position.rs:260-272`。

`Hand::count` と `Hand::added` は**同じ判定**（`index < 7`）で `None` を返すのに、
1行目は「0枚として続ける」、6行目は「起きない」と扱う。
**片方が握りつぶすので、もう片方の `expect` を守っている不変条件がコードから読み取れない。**

panic したときの見え方も悪い。`run_search` は `spawn` で `JoinHandle` を捨てているので
誰も観測せず、`EVT_SEARCH_BEGIN` は出ているのに END も ERROR も出ない。
**r1 の B1 で塞いだ「返らない」と同じ見え方に戻る。**

#### R3-K 門番が読み側にしかない。書き側は桶番号を捨てている

robustness / comment。`index_cache.rs:301-305,347`（`let _ = bucket_idx;`）。

振り分けが壊れる変更が入ると: 壊れた配置を書く → 次の起動で `Err` → 全件作り直し →
同じバグでまた書く → **起動のたびに全件構築を繰り返す。利用者に出るのは進捗バーだけ。**
5万ファイルなら毎起動が数分になる。

`encode_all` は既に `Result<(), String>` を返しているので、桶ループで検査すれば型は変わらない。
**壊れた原因の場所で止まる。**

#### R3-L `build.rs` の join error がファイルを黙って落とし、`file_id` に穴を空ける

robustness。`build.rs:49-54,86,124-131`。**この変更が作ったものではない。**

差分更新側（`project_manager.rs:342-351`）は同じ場所で `EVT_INDEX_WARN` を出すのに、
全件構築側は `continue` するだけ。`(size, mtime_ms)` が変わらないので差分更新も拾い直さず、
**そのファイルの局面は永久に検索に出ない。**

さらに `path_to_id` は全レコードぶん振ってあるのに `FileTable` に項目が入らないので、
`file_id` に穴が空く。`checked_file_id` の doc は「生きている `file_id` は必ず項目を持つ」と
**断定している**が、いまのコードがその前提を破る経路を持っている。
次の起動で `bad file_id` → **キャッシュ全体が捨てられる**。

#### R3-M `save_checkpoint` の失敗が捨てられ、`fsync` も無い

robustness。`build.rs:213-224` / `index_cache.rs:165-199`。**この変更が作ったものではない。**

`let _ =` で `Err` を捨て、`JoinHandle` も捨てている。ディスク満杯・権限なしで書けなくても
手掛かりが無く、5万ファイルの構築を毎起動やり直すことになる。

`File::flush` は no-op で `sync_all` を呼んでいないので、`rename` は原子的でも
**中身の永続化は保証されない**。`:199` で `.bak` を消しているので、
電源断のあとに「中身が0埋めの `final`」だけが残りうる。

#### R3-N `sfen_position.rs` に刈らない文字列が6箇所残っている

robustness。`:111,120-125,148-150,177-179,191-193,242-246`。

**最後の1つは `4c6e3c1a` が綴りを刈った `parse_hands_into` の中に、刈らないまま残っている。**
r2 の R2-8 はこの規約を `InvalidHand` にだけ適用して終わった。

`read/` 側は `MESSAGE_LIMIT = 300` と専用のテストで同じ失敗を既に固定している。

#### R3-O `cursorAdapter.ts` が古いパスを指す

robustness。`src/entities/search/lib/cursorAdapter.ts:7` が `src-tauri/src/search/index_builder.rs`。
実物は `search/index/index_builder.rs`。

**`87eae715` は Rust → TS の指し先だけを直し、逆向きを見なかった。**
`types.rs` が新しく「対岸は `src/entities/search/api/` の3本」と往路を張ったのに、復路が切れている。
`tests/comment_identifiers.rs` は Rust しか走査しない。

#### R3-P `InvalidHand` の doc が private な `MAX_HAND_COUNT` を指し、rustdoc の警告が1件増えた

architecture。`sfen_position.rs:40`。`cargo doc` で実測。

**r1 の M3 は `e072b9d3` で「素のコードスパンに落とす」形で片付けたのに、
同じ PR の中でその判断が守られていない。** `verify:rust` は `cargo doc` を通らないので誰も気付かない。

#### R3-Q `types.rs` の対岸に、ディスクの blob が書かれていない

comment。`types.rs:1-8` / `index_cache.rs:362-371,416-425,470-476`。

`FileEntry` は TS 側に**存在しない**（`grep -rn FileEntry src/` は0件）。
逆に `FileEntry` と `Occurrence` は `encode_all` / `decode_all` が欄ごとに手で読み書きし、
`min_bytes` と `VERSION` に縛られている。

この doc に従って `Occurrence` に欄を1つ足した人は「Rust と TS を同じ変更で直した」で緑になり、
**既存のキャッシュを全部読めない版**を出す。

#### R3-R `decode_all` が164行で、桶の節だけに7行の理由が本文に埋まっている

comment。`index_cache.rs:477-640` / `:575-582`。

CONTRIBUTING は「関数本文の中に説明コメントが何行も必要になったら、関数を分ける合図」と書いている。
`decode_all` は `///` を1行も持たないまま164行あり、契約が関数の入口から見えない。

#### R3-S `compact_bucket` の `let _ = bucket_idx;`

comment。`index_cache.rs:301-305,347`。R3-K と同じ場所。
CONTRIBUTING が「素直でない書き方をした理由は必ず書く」と定めている類。

#### R3-T 「節」が3義、「段」が3義

comment。木の節（`types.rs:44` ほか）/ blob の区切り（`index_cache.rs:982` ほか6箇所）、
しかも同じものを `区間` とも呼ぶ（`:1117,1120`）。
`段` は層（`mod.rs:3`）/ 状態（`types.rs:73`）/ 盤の段（`sfen_position.rs:142`）。

**r1 の M18 は「節が正」と結論したが、`節` は既に別の意味で使われていた。**

## 積み残しの現況（r1 / r2 から送られた9件）

`git diff 1e2608fa..HEAD` で該当箇所が動いていない。**9件すべてまだ有効**（1件は一部解消）。

| 所見                                                 | 現況                                                    |
| ---------------------------------------------------- | ------------------------------------------------------- |
| M6 `BuildError::Apply` が `fork_pointers` を読まない | 有効                                                    |
| M8 `zobrist` の可視性                                | **悪化**（R3-G）                                        |
| M11 `node_action` の置き場                           | 有効                                                    |
| M12 `IndexedFile` / `FileBuild` の名前が逆           | 有効                                                    |
| M16 `Display` に35行、`walk_sequence` に0行          | 有効                                                    |
| M17 公開面の doc の欠落                              | **一部解消**（`IndexState` に doc が付いた。残り8箇所） |
| M18 「節」と「ノード」                               | 有効（R3-T で前提が変わった）                           |
| M19 外部保証の根拠                                   | 有効                                                    |
| M21 `file_build.rs` の経緯                           | 有効                                                    |

## 実測で問題なしと判断したもの

- **鍵の値は変わっていない。** `1e2608fa` の worktree と HEAD に同じ probe を置いて
  12種の綴りで突き合わせ、**全件一致**
- **正当な綴りを1つも弾いていない。** `-` / `18P` / `9P9P` / `2R2B4G4S4N4L18P`（7種すべて上限）/
  `18P4R` / `18p18P` はすべて `Ok`
- **`decode_all` の門番は正常なキャッシュを弾かない。** 生産経路は3つだけで、いずれも
  桶ごとに昇順を出す。`encode_all` と `decode_all` の順と幅が一致。既存の VERSION 3 も通る
- **`Err` を受けた呼び手は本当に全件作り直しへ落ちる**（`commands.rs:82-88,170-176`）
- **`keys_order_by_z0_then_z1` は宣言順の入れ替えで落ちる**（73 passed / 1 failed、落ちたのは1本だけ）
- **`segment.rs` の題材は振り分けを見ている**（桶を潰す変異でこのテストだけ落ちる）
- **`add_n` の `before + n` は overflow しない**（両辺18以下で最大36）
- **`SearchEndPayload` / `types.rs` の対岸 / `search/mod.rs` の r2 の書き直しは実装と合っている**
- **bench の並びは完了**（手書きの `(z0, z1)` 比較は本番・bench とも0件）
- **並列（Semaphore 2〜8）そのものに競合は無い**
- **`cargo test --lib` 228 passed / `clippy -D warnings` 警告なし**

## 見ていない範囲

- `read/` 配下の本体（`kifu_reader.rs` は 2,200行超）
- `store/file_table.rs` の中身（r1 / r2 / r3 とも見ていない）
- `index_cache.rs` の encode / decode のうち桶以外の節の検査の網羅性
- bench の実走（#402 で bench 自体が疑われている）
- TS 側の `features/position-search/` の状態遷移
- `book` 側の受理集合（#236）との突き合わせ

## lint / hook で強制できるもの

- **rustdoc の警告数のラチェット**（R3-P）: `cargo doc --no-deps 2>&1 | grep -c '^warning:'` を
  `verify-gate.sh` で数え、**増える方向にだけ落とす**。現在値13。
  `-D` 化と違い既存を片付けなくても今日から張れて、**R3-P はこれで止まっていた**
- **`` `api` `` のような実在しない1語**（R3-I）: `comment_identifiers.rs` の候補抽出に
  「バッククォート内が1語で、`src-tauri/src` のモジュール名にも `.rs` 名にも無い」を足し、
  **走査対象に `docs/` を含める**。3ラウンド続いているので範囲を広げないと4回目が出る
- **TS のコメントが指す `src-tauri/src/...` のパス**（R3-O）: vitest 側に1本。往路だけ守られている
- **`node_id` の範囲**（R3-C）: `checked_file_id` と同じ形の `checked_node_id`。
  `rg 'node_id: r\.read_u32'` が0件になればラチェット
- **`Segment::new_sorted` の事前条件**: `debug_assert!` を置く。
  ディスク側は塞いだが、メモリ側の4経路は今も無検査
- **`19R` の文言に `Rook` が出ること**（R3-B）: いまの `matches!(.., Err(InvalidHand { .. }))` は
  欄の中身を見ないので素通りする
- **`let _ = <引数名>;`**（R3-S）: `rg 'let _ = [a-z_]+;'` で拾える
- **修正が名指しの箇所を取りこぼすこと**（R3-C / R3-H / R3-I）: **下の計画で機構にする**
- **機械では防げない**: R3-A / R3-D / R3-F / R3-J / R3-Q / R3-T

## 修正計画（r3 → r4）

### 対象そのものを疑ったか

**疑う。範囲を畳む。**

件数は r1 29 → r2 16 → r3 20（＋積み残し9）で**減っていない**。
`/review-plan` 手順4 の「3回続いたら直し方ではなく対象を疑う」に該当する。

数えると、所見の出どころは3つに分かれる。

| 出どころ                             | r3 の件数 | 例                                                           |
| ------------------------------------ | --------- | ------------------------------------------------------------ |
| **私の修正が作った / 不完全**        | 9         | R3-A / R3-B / R3-C / R3-D / R3-E / R3-F / R3-G / R3-H / R3-P |
| **この変更が触っていない既存の欠陥** | 6         | R3-L / R3-M / R3-N の一部 / R3-Q / R3-R / R3-T               |
| **積み残し（r1 から2回送った）**     | 9         | M6 / M8 / M11 / M12 / M16 / M17 / M18 / M19 / M21            |

**2つ目と3つ目を範囲から出す。** これらは「`search/position` と `search/index` の契約を
整理する」という当初の範囲の外にあり、抱えている限りラウンドごとに再掲され続ける。
`search/read` と `search/cache` の本体、`build.rs` / `project_manager.rs` の失敗経路は、
**それぞれ独立に読むべき対象**で、この PR に混ぜると差分が読めなくなる（`/implement` 手順7）。

**1つ目だけを直して閉じる。** 9件のうち機械で留まるものを機械にし、doc は削る。

### 3ラウンド続いた機構の欠陥を、機構で塞ぐ

**「所見が名指しした箇所を取りこぼしたまま、結果表で閉じる」が3回**。

| ラウンド | 所見                                   | 名指し      | 直した    | 閉じた |
| -------- | -------------------------------------- | ----------- | --------- | ------ |
| r1       | M4 `api.rs`                            | 7箇所       | 4箇所     | ✓      |
| r2       | R2-9 `api` / R2-15 訳語 / R2-3 検査3つ | 各5 / 5 / 3 | 3 / 3 / 2 | ✓      |
| r3       | R3-C / R3-H / R3-I として再掲          |             |           |        |

**規律ではなく手順にする。**

> 結果表に1件書く前に、**その所見が挙げたパスすべてが
> `git diff <base>..HEAD --stat` に現れることを確かめる**。現れないものは
> 「未完」として結果表に書く。閉じない。

r1 の M4 も r2 の R2-15 / R2-3 も、これで止まっていた。

**もう1つ。振る舞いを変えたら、その振る舞いを記録している doc を探す。**
R3-A（自分の doc）と R3-E（`docs/state-transitions/`）が同じ根。

> `fix:` / `refactor:` で振る舞いを変えたら、コミット前に
> `rg '<変えた振る舞いの語>' docs/ src-tauri/src/ src/` を1回走らせる。

### このラウンドで直すもの

| 順  | 所見                                                   | なぜこの順か                                                            | この直し方で壊しうるもの                                                                                                                                                                                                                                                            |
| --- | ------------------------------------------------------ | ----------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **R3-B + R3-D** `InvalidHand` の嘘と、桁の理由         | HIGH。利用者に誤情報が出ている。`Err` を作る箇所を1つに畳むと両方消える | 桁で `Err` を作るのをやめ `num` を飽和させる。**`add_n` に到達しない綴り**（`"9999..."` だけで駒の文字が無い）は `Err` にならず `dangling number in hand` へ落ちる — 文言が変わるので確かめる                                                                                       |
| 2   | **R3-C** `node_id` の範囲                              | HIGH。R2-3 の取りこぼし。**機構の欠陥の実例なので先に閉じる**           | 検査を足すと、**いま黙って0手目として出ていたヒットが `Err` → 全件作り直し**になる。正常なキャッシュを弾かないかが焦点で、`an_index_of_a_realistic_size_can_be_read_back`（`node_id: i % NODES_PER_FILE`）が守る。`bound == 0`（表の無い `file_id`）を `Err` にするかは分けて決める |
| 3   | **R3-K** 書き側の門番                                  | 読み側だけだと毎起動の全件構築になる。2と対                             | `encode_all` の桶ループで検査。`Result` を既に返すので型は変わらない                                                                                                                                                                                                                |
| 4   | **R3-G** `- 1` を `zobrist` に閉じる                   | 機械で強制できる（手順3-1）。M8 の悪化を戻す                            | `HAND_COUNT_SLOTS` を private に戻し `MAX_HAND_COUNT` を `zobrist` 側に置く。`sfen_position` の `const` が消えるので、そこを指す doc とテストも消える（規律4）                                                                                                                      |
| 5   | **R3-P** rustdoc の警告 + ラチェット                   | 機械で強制できる。**R3-P はラチェットがあれば止まっていた**             | 警告数のラチェットは既存13件を基準にする。増える方向にだけ落とす。`-D` 化はしない                                                                                                                                                                                                   |
| 6   | **R3-A + R3-E** 振る舞いを変えたのに追随しなかった doc | 上の手順の最初の適用対象                                                | `search.md` の実測値（672通り中513件）は**測り直さず、測定時点のコミットを併記して据える**。測り直すのは別作業                                                                                                                                                                      |
| 7   | **R3-F** `bucket.rs`                                   | **3ラウンド連続で誤り。説明を書くのをやめる**（規律4を最後まで適用）    | 段落を落として `search_occurrences_by_key` と `compact_bucket` を指すだけにする。数を書かない                                                                                                                                                                                       |
| 8   | **R3-H + R3-I** 取りこぼした訳語と `api`               | 上の手順の適用対象。`docs/` まで見る                                    | `docs/state-transitions/search.md` を触るので `verify:rust` まで走る                                                                                                                                                                                                                |
| 9   | **R3-J** `add_n` の `unwrap_or` と `expect`            | 1で `add_n` を触るので続けて                                            | `h.count(pk)` を `?` にすると `SfenParseError` が要る。1の飽和方式と噛み合うか確かめる                                                                                                                                                                                              |
| 10  | **R3-O** `cursorAdapter.ts`                            | TS 側1行                                                                | `npm run verify` だけ                                                                                                                                                                                                                                                               |

### 直さないもの

| 所見                                                                 | 行き先                   | 理由                                                                                                                                                        |
| -------------------------------------------------------------------- | ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **R3-L** join error がファイルを落とし `file_id` に穴                | **issue**                | `build.rs` の失敗経路。この変更が触っていない。**`checked_file_id` の doc の断定が破れる**ので単独で重い                                                    |
| **R3-M** `save_checkpoint` の失敗と `fsync`                          | **issue**                | 同上。`fsync` の判断は電源断の扱いを決める話                                                                                                                |
| **R3-N** 刈らない文字列6箇所                                         | **issue**（#398 に合流） | `diagnosis::capped` を `read` の下から下げるか、`query_service` の emit 直前で刈るかの判断が要る。**1箇所（`parse_hands_into` の中）だけは1で一緒に消える** |
| **R3-Q** 対岸にディスクの blob が無い                                | **r4**                   | doc 1文。r4 でまとめて                                                                                                                                      |
| **R3-R** `decode_all` が164行                                        | **issue**                | 関数の分割。2と3で同じ関数を触るので、混ぜると差分が読めない                                                                                                |
| **R3-S** `let _ = bucket_idx;`                                       | 3で一緒に消える          |                                                                                                                                                             |
| **R3-T** 「節」「段」が3義                                           | **issue**                | 用語表を置く話。`search/` 全体に及ぶ                                                                                                                        |
| **積み残し9件**（M6 / M8 / M11 / M12 / M16 / M17 / M18 / M19 / M21） | **issue 1本にまとめる**  | **2回送った。3回目は送らない。** どれも `index/` と `position/` の doc と名前で、独立に読める。M8 は4で構造的に半分解消する                                 |

### 次ラウンドの焦点

1. **R3-C の検査が正常なキャッシュを弾いていないか。** `bound == 0` の扱い
2. **R3-B の飽和方式で、`add_n` に到達しない綴りの文言が妥当か**
3. **上の2つの手順（パスの突き合わせ / 振る舞いを変えたら doc を探す）が実際に効いたか。**
   r4 で「取りこぼし」と「doc の追随漏れ」が0件なら機構が効いている
4. **範囲を畳んだことで所見が減ったか。** 減らなければ、畳み方が足りない
5. r3 で見なかった範囲: `store/file_table.rs`、`index_cache.rs` の桶以外の節

### 検証の見積り

10件 × `verify:rust`（約2分15秒）≈ **23分**。`docs/` と TS を触る回があるので `verify` も。

## 結果（r3 の修正）

| 所見                                                 | コミット   | 名指しの箇所が差分に入ったか                                                            |
| ---------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------- |
| **R3-B / R3-D / R3-G / R3-J / R3-P** ＋ R3-N の1箇所 | `d5cbf080` | ✓ `sfen_position.rs` / `zobrist.rs`                                                     |
| **R3-C / R3-K / R3-S**                               | `0fb738aa` | ✓ `index_cache.rs`                                                                      |
| **R3-P のラチェット**                                | `bbbe1115` | ✓ `package.json` / `scripts/`                                                           |
| **R3-A / R3-E / R3-F / R3-H / R3-I / R3-O**          | `f8666d71` | ✓ `position_key.rs` / `bucket.rs` / `index_store.rs` / `search.md` / `cursorAdapter.ts` |

**手順どおり、結果表を書く前に `git diff 4f5ac2ec..HEAD --stat` と突き合わせた。**
r3 の所見が名指しした全パスが差分に入っている。

### issue へ出したもの

| 所見                                                                 | issue |
| -------------------------------------------------------------------- | ----- |
| **R3-L** join error でファイルが消え `file_id` に穴                  | #406  |
| **R3-M** チェックポイントの失敗と `fsync`                            | #407  |
| **R3-T** 「節」2義・「段」3義                                        | #408  |
| **積み残し9件**（M6 / M8 / M11 / M12 / M16 / M17 / M18 / M19 / M21） | #409  |
| **R3-N** 刈らない文字列（残り5箇所）                                 | #410  |
| **R3-R** `decode_all` が164行                                        | #411  |

**積み残しは2回送ったので3回目を送らず、1本にまとめて出した。**

### 修正で新しく分かったこと

**`node_id` の検査が3つのテストを落とした。** 計画が「分けて決める」とした
「表の無い `file_id` を `Err` にするか」がそこで決まった — **表があるときだけ見る**。
表を持たない `file_id` は `is_occ_alive` が落とすので、弾く必要が無い。

**書き側の門番を入れたら、読み側の門番のテストが書けなくなった。**
題材が encode で止まるようになったため。**書けた blob を壊す形に組み直した** —
読み側の門番はビット化けが相手なので、そちらが実態に近い。3つとも変異で落ちる。

**ラチェット自身にバグがあった。** `$count（基準` のように変数展開の直後へ全角文字を
置くと、bash が変数名の一部として読む。波括弧で囲んで直した。
**両方向（増える / 減る）で効くことを確かめてから入れた。**

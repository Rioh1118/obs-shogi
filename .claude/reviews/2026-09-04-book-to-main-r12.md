# レビュー book-to-main ラウンド12

- 日付: 2026-09-04
- 範囲: r11 の所見9件を直した9コミット（`git diff f5cc630a HEAD -- . ':!.claude/reviews'`）
- 走らせた reviewer: comment / rust / robustness / oss-hygiene
- 対象コミット: `d286722a`
- 前ラウンド: `.claude/reviews/2026-09-04-book-to-main-r11.md`

所見 **9件**（BLOCK 0 / HIGH 0 / MEDIUM 9）。
推移は 22 → 18 → 23 → 10 → 12 → 7 → 5 → 4 → 6 → 8 → 9 → **9**。

**HIGH が0に戻り、9件すべてが「削る／縮める」方向で直った。**

このラウンドから**直し方の方針を変えた** —— 言い換えを足さず、**削って直す**。
r9〜r11 で増え続けた原因は、直すたびに書いた新しい散文が次のラウンドの所見面になることだった。
9件のうち5件（AC-01 / AC-02 / AC-03 / AC-04 / AC-08）は、削る以外の直し方が無い形。

issue へ送るものは無かった。

---

## 所見

### AC-01 (MEDIUM, 3人が実測): 書いた数え方が、ルートでは動かず、ラチェットとも別の数を出す

`5cd52071` が置いたコードブロックは2つ壊れていた。

- **ルートに `Cargo.toml` が無い。** そのまま打つと `error: could not find Cargo.toml` で、
  awk は空行を返す。**r11 の AB-01 が潰した「指示どおりにやると数が出ない」が、
  0 から空行に変わっただけ**
- **`src-tauri` で走らせても 276。** ラチェットが数えるのは `src` と `tests` だけで 266。
  `benches/search_bench.rs` は `Cargo.toml` の `[[test]]` なので `cargo test` は回すが、
  走査には入らない。276 を `EXPECTED_MIN` に写すと即座に落ちる（`266 < 276`）

- 結果: 対応済み `dd5109c5`（数え方ごと削った。数が要る場面はラチェットが失敗文で `found` を出す）

### AC-02 (MEDIUM, 2人が実測): 「幅が効くのは足す側」も、幅そのものではない

`0bc5ef14` は消す側の誤りを正した文の**直後で、同じ誤りを足す側で作り直していた。**
足せるのは `DRIFT_ALLOWANCE - (found - EXPECTED_MIN)` で、いま 13 本。
「幅が効くのは足す側」も偽 —— 幅は消す側の盲点の天井そのもの（`EXPECTED_MIN` を 20 下げると
20 本消しても緑）。**同じ定数について、8行離れた2つの doc が逆を名乗っていた。**

- 結果: 対応済み `3a3e3770`（1行削った）

### AC-03 (MEDIUM, 3人): Q-004 の「4形式ぶん見るテストが2本」が、片方は2形式

**r11 の AB-06 が現物を開かずに書いた誤りが、そのまま doc へ写っていた。**
`gote_start_is_written_once_and_survives_a_round_trip` が回すのは kif / ki2 の2形式で、
しかもディスクを経由しない `convert_jkf_to_string_internal`。csa / jkf の書き出し→読み戻しを
見ているのは1本だけ。

- 結果: 対応済み `05285dfb`（覆えている1本だけを名指した。r11 の報告書にも訂正）

### AC-04 (MEDIUM, 3人が実測): 表の前置きに置いた全称が、同じ表の C 行と F2 行で破れる

`907979ad` の「別リポジトリなら**形に関わらず** S3' で `exit 0`」は偽。C は S3 で deny、
F2 は S2 で deny、I は S2 の手前で終わる。**表の C 行・F2 行が自分で `deny` と書いている。**
括弧の帰属も違う（`expect_project` はディレクトリ引数5個で呼ぶだけで、コマンドの形を通さない）。

**r11 の AB-04 が「数え落とし」を直した結果が、今度は数え過ぎになっていた。**

- 結果: 対応済み `561bc99a`（2文目を削った）

### AC-05 (MEDIUM, 3人): `test_count_ratchet` の逃げ道が、2本ある assert の片方で逆向き

`c8627a11` が足した行は減る側しか書いていない。増やしたのに `EXPECTED_MIN` を更新しないと
落ちる assert がもう1本ある。**いま足せる余裕は 13 本なので1回の作業で踏みうる。**
踏んだ人が索引を開くと「下げる」としか書いていないが、下げると右辺も下がるので
**落ちている assert はさらに深く落ちる。**

- 結果: 対応済み `b2b74e71`（セルを増やさず「実測へ合わせる」に畳んだ）

### AC-06 (MEDIUM, 1人): `state_table_terms` の逃げ道が、2つある allowlist の片方しか無い

`NOT_IDENTIFIERS` が落ちていた。状態遷移表に他プロジェクトの定数をバッククォートで引いた
瞬間に落ちる側で、逃げ道を知らない人は**正しく書かれた行を消しにいく** ——
検査自身が名指して警戒している経路。

- 結果: 対応済み `da5a99f8`（`root_guard` の行と同じ形へ）

### AC-07 (MEDIUM, 1人): 表の前置きが「`npm run test` に置いてある」と言い切る

前置き自体はこの合流の変更ではないが、**Rust の行を1から4に増やしたのはこの PR。**
その4本は vitest では走らない。`verify:rust` で赤くなった人が索引を開くと、
赤の出どころと表の対応が付かない。

- 結果: 対応済み `129020a3`

### AC-08 (MEDIUM, 1人): 「テストが実質無い」の6箇所目

綴りが「実質無い」なので、r9 から4ラウンド追ってきた grep に一度も掛かっていなかった。
`/review-round` と `/review-protocol` は「件数を前提にしない」を渡すのに、
**その所見を実際に直す `/review-fix` だけが「テストは実質無い」を渡していた。**

- 結果: 対応済み `acb95b9c`（量的主張が無くても手順は成立するので節ごと落とした）

### AC-09 (MEDIUM, 1人): r11 の報告書の「写す側4箇所」が3箇所

`CLAUDE.md` を指すようになったのは3つで、`OPEN-QUESTIONS.md` は括弧ごと削っただけ。
**4ラウンド続けて落としている「直した先を数えて名乗る」形を、r11 自身がまた踏んでいた。**

- 結果: 対応済み `68330966`（数を書かない形へ）

---

## 重複・矛盾した所見

- **AC-01 / AC-03 / AC-04 / AC-05 は3人、AC-02 は2人が指した**
- **大半が r11 の直しが直接作ったもの。** 直した文の隣で同じ誤りを作り直す（AC-02）、
  数え落としを直したら数え過ぎになる（AC-04）、報告書に書いた未検証の記述が doc へ写る（AC-03）。
  **`c8627a11` が索引に足した行は、足した次のラウンドで2件（AC-05 / AC-06）の所見面になった**
- **数の名乗りが3件**（AC-01 の 276 / AC-05 の向き / AC-09 の「4箇所」）。
  r7 で「列挙をやめる」、r9 で「網羅を名乗らない」と決めた判断を、
  **5ラウンド続けてより小さい単位で置き直している**
- 所見の水増しは無かった。4人とも「確認して所見が無かったもの」を明記していて、とくに
  **免除の根拠（`994ea320` / `16b7662c`）は robustness が 5動詞 × 全オプションを進行中・
  clean・汚れたツリーで実行し、commit オブジェクト数の増減まで数えて4文とも正しいことを
  確認している。** `--skip` に手元の変更が混ざる経路を3通り試して見つからないことも確認済み

## この PR の外で観測したこと

`git commit` を `cd` と同じ呼び出しに入れると、**ゲートがメインのチェックアウトを検証した**
（このワークツリーに無い `comment_identifiers.rs` が落ちた）。#342 の既知の症状で、
このラウンドでも再現。**`git commit` は単独の呼び出しで打つこと。**

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送った16件（再掲しない前提）
- `src-tauri/src/book/` の振る舞い（doc の主張との突き合わせのみ）
- `user_book1.db` の実測3行とピーク確保の実測
- ShogiHome v1.29.0 側の引用（現物を取っていない）
- 「競合を抱えていれば検証は必ず落ちる」（r11 に続き真偽不明のまま。反例の候補
  ——`docs/*.md` だけの競合—— は robustness が突き止めたが、確かめるにはこのワークツリーに
  競合マーカーを書く必要があり実行していない。免除の結論は、もう一方の根拠で立つ）

## lint / hook で強制できるもの

このラウンドで新しく挙がったものは無い。AC-01（doc の中のコマンドが動くか）と
AC-05 / AC-06（逃げ道の欄が検査の全ての赤を覆っているか）は機械で拾える形だが、
**`ratchetIndex` が自分で「見るのは名前の対応だけ」と線を引いている外側**で、
#351 / #386 へ送済みの範囲。

機械で守れないもの: AC-02（同じ定数について2つの doc が逆を名乗る）、AC-03（doc が名指した
テストの中身）、AC-04（表の前置きが表の行と矛盾しないか）、AC-07 / AC-09。

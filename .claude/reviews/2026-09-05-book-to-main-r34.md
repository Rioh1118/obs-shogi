# レビュー book-to-main ラウンド34

- 日付: 2026-09-05
- 範囲: r33 の所見8件を直した6コミット＋報告書
  （`b2238739` と `9bc0c3e3` が2件ずつ担う。`git diff 14687859 4703515f`）
- 走らせた reviewer: architecture / oss-hygiene
- 対象コミット: `4703515f`
- 前ラウンド: `.claude/reviews/2026-09-05-book-to-main-r33.md`

所見 **6件**（BLOCK 0 / HIGH 0 / MEDIUM 6）。
推移は 22 → … → 7 → 9 → 8 → **6**。

**10ラウンド続けて HIGH 0。**

**このラウンドで初めて、検査を book-to-main の外へ当てた。**
同じ PR が持ち込む `yaneuraou-db` と `book-foundation` に同じ検査を当てると、
**12本が外れていた**（`yaneuraou-db` 3本＋`book-foundation` 9本。
揃えて13本から落とした ——`book-foundation` の `-r7.md` の1本は正しかった）。
**r35 でさらに `yaneuraou-db` 4本が外れることが分かった。**

---

## 所見

### BC-01 (MEDIUM, 1人が実測): 床が実測の 9% / 73% / 52% で、形を1種類失う変異が3通り通る

実測は `hash 194 / line 10881 / block 683`。床は `100 / 1000 / 500`。

| 変異                 | line     | block   | 注入した違反 |
| -------------------- | -------- | ------- | ------------ |
| 行頭の `//` だけ読む | **4073** | 7283    | **見逃す**   |
| `/**` だけ読む       | 10898    | **592** | **見逃す**   |
| 複数行の塊だけ読む   | 10799    | **506** | **見逃す**   |

1つ目が `line` から減らす6,808件のうち、**6,498件は字下げされた行頭コメントで、
`block` の欄へ移っているだけ**（同じ変異で `block` が 683 → 7283 に増えている）。
**走査から本当に落ちるのは行末の229件・2.1%。**
それでも `useURLParams.ts` の行末に書いた違反は緑のままで、
**行末だけを失う変異は3つの床を全て越える** ——床では取れない族。

**BA-01 が塞いだ「当たる文字が1つ残れば通る」の一段上の同型。**

- 結果: 対応済み `c057189b`（実測の直下へ。**3通りとも落ちることを確認**。
  `#` を行頭だけ・空白付きだけに絞る族は**床では取れない**ので、その旨も書いた）

### BC-03 (MEDIUM, 1人が実測): `REVIEW_TAG` が、いま採番している2文字の識別子を1つも拾わない

`\([A-Z]-[A-Z]?\d+\)` は任意の英字を**ハイフンの後ろ**に置いていて、
`(` の直後は1文字しか許さない。

- `(T-01)` `(F-27)` → 当たる
- `(BA-01)` `(AZ-05)` `(AU-03)` → **当たらない**
- `(A-B01)` → 当たるが、**この綴りは `.claude/reviews/` に1つも無い**

`.claude/reviews/` の識別子は**1文字が263件、2文字が128件**（見出しの異なり数）。
**このループの所見IDは全て2文字**で、旧式はその128件を1つも拾わない。

**「指す先の消えたレビュー識別子を止める」のが目的の枝が、
いま使っている形だけを通していた。**

- 結果: 対応済み `4a4983ba`（`\([A-Z]{1,2}-\d+\)` へ。
  誤検出0のまま、`(BA-01)` の注入で赤くなることを確認）

### BB-04 (MEDIUM, 1人が実測): 床の根拠に書いた「実測 227」が、その assert が読む量ではない

`read.hash` は `text.length > 2` を通ったものだけを数えるので実測は **194**。
227 は絞り込み前の生の一致数で、**同じ値が r33 の BA-01 の表で
変異 `/#/g` に割り当てられている。**

**AW-07 / AX-01 と同型。** コメントの指示に従って床を 227 付近へ上げると、
**違反が1件も無いのに即座に赤くなる。**
**BA-01 の直しそのものが、BA-01 が塞ごうとした型を1つ持ち込んでいた。**

- 結果: 対応済み `c68bbf53`（AT-01 / AX-03 / AZ-01 と同じく数を落とし、
  **なぜ書かないか**を代わりに書いた）

### BB-01 (MEDIUM, 1人が実測): AZ-05 の検査を book-to-main にしか当てていない

同じ PR が持ち込む `yaneuraou-db` 9本に当てると**3本が外れる。**
このシリーズの単位は**ID の異なり数**（見出しと表を通して）で、r5（7件）と r7（16件）は一致する。

|     | ヘッダ                             | 実測                                   |
| --- | ---------------------------------- | -------------------------------------- |
| r6  | 15（HIGH 3 / MEDIUM 10 / LOW 2）   | **21**（HIGH 3 / MEDIUM 15 / LOW 3）   |
| r8  | 17（BLOCK 1 / HIGH 3 / MEDIUM 13） | **15**（BLOCK 1 / HIGH 4 / MEDIUM 10） |
| r9  | 17（BLOCK 0 / HIGH 4 / MEDIUM 13） | **13**（HIGH 4 / MEDIUM 9）            |

**AZ-05 は「見つけた instance を塞いで形の全体に広げない」型だった** ——
r33 自身が「4度目」と数えている形。

- 結果: 対応済み `f0c2bc99`（`G-03` の欠番も併記。
  r6 の合体見出しの配分は、**元のヘッダ自身の HIGH 3 を成り立たせる1通り**を採った）

### BB-02 (MEDIUM, 1人が実測): `book-foundation` の累積が r8 で単位ごと切れている

`-r7.md` の `計73件` は再現する（15+17+12+10+9+10）。
**`-r8.md` の `計85件` は再現しない** —— 間に挟まるのは r7 の6件だけで 73+6 = 79。
以降 r16 の `計216件` まで、9本が同じずれを運んでいた。

**隣り合う2つの欄が互いに矛盾する単位を使っている。**

- 結果: 対応済み `d5d991af`（**正しい値へ直す側は採らなかった。**
  数え直したところ reviewer の値と1件ずれ、突き合わせると r13 の
  `R-08 / R-12` の合体見出しを1件と数えるか2件と数えるかで解釈が割れていた。
  **単位が争点になる数を、また新しい値で置き直さない** ——
  AT-01 / AX-03 / AZ-01 / BB-04 と同じ判断。r7 の正しい1本も落とした
  ——境界に根拠が無い形は AU-03 で所見になっている）

### BB-03 (MEDIUM, 1人が実測): r33 の範囲が `f21b9916` を二重に数えている

9件目の直しは**後追いの `f21b9916`** なので、「9コミット」と「後追いの2コミット」が
同じ1本を2回数えていた（9+1+2 = 12、実測11）。

**r33 自身の AZ-04 が `f21b9916` の役割を正しく書いている**のに、4行目が二重に数えていた。

- 結果: 対応済み `e171fc68`

---

## 重複・矛盾した所見

- **6件とも1人ずつ**
- **BB-01 / BB-02 が、このループで初めて「検査の適用範囲」を所見にした。**
  r33 までの検査はすべて book-to-main の中で閉じていた ——
  **同じ PR が58本の報告書を持ち込むのに、32本にしか当てていなかった**
- **BC-01 は BA-01 の一段上の同型。** BA-01 で「0 と比べる形にしない」を学んだが、
  **置いた床が実測の1割だった。** 形を1種類まるごと失っても越える
- **BB-04 は BA-01 の直しが持ち込んだ。** 「実測から取る」と書いた根拠が、
  **いまのコードを実測しても出てこない数**だった。AW-07 / AX-01 と同型で3度目
- **BC-03 はこのループで初めて出た形** —— 検査の意図が現物の形に追随できていない。
  2文字の採番は `AA` から始まっていて、この PR より前から128件ある
- **BB-03 / BB-01 / BB-02 は数え方**。9ラウンド連続
- 所見の水増しは無かった。2人とも「確認して所見が無かったもの」を明記していて、とくに:
  - **AZ-05 の2本の書き換えは両方とも正しい**（r3 = 1+1+2+19 = 23、r4 = 2+8 = 10）
  - **`BLOCK の一部` は新しく導入したものではない** —— r3 の T-02 の見出しに
    元からある綴りをヘッダへ写しただけ。他シリーズには1件も無い
  - **「10ラウンド続けて HIGH 0」の数え直し** —— AZ-05 が触った r3 / r4 は
    変更後も HIGH 2 / HIGH 2 で、r25 より26ラウンド前。**連続本数を1つも動かさない**
  - **book-to-main 全33ラウンドで、ヘッダの件数＝ID の異なり数、内訳も全部一致**（AZ-05 の直し後）。
    **見出しの数とは r9 / r22 / r23 / r24 / r25 で割れる**（合体見出しがある回）
  - **推移の列は r5〜r33 の全項が実測と一致**
  - **AZ-02 の「r31 にはこの欄が無い」は真**
  - **AZ-01 / AZ-03 / AZ-04 の直しに落とし残しは無い**
  - **`missingIn` / `walk.ts` / `IDENTIFIER` / `EXEMPT` の変異は、注入基準でも所見0** ——
    `IDENTIFIER` の文字クラスから `0-9` を外す4通りは拾う195件が完全一致、
    `EXEMPT` は7件中5件が赤くなり、残る2件（`go_ponder` / `position_sfen`）は
    `docs/` に出てこない予防的な列
  - **`codeOf` の c-like 枝は、注入基準でも BA-03 の1件だけ** ——
    生存12通りは corpus の**語の列が base と完全一致**（語数差0）
  - **`b2238739` / `12946be3` が入れたコメントは `commentHistory` を通る**（自己適用済み）

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送ったもの（再掲しない前提）
- `origin/main` が元から持っていた実装と doc の中身。
  **`game-engine`（35ラウンド）/ `skc-v040` / `skc-v050` / `file-tree-error` の60本は
  `origin/main` に既にあり、この PR は1バイトも触っていない。**
  ヘッダの検査を当てると複数が引っかかるが、範囲外として判定していない
- `docs/spec/screens/` の13面が現物の UI と合っているか
- `user_book1.db` の実測3行とピーク確保の実測
- `identifiersIn` の本体（バッククォート抽出・`()` 落とし・Set 化）の変異
- `codeOf` の shell 枝（r33 が注入基準で所見0と結論したきり）
- 3重以上の変異
- `codeOf` を使わないラチェット（`asyncResultUse` / `escapeReceivers` / `scssScale` ほか）
- `sourceText.test.ts` が `readFileSync` を相対パスで読んでいる点（差分の外）
- Rust 側（このラウンドの差分に `.rs` は無い）

## lint / hook で強制できるもの

- **BC-01 は #351 の (d) で落ちるが、床の値そのものも機械で見られる** ——
  `toBeGreaterThan(<定数>)` の定数と、その場で数えた実測の比を出し、
  比が一定以下なら落とす。今回の3つは **9% / 52% / 73%**
- **BC-03 は機械化できる** —— `.claude/reviews/**` の `### <ID>` から綴りを集め、
  `REVIEW_TAG` に食わせて**1件も当たらない形があれば落とす**。
  採番が3文字に伸びた日も同じ形で拾える
- **BB-01 / BB-03 は同じ検査で落ちる** —— r33 が書いた
  「`### <ID> (<深刻度>` を数えてヘッダと突き合わせる」を
  **book-to-main に限定せず `.claude/reviews/` 全体に掛ける。**
  見出しの書式がシリーズごとに3通りあるので、寄せるかパーサに3通り持たせるかを先に決める
- **BB-02 も数え直せる** —— `前ラウンド` 欄の累積を、参照先の見出し数の和と突き合わせる。
  接頭辞が欠番なしの連番であることも同時に検算できる（r9 の `G-03` 欠番はそれで出る）
- BB-04（コメントに書いた実測値と assert が読む量のずれ）は機械で防げない。
  **床の値をコメントに書かない**という規約なら守れる
  ——AT-01 / AX-03 / AZ-01 が既にその判断を3回している

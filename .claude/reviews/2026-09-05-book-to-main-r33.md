# レビュー book-to-main ラウンド33

- 日付: 2026-09-05
- 範囲: r32 の所見9件を直した9コミット＋報告書＋後追いの2コミット（`git diff 429987e1 14687859`）
- 走らせた reviewer: architecture / oss-hygiene
- 対象コミット: `14687859`
- 前ラウンド: `.claude/reviews/2026-09-05-book-to-main-r32.md`

所見 **8件**（BLOCK 0 / HIGH 0 / MEDIUM 8）。
推移は 22 → … → 5 → 7 → 9 → **8**。

**9ラウンド続けて HIGH 0。**

**注入基準を初めて全面的に使ったラウンド。** r32 で分かった
「(d) 各ラチェットに注入した既知の違反が検出され続けること」を判定に据えたところ、
**前ラウンドで塞いだつもりの穴が、まだ大半開いていた。**

---

## 所見

### BA-01 (MEDIUM, 1人が実測): `shellComments > 0` の床は「`#` の枝が壊れた」の大半を通す

`053ecde0` の床は**回数**を数えていて、壊し方4通りのうち**3通り**を通していた。

| `HASH_COMMENT` の変異       | `shellComments`          | 注入した違反           |
| --------------------------- | ------------------------ | ---------------------- |
| `/@[^\n]*/g`                | 1（`"${@:2}"` に当たる） | **見逃す**             |
| `/#/g`（本文を読まない）    | 227                      | **見逃す**             |
| `/#[^\n]/g`（`*` を落とす） | 194                      | **見逃す**             |
| `/§[^\n]*/g`（本当に0件）   | 0                        | 落ちる（唯一の殺し手） |

**`053ecde0` のメッセージの「当たらない綴りにすれば新しい assert が落ちる」が偽。**
当たる文字が1つでも `.sh` に在れば床を越える。

数える側が拡張子を**独立に**見ていたのも穴で、`commentsOf` の分岐を `.ts` に壊しても
`.sh` 中の `//` 3件で床を越え、注入した違反を見逃す。

- 結果: 対応済み `b2238739`

### BA-02 (MEDIUM, 1人が実測): `SLASH_COMMENT` 側には床が1つも無い

`ROOTS` が歩く568本のうち **566本を読む枝**を固定するものが無かった。

当たらない綴りにしても・`//` の枝だけ落としても・ブロックの枝だけ落としても・
本文を読まなくしても、**708本すべて緑で注入した違反を見逃す。**
（`g` フラグを落とす変異だけは `matchAll` の TypeError で殺される。）

**AY-01 で塞いだのは2ファイル分の枝で、同じ穴が566ファイル分に残っていた。**

- 結果: 対応済み `b2238739`（BA-01 と同じコミット。
  **1つの数にまとめず `#` / `//` / `/* */` を別々に数える** ——
  片枝だけ落とす変異が実在し、合計の床では通るため）

### BA-03 (MEDIUM, 1人が実測): ブロックの手前の行コメントを落とす箇所が固定されていない

`codeOf` の c-like 枝で、ブロックの手前の断片に掛けている `stripLineComments` を
外すと**708本すべて緑・`npm run verify` も exit 0。**

**r30 の「c-like 枝の生存9件は実 corpus に対して出力が1バイトも変わらない」は
この変異には当てはまらない** —— corpus は**空白を除いて +6,340 バイト**増える。
増えたぶんはコメント本文そのもので、向きは「消し足りない」側。

注入でも確かめた —— `app.md` に死んだ綴りを足し、`useURLParams.ts` にそれを説明する
`//` を足すと、原型は赤・この変異は緑。
**`docsIdentifiers` が `codeOf` を通す一番の目的が、この1箇所を外すだけで無効になる。**

既存のテストが `//` を見ているのは `open < 0` の枝と shell 側だけで、
**ブロックを持つファイル（現物のほぼ全て）が通る経路を誰も踏んでいなかった。**

- 結果: 対応済み `12946be3`（**この1本だけが落ちることを確認**）

### AZ-01 (MEDIUM, 1人が実測): 「7本」が、それを書いたコミット自身で8本になった

`d12de491` は「訂正した側のコミットも同じ綴りを持つので当たる」と書いているのに、
**その規則を自分自身に適用していなかった。**

「1本」を「7本」に直した所見なのに、直した瞬間に増える。
**この数は訂正を書くたびに増えるので、測定時点を添えても次の訂正で腐る。**

- 結果: 対応済み `2158da8e`（AT-01 / AX-03 と同じく**書くのをやめた**。
  主張の帰属は本数に依らない）

### AZ-05 (MEDIUM, 1人が実測): r3 / r4 のヘッダの深刻度内訳が、その報告書の見出しと合わない

- r3 —— 実測は BLOCK 1（T-01）＋「BLOCK の一部」1（T-02）／HIGH 2／MEDIUM 19。
  書いてあったのは HIGH 5 / MEDIUM 17
- r4 —— 実測は HIGH 2 / MEDIUM 8。書いてあったのは HIGH 3 / MEDIUM 7

**「Nラウンド続けて HIGH 0」の主張はこの欄を数えて作っている。**
合計は32ラウンド全部一致していて、**内訳が合わないのはこの2本だけ。**

- 結果: 対応済み `965c3ca8`（推移の列には影響しない）

### AZ-02 (MEDIUM, 1人が実測): 「5ラウンド連続」が非連続

r29 は「r23 / r24 に続いて3度目」、r30 は「r23 / r24 / r29 に続いて4度目」と
**非連続**で書いている。**r31 にはこの欄が無く**、AW-05 / AW-06 / AW-07 は
r30 の直しが触っていない既存コードへの所見。

**r29 の AU-01 がまさにこの取り違えを所見にしている。**

- 結果: 対応済み `9bc0c3e3`

### AZ-03 (MEDIUM, 1人が実測): issue の内訳が、1つの括弧に3つの数え方を混ぜている

`#386:1 / #342:4 / #290:5 / #351:1` —— 全コメントなら #342 は5・#290 は6、
このループのコメントなら #342 は3、所見の件数なら #386 は11・#290 は7。

**AX-03 で28本から件数を落とした、その同じ報告書の中で同型を作っていた。**

- 結果: 対応済み `9bc0c3e3`（同じく件数を落とした）

### AZ-04 (MEDIUM, 1人が実測): r32 の報告書コミットが、同じラウンド内で偽になったまま

`8bc004f2` のメッセージは「1件（AX-05）は次のラウンドへ持ち越す」。
その**18分後**の `f21b9916` が AX-05 を直し、「残した所見」の節ごと削除している。

**AV-03 が同じ形を所見にした直後のラウンドで、新たに1つ作っていた。**

- 結果: 対応済み `a0093ef6`（AW-02 / AX-05 と同じ扱いで結果欄に1行）

---

## 重複・矛盾した所見

- **8件とも1人ずつ**
- **BA-01 / BA-02 / BA-03 が、このループで最も重い型に達した** ——
  **前ラウンドで塞いだつもりの穴が、判定基準を変えたら大半開いていた。**
  r32 の AY-01 で「注入して答えが変わることを確かめたのは初めて」と書いたが、
  **その注入を1通りしか試していなかった**（当たらない綴り1つ）。
  今回は壊し方を4通り・両方の枝・566本側まで当てて、**3通り＋枝の全部が通ることが分かった**
- **BA-02 は AU-03 / AW-02 / AX-03 と同じ根**（instance を塞いで形の全体に広げない）。
  `.sh` の2本を塞いで、同じ穴の566本を見ていなかった。**4度目**
- **BA-03 は r30 の「尽きた」の6度目の取りこぼし。** ただし今回は
  **取りこぼしの原因が判定基準そのもの**だと分かっている ——
  r30 は「corpus が変わるか」で判定し、この変異は corpus が変わるのに見落とした
- **AZ-01 / AZ-02 / AZ-03 / AZ-05 は数え方**。8ラウンド連続。
  **AZ-01 は「訂正が自分自身を数に入れる」型で、このループで初めて**
- **AZ-04 は AV-03 / AW-02 / AX-05 と同じ根**（reword できないメッセージ）。4度目
- 所見の水増しは無かった。2人とも「確認して所見が無かったもの」を明記していて、とくに:
  - **AX-03 の落とし残しは無い。** 累積値を運んでいたのは r4〜r31 のちょうど28本で、
    28本すべてから落ちている。`.claude/reviews/` 全体の残りは
    各ラウンドが自分の送付数をその場で書いたもの
  - **推移の列は全32ラウンド一致した** —— ヘッダの件数と所見見出しの数を
    全ラウンド突き合わせ、`22 → 18 → 23 → … → 9` の各項が全て一致。
    **列そのものには腐りが無い**
  - **AX-03 の分析の数は全部再現した**（23件 / 19件 / 増分 r6 +2 / r13 +1 / r22 +2 / r31 +1）
  - **AX-05 の文は `42a050f2` の現物と一致**し、`origin/main` に無いことも記述どおり
  - **置き場の差（AW-02 は `.claude/reviews/`、AX-05 は `docs/`）には理由がある** ——
    前者は報告書の記述への訂正、後者は `8349177f` が書いた導線の着地点。
    `commentHistory.test.ts` の「`docs/` と `.claude/` は経緯を残す場所」とも衝突しない
  - **AX-01 の数はすべて再算して一致**（214 / 195 / 310 / 489 / 568 / 2）。
    **#351 の訂正コメントの表・比・結論も、いま報告書に書いてあることと完全に一致**
  - **AY-03 の3つの綴りは全て現物と合う**（`config_write_failed` は `docs/` に0件、
    `src/entities/file-tree/api/error.ts` に6箇所）
  - **`missingIn` の単一変異は尽きた**（r32 で10通り＋2重4通り）
  - **`stripShellComments` / `stripShellStrings` の9通りは、注入基準でも所見0**
  - **`053ecde0` の主張の片方（`commentsOf` を常に `SLASH_COMMENT` → tsc が TS6133）は再現した**

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送ったもの（再掲しない前提）
- `origin/main` が元から持っていた実装と doc の中身
- `docs/spec/screens/` の13面が現物の UI と合っているか
- `user_book1.db` の実測3行とピーク確保の実測
- 3重以上の変異
- `walk.ts` の変異（r31 が当てたきり）
- `docsIdentifiers.ts` の `IDENTIFIER` / `EXEMPT` の変異（**r30 / r31 / r32 / r33 とも未走査**）
- `HISTORY_WORDS` / `REVIEW_TAG` / `BRANCH_NAME` 各要素の変異
- `codeOf` を使わないラチェット（`asyncResultUse` / `escapeReceivers` / `scssScale` ほか）
- **変異の総数は互いに検算できない**（数え方に依存する量）。
  突き合わせたのは結果として書かれた数だけ
- `.claude/reviews/` の `book-to-main` 以外のシリーズが自分のラウンド内で書いた送付件数

## この PR の外で観測したこと

**ゲートが2度メインのチェックアウトを検証した**（#342）。
`zz_tmp_measure` / `zz_tmp_measure2` と `index_cache_guard_names` ——
**どれもこのワークツリーに存在しない。** 手元は緑で、待って通した。

## lint / hook で強制できるもの

- **BA-01 / BA-02 / BA-03 は3件とも、#351 に送った (d)（注入した既知の違反が
  検出され続けること）で落ちる。** 今回はそれを手で回して3件見つけた
- **(d) が要ることは、この3件で確定した** —— BA-01 と BA-02 は
  **corpus も答えも歩いた本数も変わらない。**
  `toBeGreaterThan(<定数>)` の走査や corpus 長の突き合わせでは取りこぼす
- **床は「0 と比べる」形にしない。** BA-01 は 0 と比べていたので、
  当たる文字が1つ残るだけで通った。**実測から取ること**（#351 の AW-07 と同じ判断）
- **AZ-05 の型（ヘッダの深刻度内訳）は機械で数え直せる** ——
  `### <ID> (<深刻度>` を数えてヘッダと突き合わせる。
  **32ラウンド中2件なので、人の目では30回素通りしている**
- **AZ-01 の型は、数え直す時点を「そのコミットの親」に取らないと同じずれが出る**
- AZ-02 / AZ-03 / AZ-04（連続回数の言い換え・混ざった数え方・
  報告書コミットが同ラウンド内で偽になる）は機械で防げない

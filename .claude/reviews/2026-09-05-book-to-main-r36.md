# レビュー book-to-main ラウンド36

- 日付: 2026-09-05
- 範囲: r35 の所見11件を直した5コミット＋報告書（`git diff d0e24021 5e153f82`）
- 走らせた reviewer: architecture / oss-hygiene
- 対象コミット: `5e153f82`
  （レビューが読んだのは amend 前の `e7ee403f`。差は r35 の「3つ誤っていた」→「5つ」の1文）
- 前ラウンド: `.claude/reviews/2026-09-05-book-to-main-r35.md`

所見 **8件**（BLOCK 0 / HIGH 0 / MEDIUM 8）。
推移は 22 → … → 8 → 6 → 11 → **8**。

**12ラウンド続けて HIGH 0。**

**このラウンドで `commentHistory` の検出穴が3つ出た。**
`REVIEW_TAG` は採番の主流（476種）を落とし、`BRANCH_NAME` は**この PR 自身の枝**を
落とし、本文を切り詰める変異は3つの床を全部越えていた。

---

## 所見

### BG-01 (MEDIUM, 1人が実測): `REVIEW_TAG` が採番の主流を落とす

`.claude/reviews/` の見出しの ID は **938種・9形**。最も多いのは
**`R14-C1` 形（476種）**で、`R2-B1` / `R10-01` / `K1-10` を含む。
**いまの正規表現はこれを1つも拾わない。**

`game-engine` の `R17-M20` は **2026-09-03 付＝現行の採番。**
**いま最も出やすい綴りが素通りしていた。**

**この形は台帳と綴りが割れる**（台帳は `F-\d+`）。走査対象の全コメントに当てて**誤検出0**。

- 結果: 対応済み `57b35e39`。注入して確認 ——
  `(R17-M20)` / `(R14-C1)` / `(R10-01)` / `(AA-01)` 赤、`(F-27)` / `(N-1)` 緑

### BG-02 (MEDIUM, 1人が実測): 本文を切り詰める変異が3つの床を全部越える

**コメントが「これをこそ止める」と書いている変異が、素通りしていた。**

`[^\n]*` を `[^\n]` に落とすと `line 10704`、`#[^\n]{0,20}` で `hash 196` ——
どちらも床を全部越え、**注入した違反も出ない。**
ガードが `text.length > 2` なので `//x` も `#ab` も通る。

**本文が数文字に切られると、`HISTORY_WORDS` も `REVIEW_TAG` も `BRANCH_NAME` も
何も拾えない。** 被害は BD-02 が「床では取れない」と書いた2族より大きい ——
**`//` 10,890本と `#` 194本の中身が全部**読めなくなる。

- 結果: 対応済み `023ed212`（**文字数の床を対で置いた**。
  切り詰め族は本数をほとんど変えずに文字数を1桁落とすので、対で見れば取れる。
  実測 `7709 / 367790 / 110476` に対して床 `6900 / 330000 / 99000`）

### BG-04 (MEDIUM, 1人が実測): `BRANCH_NAME` が、この PR 自身の枝を落とす

現存する31本のうち19本が落ちる。**その中に `feature/book` が入っている。**

- 数字の前に英字が入る枝（現存2本）—— `\/\d` を外れる
- `archive/` 配下の issue 枝 —— 数字の後に `/` を要求するので当たらない
- **`test` が列挙から落ちていた** —— `CLAUDE.md` の規約に入っているのに。
  **このラウンドのコミットは全て `test:`**

- 結果: 対応済み `4dd5f66b`（注入して確認 —— 現存ブランチ3本とも赤、
  `docs/` のパスは緑）

### BG-03 (MEDIUM, 1人が実測): `block` の床の余裕が 4.8% で、同じコミットの規約に反する

`962b9b11` は「床は実測から1割ほど下に置く」と書いたが、
満たしているのは `hash`（9.8%）だけ。`line` 8.2% / **`block` 4.8%。**

`src/entities/game/` を消すだけで **650** ちょうどになって赤くなる。
`session.rs` 1本で `line` も 9609 で割る。
**どの場合も読み取りは1文字も壊れていない** —— BD-01 が所見にした
「原因を誤って指す赤」が `block` / `line` に残っていた。

- 結果: 対応済み `023ed212`（`line` 9800 / `block` 620 へ。
  下げても床が取る変異は全部取れることを実測）

### BF-02 (MEDIUM, 1人が実測): r1 の件数を直した先で「前ラウンド（19件）」を4箇所残した

`7556afbe` が r1 のヘッダを 19 → 21 にしたが、**同じコミットが触った3ファイルに
同じ数を運ぶ場所が4つ残っていた。** 書き換え前は全部 19 で整合していたので、
**この直しが矛盾を作った。**

- 結果: 対応済み `1bc33b00`

### BF-03 (MEDIUM, 1人が実測): 宣言した単位が、9本のうち2本を説明できない

「見出しの ID ＋ 表の行の ID」は r4（表6行だが `E-04` が2行で ID は5）と
r7（`H-01` が2つの見出しに出る）で外れる。**行で数えると r4 は13、r7 は17。**

**BE-01 は「検査を全体へ広げなかった」ことを所見にした回で、
その直しが検査の単位そのものを取り違えて宣言していた。**
この宣言を読んで機械化した人は行数で数える実装を書き、正しい r4 / r7 が赤くなる。

同じずれが「book-to-main は34本すべてヘッダの件数＝見出しの数」にもある ——
見出しの数と割れる回が5本あり、合うのは ID の異なり数。

- 結果: 対応済み `1bc33b00`（`yaneuraou-db` 側）/ `dad17fe7`（報告書側）。
  `E-03` の欠番と `E-04` の重複も併記（`G-03` を r9 に書いたのと同じ扱い）

### BF-01 (MEDIUM, 1人が実測): 「228件」が実測229件で、同じラウンドの3箇所が2通りになった

r34 / r35 / `962b9b11` のメッセージが同じ量を **228 と 229** で書いていた。
実測はどの木でも **229**。

「6,808件は字下げされた行頭コメント」も不正確 —— 6,808 のうち6,498件が欄の移動で、
**残る229件が本当に落ちる分**。**全部が移動なら落ちる件数は0**になるので、
元の書き方は2文で自分と矛盾していた。

**BD-02 は「実測せずに書いた数が30倍ずれていた」を直す所見だった。
その直しが同じラウンド内で2通りの数を残した。**

- 結果: 対応済み `dad17fe7`

### BF-04 (MEDIUM, 1人が実測): 「11件のうち3件は2人が別々に到達した」が、3組＝6件

この行は「独立に同じ結論へ着いた割合」を伝えるのが役目で、
**自分でその割合を半分に見せていた。**

- 結果: 対応済み `dad17fe7`

---

## 重複・矛盾した所見

- **8件とも1人ずつ**
- **BG-01 / BG-02 / BG-04 が、このループで初めて「検査の網そのもの」を3つ同時に開けた。**
  BD-05 → BE-05 → BG-01 と**3ラウンド続けて `REVIEW_TAG` の被覆が足りない** ——
  1文字を落とし、`X-YN` を足し、今回さらに数字入り接頭辞（最多の476種）
- **BG-02 は BA-01 / BC-01 と同じ根**（床が守ると謳う族を守れていない）。3度目。
  **今回は「床では取れない」ではなく「床の設計が量を1つしか見ていない」** ——
  本数と文字数の対で初めて取れる
- **BG-04 はこのループで初めて、検査が自分の PR の枝を見逃していた**
- **BG-03 は BD-01 の落とし残し。** `hash` を緩めたが `block` / `line` は放置していた ——
  AU-03 / BB-01 / BE-01 と同じ「instance だけ塞ぐ」形の**5度目**
- **BF-02 / BF-03 は BE-01 の直しが作った。**
  件数を直した先の参照を残し、単位の宣言を取り違えた
- **BF-01 / BF-04 は数え方**。10ラウンド連続
- **書きかけの doc で自分を2度落とした** —— `commentHistory` は自分自身を走査するので、
  止めたい綴りを散文に書くと自分が違反になる。冒頭の doc がそう書いているとおり
- 所見の水増しは無かった。2人とも「確認して所見が無かったもの」を明記していて、とくに:
  - **`175` は上下とも妥当** —— `hash` の変異15通りを当て、
    **169 と 194 の間に落ちるのは 192 / 193 / 194 の3つだけ。**
    これらを取れる床は実測以上になるので存在しない
  - **書き直したコメントの4つの主張は全部実測と合う**（62.6%減 / 2.10% / 169 / 195）
  - **`7556afbe` が直した3つの値は独立に数え直して一致**（r1=21 / r2=9 / r4=12、内訳も）
  - **r3 の内訳を落とした判断は r6 と整合する** —— r3 は解が**0通り**
    （`BLOCK 2 / HIGH 6` はどちらも成り立たない）、r6 は**1通り**
  - **BE-02 / BE-03 / BE-06 の数は全部再現**（12本 / 6コミット / 263 と 128）
  - **`8372e601` → `a5da6559` の2段階は記述どおり** ——
    `F-\d+` はコメントから**25箇所**が引き、全て全角括弧か裸。半角は0件
  - **r35 の 194 / 169 / 229 / 2.1% / 42種 / 6,498 / 683→7283 は全部合う。**
    基点 `4703515f` で測ると完全一致し、木が進んで動くのは
    走査対象のファイル自身を触っているため
  - **`HISTORY_WORDS` の26語はどれも load-bearing**（1語落とすと網が縮む）。
    語どうしの包含関係で死語になる組は1つも無い
  - **`SLASH_COMMENT` の文字列リテラル誤検出は現時点で0** ——
    一致11,708件のうち64件が文字列の内側で始まるが、違反語を含まない。
    doc が限界を明記している
  - **いまの誤検出は0件**（568ファイル・一致11,708件に3つの網を当てて offenders 0）

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送ったもの（再掲しない前提）
- `origin/main` が元から持っていた60本と、実装・doc の中身
- `yaneuraou-db` r1〜r9 と `book-foundation` 16本の**本文の主張**（数え方だけを見た）
- `book-to-main` 34本の内訳（BLOCK/HIGH/MEDIUM の割り付け）の全数検証
- 3重以上の変異、`walk.ts` の `sourceFiles` / `ROOTS` に対する変異
- `codeOf` を使う他のラチェット（`docsIdentifiers` / `asyncResultUse` /
  `escapeReceivers` / `scssScale` ほか）
- `docs/spec/screens/` の13面が現物の UI と合っているか
- `user_book1.db` の実測3行とピーク確保の実測
- Rust 側（このラウンドの差分に `.rs` は無い）

## lint / hook で強制できるもの

- **BG-01 / BG-04 は同じ形で機械化できる** —— 採番の綴り（またはブランチ名）を
  集めて網に食わせ、**1件も当たらない形があれば落とす。**
  台帳やパスと衝突する形は除外リストに明示する。
  **3ラウンド続けて手で見つけているので、人の注意では止まっていない**
- **BG-02 は機械化できる** —— 本数の床の隣に文字数の床を置けば、
  切り詰め族は本数を保ったまま文字数を1桁落とすので必ず赤くなる。
  **このラウンドでそれを入れた**
- **BG-03 / BD-01 は `EXPECTED_MIN` / `DRIFT_ALLOWANCE` と同じ形** ——
  床に上限を対にして `床 < 実測 <= 床 + 幅` を assert すれば、
  余裕が規約から外れた時点で「床を動かせ」で落ちる。
  **3本の欄で率がばらばらになることも起きない**
- **BF-02 の型（「前ラウンド」行の件数）は機械で照合できる** ——
  指されたファイルのヘッダの数と突き合わせる。
  `book-foundation` は既にこの形を守っているので、規則の新設ではなく既存実態の固定
- **表の行に振られた ID の重複と欠番は連番として読めば出る。**
  件数の照合を書くなら、単位を ID の異なり数に決めた上で、
  **重複と欠番を別の赤として出さないと注釈が単位の取り違えを隠す**
- BF-01（同じ量を報告書・メッセージ・コード内コメントの3箇所に書く）と
  BF-03 / BF-04 は機械で防げない

# レビュー play-view

- 日付: 2026-09-14
- 範囲: `feat/game-view-dock-tab`（`main` = 757ec6d4 からの差分）。対局の進行を持つ層とドックの対局タブ
- 対象コミット: `eda5c010`（実装）→ `be9ecf2e` `3437ee3b` `3161635a`（レビューの反映）
- 走らせた reviewer: architecture / comment / react / robustness / ui / perf / oss-hygiene の7本（1巡）

## 入れた機械

### `viMockShape`（`src/__tests__/viMockShape.test.ts`）

`vi.mock` の第2引数は `unknown` 扱いなので tsc が素通しする。実物が export を増やしても
改名しても、ファクトリは古い形のまま緑で残る。`satisfies typeof import("…")` を要求する件数ラチェット。

**入れた回に1件捕まえた** —— 対局の差し替えが実物9本のうち6本しか並べていなかった。

変異2件で落ちることを確認: 印を1つ外す（180→181 で赤）／走査を空にする（下限で赤）。

`.claude/knowledge/mechanization-backlog.md` に回数1で載っていた項目の2件目。**入れたので表から外した。**

### `safeCentering`（`src/__tests__/safeCentering.test.ts`）

スクロールを受け持つ箱が中身を `safe` なしで中央に寄せると、入りきらないぶんが始端側へ
あふれてスクロールで届かなくなる。同じ規則が `AppLayout.scss` と `ErrorFallbackBody.scss` に
**理由つきで2回**書かれていて、3回目にこの PR が破った。

SCSS の入れ子は「**同じ箱を指す修飾子**（`&--x` / `&:hover` / `@media`）」だけ親の宣言を連れる。
子孫まで連れると、スクロールする箱の中の行を縦中央に寄せているだけの宣言まで数えて
**誤検知が本体より大きくなる**（実測 14件 → 絞って3件）。**全件が実害とは限らない**ので件数ラチェット。

変異2件で落ちることを確認: `safe` を1つ外す（3→4 で赤）／走査を空にする（下限で赤）。

### 既存の機械が自分で捕まえたもの

新しく足さずに済んだぶん。**どれも人の注意では止まらなかった。**

- `asyncResultUse` —— `close` を `AsyncResult` にした結果、同名の別関数（updater の `close`）の
  投げっぱなしを拾った。走査が名前で引く以上これは避けられないので、こちらを `closeSession` に改名した
- `ratchetIndex` —— 新しいラチェット2本を `CONTRIBUTING.md` の表に書かせた
- `sourceText` —— 自前のコメント除去を持つ検査を禁じ、`codeOf` に寄せさせた
- `contrastRatchet` / `scssScaleRatchet` —— 実装時に4件（`$color-danger-text` の比・存在しない
  `$color-accent`・コメントが指す doc の不在・knip）

### 基準を詰めたもの

- `barrelConsumers` 67 → 45（呼び出し元の無い公開名を3つ外した）
- `knip-ratchet` 175 → 163

## 直した所見

`be9ecf2e` —— 対局の裁定と後始末が黙って落ちる経路（6件）

- 判定（注入された `judge`）が投げると裁定が1つも返らず、30秒後に対局が死ぬ。try の中へ入れ、
  投げたら終局として畳む（robustness が実測で再現）
- `rulingFailure` を消す行がどこにも無く、直った対局に警告が残り続け、逆に終局では欄ごと消えていた
- `start` の `await` 後の照合が `!== null` で、待つ間に別の対局が始まると結果をそちらへ書き込んでいた
- `close` / `resign` / `abort` の断りを `void` が捨てていた。`AsyncResult` にして操作列が返す。
  `close` は落ちるまで対局を手放さない（先に捨てると `gameId` が消えて呼び直せない）
- 購読の可否を render 中に ref へ写していて、旗が1描画ぶん遅れていた
- 始め損ねた対局から `start` が黙って断っていた
- 購読の effect が `apply` に依存していて、依存が動いた回に張り直す窓があった
- SCSS: `safe` なし中央寄せ・帯の折り返し・時計の桁揃え・行長を 46ch → 60ch

`3161635a` —— 画面ができて嘘になった doc（6本）

`features/game-play.md`（4箇所）/ `state-transitions/game-session.md`（3箇所）/
`state-transitions/README.md` / ADR-0010 の `config.rs` / `RuntimeProviders` と `IDEAS.md` の
「2つ」/ `app-layout.md` の「3つ」。`screens/play-view.md` は spec の節の順に揃え直した
（**「失敗の見せ方」が無かったのがいちばん重い** —— 同じ PR で境界の表に行を足したのに、
折り返す先がこちらに無かった）。

### テストの穴を塞いだ

comment / react / robustness の3人が**独立に同じものを挙げ、全員が変異で実証した** ——
「裁定を返せなかったら黙らない」テストが、黙っても緑になっていた（`rulingFailure` を
どのアサーションからも観測していなかった）。欄を描かせて観測するようにし、
5本足して14本にした。`starter?.()` の空振りで緑になる形も潰した。

追加した変異検査（全部落ちることを確認）: 裁定の文言を落とす／`turnChanged` で消さない／
`close` の前に対局を手放す／溜めたイベントを捨てる／裁定を返さない。

## 送ったもの

**行き先がまだ決まっていない。** 下の2件は「直し方に判断が要る」ので、
`/implement` 手順7 に従ってユーザーに選ばせる（issue はまだ立てていない）。

1. **裁定が毎手 棋譜を根から組み直す。** perf が実測: 400手で1局あたり 624ms、
   1000手で 3892ms、うち8〜9割が組み直し。持ち回る形にすると 15ms / 43ms（40〜90倍）。
   `gameOutcome.ts` の doc が「対局セッションの間だけ判定器を持ち回る形にすること」と
   名指ししているが、そうなっていない。**機械の案も出ている** ——
   壁時計ではなく `ShogiRecord.prototype.append` の回数で見るラチェット
   （現状 N=100 で 5050 回、持ち回れば 100 回）。既存の `outcomeCost.test.ts` の
   「400手で20秒以内」は実測 321ms で60倍の余裕があり、**この後退を止める役に立っていない**
2. **裁定器の実装（50行）が `app/` に居る。** `GameSessionBridge` の中に、終局の綴りと
   「判定が落ちたら終局として畳む」という対局の生死を決める分岐がある。`app/` は最上層なので
   **誰も import できず、単体テストの置き場も無い**（実物の裁定器を踏むテストが1本も無い）。
   前例に挙げた `AnalysisBridge` は中身を `features/` に置いていて、形が揃っていない

`play-view.md` の「いま満たしていないこと」には、上の2つを含む12件を書いた。

## 機械にできなかったもの

`.claude/knowledge/mechanization-backlog.md` を更新済み。

- **「棋譜が入れ替わったか」の合図を単独の値で決める** —— 回数を2 → **3** に上げた。
  `PlayView` の「別の棋譜の対局か」がパス単独なので、対局中に改名すると誤検知する。
  **#538 の設計判断待ちなので走査は入れない**（どちらが正しい合図か決まる前に owners を固定すると、
  誤った側を固定する）
- **帯の段を `InlineNotice` に寄せるか** —— `design-language.md` が「空状態・エラーをどこに出すか」を
  未決の論点として挙げているので、その場で決めない
- **ドックの空状態の寄せが4箇所と違う**（既存は左寄せ・`$font-aux`、こちらは中央・`$font-lead`）
  —— 同上
- **終局の理由に Rust の英文がそのまま出る** —— 対応表の網羅は走査で見られない
- **`spec/screens/*.md` の `##` が README の8節の部分列か** —— 機械化できるが、
  既存に違反がある（`analysis-pane.md` ほか）ので件数を数えるところから

## 見ていない範囲

- Rust（`src-tauri/`）。このブランチは1行も触っていない
- 実プロセス（実エンジン）を立てた経路。この repo に固定された経路が無く、今回も動かしていない
- 実ブラウザでの描画。ui の寸法の主張は SCSS とトークンから導いたもので、実測ではない
- perf が測ったのは隔離コピーの本機実測で、`gameOutcome.ts` の doc が値を取った台とは別
- 対局を始める画面・人の着手・棋譜への書き戻し（**この PR の範囲外**）

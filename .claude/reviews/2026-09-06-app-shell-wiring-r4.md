# レビュー app-shell-wiring ラウンド4

- 日付: 2026-09-06
- 範囲: `git diff origin/main...HEAD`
- 走らせた reviewer: architecture / react / robustness / comment / perf / oss-hygiene（6）
- 対象コミット: `ad625ef9`（当時。その後 `f6cdf57d`（通知の土台）へ rebase）
- 前ラウンド: [r1](2026-09-06-app-shell-wiring-r1.md)（29件）/
  [r2](2026-09-06-app-shell-wiring-r2.md)（20件）/ [r3](2026-09-06-app-shell-wiring-r3.md)（17件）
- **perf は所見なし。** r2 の数を全部数え直し、`navigate` の回数が「棋譜 k 本で k 回」から
  「トグルを押した回数ぶんだけ」に減ったことを確認している

> **このラウンドの所見は、ほぼ全部が r3 で自分が入れた誤り。**
> 足したラチェットが空回りしていた（r4-01）、足した凡例が守れない約束をしていた（r4-02）、
> 割った記号を間違えた（r4-03）、直した spec の断言がまだ反例を含んでいた（r4-05）。

## 所見

### [HIGH] r4-01 r3 で足した走査は、`✓` を使う5表のうち1表しか読んでいない。緑は「守られている」ではなく「見ていない」

- 場所: `src/__tests__/stateTransitionCells.ts`、`.test.ts`、`CONTRIBUTING.md`
- reviewer: **architecture / comment / oss-hygiene / react（4人が独立に到達。うち3人が実測）**
- 根拠: 行を拾う綴りが `board-orientation.md` の書き方（`**B0**` 形）に固定されていた。

  | 表                        | 門を通るか | 拾えた行 |
  | ------------------------- | ---------- | -------- |
  | `board-orientation.md`    | 通る       | **4**    |
  | `game.md`                 | 通る       | **0**    |
  | `game-session.md`         | 通る       | **0**    |
  | `position-search-view.md` | 通る       | **0**    |
  | `branch-index.md`         | 通らない   | —        |

  素通りの機構は3つとも別——`game.md` / `game-session.md` は行が事象（`**E1** \`loadGame\``）、
`position-search-view.md`は行ラベルが`**L-idle**`、`branch-index.md`は`## 表`という
見出しを持たない。さらに`position-search-view.md`の節見出しは`## 埋まっていないセル（テスト項目）`で、門は`includes`（部分一致）で通るのに
`sectionOf` は完全一致なので pending が常に空になる。

- **変異でも確かめられた**（reviewer が複製で実験）——`board-orientation.md` の行ラベルを
  1語変えるだけで4表とも0行になり、テストは `toEqual([])` なので緑のまま通る。
- なぜ問題か: この repo は同じディレクトリで「空回りする検査は無いより悪い」を明文化し、
  非空振りの表明を4本（`docsSourcePaths` / `sliceBarrels` / `testsLayerBoundary` /
  `ratchetIndex`）が持っている。**新設のこれだけが持っていなかった。**
  doc と `CONTRIBUTING.md` は「`✓` を使っている表だけを見る」と書いていて、
  使っている表は全部見ていると読める。

### [HIGH] r4-02 r3 で足した凡例の約束が、同じファイルの19セルで偽

- 場所: `docs/state-transitions/board-orientation.md` の凡例と表本体、「埋まっていないセル」
- reviewer: **architecture / comment / oss-hygiene / robustness（4人）**
- 根拠: 凡例は「**`—` で ✓ が無いセルは未検証**（「埋まっていないセル」に挙がっているはず）」。
  `—` かつ ✓ 無しは19セルあり（B0: E4/E5/E8/E9、B1: E4/E5/E6/E8/E9、B2: E6/E8、B3: E2〜E9）、
  一覧に載っているのは `(B2, E7)` の1件だけで、それは `—` ではない。
  機械（r4-01）は `—` を対象外にしているので、**この約束は誰も見ていない。**
- なぜ問題か: r3-05 は「その差が判断なのか漏れなのか判定できない」を直すために記号を割った。
  割った結果、「判定できない」が「断言が偽」に変わっただけになっている。
  とくに E8 の列は4セルとも `—` で ✓ が無く、同じファイルが「比較を落とすと E8 のたびに
  `pov` が消える」と退行の筋道まで書いているのに、テストも一覧の行も無い。

### [MEDIUM] r4-03 B0 の行で E2 / E6 / E7 を `×`（起きない）にしたのは誤り

- 場所: `docs/state-transitions/board-orientation.md` の B0 の行と状態の判定
- reviewer: robustness
- 根拠: B0 の判定は `loadedAbsPath` しか見ていない。同じ行の E9 が `—` である以上、
  **B0 は `activeKifuPath` が非 null のまま到達できる**（開いたが盤に載せられなかった後）。
  その B0 では削除も再クリックも改名も踏める。
- なぜ問題か: `docs/spec/screens/app-layout.md` が**利用者に起きることとして名指しで書いている**
  「もう一度クリックしても何も起きない」が、まさにその B0 の E6。
  同じ PR の2つの doc が同じ事象について「起きる」と「起きない」を言っていた。

### [MEDIUM] r4-04 空欄のセルは走査を無言で通るのに、手順書は「空欄を残せ」と言っている

- 場所: `src/__tests__/stateTransitionCells.ts`、`docs/state-transitions/README.md`、
  `.claude/skills/state-transition-table/SKILL.md`
- reviewer: oss-hygiene
- 根拠: 走査は空文字のセルを飛ばす。README は「空のセルを未検証の経路として残す」、
  SKILL も「**空のセルが、そのまま未検証の経路。**」と書く（同じ SKILL の手順4は
  「空欄のまま残さない」で、この2行は元から矛盾していた）。
- なぜ問題か: 索引から入って手順どおりに書いた人は、**2つの逃げ道のどちらも通らずに緑になる。**

### [MEDIUM] r4-05 spec の「失敗の見せ方」の1行目が、同じ節の8行下と横断台帳の両方に否定されている

- 場所: `docs/spec/screens/app-layout.md`
- reviewer: comment / robustness（2人）
- 根拠: r3-13 で「シェル自体は失敗しない」を「レンダ例外ではシェルは畳まれない」に限定したが、
  8行下が「境界が無い場所で throw すると `/app` が畳まれる」で反例になっていた。
  しかも実際は `/app` では済まない——`AppErrorBoundary` は `AppLayout` の2箇所だけで
  上に1つも無いので **root ごと unmount** し、ウィンドウ枠も自前（`decorations: false`）なので
  閉じるボタンもドラッグ領域も消える。横断台帳は正しく「root ごと unmount する」と書いていた。

### [MEDIUM] r4-06 E16 で入れ替わるものの列挙が、ヘッダの対局者名を落としている

- 場所: `docs/spec/screens/app-layout.md`、`src/widgets/app-layout-header/lib/useHeaderCenterInfo.ts`
- reviewer: comment
- 根拠: `useHeaderCenterInfo` も `useFileTree().jkfData` から対局者名を作る。入れ替わるのは
  「ファイル名と盤の対局者名」の2つではなく3つ。
- なぜ問題か: 「盤の対局者名だけを game に寄せれば直る」と読める。片方だけ寄せると
  ヘッダの中で名前とファイル名の出どころが割れる。

### [MEDIUM] r4-07 `(B2, E7)` に振った #433 は、既存の #262 と同じ欠陥

- 場所: `docs/state-transitions/board-orientation.md`、`docs/spec/screens/file-tree.md`
- reviewer: oss-hygiene
- 根拠: `file-tree.md` は変更前から「開いている棋譜をリネーム／移動すると編集が巻き戻り、
  次の保存で消える → #262」と書いていた。`gh issue view 262` の本文は #433 と**同じ5段**。
- なぜ問題か: 片方が直されると、もう片方を指している行が解決済みの欠陥を未解決として指し続ける。
  **r1-05 では既存 issue を検索して重複を避けたのに、r2-01 では同じ確認をしていない。**

### [MEDIUM] r4-08 `hasBadges` / `loaded` を落とした跡に、常に真の分岐が2つ残っている

- 場所: `src/widgets/app-layout-header/lib/useHeaderCenterInfo.ts`
- reviewer: architecture / comment / react（3人）
- 根拠: `playersTooltip` の `!hasKifu` 側は真の枝の中でしか読まれず、`tooltip` の内側の
  `hasKifu ?` も外側が真の枝。r3-12 以前は `hasFile` と `loaded` で**別の条件だった**ものが、
  両方 `hasKifu` になって恒真化した。
- なぜ問題か: **r3-12 の狙いそのものを裏切る形が残っている**——「ツールチップにはバッジ抜きの
  形がある」と読めるので、バッジだけ条件を割りたい人がここを起点にする。

### [MEDIUM] r4-09 `e7b5ac43` が足した `catch` の `cancelled` ガードは、どこからも踏めない

- 場所: `src/entities/search/model/provider.tsx`、`src/main.tsx`
- reviewer: react
- 根拠: `main.tsx` に `StrictMode` は無く、購読 effect の依存は `[]` なので cleanup が走るのは
  アンマウント時だけ。**ガードを消してもテストは全部緑。**
- なぜ問題か: r2-08 → r3-03 と2ラウンド続けて同じ3行を反対向きに触っていて、
  **どちらの向きに壊しても検証が緑になる**領域が残っている。
  effect が名乗る「StrictMode-safe」を確かめる手段が無い。

### [MEDIUM] r4-10 同じ「なぜ」が5箇所にあり、同じ差分の `SearchRootGate` では逆の判断をしている

- 場所: `BoardOrientationBridge.tsx` / `useResetOrientationOnKifuChange.ts` /
  `board-orientation.md`（2箇所）/ `gameBoardOrientation.test.tsx`
- reviewer: comment
- 根拠: 「盤の中に置くと棋譜を閉じたとき落とす者が居なくなる」が5回、うち4回は
  `?pov=gote` の綴りまで同じ。一方 `SearchRootGate` はコメント0行で理由は provider に1箇所。
  **同じ規則（r1-17）を片方には適用し、片方には適用していない。**
- 併せて量の指摘: 新設3ファイルのコメント行/コード行が 1.20〜1.40 で、
  `CONTRIBUTING.md` の「コメントがコードより多いファイルは、たいてい設計に問題があります」を超える。
  **疑うべきは表ではなく複製**——表を作り直しても、同じ文が4箇所に散っている状態は変わらない。

### [MEDIUM] r4-11 表が locus と名指しした `GameFileTreeBridge` に、その名指しを裏づけるものが1行も無い

- 場所: `src/app/providers/bridges/GameFileTreeBridge.tsx`
- reviewer: comment
- 根拠: コメント0行。依存配列に `activeKifuPath` が並んでいることからは
  「パスだけが変わった回にも古い `jkfData` を載せ直す」は読み取れない。
- なぜ問題か: この差分は #262 を issue へ送る判断をしたうえで、**その根拠を表の側にだけ厚く書いた。**
  この effect を触る人は表を読まないので、既知の欠陥を知らずに踏むか、知らずに直して表を腐らせる。

### [MEDIUM] r4-12 注の番号が、定義の並び順とも本文の出現順とも合っていない

- 場所: `docs/state-transitions/board-orientation.md`
- reviewer: oss-hygiene
- 根拠: 本文の出現順は ※6 → ※5 → ※1 → ※4 → ※2 → ※3。定義の並びは ※1 → ※2 → ※5 → ※4 → ※3。
  **昇順でも出現順でもない。** r3 で ※4 / ※5 を足したときの挿し込みが原因。

### [MEDIUM] r4-13 `app-layout.md` が「この失敗の台帳はここ」と送っているが、台帳にその行が無い

- 場所: `docs/spec/screens/app-layout.md`、`docs/state-transitions/failure-surfacing.md`
- reviewer: oss-hygiene
- 根拠: 台帳を `E16` / `buildPlayer` / `game_loaded` で検索してヒット0。
  §4 の抽出条件（state に載るが読み手0）には正面から当たる。
- なぜ問題か: リンクを踏んだ人は行が無いので「まだ棚卸しされていない失敗」と読み、
  **別の番号を振るか別の issue を立てる。** r2-14 が F-17 で潰したのと同じ形。

### [MEDIUM] r4-14 `docs/IDEAS.md` の「到達しない分岐」の節が、冒頭の運用ルールと食い違う

- 場所: `docs/IDEAS.md`、`docs/spec/screens/board.md`
- reviewer: oss-hygiene
- 根拠: IDEAS は「6週間以内に着手しないもの置き場」。だがこの項目は #434 の**前提**として
  書かれている（#434 は issue ＝6週間以内に着手する）。
  加えて `board.md` の嘘は2箇所（状態表の P0 と「失敗の見せ方」）あるのに片方しか名指ししていない。
- なぜ問題か: r3-14 は「SCSS の山だと届かない」を直したが、**届かない先が
  「6週間以内に着手しない置き場」に変わっただけ。**

### [MEDIUM] r4-15（差分外）`npm test` が `Errors` を出しながら exit 0 で終わる

- 場所: `src/entities/analysis/model/provider.tsx`
- reviewer: react
- 根拠: 解析のデバウンスタイマーを unmount で止める者が居らず、テスト環境の破棄後に発火して
  `window is not defined` を投げる。タイマーを張る effect は cleanup を返さず、
  もう1つの effect は4つの早期 return を踏んだ回に登録しない。
- なぜ問題か: `verify-gate.sh` は exit code しか見ないのでコミットは通る。
  **「テストが通ったので安全」と読める状態が harness の側にある。**

## 重複・矛盾した所見

- **r4-01 と r4-02 は4人ずつが独立に到達。** どちらも r3 の修正そのものへの指摘で、確度は最高
- **r4-01 / r4-02 / r4-03 / r4-04 は同じ根**——表と機械のどちらが何を要求するかが揃っていない。
  機械を直してから doc を揃える順でないと、どちらが正か決まらない
- **r4-05 と r4-06 は同じ節**。片方だけ直すと節の中で食い違う
- **r4-08 は r3-12 の、r4-09 は r3-03 の、直接の副作用**
- 矛盾は無い

## 見ていない範囲

- Rust 側は誰も読んでいない（この差分は `src-tauri/` を1行も触っていない）
- SCSS は4ラウンドとも1行も読んでいない。`docs/IDEAS.md` の SCSS 5件の**内容**は未確認
- `docs/state-transitions/` の他の表の本文（`app.md` / `game.md` / `file-tree.md` / `search.md`）
- 実機で E16・改名・購読の失敗を踏んでいない。すべてコードの読み
- reviewer は変異を当てていない（ファイルを書き換えないため）。ただし r4-01 だけは
  3人が走査本体を import して実測し、1人は複製に変異を当てている

## lint / hook で強制できるもの

- **r4-01 は走査自身の穴で、走査に1条件足せば機械が守る側に入る**——「門を通ったのに
  行または列が0」を失敗にする。**「見つけた件数が0」と「見たセルが0」を必ず区別すること**が、
  この repo で繰り返し確認された形
- **r4-04 の空欄も同じ走査で拾える**（`!cell` の飛ばしを外す）。ただし README と SKILL を
  先に直さないと、手順どおりに書いた人が赤くなる
- **r4-12（注の番号順）は落とせる。** `※N` の定義の並びと本文の初出順を突き合わせる走査は20行程度
- **「`src/__tests__` に検査を足したら非空振りの表明も置く」こと自体をラチェットにできる。**
  `toEqual([])` を持ちながら `toBeGreaterThan` を1つも持たないファイルを数える
- r4-05 / r4-06 / r4-07 / r4-08 / r4-10 / r4-11 / r4-14 は機械では防げない

## 修正計画（r4 → r5）

**このラウンドで全件を処理する。** r5 は回さない——ユーザーの判断（PR を出す）による。
残ラウンドが0でないことは PR 本文に明記する。

順序は「機械 → 表 → コード → doc」。機械（r4-01 / r4-04）を先に直さないと、
表の記号を直しても正しくなったかを確かめられない。

### このラウンドで直すもの

| 順  | 所見          | この直し方で壊しうるもの                                                            |
| --- | ------------- | ----------------------------------------------------------------------------------- |
| 1   | r4-01         | 走査を一般化すると、他の表が一斉に赤くなる。**掛ける先を名指しにして範囲を決める**  |
| 2   | r4-02 / r4-03 | 凡例を狭めると「未検証がどこにあるか」が読めなくなる。19セルの扱いを本文に明記する  |
| 3   | r4-07         | issue を統合すると、片方を指している doc が宙に浮く。4箇所とも付け替える            |
| 4   | r4-05 / r4-06 | 冒頭を限定すると境界の節の位置づけが変わる。台帳の綴りに寄せる                      |
| 5   | r4-08         | 三項を畳むと表示が変わりうる。`hasKifu` の3択が動かないことを確かめる               |
| 6   | r4-09         | ガードを踏むテストは StrictMode が要る。**両方向の変異で落ちること**を確かめる      |
| 7   | r4-10 / r4-11 | 複製を寄せると指す先を失う。橋の doc を本体にし、他は指すだけにする                 |
| 8   | r4-12         | 番号を振り直すと参照が全部ずれる。本文の参照も同時に置換する                        |
| 9   | r4-13         | 台帳に番号を足すと、既存の番号と衝突しうる（実際した。rebase 後に F-31 へ振り直し） |
| 10  | r4-14         | IDEAS から出すと行き先が無くなる。issue のコメントに積む                            |
| 11  | r4-04         | 空欄を落とすと、既存の表が赤くなりうる。掛けている表だけなので影響は無い            |

### 直さないもの

| 所見                  | 行き先    | 理由                                                                 |
| --------------------- | --------- | -------------------------------------------------------------------- |
| r4-15                 | **issue** | `entities/analysis` はこの差分が1行も触っていない。#437 として立てた |
| r4-01 の残り（他4表） | **issue** | 掛けると `position-search-view.md` で20セル落ちる（測定済み）。#435  |

## 修正の結果（r4）

| 所見  | 結果     | コミット                | 一行                                                                                     |
| ----- | -------- | ----------------------- | ---------------------------------------------------------------------------------------- |
| r4-01 | 対応済み | `aeaa4324`              | 表の形を選ばない読み方にし、見たセル数の下限を固定。掛ける先を名指しに（残りは #435）    |
| r4-02 | 対応済み | `aeaa4324`              | 凡例を機械が要求する範囲に揃え、19セルが誰にも見られていないことをそのまま書いた         |
| r4-03 | 対応済み | `aeaa4324`              | B0 の E2 / E6 / E7 を `—` に戻し、B0 が `activeKifuPath` 非 null でも成立する注を足した  |
| r4-04 | 対応済み | `11f78ae8`              | 走査が空欄を拾うようにし、README と SKILL の「空欄を残せ」を直した                       |
| r4-05 | 対応済み | `0c8a442e`              | 「守られているのは2箇所だけ、それ以外は root ごと unmount」に直し、#436 を立てた         |
| r4-06 | 対応済み | `0c8a442e`              | 入れ替わるものを3つ（ヘッダのファイル名・ヘッダの対局者名・盤の対局者名）に直した        |
| r4-07 | 対応済み | —                       | **#433 を #262 の重複として閉じ**、材料を #262 にコメントで移し、doc 4箇所を付け替えた   |
| r4-08 | 対応済み | `34de3c16`              | 恒真な三項を2つ畳んだ。表示は変わらない                                                  |
| r4-09 | 対応済み | `e65a70cb`              | 1回目を失敗・2回目を保留にして二度マウントする形にし、**両方向の変異で落ちる**ことを確認 |
| r4-10 | 対応済み | `a9751a42`              | 本体を橋の doc に置き、フック・表・テストは指すだけにした                                |
| r4-11 | 対応済み | `a9751a42`              | 載せ直しの前提と `TODO(#262)` を橋のコードに書いた                                       |
| r4-12 | 対応済み | `9df86004`              | 出現順に振り直し、定義もその順に並べた                                                   |
| r4-13 | 対応済み | `2711986a` / `94ed8e0c` | 台帳に採番。rebase で main の F-21 と衝突したので F-31 へ振り直した                      |
| r4-14 | 対応済み | `ec4c3520`              | 6週間ルールの例外だと明記し、`board.md` の2箇所を名指し。#434 にもコメント               |
| r4-15 | 見送り   | —                       | **issue #437**（`npm test` が Errors を出しつつ exit 0）。差分外                         |

### 自分が作った退行（隠さずに書く）

- **r3 で足した走査が空回りしていた**（r4-01）。「機械に渡した」と r3 の報告書に書いたが、
  渡っていたのは5表のうち1表だけ。しかも**この repo が4本で守っている非空振りの表明を
  自分だけ書いていなかった**
- **r3 で足した凡例が、同じファイルの19セルで守れない約束をしていた**（r4-02）
- **r3 で割った記号を1行間違えた**（r4-03）。B0 の E2 / E6 / E7 は起きる
- **#433 を重複で立てた**（r4-07）。r1-05 では検索して避けたのに、r2-01 では確認していない
- **r3-12 / r3-03 の修正が、それぞれ恒真な分岐と踏めないガードを残した**（r4-08 / r4-09）

### 計画からの逸脱

- **1所見1コミットを4回破った**（`aeaa4324` = r4-01+02+03、`0c8a442e` = r4-05+06、
  `a9751a42` = r4-10+11、`34de3c16` は単独）。`aeaa4324` は分けるつもりが `git add -A` で
  まとめて拾ったもので、コミットメッセージを amend して事実に合わせた
- **r5 を回していない。** `/implement` は所見0のラウンドが出るまで終わらないと書いているが、
  ユーザーの判断で PR へ進んだ。残ラウンドが0でないことは PR 本文に書く

# レビュー game-engine ラウンド2

- 日付: 2026-09-02
- 範囲: r1 の修正8本（`4ed9a8e` 〜 `a3d4a9c`）と、それが触った `protocol.rs` / `session.rs` / `search.rs` / `clock.rs` / 表
- 走らせた reviewer: `rust` / `robustness` / `comment`
- 対象コミット: `07d7aaf`
- 前ラウンド: `.claude/reviews/2026-09-02-game-engine-r1.md`

**BLOCK 4件のうち3件は r1 の修正が作った退行、または直し切れていないもの。**

## 所見

### BLOCK

#### R2-B1 `restart_after_abort` を消費するだけで、`Aborted` 以外の枝が見なくなった（`6be0c53` の退行）

`rust`

`session.rs:534`、`:564-601`。r1 で早期 return をやめて `let restart = take(...)` に変えたが、**`restart` を見ているのは `Aborted` の枝だけ**。

`hand_turn_to` の `StopThenStart` は `cancel()` と `restart_after_abort = true` を置くだけで `activity` は `Busy { req: R }` のまま残るので、世代照合を通る。先読みが**自分から終わって** `bestmove` を返すと（詰みを見つけた／`USI_Ponder` 非対応のエンジンが `go ponder` を普通の `go` として `byoyomi` で打ち切る）、`run_loop` のキュー順が `Continue` → `Outcome` になったときに:

1. `accept_continue` → `hand_turn_to` で `restart_after_abort = true`、`phase = Thinking{Black}`
2. `Outcome { Move("2g2f") }` → `req` 一致 → `restart` は消されるだけ → `is_to_move(Black)` が true → `decide_move`

`2g2f` は「7g7f 3c3d の後」に対する答えで、**実際の局面のものではない**。`Resign` / `DeclareWin` も同じで、**起きていない局面での投了・入玉宣言がそのまま `G2` になる**。

`select!` は ready な枝からランダムに選ぶので、cancel が先に効くことに依存できない。

#### R2-B2 `spawn_search` が探索タスクへ **strong sender** を渡しているので、B-2 で切った輪が探索1本ごとに復活する

`robustness`

`session.rs:791-809`。`upgrade()` で作った strong を `forward` タスクへ move している。`forward` は `run_search` が戻るまで生きる。

`go ponder` は `ponderhit` / `stop` が来るまで `bestmove` を返さない。先読み中に `GameSession` が drop されると、cancel を持っているのは `run_loop` の中の `Runner` だけなので誰も止められず、**strong sender が生き残って `run_loop` が終わらない**。`tick_loop` の `upgrade()` も成功し続けるので、**誰も知らない対局の終局イベントがフロントへ流れる**。

r1 で足したテストは `two_humans` で**探索タスクが1本も無い**ので、この破れを踏まない。緑のまま輪が戻っていた。

#### R2-B3 EOF は拾えるようになったが、読み取り**エラー**ではフックが呼ばれない

`robustness`

`usi-0.6.2/src/process/engine.rs:191-206` のループは `Err(Error::IllegalSyntax)` だけを `continue` し、**それ以外の `Err` ではフックを通らずにスレッドを抜ける**。

`Err` になるのは2種類。どちらも `IllegalSyntax` ではない。

- `reader.rs:56` の `read_line` が返す `io::Error`。`String` への読み込みなので**非 UTF-8 が1バイト混ざれば `InvalidData`**
- `parser.rs:136` / `:161` の `cp.parse()?` / `ply.parse()?`。`i32` に収まらない値で `ParseIntError`

`listen` は `JoinHandle` を捨てているので、この `Err` は**どこにも記録されない**。listeners もクリアされないので `raw_rx.recv()` は永久に返らない。**B-1 の再現条件が「プロセスが死ぬ」から「1行が読めない」に変わっただけで、症状も無音さも同じ。**

#### R2-B4 表が、H-1 で直したはずの振る舞いをそのまま「残っている穴」として説明している

`comment`（`rust` / `robustness` も HIGH で同じ箇所を指摘）

`docs/state-transitions/game-session.md:156-159`（※8）と `:224`（埋まっていないセル）。

> 超えたら `warn` を出して `Aborted` として戻る。→ **穴**
> **`STOP_GRACE`（5秒）で `bestmove` が返らないと、探索中のエンジンへ `go` を出す。**

現物は `StopTimedOut` を返して `EngineFailure` で終局する。`go` は出さない。
`git diff 476c560..HEAD -- docs/state-transitions/game-session.md` の差分は「責任の切れ目」節だけで、**H-1 / H-2 / B-2 / B-3 の結果は1行も反映されていない。**

次の人は `:224` を根拠に H-1 を二度直そうとするか、逆に「表が正」と読んで `StopTimedOut` を `Aborted` に潰し戻す。潰し戻しても人間だけのテストは全部通る。

### HIGH

| #     | 所見                                                                                                                                                                                                                                                                                                      | reviewer                 |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ |
| R2-H1 | **EOF の `clear()` が転送タスクより前**（`protocol.rs:185` と `:161-167`）。`bestmove` を書いた直後に終了するエンジンでは、`line_tx.send` の後に `clear()` が走り、転送タスクが起きたときには listeners が空。**手を返したのに `EngineFailure` で負けにされる。** 起動時にも同じ形で `usiok` を取りこぼす | `rust`                   |
| R2-H2 | **`StopTimedOut` の側を `Idle` に戻してから `finish` するので、まだ探索中と断定したエンジンへ `gameover` を送る**（`session.rs:521` → `:821-836`）。不変条件3 のための仕掛けを、自分で足した経路で迂回している                                                                                            | `rust` / `robustness`    |
| R2-H3 | **`drained == Ok(false)`（＝エンジンが死んだ）が `StopTimedOut` に潰れる**（`search.rs:159-180`）。`detail` に `"engine did not stop searching in time"` が載るが実際は落ちている。第1相は同じ死因を `Failed("engine stopped responding")` と正しく呼ぶので、**相によって説明が変わる**                   | `rust` / `comment`       |
| R2-H4 | **EOF が `ensure_ready` の待ち手に届かない。** EOF で落ちるのは `listeners` だけで `ready` の watch は触られない。評価関数が無くて即死するエンジンで **`start_game` が 120 秒返らない**（`READY_TIMEOUT`）。メッセージも「readyok を時間内に返さなかった」で実際と違う                                    | `robustness`             |
| R2-H5 | **`EngineFailure` に落ちる経路が5本に増えたのに理由は1値のまま。** 区別できるのは英文の `detail` だけで、`types.rs:178` は `EngineFailure` の文言をフロントが持つ契約を書いていない。r1 の H-9 と同じ形が4倍に                                                                                            | `robustness`             |
| R2-H6 | **`restart_after_abort` の doc / 表が「先読みを打ち切った印」のまま。** H-2 の修正で `A1`（手番でない側の本番思考）でも立つようになった。ponder 専用と読んで判定を書き換えると、H-2 で塞いだ経路が黙って復活する                                                                                          | `comment`                |
| R2-H7 | **不変条件2 の根拠が、実際にそれを成り立たせている機構と違う。** 「リスナーが `go` ごとなので古いものは届かない」は偽（`run_search` は戻る直前に外すので、遅れた `bestmove` は次のリスナーが受け取る＝H-1 そのもの）。いま守っているのは `StopTimedOut` で終局する別の機構で、表のどこにも書かれていない  | `comment`                |
| R2-H8 | **表が r1 の修正を8箇所反映していない**（※8 / `:224` / E11 / ※2 の `Handover` 4分岐 / 不変条件1 / 不変条件3 / 1回の `go` の表 / E10 の発生源）。`StopTimedOut` に対応するイベント記号が無い                                                                                                               | `robustness` / `comment` |

### MEDIUM

| #      | 所見                                                                                                                                                                                                   | reviewer     |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------ |
| R2-M1  | B-2 のテストが見ているのは「捨てたら止まる」だけ。本番で `GameSession` が drop されるのは `close_game` だけなので、**終局しただけの対局では拍が回り続ける**（`manager.rs` が `Arc` を保持）            | `rust`       |
| R2-M2  | `spawn_search` に足した門番は cancel するだけで直後に `position` / `go` を送るので、線に出る順が `position` / `go` / `stop` になりうる。`Handover` の doc が「潰すな」と言っている状態そのもの         | `rust`       |
| R2-M3  | `close` が「止める → 待たない → 落とす」なので、**正常に閉じただけで毎回「stop に応じなかった」「gameover を送れなかった」の故障ログが出る。** 本物の故障と区別が付かなくなる                          | `robustness` |
| R2-M4  | `StopThenStart` の手番は、`turn_started` を更新してから最大5秒待って `go` を出すので、**エンジンが1手も読んでいない時間が持ち時間から引かれる。** `btime` には満額を載せるので GUI と食い違う          | `robustness` |
| R2-M5  | `ponderhit` の送信失敗が `warn` だけで、`kind` を `Search` に書き換える。エンジンは `go ponder` のままなので `bestmove` を返さず、**手番のエンジンが何も考えていない状態で時計が進む**                 | `robustness` |
| R2-M6  | `不変条件N` の参照が2箇所とも別の項を指す（`session.rs:256` は不変条件1 が正、`manager.rs:47` は不変条件5 が正）                                                                                       | `comment`    |
| R2-M7  | **`Aborted` が2つの型にあり意味が正反対**（`SearchOutcome::Aborted` は対局が続く／`GameOverReason::Aborted` は終局）。同じ関数の中で両方が出る                                                         | `comment`    |
| R2-M8  | `EngineClosed` の doc の「hook が `Err` を返したときにだけ」が偽（R2-B3）。`line_tx.send` 失敗の枝は `listeners` を clear しないので、待っている側に何も届かない非対称が型名で隠れる                   | `comment`    |
| R2-M9  | 落とした後のエンジンに `NotInitialized` を返している。`Engine not initialized: engine process has been shut down` という逆を指す文字列になる                                                           | `comment`    |
| R2-M10 | TS の doc の2つの断言が矛盾。「`rule` だけがフロント発」は `resign` / `aborted` を落としている。「`continueGame` しか返せない／終局に辿り着かない」も、投了・中断・時間切れ・エンジン異常の4経路がある | `comment`    |
| R2-M11 | `start_listening` が62行で説明ブロック3つ。`Err(EngineClosed)` を返す枝が2つできたのに片方だけ clear する非対称が、説明の中に埋もれた                                                                  | `comment`    |
| R2-M12 | `register_listener` に「**プロセス1つにつき1回しか読み取りを始められない**」が書かれていない（`usi` の `listen` が reader を `take` する）。B-1 の設計がこの制約に立っている                           | `comment`    |

## 重複・矛盾した所見

- **R2-B4 / R2-H8 / R2-H7 / R2-M6 は全部「表と実装の食い違い」**。3つの reviewer が別々の箇所を挙げた。まとめて1件として直す
- **R2-H2 は `rust` が MEDIUM、`robustness` が HIGH。** 不変条件3 を破る点を重く見て **HIGH** を採る
- **R2-H1 と R2-B3 は同じ1行（`clear()` の位置）から出ている。** 転送タスクの末尾へ移すと**両方消える**（`line_tx` が落ちるのは EOF でも読み取りエラーでも同じなので、Err の経路も拾えるようになる）
- **R2-H3 と R2-M8 の「非対称」は、R2-H1 の直しで片方が消える**

## 見ていない範囲

- **実機のエンジンを1つも起動していない。** 3つとも静的読解
- `npm run verify:rust` は誰も走らせていない
- `protocol.rs` の `pending_after_ready` の世代管理（r1 から2ラウンド持ち越し）
- `broadcast_to_listeners` が1行ごとに `HashMap` を clone するコスト（転送タスクが1本になって唯一の直列点になった）
- TS 側の受け口。`detail` の英文がどう描画されるか
- r1 から持ち越して未修正のもの（H-3 / H-5 / H-6 / H-9 / H-10 / H-11 / H-12 / M-1〜M-31 の大半）は再確認していない

## lint / hook で強制できるもの

| 何を                                                                       | どう                                                                                                                                            |
| -------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| `SearchOutcome` / `GameOverReason` のバリアントが表に現れること            | `game-session.md` を enum のバリアント名で引く検査。`docsSourcePaths.ts` の `stripFences` が使える。**`StopTimedOut` の載せ忘れはこれで落ちる** |
| `不変条件N` の参照先が実在すること                                         | `src-tauri/src/**/*.rs` の `不変条件(\d+)` と表の項目数を突き合わせる。**意味の対応は落とせない**ので見出し語の併記を規約にする                 |
| `Command` の strong sender が `tokio::spawn` へ move されないこと（R2-B2） | 機械では難しい。代わりに**探索タスクを模した strong 保持者がある状態で drop する**テスト                                                        |
| 終局後に拍が止まること（R2-M1）                                            | `resign` の後に拍が来なくなることを見るテスト                                                                                                   |
| `drained` → `SearchOutcome` の写像（R2-H3）                                | 純関数に切り出して `search.rs` に `#[test]`。いま `search.rs` の `#[test]` は0個                                                                |
| **機械で防げないもの**                                                     | 消費するだけで見ていないフラグ、`clear()` を置く位置、doc の断言と実装の対応、`Aborted` の語の衝突                                              |

## 修正計画（r2 → r3）

### 対象そのものを疑ったか

**疑った。2つの機構を作り直す。**

#### 1. `restart_after_abort`（bool）を落とし、`Activity` に畳む

所見の集まり方を数えた。

| ラウンド | 所見                                  |
| -------- | ------------------------------------- |
| r1       | H-1 / H-2 / M-24                      |
| r2       | R2-B1 / R2-H2 / R2-H6 / R2-M2 / R2-M7 |

**8件。** 根は1つで、「走っている探索をどう扱うか」を `Activity`（enum）と
`restart_after_abort`（bool）の**2つに割って持っている**こと。判断する場所も
`hand_turn_to`（立てる）と `on_search_outcome`（消費する）に割れている。

r1 の計画で「r1 では取らない。それでも所見が出るなら r2 で機構ごと落とす」と
書いた条件に当たった。**畳む。**

```rust
enum Activity {
    Idle,
    /// `go` / `go ponder` を送って `bestmove` を待っている
    Searching { req: u64, kind: SearchKind, cancel: CancellationToken },
    /// 止めた。**この req の結果は採らない。**
    /// 捨てる `bestmove` が返ったら、`restart` なら本番の思考を始める
    Stopping { req: u64, restart: bool },
    /// 止めたのに応答が無い。**エンジンは探索中とみなす**（`gameover` を送らない）
    Unresponsive,
}
```

R2-B1（採らない）・R2-H2（`gameover` を送らない）・R2-M2（`Idle` のときだけ
始める）・R2-H6 / R2-M7（bool と名前が消える）が**型で決まる**ようになる。

#### 2. `protocol.rs` の「もう来ない」の届け方を1箇所にする

| ラウンド | 所見                                    |
| -------- | --------------------------------------- |
| r1       | B-1 / H-4                               |
| r2       | R2-B3 / R2-H1 / R2-M8 / R2-M11 / R2-M12 |

**7件。** 根は「読み取りが終わったことを、フックの中で `clear()` して伝える」形。
フックは EOF でしか呼ばれず（R2-B3）、呼ばれても転送タスクより先に走る（R2-H1）。

**`clear()` を転送タスクの `while let` の後ろへ移す。** 転送タスクが抜けるのは
`line_tx` が落ちたときで、それは EOF でも読み取りエラーでもランタイム停止でも同じ。
**1箇所で全部の終わり方を拾える。**

### このラウンドで直すもの

| 順  | 所見                                      | なぜこの順か                                                        | この直し方で壊しうるもの                                                                                                                |
| --- | ----------------------------------------- | ------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **R2-H1**（+ R2-B3 / R2-M8 が消える）     | 機構の作り直し2。1行の移動で3所見が消える                           | 溜まった行を配り切ってから閉じるので、**EOF の通知が最大で「溜まっていた行数 × 配布1回」ぶん遅れる**。`STOP_GRACE`（5秒）と競合しないか |
| 2   | **R2-B2**                                 | 輪が戻っている。R2-M1 のテストもここに乗る                          | weak にすると、**探索の結果が対局へ届かなくなる窓**ができる（対局が生きているのに `upgrade` が失敗する経路が無いことを確かめる）        |
| 3   | **R2-B1 + R2-H2 + R2-M2 + R2-H6 + R2-M7** | 機構の作り直し1。5所見が1つの型で決まる                             | **`Unresponsive` に入ったエンジンは二度と使われない。** `close_game` まで残り、`gameover` も届かない。それが正しいかは #353 と絡む      |
| 4   | **R2-H3**                                 | 3 の後。`Stopping` の結果の分類が確定してから                       | `Ok(false)` を `Failed` にすると、**正常な `close` で必ず `Failed` が出る**（R2-M3 と同根）。5 と一緒に見ること                         |
| 5   | **R2-M3**                                 | 4 の直後。`close` の順序を直さないと 4 が偽の故障を量産する         | 待つ時間が増える。`close_game` の応答が `STOP_GRACE` ぶん遅れうる                                                                       |
| 6   | **R2-H4**                                 | 独立                                                                | `ready` を3値にすると、`Closed` を見た `ensure_ready` が即座に失敗する。**再起動で復帰する経路が塞がらないか**                          |
| 7   | **R2-M5**                                 | 独立。小さい                                                        | `ponderhit` 失敗で終局させると、**一時的な書き込み失敗で対局が死ぬ**。`StopThenStart` へのフォールバックのほうが安全                    |
| 8   | **R2-M4**                                 | 3 の後（`Stopping` から `Searching` へ移る場所が確定してから）      | `turn_started` を後ろへずらすと、**エンジンが `go` を受けるまでの時間が誰の持ち時間にも入らない**。それでよいかを表に書くこと           |
| 9   | **R2-B4 + R2-H7 + R2-H8 + R2-M6**         | 全部の修正が終わってから表を書き換える。先にやると2度書くことになる | 無し（doc のみ）。ただし**書いた条件が実装のどの行かを指せるか**を1件ずつ確かめること（この repo で4ラウンド続いた故障）                |
| 10  | **R2-M10**                                | doc のみ                                                            | 無し                                                                                                                                    |

### 直さないもの

| 所見                                                                        | 行き先      | 理由                                                                                              |
| --------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------- |
| R2-H5（`EngineFailure` が1値）                                              | **r3**      | `GameOverReason` を分けると TS の型と wire の綴りが変わる。r1 の H-9 と同じ根なので**一緒に取る** |
| R2-M1（終局後も拍が回る）                                                   | **r3**      | R2-B2 の修正でテストの足場ができてから                                                            |
| R2-M9（`NotInitialized` の意味）                                            | **r3**      | R2-H5 と同じ「エラーの型を分ける」束                                                              |
| R2-M11 / R2-M12（`protocol.rs` の分割と doc）                               | **r3**      | 1 の作り直しで本文が変わるので、その後に                                                          |
| r1 からの持ち越し（H-3 / H-5 / H-6 / H-9 / H-10 / H-11 / H-12 / M-1〜M-31） | **r3 以降** | 検証コスト。**r2 で1件も取れていないことを次の計画に明記すること**                                |

### 次ラウンドの焦点

1. **`clear()` を転送タスクの末尾へ移したことで、EOF の通知が遅れていないか。** `STOP_GRACE`（5秒）以内に届くか
2. **weak sender にした `forward` タスクで、対局が生きているのに結果が届かない窓が無いか**
3. **`Activity` の4値で、`Stopping` / `Unresponsive` から抜ける経路が全部あるか。** 入ったまま出られない状態が無いか
4. **`Unresponsive` のエンジンが `close_game` まで残ることの帰結**。プロセスも探索も生きている
5. **`close` の順序を変えたことで、`close_game` が返らなくなる経路が無いか**
6. **`ready` の3値化で、エンジンの再起動から復帰できるか**
7. **表の書き換えで、書いた条件が実装のどの行かを1件ずつ指せるか**

### 検証の見積り

10件 × 約2分15秒 ≒ **23分**。`/review-plan` 手順7 の目安（20分）をわずかに超えるが、
**BLOCK 4件のうち3件が r1 の退行**なので減らさない。r3 へ送ったのは HIGH 1件・
MEDIUM 4件と、r1 からの持ち越し全部。

## 修正の結果（r2）

| 順  | 所見                                  | コミット  | 結果                                                                                                                                                                                   |
| --- | ------------------------------------- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | R2-H1（+ R2-B3 / R2-M8 が解消）       | `0e0c3c4` | `clear()` を転送タスクの末尾へ。**読み取りエラーでも拾えるようになった**（`line_tx` が落ちるのは終わり方によらないため）。hook は `listeners` を掴まなくなり `blocking_write` も消えた |
| 2   | R2-B2                                 | `62fb1e9` | `forward` へ weak を渡し、`Runner` に `Drop` を書いた。**変異（`Drop` を外す）で落ちることを確認**                                                                                     |
| 3   | R2-B1 / R2-H2 / R2-M2 / R2-H6 / R2-M7 | `441e111` | `Activity` を4値に畳んだ（**機構の作り直し**）。テスト4本、変異2件で3本が落ちる                                                                                                        |
| —   | **時計の渡し方**（ユーザーの指摘）    | `500e5cb` | 計画外。減る値ではなく**尽きる時刻**を渡す形に変えた。フロントに減らすループも規則も生えない                                                                                           |
| 4   | R2-H3                                 | `7507826` | 第2相を3値に。写像を純関数へ切り出してテスト（`search.rs` は `#[test]` が0個だった）                                                                                                   |
| 5   | R2-M3                                 | `0ca2a7a` | `close` が「止める → **待つ** → 落とす」に。`IsQuiet` を足した                                                                                                                         |
| 6   | R2-H4                                 | `8b2c022` | `ready` を3値（`Waiting` / `Ready` / `Closed`）に。**即死するエンジンで 120 秒待たなくなった**                                                                                         |
| 7   | R2-M5                                 | `441e111` | **3 に含めて解消。** `ponderhit` の失敗は `kind` を書き換えず `stop_then_start` へ倒す                                                                                                 |
| 8   | R2-M4                                 | `60b39ee` | 止めた探索を畳んでいる間を持ち時間に入れない。**どちらの持ち時間にも入らない**ことを表に書いた                                                                                         |
| 9   | R2-B4 / R2-H7 / R2-H8 / R2-M6         | `dc5cc50` | 表を全面書き直し。**不変条件2 の根拠が偽だった**（守っているのは別の機構）ので、そこも直した                                                                                           |
| 10  | R2-M10（+ r1 の M-19）                | `417a632` | TS の doc 3つ。`types.ts` → `types.rs`、`searchInfo.result` だけ snake_case、`aborted` が2つの意味を持つこと                                                                           |

### 提案どおりに直さなかったもの

- **R2-M2 で `debug_assert!` を入れる案**（`rust` の提案）は採ったが、`Handover` の
  `StopThenStart` へ寄せる部分は 3 の機構の作り直しに吸収した。門番が cancel して
  続行する形は消え、`spawn_search` は `Idle` でなければ何もせずに戻る
- **R2-H4 で `collect_engine_info` の文言を分ける案**も採った。ただし
  「出力が終わった」と「`usiok` までに `id name` が無かった」の2つだけで、
  `EngineError` の型は分けていない（それは R2-M9 として r3 へ）

### 新しく立てた issue

| 番号 | 内容                                                                      |
| ---- | ------------------------------------------------------------------------- |
| #358 | 中断した対局を再開できない（`GameSettings` に残り時間の持ち込み口が無い） |

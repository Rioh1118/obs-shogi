# レビュー 441-unmount-session ラウンド5

- 日付: 2026-09-07
- 範囲: `git diff main...HEAD`（`fix/441-stop-analysis-on-unmount`）
- 走らせた reviewer: react / robustness / comment / oss-hygiene
- 対象コミット: `2b0a9dcb` 前後
- 前ラウンド: [r1](2026-09-07-441-unmount-session-r1.md) / [r2](2026-09-07-441-unmount-session-r2.md) / [r3](2026-09-07-441-unmount-session-r3.md) / [r4](2026-09-07-441-unmount-session-r4.md)

## 所見

### [HIGH] 1. 「局面が消えた」を世代が知らない

reviewer: react（実測）

r4 で足した effect が `state.isAnalyzing` を先に見るので、**席が返る前**
（開始が飛んでいる間）は世代を上げない。棋譜を閉じても同期待ちが上限まで回り、
閉じた棋譜のために断りを積み、閉じた局面で「解析中」が1回 commit される。
r3-12 で直した形が、r4 で足した引き金には効いていない。

### [HIGH] 2. 席を握ったまま「停止中」の画面で棋譜を閉じると、停止が1本も飛ばない

reviewer: robustness（実測）

同じ effect の門。※7 の後（停止が届かず席だけ残った回）に棋譜を閉じると、
`isAnalyzing` が false なので抜けてしまう。エンジンは閉じた棋譜を読み続け、
ペインごと消えるので ▶ も ■ も無い。**`analysis.md` が「`state` の写しから
導いてはいけない」と名指しで禁じた形。**

### [BLOCK/HIGH] 3. 「▶ では復帰できない」と書いた doc が5箇所残っている

reviewer: comment / oss-hygiene / robustness（3人が独立に）

r4-10 で ▶ が握った席を返してから始めるようになったのに、
表の E1 セル（S0/P1・S6/P1）、不変条件3、※2 の手順、F-7 の復帰導線、
`analysis-pane.md` の #172 の行が古いまま。**この PR の主要な改善が
「破れている」と書かれている。** 5ラウンド続けて同じ形。

### [HIGH] 4. ※12 の枝2つが、r4 で書き換えた後もコードで作れない

reviewer: comment / oss-hygiene

指さない停止を撃つのは `sweepOnUnmount` だけ（畳まれた後）。その後にこの provider が
開始することは無いので、「次の開始が席を取るとずれる」は起こせない。
**指さない理由が空のまま**で、読み手が「指す形に戻しても安全そうだ」と判断できる。

### [HIGH] 5. `no-position`（棋譜を閉じた回）がどの表にも無い

reviewer: oss-hygiene / robustness

`app.md` の ※4 が「Rust 側のセッションが残るかは analysis.md の外部状態の列を見る」と
委譲しているのに、その列が無い。表の `S1/P1 × E3` は `→ S2` だが、
`currentSfen → null` の回だけは `→ S0`。画面仕様の「操作と結果」にも行が無い。

### [HIGH] 6. `(S3, E11)` が `—` なのに、※9 が同じ停止の失敗を扱っている

reviewer: oss-hygiene

同期待ちの打ち切りで撃つ停止が落ちた回。r4 で `(S4/S5, E11)` は埋めたが S3 が残った。

### [HIGH] 7. ※1 の「破るのは ※7 だけ」が、不変条件1 と `(S4/S5, E11)` の両方と矛盾

reviewer: comment

## MEDIUM

8. **返却が飛んでいる最中の席へ、`no-position` がもう1本撃つ**（react）。
   1本目が空けた後の2本目が「知らない席」で断られ、その失敗で
   **もう存在しない ID を握り直す**。畳まれたときの後始末が指さない停止に落ちる。
9. **`isHeld()` の門が呼び手に2箇所残っている**（react）。片方は `releaseHeld` の中と
   逐語で同じ判定、もう片方は掃除の有無だけを分けている。
10. **context の7つのうち4つが読み手0**（react）。`state.sessionId`（r3-10）、
    `analysisResults`（r4-12）に続いて3回目。
11. **`by` の受け渡しを見ている検査が1本も無い**（robustness / oss-hygiene）。
    テストの mock が第2引数を落としているので、口の名前が入れ替わっても全部緑。
12. **`SeatReleasePoint` の doc が「`releaseHeldQuietly` に書き足せ」と言うが、
    結末の列挙は非公開の関数に在る**（comment）。`discard` の握り直しの契約も公開面に無い。
13. **席を握る／捨てる直前の同じコメントが、2つの開始経路に逐語で写されている**（comment）。
14. **#172 の本文が、存在しない関数名（`ensure_no_active_session`）と、この PR で
    落とした欄（`sessionId`）で書かれている**（oss-hygiene）。

## 重複・矛盾した所見

- 所見1 と2 は同じ effect の門を、別の入口（世代／席）から指している。まとめて直す。
- 所見3 は3人が独立に、同じ5箇所を挙げた。
- 所見4 は comment と oss-hygiene が同じ結論（列挙を落として理由を書き直す）。
- 矛盾は無し。

## 見ていない範囲

- `analyzer.rs` / `protocol.rs` の内部と #463 の窓の幅。**5ラウンド続けて未見。**
- `features/engine-position-sync` の実装。**5ラウンド続けて未見。**
- 実プロセスを使った検証は誰もしていない。
- `analysis.md` の表のうち、名指ししていないセル。

## lint / hook で強制できるもの

- **`SeatReleasePoint` の各値が `docs/state-transitions/analysis.md` に1度は現れるか。**
  所見5 はこれで落ちる。
- **`invoke(...)` のキーと `#[tauri::command]` の引数名の突き合わせ**（所見11 の一部）。
- **context の口に、スライス外の呼び手が居るか**（所見10）。3回目なので two-strikes を超えている。
- 所見1〜9・12〜14 は機械では止まらない。

## 修正計画

1. **所見1・2 → 世代を門の前に出し、席を握っていれば表示が停止中でも返す。** テスト2本
2. **所見8 → 返却の in-flight をフックに持ち、返し終えた席は握り直さない**
3. **所見9・13 → 呼び手の門を畳み、握る／捨てるの判断を1箇所に**
4. **所見10 → context を3つに絞る**
5. **所見11 → テストの mock を直し、`by` の値を見る**
6. **所見3・4・5・6・7・12 → doc とフックの doc**
7. **所見14 → #172 にコメント**

### 次ラウンドの焦点

- 所見1・2 で足した門が、**4つの引き金（畳まれる・棋譜を閉じる・止める・次の要求）で
  同じ形になっているか**
- `releasingRef` が新しい取りこぼしを作っていないか（相乗りした側が返り値を待てるか）
- E14 を足した表が、他の列（E3）と噛み合っているか
- doc の断定が、また実装とずれていないか（**5ラウンド続けて出ている**）

## 修正の結果

| 所見                         | 結果                                                                     | コミット / 送り先                   |
| ---------------------------- | ------------------------------------------------------------------------ | ----------------------------------- |
| 1 / 2 / 8 / 9 / 10 / 11 / 13 | 直した。世代を先に上げ、席で門を作り、返却の相乗りと context の絞り込み  | `ee58cbfc`（テスト2本、変異で確認） |
| 3 / 4 / 5 / 6 / 7 / 12       | 直した。E14 の列と ※14、E1 のセル、不変条件1・3、`(S3, E11)`、※12 の理由 | 直後の docs コミット                |
| 14                           | #172 にコメントを付けた                                                  | —                                   |

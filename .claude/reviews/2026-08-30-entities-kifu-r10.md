# レビュー entities-kifu ラウンド10

- 日付: 2026-08-30
- 範囲: `refactor/163-entities-kifu`（issue #163 / #164 / #165 / #166）の `origin/main` からの差分
- 対象コミット: `4ffc4d7`
- 走らせた reviewer: robustness / comment
- 前ラウンド: `-r1.md` 〜 `-r9.md`

## 所見

| 番号   | 深刻度 | reviewer | 内容                                                                                                                                                                                                                                         | 結果                                                                               |
| ------ | ------ | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| R10-01 | HIGH   | comment  | `computeLeafTesuu` / `buildPlayer` の doc「実在しない変化は本譜へ落ちる」が負・非整数で成り立たない。`forkAndForward` が `false` を返すのは `forks.length` 以上のときだけで、`-1` / `0.5` / `NaN` は `forks[-1]` を掴んで `TypeError` になる | 直した（ガードを入れて doc を真にする側へ。変異で確認）（`2797fa1`）               |
| R10-02 | MEDIUM | comment  | `while (limit-- > 0)` の off-by-one で実効上限が 9998。9999手の棋譜は**葉に着いてから** throw していた                                                                                                                                       | 直した（数える形に。10000手ちょうどまで通ることを実測とテストで固定）（`4336c8f`） |
| R10-03 | MEDIUM | comment  | 状態遷移表の「テスト」欄が、存在しないテストに ✓ を付けていた（`neighborBranchIndex(b, "down")`）                                                                                                                                            | 直した（`"up"` 側と対称なテストを追加）（`2797fa1`）                               |
| R10-04 | MEDIUM | comment  | `TesuuPointer` の TSDoc が brand 宣言に付き、公開型なのにホバーで説明が出ない。「方で明示する」の誤字                                                                                                                                        | 直した（`branch.ts` と並びを揃えた）（`4336c8f`）                                  |
| R10-05 | MEDIUM | comment  | 状態遷移表の冒頭がレビューの経過報告（回数・PR 番号）で、書いた時点で既に数が合っていない                                                                                                                                                    | 直した（回数と PR 番号を落とし、表の存在理由だけ残した）（`2797fa1`）              |
| R10-06 | MEDIUM | comment  | `docs/state-transitions/README.md` の索引に `branch-index.md` が無い。README 自身が在庫の一覧として使うと宣言している                                                                                                                        | 直した（索引に追加。**食い違いを拾うテストも追加**）（`fc39da1`）                  |

## 検証で所見にならなかったもの

**robustness は所見なし。** ラウンド9の書き換え4点をすべて `origin/main` との差分ハーネスで確かめた。

| 検証                                    | 件数   | 結果                                                                                                                                |
| --------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------- |
| `branchLabel` が total になった         | —      | 正常入力の文字列は `origin/main` と逐語同一。ブランチ途中で一時的に partial になっていたのを戻しただけ                              |
| `leafTesuu` のガードと `JKFData` 化     | 30,000 | 値の差 **0**。main が通って HEAD が throw する例 **0**。差は「main が throw して HEAD が値を返す」2186件のみ（R9-05 が狙った1方向） |
| `totalMoves` まで含めた突き合わせ       | 20,000 | same 19998 / 増 1 / 減 1。減った1件は**新の方が正しい**（旧は計画を適用していない本譜長を出していた）                               |
| `buildCursorWithForkSelection` の正規化 | 50,000 | 正規化済みの `base` では差 **0**。未正規化では差が出るが、実際の `base` は3経路とも正規化済み                                       |
| `assertBranchIndex` の非公開化          | —      | 検査を飛ばす新しい経路は無い                                                                                                        |

**コミットメッセージと差分の一致**（R9-02 の再発チェック）: comment / robustness の両方が
ラウンド9の7コミットを `--stat` と差分で突き合わせ、**食い違い0件**。空コミットも無し。

`branchIndexFromForkIndex` は R9-04 で `branchLabel` が使わなくなり、production の外部呼び出しが
0になった。ただし `branchIndexFromSelection` が内部で使っており、テストが `BranchIndex` を
正規に作る唯一の手段でもある（`as never` を避けるため）。**意図的に export のままにする。**

## 見ていない範囲

- Rust 側。この PR に `src-tauri/` の差分が無いため `npm run verify:rust` は未実行
- 実機での操作確認。再現はすべて Node とコード読解
- SCSS / レイアウト / a11y
- perf（ラウンド6で総括、以降走らせていない）、react / architecture（所見が既送 issue に収束）

## lint / hook で強制できるもの

- `docs/state-transitions/` の在庫と README の索引の突き合わせ → **入れた**（`src/__tests__/stateTransitionIndex.test.ts`）
- doc コメントと `declare const ...Brand: unique symbol;` の順序（R10-04 の形を禁止できる）
- `docs/**/*.md` の「✓ テストあり」印と、そこに書かれた識別子の実在の照合（R10-03 は grep 1回で落とせた）
- `docs/**/*.md` 内の `PR #N` / `ラウンドN` への言及の検出（コードコメントの規約が `docs/` に及んでいるかが曖昧）
- 拾えないもの: `@throws` の内容が実際の throw を網羅しているか（R10-01 は外部ライブラリ内部からの
  `TypeError` なので静的には見えない）、`while (limit-- > 0)` のような境界の off-by-one

## コミットのタグと所見番号の対応

1所見1コミットを守れず、6件の所見を3コミットに畳んだ。タグも所見番号と対応していない
（`R10-03` のタグが付いたコミットの中身は所見 R10-06）。**番号で grep しても辿れない。**
上の表に SHA を入れてあるのでそちらを見ること。履歴は書き換えない。

以後は畳んだ側のコミット本文に、含めた所見番号を明記する。

## 次ラウンドの対象

R10-01〜R10-06 を直したうえで、修正で新しい問題が入っていないかを見る。
robustness が2ラウンド続けて所見0で、comment の所見も doc と表に収束しているので、
次で0件が出れば打ち切る。

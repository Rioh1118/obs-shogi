# レビュー skc-v040 ラウンド1

- 日付: 2026-08-31
- 範囲: `origin/main..HEAD`（7コミット）。`src-tauri/` のみ。TS は未変更
- 対象コミット: eb0b251
- 走らせた reviewer: rust / robustness / architecture / comment / perf

## 所見

### [BLOCK] R1-01 CSA から `catch_unwind` を外したのは誤り。パニックは上流で消えていない

- 場所: `src-tauri/src/search/kifu_reader.rs:39`
- reviewer: rust
- パニックは `shogi-kifu-converter` ではなく**その下の `csa` クレート**にある。
  `Cargo.lock` の `csa` は v0.3.1 のときも今も **1.0.2 で据え置き**。
  上流の `deny(clippy::panic, unwrap_used, ...)` は自分の crate しか見ない。
- 自分で再現した（v0.4.0 で実測）:

  ```
  正常な日付:              Ok(3 moves)
  存在しない日付 2/30:     *** PANIC ***  csa-1.0.2/src/parser/time.rs:57 unwrap
  桁あふれの消費時間:      *** PANIC ***  csa-1.0.2/src/parser/game.rs:40  unwrap
  ```

- **これは自分が入れた退行。** コミット `8e26acd` のメッセージは「v0.4.0 は全種 Ok」と
  書いたが、確かめたのは KIF の手合割だけで CSA を確かめていなかった。
- 直す: `KifuKind::Csa` にだけ `catch_unwind` を戻す。回帰テストを足す。

### [HIGH] R1-02 `{preset: HIRATE, data: 別盤面}` の開始局面が黙って平手になる

- 場所: `src-tauri/src/search/initial_position.rs:24-29`
- reviewer: robustness（perf も範囲外の観測として同じ点を挙げた）
- 変更前は `(_preset, Some(state))` で **`data` が preset に勝つ**規則だった。
  委譲先は `PresetHirate => startpos()` で `data` を見ない。
- 自分で再現した:

  ```
  parse_jkf_str: preset=PresetHirate data=true
  クレートの TryFrom -> lnsgkgsnl/1r5b1/ppppppppp/9/9/9/PPPPPPPPP/1B5R1/LNSGKGSNL b - 1
  ```

  詰将棋の盤面を入れた JKF が、平手の初期局面として索引に入る。
  `normalize()` は畳まないので `parse_jkf_file` を素通りする。KIF/KI2/CSA は
  盤面があれば必ず `PresetOther` を付けるのでこの経路に来ないが、`.jkf` は外部の JSON。

- **これも自分が入れた退行。**
- 直す: `data` がある場合は `data` を優先する（旧規則を保つ）。テストを足す。

### [HIGH] R1-03 `ConvertError` は何手目かを言わない。コメントが嘘

- 場所: `src-tauri/src/file_system/operations.rs:85-86`
- reviewer: comment
- 書いたコメント: 「綴れなかった理由は ConvertError の Display が言う（**何手目の何の手か**）」
- 実際に KIF/KI2/CSA の書き出しが返しうるのは
  `InvalidSquare` / `UnknownPreset` / `UnspellableMove` / `UnspellableNumber` / `Write` の5つで、
  **どれも ply を持たない**。ply を持つのは `Normalize` だけで、それを作るのは
  `TryFrom<&JsonKifuFormat> for Position`（`from.rs:139`）= USI 書き出しの経路のみ。
- 自分で数え直して確認した（クレートの `src/converter/` を grep）。
- **`CLAUDE.md` 手順5 の「コメントに書いた理由が実装と違う」そのもの。** 4ラウンド続いた故障を
  自分でもう一度やった。
- 直す: 「何手目か」を落とし、実際に言うこと（綴れなかった手・駒の枚数）に直す。

### [HIGH] R1-04 `ParseError::Decode` のとき理由が消える。doc の主張が成り立たない

- 場所: `src-tauri/src/search/kifu_reader.rs:108-110`, `:77`, `:87`
- reviewer: robustness（comment も同じ点を別の角度で）
- 書いた doc: 「クレートが返した理由のほうが具体的（何行目の何が読めないか）」
- 成り立つのは `ParseError::Kif` / `Ki2` のときだけ。`Decode` の Display は
  **`Decode Error` の一語**。壊れた UTF-8 の棋譜では、利用者に出るのが
  `parse failed: <path>: Decode Error` だけになり、試した文字コードも消える。
- 足した回帰テスト（`:196-217`）は `ParseError::Kif` 側しか見ていないので、この穴を守っていない。
- 直す: `reason` の変種で分ける。`Decode` / `FileExtension` / `Io` のときは試した文字コードを添える。

### [MEDIUM] R1-05 `VERSION` の doc の無効化条件が実装と逆向き

- 場所: `src-tauri/src/search/index_cache.rs:33`、実装は `fs_scan.rs:177`
- reviewer: comment
- 書いた doc: 「古い索引が捨てられる条件は `(size, mtime_ms)` の**一致**だけ」
- 実装は「`(size, mtime_ms)` が**食い違ったとき**にその1件を読み直す」。主語も条件も逆。
- 直す: 実装に合わせる。

### [MEDIUM] R1-06 `read_many_to_jkf` / `ReadOk` / `ReadErr` がデッドコード

- 場所: `src-tauri/src/search/kifu_reader.rs:10-11`, `:51-64`
- reviewer: architecture / rust
- 呼び出し元が repo に1つも無い。`pub` なので `dead_code` が拾わない。
  現在の索引作りは1ファイルずつ `spawn_blocking` に載せる形で、
  逐次でまとめて読むこの形は使いようがない。
- 直す: 削除する。

### [MEDIUM] R1-07 テストの材料が2ファイルに丸写しされ、ヘルパがあるのに手書きしている

- 場所: `kifu_reader.rs:221-237` と `initial_position.rs:36-52`（同じ `HANDICAPS`）、
  `kifu_reader.rs:136-144`（`temp_dir`）に対して `:250-255` が同じ処理を手書き
- reviewer: rust / architecture / comment（3人が独立に挙げた）
- クレートが手合割を1つ足したとき直す場所が2つあり、片方だけ更新しても**両方緑のまま通る**。
- 直す: `HANDICAPS` と KIF 雛形を1箇所に寄せ、`:250-255` は `temp_dir("handicap")` にする。

### [MEDIUM] R1-08 回帰テストの assert が空振りしている

- 場所: `src-tauri/src/search/kifu_reader.rs:211-214`
- reviewer: rust
- `message.contains("utf-16")` は小文字。総当たりの失敗を並べる実装が戻るとしたら
  `encoding_rs::Encoding` の Debug を使うのが自然で、それは **`UTF-16LE` と大文字**で出る。
  `contains` は大小を区別するので、**一番起きそうな戻り方を捕まえられない**。
- 直す: 試した文字コード名の一覧を定数にして、大小を無視して交差しないことを見る。

### [MEDIUM] R1-09 doc が主張する「クレート単体では読めない」をテストが検証していない

- 場所: `src-tauri/src/search/kifu_reader.rs:146-189`
- reviewer: comment
- テストが見ているのは `read_path_to_jkf` が読めることだけ。クレートが将来 EUC-JP を
  自前で扱うようになっても緑のまま、`try_other_encodings` が要る根拠だけが嘘になる。
- 直す: 同じループで `parse_kif_file(&path).is_err()` を先に確かめる。

### [MEDIUM] R1-10 テストの doc に変更の経緯が入っている

- 場所: `kifu_reader.rs:241-247`, `:194-195`, `kifu.rs:199`
- reviewer: comment
- 「包みを外した」「補正が戻ってきたら」は、既に消えた `catch_unwind` / `patch_gote_start` を
  指しており、コード中に辿れない。`CONTRIBUTING.md` の「変更の経緯を書かない」に当たる。
- ただし `index_cache.rs` の `VERSION` の版ごとの記録は**経緯ではなく現在の契約**なので残す。
- 直す: 現在形に書き換える。

### [MEDIUM] R1-11 `initial_partial_position` の doc に失敗条件が無い

- 場所: `src-tauri/src/search/initial_position.rs:14-29`
- reviewer: comment
- `pub` で `Result` を返すのにエラーの意味が書かれていない。委譲した以上、
  委譲先の失敗条件はこの doc の担当。
- 直す: `# Errors` を足す。

### [MEDIUM] R1-12 `spell` / `failed` が動作を表していない

- 場所: `operations.rs:87-89`, `kifu_reader.rs:44-49`
- reviewer: comment
- `spell` は何も綴らない（綴るのは `try_to_kif_owned`）。しているのは
  `ConvertError` → `FsError` の詰め替えだけ。`failed(path, e)` は述語に見えるが構築子。
- 直す: `to_fs_error` / `parse_failed` に改名。

### [MEDIUM] R1-13 `InitialPosError` が1バリアントの包みになり、文言が二重になる

- 場所: `initial_position.rs:8-12`、受け手は `index_builder.rs:39-40`
- reviewer: architecture
- 利用者に出るのは
  `failed to create initial position: cannot build the initial position: No board for preset: ...`。
  同じ意味の英文を2回名乗る。バリアントが1つなので分岐にも使えない。
- 直す: R1-02 で `data` 優先を戻すなら独自のバリアントが要るので、**R1-02 と合わせて判断**する。

### [MEDIUM] R1-14 `MAGIC` の `v01` とファイル名 `index.v1.*` が `VERSION` と食い違う

- 場所: `index_cache.rs:29`, `:89-90`
- reviewer: comment
- 版を表す綴りが3つあり、2つは 1 のまま。次に上げる人がどれを動かすか判断できない。
  据え置きは意図（コミット `eb0b251` に理由を書いた）だが、**コードのそばに無い**。
- 直す: `VERSION` の doc に1行足す。

## 重複・矛盾した所見

- **`catch_unwind` の扱いで rust と comment が逆を向いた。** rust は「CSA には戻すべき」（R1-01）、
  comment は「『索引作りが死ぬ』という記述が過大で、実際は `spawn_blocking` が受け止める」。
  **両方正しい。** 受け止めるのは事実だが、受け止めた結果のメッセージが
  `spawn_blocking join error: task N panicked` に劣化する。CSA は戻し、コメントは
  `spawn_blocking` の実態に合わせて書き直す。
- `HANDICAPS` の重複は rust / architecture / comment の3人が独立に挙げた（R1-07）。

## 範囲外（issue へ送る）

TS 側、または `src-tauri` でもこの PR が触っていない箇所。

| 内容                                                               | reviewer          | 送り先                                                     |
| ------------------------------------------------------------------ | ----------------- | ---------------------------------------------------------- |
| `try_to_*_owned` の失敗が画面に出ない（`makeMove` が結果を捨てる） | robustness        | #277 に追記 or 新規                                        |
| restore 経路では読めなかった件が誰にも見えない                     | robustness        | 新規                                                       |
| 索引の警告が `{:?}` で `CursorLite { .. }` のまま出る              | robustness        | 新規（Rust だが `api.rs` / `project_manager.rs` は未変更） |
| 警告が5件しか出ず、200件で頭から捨てるのに件数は全数を言う         | robustness        | 新規（TS）                                                 |
| lossy で読めた棋譜が無言で索引に入る                               | robustness        | #293 に追記                                                |
| `open_project` 失敗で `Restoring` のまま行き止まる                 | robustness        | 新規                                                       |
| `position_apply` も委譲すべきでは                                  | architecture      | 新規                                                       |
| 文字コードの答えが3層に散っている（索引には入るが開けない）        | architecture      | 新規                                                       |
| 「JKF→形式文字列」が3箇所                                          | architecture      | #322 に追記                                                |
| `write_new_file` が tmp+rename でない                              | rust              | 新規                                                       |
| `ConvertError` の英文がそのまま利用者に出る                        | rust / robustness | 新規                                                       |
| `convert_jkf_to_format` が同名で2つある                            | comment           | 新規（改名は TS と同時）                                   |
| `write_kifu_file_internal` にテストが無い                          | rust              | **範囲内。R1-15 として直す**                               |

## 見ていない範囲

- perf: KI2 / CSA / JKF 経路の実測（コーパス609件は全て `.kif`）。差分更新の計測
- rust: TS 側は `write.ts` / `service.ts` 周辺のみ
- robustness: `index_cache.rs` の符号化・復号本体、`position_key` / `node_table` への影響
- architecture: `Cargo.lock` の推移的依存の差分
- comment: `docs/` 配下

## lint / hook で強制できるもの

- `src-tauri/src/lib.rs` に `#![deny(deprecated)]`（または `[lints.rust]`）→ `to_*_owned` の再混入を止める
- 内部 API を `pub(crate)` に落とす → `dead_code` が `read_many_to_jkf` 型の死骸を拾えるようになる
- `env::temp_dir()` をテストヘルパ以外で呼ぶことを禁じる grep hook
- `encoding_rs` の `decode` の3つ目の戻り値を `_` で潰す書き方を禁じる走査（`operations.rs:44`）
- **コメントとコードの食い違い（R1-03 / R1-04 / R1-05）は機械で拾えない。** 3件とも今回出た

## 次ラウンドの対象

直す順（自分が入れた退行から）:

1. R1-01 CSA の `catch_unwind` を戻す（BLOCK）
2. R1-02 `data` 優先を戻す（HIGH）
3. R1-03 嘘コメントを直す（HIGH）
4. R1-04 `Decode` のとき理由を残す（HIGH）
5. R1-05 〜 R1-14 と R1-15（`write_kifu_file_internal` のテスト）

範囲外の12件は issue にしてから次ラウンドへ。

---

## 修正結果（`/review-fix`）

直したコミット4本。すべて `npm run verify:rust` と `npm run verify` を通してある。

| 所見                        | 結果                                                            | コミット                                        |
| --------------------------- | --------------------------------------------------------------- | ----------------------------------------------- |
| R1-01 CSA の `catch_unwind` | 直した。CSA だけ戻し、根拠を doc に書いた                       | `fix: CSA だけ catch_unwind を戻す`             |
| R1-02 `data` 優先           | 直した。`data` があれば `PresetOther` として渡し直す            | `fix: 盤面が書いてあれば preset より盤面を採る` |
| R1-03 嘘コメント            | 直した。ply を言わないことを明記                                | `fix: コメントが言う条件を実装に合わせ…`        |
| R1-04 `Decode` の理由       | 直した。`reason_for` で変種ごとに分け、試した文字コードを添える | `fix: CSA だけ…`                                |
| R1-05 `VERSION` の doc      | 直した                                                          | `fix: コメントが言う条件を…`                    |
| R1-06 デッドコード          | 削除した                                                        | `fix: CSA だけ…`                                |
| R1-07 テスト材料の重複      | `search/test_kifu.rs` に寄せた                                  | `fix: CSA だけ…`                                |
| R1-08 空振りの assert       | 名前の一覧と大小無視で突き合わせる形にした                      | `fix: CSA だけ…`                                |
| R1-09 doc の主張が未検証    | 各文字コードで `parse_kif_file` が失敗することを先に見る        | `fix: CSA だけ…`                                |
| R1-10 テスト doc の経緯     | 現在形に直した                                                  | `fix: CSA だけ…` / `fix: コメントが…`           |
| R1-11 `# Errors` 欠落       | 足した                                                          | `fix: 盤面が書いてあれば…`                      |
| R1-12 `spell` の命名        | `to_fs_error` に改名                                            | `fix: コメントが言う条件を…`                    |
| R1-13 `InitialPosError`     | 削除し `ConvertError` を直に流す                                | `fix: 盤面が書いてあれば…`                      |
| R1-14 `MAGIC` / ファイル名  | `VERSION` の doc に据え置きの理由を書いた                       | `fix: コメントが言う条件を…`                    |
| R1-15 書き込み経路のテスト  | 足した                                                          | `fix: コメントが言う条件を…`                    |

### 変異を当てて確かめたもの

- R1-01: `parse_csa_guarded` を外すと `csa-1.0.2/src/parser/time.rs:57` で
  パニックしてテストが落ちる
- R1-02: `data` 優先の分岐を外すと
  `assertion left != right failed: 盤面を捨てて平手にしている` で落ちる

### 自分が入れた退行

**R1-01 と R1-02 は、このブランチの前半のコミットで自分が作ったもの。**

- R1-01: 「上流が panic を消した」を KIF の手合割だけで確かめ、CSA を確かめずに
  6箇所すべての `catch_unwind` を外した。CSA のパニックは別クレート（`csa` 1.0.2）にあり、
  上流の lint は届かない
- R1-02: 盤の組み立てを委譲したとき、委譲先が `preset` を優先して `data` を読まないことを
  確かめなかった

どちらも**「上流が直した」を1形式でしか確かめずに全形式へ広げた**のが原因。

### R1-03 について

`CLAUDE.md` 手順5 が「4ラウンド続けて出た故障」として名指ししている
「コメントに書いた理由が、実装している条件と違う」を、同じ PR でもう一度やった。
機械では防げないので、次のラウンドでも**コメントの「〜だから」を1件ずつ
コードの行に突き合わせる**こと。

なお `src/__tests__/commentHistory.test.ts` は動いており、
修正中に書いた「この PR が〜」をコミット前に止めた。経緯の混入は機械で防げている。

### 範囲外として送ったもの

| 内容                                                              | 送り先      |
| ----------------------------------------------------------------- | ----------- |
| 読めなかった警告が2回目以降見えない / 件数が嘘 / 5件しか出ない    | #324        |
| 文字コードの判定が3層に散り、検索に出るのに開けない               | #325        |
| 全件再構築が入口で失敗すると「復元中」で行き止まる                | #326        |
| `ConvertError` の英文がそのまま出る / `makeMove` が結果を捨てる   | #327        |
| `position_apply` だけ変換を手書きで持っている                     | #328        |
| `write_new_file` が atomic でない                                 | #329        |
| 「JKF→形式文字列」が3箇所 / 同名関数2つ / `normalized_jkf` の名前 | #322 に追記 |
| lossy で読めた棋譜が無言で索引に入る                              | #293 に追記 |

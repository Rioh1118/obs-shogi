# レビュー book-to-main ラウンド3

- 日付: 2026-09-04
- 範囲: r2 の所見18件を直した18コミット（`git diff eb3af91e HEAD`、報告書を除いて722行）
- 走らせた reviewer: architecture / comment / rust / robustness / oss-hygiene
- 対象コミット: `b6a73c5b`
- 前ラウンド: `.claude/reviews/2026-09-04-book-to-main-r2.md`

所見 **23件**（BLOCK 1 / HIGH 5 / MEDIUM 17）。

**所見が減っていない。** r1 22件 → r2 18件 → r3 23件。
`/review-plan` 手順4 の「3ラウンド続いたら直し方ではなく対象を疑う」に**到達した。**
判断は末尾の「対象そのものを疑ったか」に書く。

---

## 所見

### T-01 (BLOCK, architecture / robustness / comment / oss-hygiene): `CLAUDE.md` の検証の節が、r2 の修正で嘘になった

`CLAUDE.md:14-15`（未変更）:

> docs や `.claude/` だけの変更は素通しする。**ただし `docs/state-transitions/*.md` は
> `state_table_terms.rs` が実装と突き合わせるので Rust 側が走る。**

実測（`GATE_LIB_ONLY=1` で `gate_kinds_for_path` を直接叩いた）:

```
docs/IDEAS.md                    -> [ts]
docs/spec/screens/board.md       -> [ts]
docs/state-transitions/README.md -> [ts rust]
```

**`docs/**.md`はもう素通ししない。**`CLAUDE.md` はこのリポジトリで最初に読まれる1枚で、
「どの変更でどの検証が走るか」の入口。`docs/spec/` だけを直した人はここを読んで
「検証は走らない」と判断し、`git commit`が vitest を回して落ちたときに
**ゲートの誤発火だと読む** →`gate_kinds_for_path`の`docs/\*.md` を消しに行く。
r2 の R-12 が塞いだ穴がそのまま開く。

同じラウンドの `2c90ff3e` は「**件数や名前をここに書くと、1本畳んだ人が
『もう ts は要らない』と読んで分類ごと戻す**」と書いて `verify-gate.sh` から検査名を消したが、
**`CLAUDE.md` には `state_table_terms.rs` の名前が残っている。** 規則を1箇所にしか適用していない。

**4人が独立に指した。**

### T-02 (BLOCK の一部, robustness / oss-hygiene): ゲートの新しいコメント自身が、掛かる範囲を誤って書いている

```sh
# `docs/` は ts 側の検査が丸ごと見る（リンク・索引・指すパスの実在）。
```

`docs/` 全体に掛かるのは**リンクの2本だけ**（`brokenLinksInBody` / `brokenReferencesInBody`）。
索引・階層図・未作成・指すパスの実在は `docs/state-transitions/` 限定
（`docsSourcePaths.test.ts:30` の `filter((f) => f.startsWith("state-transitions/"))`、
索引側は `TABLES_DIR` 限定）。

実測: `docs/spec/features/game-play.md` は死んだパスを3本持ったまま緑で通る。

しかも**「どの検査が掛かるかを数えて書かない」と宣言した3行下で3種類を列挙している。**
`verify-gate.test.sh:244` のコメントも「パスの検査が docs 全体に掛かっている」と同じ誤り。

### T-03 (HIGH, comment / rust / oss-hygiene / architecture): 「本家に合わせない理由」が、先勝ちにも同じだけ当てはまる

`yaneuraou_db.rs:798-801`（r2 の `43601033` で足した）:

> 後勝ちにすると読む順序（ブロックの並び）に結果が依存し、
> 同じ内容を別の順で書いたファイルで引ける手が変わる。

**先勝ちも同じだけ順序に依存する。** 同じ doc が3行上で挙げている例
（`7g7f 8c8d 50 10` と `7g7f 8c8d 900 20`）の2行を入れ替えれば、先勝ちの結果も 50 → 900 に変わる。
「引ける手が変わる」も誤りで、どちらの方針でも残る指し手の集合は同じ。変わるのは属性だけ
（同じ doc が「本家は 900 を、こちらは 50 を返す」と正しく書いている）。

**R-01 の修正意図は「次の人が本家に合わせに行かないよう理由を置く」だった。**
置いた理由が30秒で反証できるので、**気づいた人はむしろ「理由になっていない＝合わせてよい」と
判断する。** 塞ごうとした経路がそのまま開いている。

3人が独立に指した。

### T-04 (HIGH, rust / comment): 同じファイルの `flush` のコメントが「本家も同じ」と言い続けている

`yaneuraou_db.rs:750-752`（触っていない）:

> 本家 `BookMoves::insert`（`book.cpp:123-149`）も、同じ指し手があれば
> 追加しない。ShogiHome は違う方針（手数の小さいエントリで丸ごと置換）なので、…

**T-03 が名指した文型そのもの。**「本家**も**追加しない」＋「ShogiHome は違う」＝
違うのは ShogiHome だけ、と読める。60行下の新しい doc は「本家は後勝ち」と書いているので、
**同じファイルの2つのコメントが上流の挙動について逆のことを言う。**

行番号も外れている。v9.40 で `120` が関数の開始、`145` が終わり、`148-149` は
`MemoryBook::merge`。**`123-149` は3行内側から始まって別の関数へ4行食い込む。**

### T-05 (MEDIUM, rust): r2 が md で直した誤った出典2件が、Rust 側の同じ主張に残っている

| 引用                         | v9.40 の実際                     | 正しい所在           |
| ---------------------------- | -------------------------------- | -------------------- |
| `book.cpp:705`（`:665`）     | Apery 定跡の `entries` ループ    | `misc.cpp:1133-1140` |
| `book.cpp:688-694`（`:743`） | Apery の分岐（`kAperyBookName`） | `book.cpp:406-409`   |

引用している `while (reader.ReadLine(line).is_ok())` という行は v9.40 の `book.cpp` に存在しない。
**同じ主張の出典が md と Rust で食い違ったままになった。**

### T-06 (MEDIUM, comment / oss-hygiene ×2): `union` の語が、定義元を書き換えた後も隣の行に残っている

`yaneuraou-db-parse.md:24`:

> 重複局面は minPly 側で**丸ごと置換**（こちらの union とは違う）

`union` はこの1行にしか残っていない。**こちらの挙動を `union` と定義していた `:19` が
「先勝ち」へ書き換わったので、行き先の無い参照になった。** 読み手は定義の無い `union` を
「集合の和」と解釈する —— それは r2 が誤りと判定した読み方。

### T-07 (MEDIUM, architecture / robustness / oss-hygiene ×2): 階層図の検査が情報文字列 `text` に暗黙で依存する

実測（README の ` ```text ` を差し替えて同じ判定を回した）:

````
```text        → missing: []
```（裸）      → missing: 14本すべて
```txt         → missing: 14本すべて
````

**図に14本全部が書いてある状態で、フェンスの情報文字列を落とすだけで
「階層図に無い表:」に14本が並ぶ。** 落ちた人は図を開いて全部あるのを見てから、
無いものを探して図をいじり始める。最短の緑化は第2引数を落とすことで、
それは R-04 が塞いだ例フェンスの穴を開け直す。

**「図は `text` で開く」はどこにも書いていない。** README にも `CONTRIBUTING.md` にも
失敗文言にも出てこない。r2 の R-06（`TABLES[0]` を並べ替えると嘘の理由で落ちる）と同じ形で、
**Rust 側だけガードを入れ、TS 側の同型には入れなかった。**

### T-08 (MEDIUM, robustness / oss-hygiene): `CONTRIBUTING.md` がゲートの分類から漏れている

```
CONTRIBUTING.md -> []
```

`ratchetIndex.test.ts` は `CONTRIBUTING.md` の表を正規表現で解析して落ちる**唯一の検査**で、
行を消す・見出しを変える・1列目のバッククォートを外すのどれでも赤くなる。
だが `CONTRIBUTING.md` だけのコミットは検証が1つも走らない。

**このラウンドの `ae3c478f`（`CONTRIBUTING.md` 1ファイルのみ）が、まさにその表を書き換えた
コミットで、ゲートは何も走らせていない。** r2 の見積り表は「`CONTRIBUTING.md`（順18）| ts | 1」と
書いていたので、走った前提で計画が立っていた。

### T-09 (MEDIUM, architecture / oss-hygiene): `CONTRIBUTING.md:300` が、`docs/` 全体に掛かる検査を名指していない

`stateTransitionIndex.test.ts` が持つ検査は5本。うち**相対リンクの検査は `docs/` 全体**。
索引の行は「状態遷移表を足して索引に載せ忘れること。参照リンクの定義が使用と食い違うこと」で、
逃げ道は「在庫表と階層図の両方に足す」。

`docs/spec/` でリンクを書き間違えて落ちた人は、**状態遷移表を1本も触っていないのに
「在庫表と階層図に足せ」と指示される。** `docs/**` を `ts` に入れた `3794e302` で、
この経路で落ちる人の母数が一気に増えた。

### T-10 (MEDIUM, robustness / comment): `FenceLine.info` の doc が実装と食い違う

> そのフェンスの情報文字列（```markdown の "markdown"）。**中身と閉じ記号にだけ付く**

`scanFences:275` は**開きの marker にも** `info` を入れている。`scanFences` は非公開なので、
この型の doc が唯一の契約。読み手が `kind === "marker" && info === x` で閉じだけを取ろうとすると
開きも返る —— **フェンスを1つ余分に取る側（＝緑で通る側）へ倒れる。**

### T-11 (MEDIUM, rust): `without_comments` を照合から外しても10本とも緑

実測: `missing_in(&text, &without_comments(&code))` → `missing_in(&text, &code)` に変えて
`cargo test --test state_table_terms` → **10 passed**。

r2 の計画が R-10 の欄に自分で書いたリスク（「剥がしを外しても緑なので、テストを1本足さないと
次に消される」）が塞がっていない。新設した
`a_declaration_inside_a_block_comment_is_not_a_declaration` は `without_comments` **単体**の
意味論しか固定していない。

### T-12 (MEDIUM, rust): `declared_constants` の doc が、起こりえない失敗を理由にしている

> 名前は語の区切りまでで切る。切らないと `MAX_MOVE` を書いた表が
> `MAX_MOVE_CHARS` の宣言行で通ってしまう

切らないと `declared` に入るのは `MAX_MOVE_CHARS:` なので、表の `MAX_MOVE` は
**依然として missing のまま。** 実際に壊れるのは逆で、**正しく宣言されている
`MAX_MOVE_CHARS` が「実装に無い」と誤って報告される。**

### T-13 (MEDIUM, rust): doc が「表ごと」と宣言している2つの性質を、suite が観測していない

2つの変異を掛けて実測。どちらも **10 passed**。

1. `declared` を全表の sources を束ねて作る（自然な最適化。`sfen.rs` は2つの表の sources に
   載っているので、いま同じファイルを2度読んでいる）
2. `every_table_yields_at_least_one_constant` の走査を `&TABLES[..1]` に縮める

(2) は同じ関数が自分で理由を書いている性質そのもの。**R-06 が潰した「添字で対象を選ぶ」形が、
隣の関数に残っている。**

### T-14 (MEDIUM, comment): `RICHEST` の名前と、その doc の件数に根拠が無い

- `RICHEST` は「最も多い」という**他の表との比較**を名乗るのに、assert が見るのは
  `TABLES` に居ることだけ。他の表の定数が増えれば名前が嘘になる
- doc の `（2件しか書いていない表がある）` は、r2 で5件直したのと**同じ「件数を doc に書く」形**
- 閾値 `5` の根拠がどこにも無い（現在値7）。6件に減ったときに下げてよいか判断できない

### T-15 (MEDIUM, comment): `a_declaration_inside_a_block_comment_is_not_a_declaration` が、名前の逆を最初に assert している

1本目の assert は `declared_constants` が**宣言として拾う**ことを固定している。
テスト名だけを一覧で見る人（`cargo test` の出力）は「`declared_constants` が処理している」と読み、
照合側の `without_comments` を冗長だと判断して外す。**r2 の R-10 が警告した経路そのもの。**

### T-16 (MEDIUM, comment): 「切り出す前は」という経緯が入り、`commentHistory` を素通りしている

`state_table_terms.rs`:

> 切り出す前は `contains` を `starts_with` に変えても全てのテストが緑で通っていた。

`CONTRIBUTING.md` の「元は〜だった」の禁止に当たる。`commentHistory` の `HISTORY_WORDS` は
`"る前は"` しか持たず、**`切り出す前は` は `す前は` なので素通りする。**

### T-17 (MEDIUM, rust / comment): 本家との差の doc が、その差を実装していない側の関数に付いている

先勝ちを決めているのは内側の `keep_first_of_each_move`。だが本家との差と「合わせない」判断は
外側の `keep_first_of_each_move_everywhere` の doc にある。**tie-break を変える人が触るのは
内側で、そこには理由が無い。**

加えて外側の doc は、その関数の存在理由（読み切った後に1回だけ畳む。二乗を避ける）が
3番目の段落に落ちた。関数名 `..._everywhere` が示す責務と doc の先頭がずれている。

### T-18 (MEDIUM, architecture): `scanFences` を「唯一の走査」と宣言したのに、`docsSourcePaths` だけがフェンスを見ない

実測:

````
"```markdown\n`src/background/book/yaneuraou.ts`\n```\n" → missing: [...]
````

`stateTransitionIndex` 側は `brokenLinksInBody` / `brokenReferencesInBody` の両方で
「例まで解決すると規約が書けなくなる」と明記して塞いでいる。**同じ `docs/` を走る2つの判定が、
markdown の同じ規則で割れている。** 規約の節に「悪い例」を並べると、規約ファイル自身が赤くなる。

### T-19 (MEDIUM, oss-hygiene): `YaneuraOuBookLib.py` は別リポジトリのファイルで、新しい版の注記がそれを覆った

`gh api repos/yaneurao/YaneuraOu/git/trees/v9.40?recursive=1` の `.py` は `script/` 6本と
`nnue_arch_gen.py` だけ。`YaneuraOuBookLib.py` は
**`yaneurao/YaneuraOu-ScriptCollection` の `CommonLib/YaneuraOuBookLib.py`**。

r2 で足した「**このファイルが引くやねうら王の行番号は、すべて `v9.40` 時点**」が、
別リポジトリのファイルまで覆っている。突き合わせる人は v9.40 を取って探し、**無い。**
リポジトリ名も版も書かれていないので、次にどこを探すかの手掛かりがゼロ。

### T-20 (MEDIUM, oss-hygiene): GPL-3.0 のコードを逐語で載せたのに、ライセンスと著作権者の記載が無い

`0a075714` が本文へ逐語のコードを入れた（`if (sfen.size() == 0) continue;`）。
`yaneurao/YaneuraOu` は **GPL-3.0**。このリポジトリは MIT で配布している。

`research/README.md:23` は自分でこう決めている:

> **引用元のライセンスと著作権者を、各ディレクトリの README に書く。**

`research/shogihome/README.md` はこれに従っているが、`docs/state-transitions/` には1行も無い。
**MIT の ShogiHome を引くときは帰属を書き、GPL-3.0 のやねうら王を引くときは書かない**という
逆向きの運用になっている。

### T-21 (MEDIUM, oss-hygiene): 一次資料の置き場と版の固定が、リポジトリ内で3通りに割れた

`docs/OPERATING-MODEL.md:46` は「他実装の観測事実は `research/`。版を固定して記録する」と定めている。
ところが r1〜r3 は `docs/state-transitions/` の中に**もう1つの一次資料レジストリ**を、
独自の版固定（タグ）・独自の参照名規約つきで作った。

結果、ShogiHome が repo 内で**3つの異なる版**で引かれている
（`research/findings/L3` の `f07e934a`、`research/shogihome/README.md` の `de27f0c1c352`、
状態遷移表の `v1.29.0`）。相互参照は無い。

### T-22 (MEDIUM, oss-hygiene): 「手順は SKILL.md」と宣言しているのに、SKILL.md に従うと必ず赤になる

`.claude/skills/state-transition-table/SKILL.md` の「置き場」は
`docs/state-transitions/<スライス名>.md` の1行だけ。**在庫表への登録も階層図への追記も無い。**

手順どおりに表を作って commit すると、`README がすべての表を列挙している` と
`README の階層図がすべての表を挙げている` の2本で落ちる。
r1 / r2 とも「未確認」に置いて持ち越した箇所で、その間に README 側だけがさらに厚くなった。

### T-23 (MEDIUM, robustness): 参照リンクの失敗に行番号も復帰手段も無い

出るのは `spec/screens/board.md  [s]  （定義が無い。…）` の1行だけ。
同じテストファイルの `staleUncreatedInBody` は行番号を返すのに、こちらは返さない。
地の文の角括弧2連（`[Ctrl][S]` など）は意図として受け入れた挙動だが、
**その逃げ道（行内コードで囲む）は doc コメントにしか書いていない。**

---

## 重複・矛盾した所見

- **T-01 は4人、T-03 は3人、T-06 / T-07 は2人ずつが独立に指した**
- **T-02 と T-01 は同じ根**（`docs/**` を `ts` へ広げたときに、掛かる範囲を3箇所で誤って書いた）
- **T-03 / T-04 / T-05 / T-17 は同じ根**（本家との差を Rust 側と md 側の2箇所で書いていて、
  片方だけ直った）。T-17 の「置き場を1つにする」を採ると残り3件の指摘箇所は消える
- **T-07 / T-13 / T-14 は同じ根**（r2 で潰した形が、隣の検査・隣の関数に残っている）
- 直し方が割れたもの: T-02 の shortcut 参照。(a) doc を実装に合わせる
  (b) 参照名の綴り規則（`<リポジトリ名>-<対象>`）で拾う。
  **(b) は規約に依存するので、規約が変わると黙る**

## 見ていない範囲

- `src-tauri/src/book/` 本体の振る舞い（doc の主張との突き合わせのみ）
- `docs/state-transitions/*.md` の表のセルが実装の挙動と合っているか
- `user_book1.db` の実測3行とピーク確保の実測（外部の実測。手元で再現していない）
- #386 / #387 / #342 へ送った所見

## lint / hook で強制できるもの

| 何を                                                     | どこで                                                              |
| -------------------------------------------------------- | ------------------------------------------------------------------- |
| `CONTRIBUTING.md` の変更で vitest を走らせる             | `gate_kinds_for_path` に case、`verify-gate.test.sh` に期待値       |
| 図が読めていない状態を、図から表が抜けた状態と別に落とす | `expect(named.size).toBeGreaterThan(0)` を先に置く                  |
| 照合が `without_comments` を通ること                     | `missing_in` 経由の assert を1行                                    |
| `declared` が表ごとであること                            | 各表 × 他表の sources で `missing_in` が非空                        |
| 「切り出す前は」型の経緯                                 | `commentHistory` の `HISTORY_WORDS` に `"す前は"`                   |
| 外部の行番号が2箇所に散らないこと                        | `src-tauri/src/**/*.rs` に `book.cpp:` / `misc.cpp:` が出たら落とす |
| `docs/` を読む検査が全部 `scanFences` を通ること         | `sourcePathsIn` の入り口で `stripFences`                            |

機械で守れないもの: T-03（理由が論理として成立しているか）、T-20（ライセンス記載の要否）、
T-21（一次資料の正をどちらにするか）、`CLAUDE.md` / `CONTRIBUTING.md` の文面の整合。

## 修正の結果

**案A を採った**（ユーザーの判断）。事実として誤っている10件だけ直し、残り13件を issue へ送った。

| 所見 | 結果                | 何をしたか                                                                             |
| ---- | ------------------- | -------------------------------------------------------------------------------------- |
| T-01 | 対応済み `daf44ec3` | `CLAUDE.md` の検証の節を分類へ合わせ、一覧を写さず出典を指す形に                       |
| T-02 | 対応済み `c2b9b42f` | ゲートのコメントの括弧の列挙を落とし、掛かる範囲（リンク）だけ残した                   |
| T-16 | 対応済み `d330bcbb` | 経緯コメントを、いまの構造への要求として書き直した                                     |
| T-12 | 対応済み `e2bbdacd` | 語の区切りで切る理由を、実際に起きる失敗（偽陽性）へ                                   |
| T-04 | 対応済み `950228da` | `flush` の差の記述を消し、1箇所への参照に                                              |
| T-05 | 対応済み `8297c32b` | 出典2件を `misc.cpp:1133-1140` / `book.cpp:406-409` へ。版の注記をモジュール doc に1つ |
| T-03 | 対応済み `c45f5ff3` | 「合わせない理由」を成り立つもの（畳む側が1パス）へ。順序依存は理由にならないと明記    |
| T-06 | 対応済み `38f3a97f` | `union` を「先勝ち」へ揃え、置換の単位の違いも書いた                                   |
| T-19 | 対応済み `f2b0229d` | `YaneuraOu-ScriptCollection` の外部リンクへ（SHA 固定）。版の注記を本体に限定          |
| T-20 | 対応済み `5626b84e` | 逐語で引く3リポジトリのライセンスを、`research/README.md` の規律を指す形で書いた       |

### issue へ送ったもの

| 所見                                                  | 行き先                                                                                         |
| ----------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| T-07 / T-10 / T-11 / T-13 / T-14 / T-15 / T-18 / T-23 | **#386**（3ラウンド連続で集まった機構。**割れが TS 側の2モジュール間にも出た**ことを追記した） |
| T-08 / T-09                                           | **#342**（`CONTRIBUTING.md` が分類外。`ae3c478f` が実際に検証0本で通った）                     |
| T-17 / T-21 / T-22                                    | **#290**（一次資料の正が2つ。SKILL.md に従うと必ず赤になる）                                   |

## 修正計画（r3 → r4）

### 対象そのものを疑ったか

**`/review-plan` 手順4 の打ち切り条件に到達した。** 所見は 22 → 18 → 23 で減っていない。

形を数えると、繰り返しているものがはっきりしている。

| 形                                               | r1  | r2  | r3                                                         |
| ------------------------------------------------ | --- | --- | ---------------------------------------------------------- |
| doc に列挙・件数・位置を書いたことによる腐り     | 3   | 5   | 7（T-02 / T-06 / T-12 / T-14 / T-16 / T-19 / T-22）        |
| 新しく足した／直した検査の厳密さ・偽陽性・黙る形 | 0   | 7   | 8（T-02 / T-07 / T-10 / T-11 / T-13 / T-15 / T-18 / T-23） |
| 同じ事実を2箇所に書いて片方だけ直した            | 2   | 3   | 6（T-01 / T-03 / T-04 / T-05 / T-09 / T-17）               |

**3ラウンドとも、直しそのものが次のラウンドの所見を作っている。**
r2 で足した検査5本のうち4本に所見が付き、r3 で直した文面のうち7箇所が新しい列挙を作った。

原因は機構ではなく**この PR の範囲**にある。

- **この PR の本体は合流である。** 手で書いた変更は当初5ファイル。
  そこに積み上がった40コミットの大半は、**合流が要求していない検査の強化と、
  その検査についての文章**になっている
- 検査を1本足すたびに、その検査の doc・失敗文言・索引（`CONTRIBUTING.md`）・
  入口（`CLAUDE.md`）・規約（`README.md`）の**5箇所に事実が増える。**
  T-01 / T-09 / T-22 はどれも「5箇所のうち1〜2箇所しか直さなかった」形
- **`/implement` 手順7 の「範囲の外はその場で直さない」を、レビューの所見に対しては
  適用してこなかった。** 所見が正しいことと、この PR で直すべきことは別

**落とす案**: 合流が要求していない検査の強化を、この PR から降ろす。
残すのは「**事実として誤っているもの**」だけ（T-01 / T-03 / T-04 / T-05 / T-06 / T-12 /
T-16 / T-19 / T-20 の9件）。それ以外の「より厳密にできる」は #386 へ送る。

**この判断はユーザーに委ねる**（`/implement` 手順7「直し方に判断が要る」）。
下の2案は、どちらを採るかで残りのラウンド数が桁で変わる。

### 案A: 誤りだけ直して合流を閉じる（推奨）

このラウンドで直すのは**事実として誤っている9件**だけ。

| 所見 | なぜ「誤り」か                                                         |
| ---- | ---------------------------------------------------------------------- |
| T-01 | `CLAUDE.md` が実装と逆のことを書いている                               |
| T-02 | ゲートのコメントが、掛かる範囲を誤って書いている                       |
| T-03 | 「合わせない理由」が論理として成立していない                           |
| T-04 | `flush` のコメントが本家の挙動を誤って書き、行番号が別の関数に食い込む |
| T-05 | Rust 側の出典2件が別の話題を指している                                 |
| T-06 | `union` が定義を失った参照になっている                                 |
| T-12 | doc が、起こりえない失敗を理由にしている                               |
| T-16 | 経緯コメントの混入（規約違反）                                         |
| T-19 | 別リポジトリのファイルに、誤った版の保証が掛かっている                 |

**T-20（GPL-3.0 の帰属）も足す。** 逐語引用を入れたのはこのラウンドの `0a075714` で、
`research/README.md` が自分で要求している規律。

残り13件（T-07 / T-08 / T-09 / T-10 / T-11 / T-13 / T-14 / T-15 / T-17 / T-18 / T-21 /
T-22 / T-23）は **#386 と #342 へ送る。** どれも「いま緑だが、より厳密にできる／
案内がもっと親切にできる」で、**合流の成立を妨げない。**

### 案B: 全23件を直す

r2 と同じやり方を続ける。**r4 で同じ形の所見が20件前後出ると見込む**（r2 → r3 の実績）。
根拠は上の表 —— 直しが次の所見を作る比率が3ラウンド安定している。

### このラウンドで直すもの（案A を採る場合の順）

機械で強制できるものから。**壊しうるものは各行に書く。**

| 順  | 所見                             | この直し方で壊しうるもの                                                                                                                                               |
| --- | -------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | T-01 `CLAUDE.md`                 | 検査名を書かない形にすると、**どの検査が掛かるかを知る経路が `verify-gate.test.sh` だけになる**。入口の doc としては薄くなる                                           |
| 2   | T-02 ゲートのコメント            | 括弧の列挙を落とすと、「なぜ `docs/` 全体なのか」の理由が消える。**掛かる範囲（リンクだけ）は残すこと**                                                                |
| 3   | T-16 経緯コメント                | 落とすだけ。壊しうるものは無い                                                                                                                                         |
| 4   | T-12 `declared_constants` の doc | 理由を逆へ直すと、**接頭辞の話が `missing_in` 側にしか無くなる**。参照を1本残す                                                                                        |
| 5   | T-04 `flush` のコメント          | 本家との差の記述を消して1箇所へ寄せると、**`flush` を読む人が差を知らないまま `append` を変えうる**。参照を1行残す                                                     |
| 6   | T-05 Rust 側の出典2件            | 直すと md と Rust に同じ出典が2箇所できる（T-17 の根）。**Rust 側は md への参照だけにする**なら T-17 も同時に閉じるが、それは案 B の範囲                               |
| 7   | T-03 「合わせない理由」          | 成り立つ理由が書けないなら「どちらでもよいので単純な側を採った。本家と差があることだけ記録する」と正直に書く。**その場合、次の人が本家に合わせに行くのを止められない** |
| 8   | T-06 `union`                     | 語を「先勝ち」に統一すると、ShogiHome の行の差の説明が1語で足りなくなる。置換の単位も書くこと                                                                          |
| 9   | T-19 `YaneuraOuBookLib.py`       | 外部リンクにすると参照リンクの検査に載る。**定義の置き場と参照名の規約に従うこと**                                                                                     |
| 10  | T-20 ライセンス                  | `research/README.md` の規律を参照する形にする。**規律の出所を2つにしない**                                                                                             |

### 直さないもの（案A を採る場合）

| 所見                                                  | 行き先                                                                             |
| ----------------------------------------------------- | ---------------------------------------------------------------------------------- |
| T-07 / T-10 / T-11 / T-13 / T-14 / T-15 / T-18 / T-23 | **#386**（検査の厳密さと置き場。3ラウンド連続で集まった機構そのもの）              |
| T-08 / T-09                                           | **#342**（ゲートの分類と索引。`CONTRIBUTING.md` が分類外なのは同じ根）             |
| T-17 / T-21 / T-22                                    | **#290**（docs が現物とずれている。一次資料の正をどちらにするか、SKILL.md の手順） |

### 次ラウンドの焦点

案A を採った場合、r4 は**直した10件だけ**を見る。焦点:

1. `CLAUDE.md` / ゲートのコメント / `CONTRIBUTING.md` の3箇所が、**同じことを言っているか**
2. 本家との差の記述が Rust と md のどちらに寄ったか。**片方だけ直っていないか**
3. T-03 の書き直しが、また反証できる理由になっていないか
4. **新しい列挙・件数・位置を書いていないか**（3ラウンド連続で出ている形）

### 検証の見積り

案A は10件。`.md` と `.rs` が半々で、`docs/**` は ts + rust を呼ぶので約8分。
案B は23件で、20分を超える。

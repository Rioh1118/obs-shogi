# レビュー 113-position-editor-entities

- 日付: 2026-09-11
- 範囲: `origin/main..HEAD`（基点 `4e5bc49c`）。#113 の C1〜C4 —— 手合割の共有／
  `positionDraft.ts`／`inspectPosition.ts`／状態遷移表。**画面はまだ1行も無い**
- 走らせた reviewer: architecture / comment / react / robustness / perf /
  oss-hygiene / rust（7つ。観点を絞らず）
- 対象コミット: `707a1a5b` `c836fd15` `5c20586f` `541b4d06`

## 入れた機械

| 何を                                                                      | 落とせるようになったもの                                                                                                                                          | 変異                                                                                         |
| ------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| `src/__tests__/crossSliceImports.test.ts`                                 | **同じ層の別スライスを読む import**（`entities/A` → `entities/B`）。13組を控えに置いて増減の両方で落とす。lint も `import/no-cycle` も1本も掛かっていなかった範囲 | 4件。新しい組を足す／束縛の無い `import "@/…"` で足す／走査が0件を返す／動的 import を見ない |
| `stateTransitionIndex` の `countClaimsIn`                                 | **表を指す散文の数**（「7状態 × 16事象」）と表の行数のずれ。**入れた時点で1件捕まえた**                                                                           | 走査が主張を1つも見つけない形を下限で止める                                                  |
| `stateTransitionIndex` の `tablesClaimedUnverified` / `unfilledCellCount` | **在庫表の摘要**が「未検証」と言っているのに本体が「埋まっていないセル」を1件も挙げていない形。箇条書きと表の両方を数える                                         | 摘要を1つも読めない形を下限で止める                                                          |
| `HandicapPreset` を一覧から導く／`Select` の総称化                        | 一覧に無い綴りが型に載ること、`Select` の呼び手が `as` を書くこと。**4呼び出し中3がキャストだった**                                                               | 3件。一覧に無い綴りを渡す／`satisfies` を外す／存在しない綴りを一覧に足す                    |

いずれも `.claude/knowledge/mechanization-backlog.md` の two-strikes に達していた項目
（同層横断は2件目、散文の数は4件目、在庫表の摘要は2件目）。

**測ったうえで入れなかったもの**（数字は backlog に書き戻した）。

- **本番 entry だけの knip 計測**（#171、2件目）。`--production` はこの構成で
  未使用 export 0件を返し、代わりに `shogi.js` を含む24件の「未使用の依存」を出す。
  手で書いた本番専用の構成でも163件（既定は179件）で、**測りたかった
  `positionDraft.ts` の約25の export がどちらの一覧にも出ない**
- **状態の記号の到達可能性の走査**。正しい指摘1件に対して、他の16表から
  誤検知が40件を超える（遷移の行き先を太字で書くのはこの表だけ）

## 直した所見

| 短ハッシュ | 何を                                                                                                                                                                                                |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `8fdb65f7` | `HandicapPreset` が `HIKY` を含むのに一覧に無く、型ガードが自分の型の値を弾いていた（architecture / comment / robustness が独立に指摘）。型を一覧から導き、`Select` を総称にしてキャスト3件を消した |
| `7238ab6e` | `stateFromSfen` が**返らない入力**（`b 250000P`）と**黙って別の局面になる入力**（3段の盤）を受けていた。doc の「読めない SFEN は throw する」が偽。返り値で断る形に変えた                           |
| `1a467cf5` | 裏返しの巡目を升ごとに覚える設計で、**無効化の呼び出し元が4箇所に散っていた**。3手で「押しても何も起きない」に落ちる再現つき。覚えを駒の姿と対にして照合する形にし、消す口を0にした                 |
| `51458d25` | `stateToShogi` の export で、唯一の不変条件をコメントだけで守っていた。答えだけ返す `isCheckOn` にして非公開に                                                                                      |
| `0649a979` | 盤の内外判定が同じスライスに2つ（差は `Number.isInteger` の有無だけ）。`BOARD_SIZE` の綴りも3通りあった                                                                                             |
| `c35cd73a` | `moveToHand` の第3引数が送り先か持ち主か決まらない。`sendToHand` へ改名して綴る。`HandKind` を `RawKind` に寄せた                                                                                   |
| `d458ddb2` | ファイル冒頭の `nextTurn` の断りが**編集モードでは成立しない**。逆に効いたまま衝突する `move` の強制成りが落ちていた。`capture` の向きの説明も逆                                                    |
| `95ce4e9d` | 「盤が空も起きない」の根拠2つがどちらも崩れる（空盤の SFEN は正当、玉の無い種なら全部送れる）。条件を実装に合わせて断りに足した                                                                     |
| `ce9bdc87` | 状態遷移表。**`B2` に入る遷移が1つも無い**（BLOCK）、面が2変数で排他になっていない、Esc が6通りなのに5と書いてある、決まっていない入口を前提にした列                                                |

## 提案どおりに直さなかったもの

- **`sfenToJkfInitial` を `stateFromSfen` に寄せる**（architecture / robustness）。
  変換が2実装で失敗の契約も玉の扱いも割れているのは事実。ただし呼び手
  （`SfenKifuCreateModal`）に触るのは C15 の範囲なので、そこで寄せる。
  `stateFromSfen` を `null` 返しにしたので、寄せる側の段差は既に無い
- **表の記号の到達可能性の走査**。上のとおり誤検知が本体を上回る

## 送ったもの

| 行き先                                                   | 何を                                                                                                                                                                                                  |
| -------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [#548](https://github.com/Rioh1118/obs-shogi/issues/548) | 駒箱を作らないので詰将棋を最初から組めない（#113 の受入条件の切り出し。ユーザーが②を選択）                                                                                                            |
| [#549](https://github.com/Rioh1118/obs-shogi/issues/549) | `entities/kifu/model/jkf.ts` に4スライスが依存。同層横断13組のうち4組がこの1ファイル。**判断が要る**ので選択肢を並べた                                                                                |
| `docs/IDEAS.md`                                          | `moveValidation.ts` の到達しない駒打ちの規則5つと、`inspectPosition` の規則の重複                                                                                                                     |
| この PR の C15                                           | `sfenToJkfInitial` を `stateFromSfen` へ寄せる                                                                                                                                                        |
| この PR の C17                                           | `docs/spec/features/position-edit.md` と `docs/spec/screens/create-file.md` が、この表と別の設計を書いている（`ModalType` を増やす案／`Shogi` を状態に持つ案／検査が未決／`move` の `nextTurn` の嘘） |

## 機械にできなかったもの

backlog へ回数を書き戻した。`@throws` の有無の静的検出は 7 → **8**
（`stateFromSfen` の doc が throw を主張していたのが実際は嘘だった形）。

`.claude/knowledge/mechanization-backlog.md` の「機械では止まらないもの」の
1（コメントが主張する条件と実装のずれ）と 4（一意性の断言）に、今回の所見の多くが当たる。
`d458ddb2` の2件はどちらも**外部ライブラリの分岐条件と散文の対応**で、走査にできない。

## 見ていない範囲

- **画面（C5〜C14）。この差分に1行も無い。** `inspectPosition` / `serializeDraft` が
  1レンダーに何回呼ばれるかは測れていない（1回あたりの値段は perf が測った ——
  `inspectPosition` 0.024ms、1フレームに効き始めるのは約690呼び出し/レンダーから）
- 課題局面の一覧が行ごとにプレビューを組む場合の件数依存。1件あたり
  `stateFromSfen` 0.42ms なので、100件を一度に組むと 42ms
- `src-tauri/` 側。この差分の Rust は `state_table_terms.rs` の1行のみ（rust reviewer: 所見なし）
- `docs/spec/` 本体。C17 の範囲

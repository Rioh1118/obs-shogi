# レビュー book-to-main ラウンド25

- 日付: 2026-09-05
- 範囲: r24 の所見6件を直した5コミット（`git diff 34d5840e 81cf3870`）
- 走らせた reviewer: architecture / oss-hygiene
- 対象コミット: `81cf3870`
- 前ラウンド: `.claude/reviews/2026-09-05-book-to-main-r24.md`

所見 **4件**（BLOCK 0 / HIGH 0 / MEDIUM 4）。
推移は 22 → … → 7 → 8 → 6 → **4**。

**HIGH が3ラウンドぶりに0。** 数え上げをやめた形（`0165bc18`）は、
oss-hygiene が5つの列挙を24文書ぶん検証して**漏れが無い**ことを確認した。

**4件のうち2件は報告書の数字**、1件は規約の唯一の反例、1件はテストの空白。

issue へ送るものは無かった。

---

## 所見

### AQ-04 (MEDIUM, 1人): 「どの表かを必ず添える」の唯一の反例が、この PR が持ち込むファイルに残っている

`yaneuraou_db.rs` の14箇所が `S` / `E` / `H` を裸で書いている。module doc は
「形式そのものの突き合わせは …」としか言わず、**記号と表を結び付けていない。**
`S` は8文書、`E` は10文書が別の意味で使う記号。

`0165bc18` は `session.rs` と `InlineNameEditor` を直したが、**件数が最も多いファイルが
未処理のまま残った。** `src/` 側は0件になったので、
**規約に対する唯一の反例がこのファイルだけ**になっていた。

- 結果: 対応済み `f83767e3`（`session.rs` と同じ形。module doc に1行置いて委ねる）

### AQ-01 (MEDIUM, 1人が実測): 「文字列を先に潰す」順が、どのテストにも固定されていない

`55dc0608` は語頭規則へ変えたときに順を仕様として宣言したが、
**逆順へ戻しても 272 本すべて緑**だった。足した4本はどれも `#` と引用符が同じ行に無い入力。

逆順だと**両方向に壊れる** —— 行の後半が corpus から消えて実在する識別子が「無い」になり、
引用符の中の名前は残って検査の期待値がソースに見える。
**いまの hooks に該当する行が0行なので、戻されても誰も気付かない。**

AP-05 で直したのと同じ形が、**同じラウンドの隣のコミット**に残っていた。

- 結果: 対応済み `70f2bb0e`（順に依存する入力を1本足し、**逆順で落ちることを確認**）

### AQ-02 / AQ-03 (MEDIUM, 1人が実測): r24 の報告書の数とヘッダ

- 「記号を持つ26表のうち8本と9本」→ 実測は**24文書のうち 8 と 10**。
  `S` の 8 を成り立たせる数え方で `E` を数えると 10、分母も 24。
  **同じ数え方から3つの数が出ていなかった**
- 「Rust の差分が無いので2人」→ `.rs` が3ファイル・4行入っている。
  しかもそのうち3行は AP-04 が指摘した対象そのもの。
  **r24 は AP-06 でヘッダの精度を要求する規約を自分で敷いた**

- 結果: 対応済み `0461f8d3`

---

## 重複・矛盾した所見

- **4件とも1人ずつ**
- **AQ-01 は AP-05 と同じ根**（新しく前提にしたものにテストが無い）。
  r24 で「テストが枝を突いていない」を直したのに、**同じラウンドの隣のコミットで再発**
- **AQ-02 / AQ-03 は AP-06 と同じ根**（報告書のヘッダと数）。こちらも直した次のラウンドで再発
- **数え上げをやめた判断は正しかった。** oss-hygiene が記号を定義している24文書の
  頭文字を全て数え、**2文書以上が使うのは `A` / `E` / `F` / `G` / `P` / `S` の6つだけ**で、
  残した5つの列挙に**漏れが無い**ことを確認した（`F` は同じ段落が別枠で扱っている）。
  意味が別であることも現物で確かめられた
- 所見の水増しは無かった。2人とも「確認して所見が無かったもの」を明記していて、とくに:
  - **`session.rs` の委譲が成立している** —— module doc は1〜25行目で読み手が先に通る位置にあり、
    このファイルが使う記号は `A` / `E` / `G` の3種だけで**過不足なく覆っている**
  - **失われた情報は無い** —— `game.md` × `game-session.md` の重なりは
    `game-session.md` と `session.rs` の2箇所に残り、**削除前より参照点が1つ増えている**
  - **`F-4` → `F-1` の差し替えは訂正だった** —— `F4` は `docs/` 全体に存在せず、
    実在する対は `F-1` / `F1`
  - **architecture が語頭規則を hooks 全行に当てて差分0行を再確認し**、
    発火した224行が全て純粋なコメント行であること、
    `docsIdentifiers` の corpus が1バイトも変わっていないことまで確かめた
  - **免除の4条件が、いまそれぞれ独立に固定されている**（条件を1つずつ落として
    6本 / 2本 / 3本 / 1本が落ちる。重複なし）

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送ったもの（再掲しない前提）
- `origin/main` が元から持っていた実装と doc の中身
- `docs/spec/screens/` の13面が現物の UI と合っているか
- `user_book1.db` の実測3行とピーク確保の実測
- 数字を持たない記号（`search.md` の `E` / `R` / `B` / `U` / `Y` など）の衝突。
  走査が `[A-Z][0-9]` の形に限られている

## lint / hook で強制できるもの

- **AQ-01 は変異で確かめたテストが入った**（順に依存する入力1本）
- AQ-04 の型（記号を裸で書いた注記）は `docsIdentifiers` / `stateTransitionIndex` の側で
  機械化できるが、**#351 へ送済みの候補と同じ型**
- AQ-02 / AQ-03（報告書の数とヘッダ）は機械で防げない

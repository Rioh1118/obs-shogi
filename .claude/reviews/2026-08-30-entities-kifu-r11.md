# レビュー entities-kifu ラウンド11（最終）

- 日付: 2026-08-30
- 範囲: `refactor/163-entities-kifu`（issue #163 / #164 / #165 / #166）の `origin/main` からの差分
- 対象コミット: `20ef833`
- 走らせた reviewer: robustness / comment
- 前ラウンド: `-r1.md` 〜 `-r10.md`

**このラウンドで打ち切った。所見はゼロではない。** 判断はユーザー。
残った所見は下の「送り先」のとおり issue へ回した。

## 所見

| 番号   | 深刻度 | reviewer   | 内容                                                                                                                                                                                                                                               | 結果                                                                                                |
| ------ | ------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| R11-01 | BLOCK  | comment    | 状態遷移表の不変条件1が「計画を辿る側は両方とも捨てる」と書いたが、捨てていたのは `computeLeafTesuu` だけ。**捨てていない `buildStreamRowsFromCursor` の方がレンダ中に裸で呼ばれる**ので、負の計画が1つ混じると `TypeError` で棋譜ペインごと消える | 直した `08acb97`（実棋譜で再現。ガードを入れ、この widget の lib に初めてテストを新設。変異で確認） |
| R11-02 | MEDIUM | comment    | `leafTesuu.test.ts` の fixture で本譜と変化の長さが同じだったため、「降りた葉」と「落ちた葉」がどちらも 3。**計画を丸ごと無視する実装でも全テストが通る**                                                                                          | 直した `3ec982e`（変化を1手にして別の値で固定。変異で確認）                                         |
| R11-03 | MEDIUM | comment    | ラウンド10のコミットタグと報告書の所見番号が対応しておらず、番号で grep しても差分に辿れない                                                                                                                                                       | 直した `3ec982e`（報告書に SHA を記載）                                                             |
| R11-04 | MEDIUM | robustness | `buildPlayer` の doc が「`tesuu` が一致するとは限らない」としか書かず、`tesuu` を比べれば分かると読める。実際は `goto` が `forkAndForward` の返り値を見ないので、**要求した `tesuu` ちょうどで別の線に着く**                                       | 直した `9fd4696`（`getTesuuPointer` で突き合わせるよう書き直し）                                    |
| R11-05 | MEDIUM | robustness | 不変条件1の担い手の列挙から `goto` 自身と `PositionSearchContinuation` が漏れている。同じ検査が3箇所に書き写された状態                                                                                                                             | doc を直した `9fd4696`。**寄せ直しは #213 へ**                                                      |

## 検証で所見にならなかったもの

robustness が差分ハーネスで確かめた。

| 検証                                                                    | 件数        | 結果                                                                                            |
| ----------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------- |
| R10-01 のガードで値を返すようになった入力の正しさ（独立オラクルと突合） | 30,000      | 値の差 **0**。オラクル不一致 **0**。差は「main が throw して HEAD が正しい値を返す」6,409件のみ |
| R10-02 の `for` 化で `continue` が増分を通るか                          | 境界3点     | 通る。上限は名前どおり **10000手ちょうど**まで                                                  |
| 分岐を何度も辿る棋譜での無限ループ                                      | —           | 起きない（`tesuu` も `plannedMap` のキーも単調増加）                                            |
| `plannedMap` の不正値が別の場所へ運ばれるか                             | —           | 運ばれない。`plannedCursor` は `cursorView` 内のローカル                                        |
| `branchEdit` の不変条件 fuzz                                            | 19,851 編集 | **0件**                                                                                         |
| `nextCursor` が実際に辿れる線と一致するか                               | 29,781 編集 | **0件**。退避が存在しない変化を指すことは無い                                                   |
| `sanitizeJkf` を parse の出口へ移した件（安全網の撤去）                 | —           | `state.jkf` の生成元は1本のみ。編集経路は空の変化を作らない                                     |

## 別の issue へ送る

| reviewer   | 内容                                                                                | issue       |
| ---------- | ----------------------------------------------------------------------------------- | ----------- |
| robustness | 壊れた `forkIndex` を捨てる境界を `goto` に渡す直前の1点に寄せる（同じ検査が3箇所） | #213 に追記 |
| robustness | `PositionSearchContinuation` の計画走査にガードが無い                               | #213 / #187 |

R11-05 の到達性は robustness が正直に書いている。**いまは踏めない**
（外部供給元は Rust の `fork_index: u32` で負も非整数も作れず、UI 経路はすべて配列添字）。
「doc が約束した保証が実装の半分にしか無い」ことの指摘であって、クラッシュ報告ではない。

## この PR の総括

11ラウンドで所見 100件。うち **PR 内で修正 94件**、issue へ 33件、反論1件。

自分が入れた回帰は3件で、すべてレビューが見つけた。

1. `useEffect` の統合で「開いた最初のフレームが古い値で描かれる」（r2 → r4 で発覚、範囲外と判断して巻き戻し）
2. コミットメッセージが実際の差分を上回った（r8 → r9 で発覚）
3. 状態遷移表が実装より安全側に書かれていた（r10 → r11 で発覚、**BLOCK**）

3件目は「表を書けば安心」ではないことの実例。表が実装より安全側だと、
読み手を「この経路は見なくてよい」と誤らせる。表そのものをレビューの対象にする必要がある。

## 見ていない範囲

- Rust 側。`src-tauri/` に差分が無いため `npm run verify:rust` は未実行
- **実機での操作確認。** 再現はすべて Node 上のハーネスとコード読解
- SCSS / レイアウト / a11y / perf（perf はラウンド6で総括）
- react / architecture（ラウンド9以降は所見が既送 issue に収束したため走らせていない）

## lint / hook で強制できるもの

11ラウンドを通して繰り返し挙がったもの。**どれも入っていない。**

| 提案                                                                            | 出た回数 | 何を止められるか                                  |
| ------------------------------------------------------------------------------- | -------- | ------------------------------------------------- |
| 未使用 export の検出（`knip` 等）                                               | 9        | 通算14件の死んだ export                           |
| `model/**` の doc から `lib/**` の識別子を参照することの禁止                    | 3        | doc が非公開関数を名指しして静かに腐る            |
| `as BranchIndex` / `as Candidates` / `as TesuuPointer` を所有ファイル以外で禁止 | 3        | brand の規約を機械で守る                          |
| `forkAndForward(x)` を `Number.isInteger(x) && x >= 0` 無しで書けなくする       | 3        | 4箇所目の書き写し（`PositionSearchContinuation`） |
| 「`throw` を含む関数を呼ぶ export に `@throws` があるか」                       | 7        | `@throws` の**有無**（内容の正しさは拾えない）    |
| `src/widgets/*/lib/**` → `../ui/**` の import 禁止                              | 2        | スライス内のセグメント逆流（#208）                |

拾えないものも記録しておく: `@throws` の内容が実際の throw と合っているか、
doc の主張が実装と合っているか、テストの assertion が実装差し替えで落ちるか。
**この PR で見つかった BLOCK / HIGH の大半はこの3つに属する。**

# レビュー book-to-main ラウンド41

- 日付: 2026-09-06
- 範囲: r40 の所見9件を直した8コミット（`git diff 91961a66 bd7626af`）
- 走らせた reviewer: rust / robustness
- 対象コミット: `bd7626af`
- 前ラウンド: `.claude/reviews/2026-09-06-book-to-main-r40.md`

所見 **13件**（BLOCK 0 / **HIGH 3** / MEDIUM 10）。
推移は 22 → … → 10 → 9 → **13**。

**13件すべてが r40 の直しから出た。**
「塞いだ」と書いた8つのうち、**6つが塞げていないか、別の穴を開けていた。**

---

## 所見（HIGH）

### BO-01 (HIGH, 1人が実測): 開く経路の注記が後置で、不変条件3が本番経路で成り立っていない

`annotate` は復帰操作の**後ろ**にパスの注記を足していた。

```
…別の定跡を開くこと（実体 /private/var/…）
```

**macOS では `/var` も `/tmp` も symlink** なので `canonicalize` の結果は
要求の綴りと必ず食い違い、**この枝に必ず入る。**
`open_at` は唯一の開き口なので、そこを通った失敗は**全部** `）` で終わる ——
**r40 で直した `UnsupportedFormat` も、実経路では元に戻っていた。**

`every_code_ends_with_something_the_user_can_do` は `open_reader` を直接叩くので
**この経路を1度も通らない。** 台帳 F-31 の主張は現状 false。

- 結果: 対応済み `e994c1f5`（`annotate_line` と同じ前置へ。
  `open_at` を通すテストを足した）

### BP-01 (HIGH, 1人が実測): 不変条件1を偽にしたのに、doc は「起きない」と書いたまま

r40 で S2 から alias を外したので、**`$(which git) ci -m x` は S1 も S2 も落ちて素通しする。**

実装のコメントは穴を自認しているのに、
**判定の全体を持つはずの表には1文字も無く、照合欄に ✓ が付いていた。**
次に触る人は「S2 が最後の網として完全」という前提で S1 側を削れる。

**r40 の BN-01 と同じ事故（表が実装より強いことを主張している）を、
今度は不変条件の側で作っていた。**

- 結果: 対応済み `56943fed`（限定を書き、✓ にも同じ限定を付けた）

### BP-02 (HIGH, 1人が実測): payload の形が変わると無音で全部素通しする

r40 は3つの危険（jq 不在／空／**形が変わる**）を挙げて deny 側へ倒したと書いたが、
**3つ目だけ塞がっていなかった。**

`jq -r … // ""` はフィールドが無くても成功するので、
「読めなかった」が「コマンドが空」にすり替わって `exit 0`。
**`tool_input.command` が改名された日、全 Bash 呼び出しに無言で通り続ける。**
足した入口ケース2本もこの枝を踏んでいない。

- 結果: 対応済み `56943fed`（`jq -er` にして deny 側へ。ケースを1本足した）

---

## 所見（MEDIUM）

### BP-03: 読むだけの動詞でも `--output=` で追跡ファイルを潰せる

許可正規表現は `>` を弾いているので「ファイルへ書かせない」は明示された意図だが、
`git diff --output=src/app/App.tsx && …` は記号なしで同じことをする。
**r40 が塞いだ形が、許可リストの内側に残っていた。**

- 結果: 対応済み `56943fed`

### BO-02: `Unknown` 枝を誰も見ておらず、「4枝とも落ちる」は3枝だった

`sample` は `None` にして「文面は `join_error` が組む」と書いたが、
`api.rs` にその経路を通す行が1本も無い。
**`Unknown` は blocking プールの panic でしか出ない＝再現の難しい失敗の唯一の案内**で、
崩れても気づかれない。

- 結果: 対応済み `ce5e5844`（文面の組み立てを `unknown_message` に切り出した）

### BO-03: `TooLarge` の「実ファイルを置けない」が偽、`Io` に理由が無い

`formats.rs` は sparse file で 2GiB を置いて実際に受け取っている。
代役として名指した既存テストは末尾を見ておらず（`contains("こと")`）、
**文面末尾に `（原文）` を足す変異が緑で通っていた。**

- 結果: 対応済み `ce5e5844`

### BO-04: `要らない` 枝を一度も通っていない

判定を緩めた理由（close の「操作は要らない」も正しい終わり方）は正しいが、
`sample` の `InvalidHandle` は「開き直すこと」側で、close 側は入らない。
**緩めた代価だけが残っていた。**

- 結果: 対応済み `ce5e5844`

### BO-06: separator の自己適用が包含の片側しか見ていない

「separator が引き金の部分文字列でない」だけを見ていたので、
**引き金より長い綴りを separator にすると同じ状態になる**
（実測で、固定名の違反が緑のまま通る）。
**BM-02 が塞いだのと同じ状態が再現する。**

- 結果: 対応済み `ce5e5844`（綴りの比較をやめ、
  **判定そのものに既知の違反を食わせる陽性対照**へ。判定を関数に切り出した）

### BO-05: `ALL_CODES` が手書きで、種別を足しても更新しなければ黙って外れる

r40 の所見の根は「実物を手で並べていた」ことで、
**その手書きは `seen` から `ALL_CODES` へ移っただけだった。**

- 結果: 対応済み `ec3e314a`（列挙と全件を1つの綴りから作る。
  **種別だけ足すとコンパイルが止まり、腕も足すと配列が自動で伸びて新しい文面が赤くなる**）

### BP-04 / BP-05 / BP-06 / BP-07: 表・入口・案内の食い違い4件

- **(B, S4) の段を取り違えていた** —— deny するのは S1 ではなく S3
- **J 行の ✓ に対応するケースが無かった** —— I を割ったとき根拠の無い ✓ が2つ増えた
- **`CLAUDE.md` が撤去した挙動を教えていた** —— 毎セッション読まれる入口
- **deny の案内が拒否リストの形** —— 実装は許可リストなので、
  `git ls-files &&` を打った人は並んだ7語のどれも使っておらず、
  **自分のコマンドのどこが悪いか特定できない**

- 結果: 対応済み `56943fed`（J 行はケースを3本足す側で対応）

---

## 重複・矛盾した所見

- **13件とも1人ずつ**
- **13件すべてが r40 の直しから出た。**
  「塞いだ」と書いた8つのうち**6つが塞げていないか、別の穴を開けていた** ——
  BM-01 は実経路を通らず（BO-01）、BM-02 は片側しか見ず（BO-06）、
  BN-02 は不変条件を偽にし（BP-01）、BN-03 は3つのうち1つを残し（BP-02）、
  BN-01 は許可リストの内側に穴を残し（BP-03）、
  BM-01 の「4枝とも落ちる」は3枝だった（BO-02）
- **同じ形が3ラウンド続いている** ——
  r39 で塞ぐ → r40 で「塞げていない」→ r41 で「その直しも塞げていない」。
  **共通するのは「テストが通る経路と、本番が通る経路が違う」こと**
- **BO-05 / BO-06 で、手書きの列と綴りの比較をやめた。**
  どちらも「網羅を人が保つ」形で、**移し替えただけでは消えない**
- **私自身が、このラウンドでゲートの誤発火に3度当たった** ——
  コマンドに `git commit` の綴りを含めただけで呼び出しが丸ごと拒否され、
  編集が適用されないまま進んだ。BP-07 が指す形そのもの
- 所見の水増しは無かった。2人とも「確認して所見が無かったもの」を明記していて、とくに:
  - **ツリーを変える git を手前に置く綴り31個は全て deny**（`apply` / `read-tree` /
    `clean` / `worktree add` / `submodule update` / `update-index` ほか）。
    **動詞の側では抜けていない**
  - **許可10語のうち `--output=` 以外にツリーへ届く経路は無い** ——
    `git fetch` は refs だけ、`git config -f <追跡ファイル>` は実測で書かない
  - **`gate_read_only_verbs` の alias 解決は全ケースで正しい**（引数付き・
    シェル alias・循環・連鎖・値に改行、いずれも安全側）
  - **`deny` の定義位置の移動に副作用は無い**
  - **`sample` の `match` は本物の forcing function**（種別を足すと `E0004`）
  - **`/tmp` 依存は消えた**
  - **BM-03 の3ホップは変異で赤くなる**（それぞれ単独で確認）
  - **`cargo doc` の4本は消え、増えてもいない**
  - **`PermissionDenied` を `None` にした理由は正しい**（root で通ってしまう）

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送ったもの（再掲しない前提）
- **`.claude/reviews/` の報告書**（4ラウンド続けて意図的に外している）
- `origin/main` が元から持っていた60本と、実装・doc の中身
- **`run_gate` が実際に検証を走らせて落ちる経路の中身**
- **Claude Code が渡す payload の実物のスキーマ**（#342 / #394 の範囲）
- **`git status` が失敗したときの素通し** —— 筋道を書けなかったので所見にしていない
- `yaneuraou_db.rs` / `sfen.rs` のテスト本体
- 実物の定跡ファイル、Windows 経路、外部リポジトリの一次資料
- 3重以上の変異

## lint / hook で強制できるもの

- **BO-05 / BO-06 は「人が保つ網羅」をやめた** ——
  宣言から導く／既知の入力を食わせる。移し替えでは消えない形だった
- **BO-02 / BO-03 / BO-04 は経路を通すテストで塞いだ**
- **BP-02 / BP-03 / BP-05 はケースで塞いだ**（入口1本＋`--output` 2本＋J 行3本）
- **`annotate` 型の後置は grep では止まらない** ——
  `BookError` に `note` 欄を分けて `Display` でだけ連結する形が唯一の機械的な歯止め。
  文字列を組む側で規約を守らせる限り、次の注記でまた取り残す
- **判定表の ✓ が実在ケースを指すことは機械では止まらない** ——
  行を割ると ✓ が自動で嘘になる。表の各行に `expect_*` を1本ずつ持たせ、
  **行ラベルをケース名として書く**以外に手が無い
- **`CLAUDE.md` と判定表の食い違いも止められない** ——
  同じコミットに入っているかは見られるが、内容の一致は見られない

# レビュー book-to-main ラウンド17

- 日付: 2026-09-04
- 範囲: r16 の所見3件を直した分（`git diff 5a6e6751 HEAD`）
- 走らせた reviewer: comment / oss-hygiene（差分が `.claude/reviews/` だけなので2人）
- 対象コミット: `b68c2f0c`
- 前ラウンド: `.claude/reviews/2026-09-04-book-to-main-r16.md`

所見 **3件**（BLOCK 0 / HIGH 0 / MEDIUM 3）。
推移は 22 → 18 → 23 → 10 → 12 → 7 → 5 → 4 → 6 → 8 → 9 → 9 → 6 → 7 → 7 → 3 → **3**。

**2ラウンド続けて、実装にも doc にも掛かる所見は0。** 3件とも記録側。

**このラウンドで、r16 に記録した実測が誤りだったことが分かった**（AH-03）。
robustness が「20通りすべてが deny される」と報告した計測は、**検証を常に失敗する形に
差し替えた版**で取ったもので、条件が違っていた。r15 の実測が正しい。

issue へ送るものは無かった。

---

## 所見

### AH-01 (MEDIUM, 2人): AG-03 が出所として名指した r9 に、剥がした全称が肯定形で残っている

r9 の Z-06 の括弧「後半の理由 —— 競合を抱えたツリーでしか使わない —— は `--skip` にも当たる」は、
**次のラウンドの AA-05 が偽として潰した命題を、reviewer の断定として肯定している。**
reviewer が再現 —— `git rebase -i` を `break` で止めた clean なツリーで `--skip` は rc=0 で通る。

AG-03 はこの段落を「偽の出所」の参照先として名指した。**指されたページを開いた人は、
同じ段落の2文あとで、まだ剥がしていない方の全称を読む。**
5ラウンド続いた理由（同じ命題が複数箇所にあって片方だけ直る）が、
剥がした側の索引ページ自身で再生産されていた。

- 結果: 対応済み `daf493bc`（括弧を削った。r16 の「出所**だけ**が手つかず」も縮めた）

### AH-03 (MEDIUM, 1人が再測): r16 に記録した実測が、条件の違う版で取ったものだった

「免除を潰した版で `docs/IDEAS.md` だけが `UU` の状態では20通りすべてが deny される」は、
**検証を常に失敗する形に差し替えた版**での計測。その条件では当然すべて deny になる。

r15 の AF-01 は同じ設定で標準出力0バイト・rc=0（素通し）を測っている。
oss-hygiene が現物で再確認 —— `docs/IDEAS.md` に競合マーカーを入れても `npm run verify` は
rc=0、`run_gate` が成功するので `exit 0`。

**この行は「3ラウンド往復した系統が止まった」という r16 の結論を支える実測の記述で、
しかも `1ca271b2` は AF-01 の表を根拠にした変更。** 次のラウンドが引くと、
3ラウンドかけて偽と確定した命題が実測の裏付きで復活する。

- 結果: 対応済み `78db7c61`（削って、落とした理由を書き残した。
  robustness の結論そのもの（所見なし）は残る2つの箇条書きで成立する）

### AH-02 (MEDIUM, 1人): 本文を条件付きに直した所見の見出しに、全称が残っていた

`8349177f` は本文を「検証が落ちるなら deny され、行き止まりになる」へ縮めたが、
**見出しだけ「全て塞いでいる」という無条件の全称のまま。**
免除前のゲートでも、種類を返さない変更しか無いツリーや clean な途中状態では素通しする。

- 結果: 対応済み `a7c861a6`（本文が言っているとおりの形へ）

---

## 重複・矛盾した所見

- **AH-01 は2人、AH-02 / AH-03 は1人ずつ**
- **AH-01 / AH-02 / AH-03 は同じ根。** どれも「本文（あるいは実装）は直したが、
  それを指す側・見出し・記録が古い」形。r13 以降ずっと同じで、
  **今回は3件とも `.claude/reviews/` の中に閉じている**
- **reviewer の実測を鵜呑みにしていたことが分かった。** AH-03 は、r16 が
  「robustness が全部実測に掛けた」と書いた実測の**測定条件を読まずに写した**もの。
  reviewer の出力に「`verify` は常に rc=1」と書いてあったのに、その意味を確かめていない。
  **前ラウンドの実測が前々ラウンドの実測と逆向きなら、条件を疑うこと**
- 所見の水増しは無かった。2人とも「確認して所見が無かったもの」を明記していて、とくに
  **oss-hygiene が `.claude/reviews/` 全体（book-to-main / book-foundation / 他シリーズ）を
  命題ごとに走査し、AH-01 以外に落とし残しが無いことを確認している。**
  併せて公開リポジトリとしての最終確認（`SECURITY.md` / テンプレート / CI / 帰属 /
  検査表の識別子の実在 / pre-commit の記述）も取り、新たな所見なしと返した

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送った17件（再掲しない前提）
- `src-tauri/src/book/` の振る舞い（4ラウンド続けて Rust の差分が無い）
- `user_book1.db` の実測3行とピーク確保の実測
- ShogiHome v1.29.0 側の `LICENSE` の綴り（reviewer がネットワークを持たず現物を取れていない）
- 本物の `UU` 状態での end-to-end 再現（作業ツリーに競合を作る必要があるため、
  競合マーカー入りのファイル＋ゲート関数の単体実行で同じ経路を確かめた）
- reviewer の人数の申告（個別の出力がリポジトリに残らないため検証できない）

## lint / hook で強制できるもの

このラウンドで新しく挙がったものは無い。3件とも意味の同一性の判定が要る形で、
機械では拾えない（AH-01 は過去の断定が後に偽と確定した命題と同義か、AH-03 は
実測と称する記述が別の実測と逆向きでないか、AH-02 は見出しと本文の量化子）。
掛け先はいずれにせよ CI 側で、#351 の範囲。

# レビュー 502-engine-never-returns ラウンド12

- 日付: 2026-09-09
- 範囲: `fix/441-stop-analysis-on-unmount...HEAD`
- 走らせた reviewer: architecture / react / robustness / comment / oss-hygiene
- 対象コミット: `d6b7660e`
- 前ラウンド: [r1](2026-09-08-502-engine-never-returns-r1.md) 〜 [r11](2026-09-08-502-engine-never-returns-r11.md)

## このラウンドで分かった一番大きなこと

**r8 から4ラウンド追っていた flake の機構が特定できた。**

architecture が全件を **29回**走らせて **7回**落とした。落ちたのは1つの時間帯（load average 12 台）に
固まっており、原因は**実時計の固定 sleep**。`entities/analysis/model/waits.ts` が
自分で予告していた形そのものだった:

> 並走する機械では**コードを1行も触っていないコミットが `verify` でランダムに落ちる**。
> 落ちた側は自分の変更が壊したと読む。

**私は「再現できない」を4ラウンド書き続けたが、単体で回していたので出なかった。**

もう1つ、**私のマージ検証は片方向しか取っていなかった**。r11 で「消えたのは4行だけ」と
結論したのは**自分の枝が失った行**だけを見た結果で、**基底側が失った行**を見ていなかった。

## 所見

### HIGH-1 テストスイートが非決定で、この PR が新設・変更したテストが落ちる（architecture。実測 7/29）

engine 側・解析側の両方で落ちた。**この PR の主張（#502 が直っている／世代の門が効いている）を
支えているのが、まさにその落ちるテスト群**。

### HIGH-2 世代の門のうち3つは変異が生き残る（robustness。実測）

r11 で私が書いた「観測できていないのは `finally` の同一性判定だけ」に**反例が3つ**。
うち `shutdown` の世代の繰り上げは**観測できる列が在る**——畳みを待っている間に起動が着地すると
`initialize_success` が通り、**利用者がもう捨てた設定**が `activeRuntime` に入る（不変条件1 の破れ）。

### HIGH-3 起動が重なると、参照されないエンジンのプロセスが1本残る（architecture）

`initialize_engine` が2本同時に飛ぶ列が在り、Rust 側は直列化していない。
台帳には2本載るが `engine_id` は最後の1本しか指さない。**画面には何も出ない。**
→ **#525**（差分の外）

### HIGH-4 `EngineInitializer` の契約が、3条とも現物と食い違う（comment / architecture / robustness）

r11 で私が「現物に合わせた」と書いた第3条が、**`shutdown_engine` は `Err` を返しうる**を
根拠にしていた。`EngineAnalyzer::shutdown` は常に `Ok` で、**r8 で一度潰した誤りを
別ファイルに書き直す形で戻していた**。第2条（待ってから畳む）は実装と逆、
第1条は記録先を `lastTriedRef` と書くが実際に食い違うのは `activeRuntime`。

### HIGH-5 「門は世代であって bool ではない」の根拠が、差が出る向きと逆（comment）

門の判定自体は `null` か否かで bool と同じ。世代が効くのは `finally` の所有者判定だけで、
そこは門を**より早く開ける**方向。**同じ1行について、コードは「潰すと #502 が再発する」、
doc は「潰しても結末は変わらない」と書いていた。**

### MEDIUM-6 〜 MEDIUM-13

- 新設2本の `not.toContain` が、**緑では空配列に当たる**（react）
- singleton の後始末が assert の後ろにあり、1本落ちると次が**足場で**落ちる（robustness / react。実測）
- `twoPendingStarts` の doc が、起こり得ない機構を理由に書いている（comment）
- 鏡4本を effect にした理由が、現物のどの経路にも載っていない（react / comment）
- `analysisRefusals` のコメントが、もう存在しない `ENGINE_FAILED_MESSAGE` を例に使う（comment）
- 「埋まっていないセル」が、※7 が明示的に否定した断定を ※7 の言葉として引く（oss-hygiene）
- `CONTRIBUTING.md` のラチェット命名から `.tsx` が落ち、機械より狭くなった（oss-hygiene）
- F-9 / §1 / G-6 が、同じファイルが義務づけている測定日を持たない（oss-hygiene）
- 仕様2枚が、**この PR 自身が直した**タブ名の欠陥を「いまも在る」と書く（oss-hygiene）
- `analysis-pane.md` が、自動再開の失敗で「解析中」が残ると書く（現物は「停止中」）（oss-hygiene）
- `analysis.md` が寸法を直値で書き、`waits.ts` の欄名と二重になった（oss-hygiene）
- engine の provider テスト2本が、41行の同じ足場を写している（architecture）
- `restart()` の署名が失敗の形を1つも表していない（architecture）

## 帰属の訂正（私が確かめた）

oss-hygiene の **BLOCK-2（マージがテスト2本を消した）と、`general` / 「解析中」 / S6 のセルの件は、
帰属が誤り**だった。**基底ブランチは私のマージ後にさらに12コミット進んでおり**（別のセッションが
並行して作業している）、それらは「マージが捨てた」のではなく「まだ取り込んでいない」もの。
取り込んだ地点（`1eaa35c3`）を基準に取り直して確かめた。

**ただし中身は正しい**——HEAD の doc が HEAD 自身のコードと食い違っているので、
基底を追いかけずに自分の枝で直した。

## 修正の結果（`/review-fix`）

| 所見                   | 結果                                                                                      |
| ---------------------- | ----------------------------------------------------------------------------------------- |
| HIGH-1                 | `8a1a37b8`。engine 側の待ちを**実時計から外した**（タイマーが無いので決定的に排出できる） |
| HIGH-2 / MEDIUM-6 / -7 | `8a1a37b8`。畳みの世代を見るテストを追加、後始末を `finally` へ                           |
| HIGH-3                 | **#525**（差分の外）。テストにも「本数は見ていない」を書いた                              |
| HIGH-4 / HIGH-5        | `39b5f4d0`。契約を3条とも現物へ、門の根拠を差が出る向きへ                                 |
| MEDIUM-8 〜 MEDIUM-13  | `39b5f4d0`                                                                                |

**変異を当てて確かめたもの**——世代の門5つすべてで、名指しした assert が落ちる:

| 変異                               | 落ちるもの                       |
| ---------------------------------- | -------------------------------- |
| `initialize` 成功側の照合を削る    | 新設2本とも                      |
| `initialize` 失敗側の照合を削る    | 「終端を名乗らない」             |
| 畳みの `dispatch` の門を外す       | 「起動し直す」                   |
| **畳みが世代を上げない**           | **「ready へ進まない」**（新設） |
| 起動の門を削る                     | StrictMode                       |
| `initializer` の追い越し判定を外す | 「起動し直す」                   |

**flake の検証**——負荷（`yes` 6本）をかけて engine を15回・全件を10回、**すべて緑**。
ただし**解析側の `advance()` は実時計のままで、こちらは触っていない**。
architecture が落とした解析側の4本は10回では再現しなかったが、**直したとは書けない。**

## 見ていない範囲

- `perf` / `ui` / `rust` reviewer は12ラウンドとも走らせていない
- **実プロセスでの確認は12ラウンドを通して1件も無い**（#525 もコードから導いた結論）
- **基底ブランチが12コミット進んでいる。** PR を出す前にもう一度取り込むこと
- 解析側のテストの非決定は残っている（`waits.ts` は基底の持ち物）

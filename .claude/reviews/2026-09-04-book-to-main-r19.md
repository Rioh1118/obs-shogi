# レビュー book-to-main ラウンド19

- 日付: 2026-09-04
- 範囲: r18 の所見2件を直した分（`git diff 7ae4f479 HEAD`）
- 走らせた reviewer: comment / oss-hygiene（差分が `.claude/reviews/` だけなので2人）
- 対象コミット: `d6336676`
- 前ラウンド: `.claude/reviews/2026-09-04-book-to-main-r18.md`

所見 **2件**（BLOCK 0 / HIGH 0 / MEDIUM 2）。
推移は 22 → 18 → 23 → 10 → 12 → 7 → 5 → 4 → 6 → 8 → 9 → 9 → 6 → 7 → 7 → 3 → 3 → 2 → **2**。

**4ラウンド続けて、実装にも doc にも掛かる所見は0。** 2件とも r18 の報告書の記述。

**2件とも「数・大きさを名乗った文が現物と合わない」形**で、どちらも名乗りを落として直した。

issue へ送るものは無かった。

---

## 所見

### AJ-01 (MEDIUM, 2人が実測): r18 の「148コミット」が実測と合わない

r18 の対象コミット時点で `git rev-list --count main..7ae4f479` は 156（非マージ 154）。
148 になる数え方が見つからない —— `origin/main` 起点も 156、`--cherry-pick --right-only` も同じ、
`(#N)` 付きを引いて 151、`.claude/reviews` 以外に触るものが 116。

**AH-03 と同じ形** —— reviewer が出した数を、確かめずに写した。

読み方は2つしかなく、どちらでも記録が偽になる —— (a) 数が誤りで実際は 156 を見た、
(b) 実際に 148 本しか見ておらず、残りの型は未確認なのに「PR 全体」と書いた。

- 結果: 対応済み `6e63fc0b`（数を書かない形へ。**型そのものは自分で数え直した** ——
  `main..HEAD` の非マージ 157 本すべてが `<type>: ` に一致し、外れるのは
  git が生成するマージ2件だけ。主張は真で、数だけが偽だった）

### AJ-02 (MEDIUM, 1人): 「どちらも1語の削除」が `8fdec979` の現物と違う

`9046b52a` は1語の削除だが、`8fdec979` は述語も置き換えている
（「一律に不可能になる」→「deny される」）。**見出しの主張が「使えなくなる」から
「ゲートが deny する」へ変わっている。**

「1語の削除」は編集が純粋に減らす向きだったことを主張する要約で、
このシリーズは AD-04（r12 の「新しい文は1つも書いていない」が偽）で同じ形を所見にしている。

- 結果: 対応済み `51747598`（編集の大きさを名乗らない形へ）

---

## 重複・矛盾した所見

- **AJ-01 は2人が独立に実測した。** AJ-02 は1人
- **2件とも同じ根**（数・大きさを名乗って、現物と合わない）。この PR で
  AF-06 → AG-01 → AJ-01 / AJ-02 と続いている形で、**名乗りを落とす以外の直し方が無い**
- 所見の水増しは無かった。2人とも「確認して所見が無かったもの」を明記していて、とくに:
  - **comment が `.claude/reviews/` の全シリーズの見出しを走査**し、全称語を含むものが
    すべて「被検査対象の主張の引用」か「実測どおりの記述」であることを確認した
    （`8fdec979` / `a7c861a6` が剥がしたのと同種の残りは無い）
  - **oss-hygiene が `yaneuraou-db-parse.md` の一次資料の表と `yaneuraou_db.rs` の
    実装を初めて突き合わせた** —— 実測3行（`493,157,464 B` / `2,252,118 局面` /
    `16,097,817 手`）が定数と一致、`3.32 GB` / `5,170 万局面` / `91.9%` の算術も一致、
    状態表 S0/S1/S2 の各セルと `parse_limited` の分岐も食い違いなし
  - **「同じ所見を2回直したコミットが無い」を `LC_ALL=C` で数え直した**
    （既定のロケールでは日本語の見出しが誤って同一視される）

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送った17件（再掲しない前提）
- **`user_book1.db` の実測3行とピーク確保の実測** —— 現物がリポジトリに無く、
  `find` で0件。**確かめられない**（doc 自身も「機械では守れない」と明示している）
- `src-tauri/src/book/` のうち `session.rs` / `open.rs` / `api.rs` / `error.rs` の振る舞い
- 上流の `LICENSE` の再照合（r18 で1人が取った。今回の2人はネットワークを使っていない）
- reviewer の人数の申告（個別の出力がリポジトリに残らないため検証できない）

## lint / hook で強制できるもの

**AJ-01 の型（報告書が名乗るコミット数と `git rev-list --count` の食い違い）は機械で数えられる。**
報告書の `対象コミット` を解決して本文の数と突き合わせる形。ただし `.claude/reviews/` は
`gate_kinds_for_path` が種類を返さないので掛け先は CI 側で、**r14 以降ずっと #351 の候補**。
このラウンドでも足さない（案A の方針）。

機械で守れないもの: AJ-02（編集の大きさの言い換えが現物と合うか）。

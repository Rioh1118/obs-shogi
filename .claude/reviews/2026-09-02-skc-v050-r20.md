# レビュー skc-v050 ラウンド20

- 日付: 2026-09-02
- 範囲: `git diff 35e8a11..a7372a2`（ラウンド19 の修正6コミット）
- 走らせた reviewer: rust / robustness / comment
- 対象コミット: `a7372a2`
- 前ラウンド: `.claude/reviews/2026-09-02-skc-v050-r19.md`

perf は走らせていない（r19 と同じ理由）。**コーパスの実測は自分で取った** —
608 / empty 0 / 87,277エントリで r17 から変化なし。

## 所見

**16件。BLOCK 2 / HIGH 4 / MEDIUM 10。**

### 3人が同じ退行を指した

| 番号            | reviewer                                   | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --------------- | ------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R20-01（BLOCK） | rust / robustness / comment（**3人一致**） | **r19 が検査を `says_nothing` の後ろへ動かしたので、その門に掛かる CSA では警告が作られない。** 掛かるのは「ヘッダが1行も無い・平手・1手目で切れた」形で、**アプリ自身が対局者名なしで書き出す `.csa` がちょうどこれ**（`try_to_csa_owned` は空のヘッダを書かないので `V2.2\nPI\n+\n`）。実測 `moves=1 / header={} / says_nothing=true`。200手あっても索引に1局面も入らず、何も出ない。**r18 までは `ParseFailed` で警告が出ていた入力**。しかも `warn_if_moves_were_dropped` の doc とテストの doc が、両方ともこの形を**機構を作る理由として**名指ししている（テストの題材4件は全部 `N+山田` 入りで門を踏んでいない） |

### それ以外

| 番号             | reviewer          | 内容                                                                                                                                                                                                                          |
| ---------------- | ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R20-02（BLOCK）  | comment           | `%` で打ち切る腕の行内コメント「終局の行より後ろは、この記録の指し手ではない」が偽。`csa` の `move_records` は特殊手の後ろも読む（実測: `%MATTA` の後ろに2手 → `read=3`）。**同じ関数の doc が6行上で自分で反例を挙げている** |
| R20-03（HIGH）   | robustness        | **1手も読めていない CSA が、平手の初期局面1件だけを索引に入れるようになった。** r18 までは `Err` で0件。`NothingToIndex` の doc が「平手の初期局面で検索したときに全部ヒットし…だから入れない」と書いた当のもの               |
| R20-04（HIGH）   | comment           | `warn_if_moves_were_dropped` の doc が、**この関数に存在しない引数 `path`** を約束の主語にしている（検査を移したときの直し漏れ）                                                                                              |
| R20-05（HIGH）   | comment           | テストの doc「素通しを支えているのは `%` の打ち切り1箇所だけ」が、5題材のうち1つで偽。`{whole}-3122GI\n` は**クレートもその行を1手として読む**ので数が揃い、打ち切りを外しても黙る                                            |
| R20-06（HIGH）   | comment           | doc・テスト名が「黙らない」と宣言しているのに、doc 自身が挙げた最悪の形（R20-01）は黙る                                                                                                                                       |
| R20-07（MEDIUM） | rust / robustness | `read_path_to_jkf` は**本番の呼び手が0件**なのに `pub` で、`# Errors` の表もこちらに付いたまま。次に棋譜を読む人がここを入口に選ぶと**警告が黙って消える**。`dead_code` は `pub` を見ない                                     |
| R20-08（MEDIUM） | rust              | 早返しを消したので、**健全な CSA も必ず2度読まれる**ようになった。r19 の変異テストの結論（redundant）は正しさについてだけで、費用は逆。doc は「`open` は増えない」としか言っていない                                          |
| R20-09（MEDIUM） | rust              | 版の等号を見る `const _` は `#[cfg(test)]` の中なので **`cargo build` では落ちない**。doc は「ビルドが落ちる」と書く。留めているのは言語ではなく `verify-gate.sh`                                                             |
| R20-10（MEDIUM） | robustness        | `search.md` の「呼び手は**戻り値を見ずに**」が偽。`run_rescan_diff_apply` は `()` を返すので、呼び手には失敗と「差分0」を区別する手段が無い。早期 return の2つも出方が違う（`root_dir` が `None` の腕は無音）                 |
| R20-11（MEDIUM） | robustness        | 整えた日本語の警告のすぐ隣に `CursorLite { .. }` の Debug と `ApplyError` の英語が並ぶ。r19 の `chain` で1本のリストに合流させたので不揃いが見える形になった                                                                  |
| R20-12（MEDIUM） | comment           | `search.md` の「変化はすべて入る」が2文あとで自分に否定されている（`break` は変化の内側でも効く）。見出しの `Ok(jkf)` も表の行名と揃っていない                                                                                |
| R20-13（MEDIUM） | comment           | 「断らない理由」が5箇所に言い換えで書かれている。R19-14（逐語重複）を直した直後に、逐語でない重複が増えた                                                                                                                     |
| R20-14（MEDIUM） | comment           | 読む口が3つになり、名前が返すものと対応していない（`read_to_jkf` はもう `Jkf` を返さない）                                                                                                                                    |
| R20-15（MEDIUM） | comment           | `unreadable_record` の doc が `read_path_to_jkf` から直に呼ばれると書くが、呼び口は `read_path_inner`                                                                                                                         |
| R20-16（MEDIUM） | robustness        | 文言の `K` は常に `N+1` で、2つの数の差に意味があると読みうる。`read == 0` のとき「0 手までしか読めませんでした」になる                                                                                                       |

## 確かめて「穴が無かった」もの

- **`%MATTA` / `%CHUDAN` は数え落として黙る**（doc の記述どおり）。終局行の無い記録が2つ繋がると余計な警告が1件出るのも doc どおり。**doc と実装は一致している**
- **`strip_suffix(b"\r")` の削除は CRLF を壊さない。** `\r` は行末にしか来ないので `starts_with(b"%")` の結果を変えられない
- **`warns` の取りこぼしは無い。** `api.rs` / `project_manager.rs` はどちらも読み手の warns と `BuildWarn` を `chain` して同じ口へ流す。順序（ファイル全体 → 個々の手）も妥当
- **文言が挙げた4つの原因は全部実在した**（行末の空白・タブ、`'` だけの行、末尾の改行なし、カンマ）。「アポストロフィ**だけ**の行」という限定も正しい（`'x` は止まらない）
- **`ReadOutcome` に無駄な確保は無い。** `Jkf` は move、`warns` は空なら確保なし
- **200件の上限は悪化していない。** 切れた CSA は以前も1件の警告を出していた
- **R19-15 の直し（変化は歩かれる）は実装どおり**（`forks` は本線の手を指す前に再帰）
- **等号の `const _` は上げ・下げの両方でコンパイルを止める**（実測）

## 対象そのものを疑ったか

**疑ったが、今回は機構ではなく「腕の順序」だった。**

R20-01 は `warn_if_moves_were_dropped` の中身の問題ではなく、
**`read_path_inner` の中で門と検査をどちらが先に見るか**の問題。
r19 で出口を変えたときに位置も動かしてしまったのが原因で、機構そのものは
r19 の作り替えで正しくなっている（`%MATTA` の扱い・誤検知の向き・
doc の記述が今回すべて「穴が無かった」側に入った）。

**所見の総数は 17 → 16 でほぼ横ばいだが、中身が変わった。**
r19 は12件が1つの関数に集中していたのに対し、r20 は最大でも
`read_path_inner` 周辺の4件（R20-01 / 03 / 06 / 07）。残りは doc の精度。

## 見ていない範囲

- **実在の CSA コーパスが無い**（`~/Desktop/temp` に `.csa` は0件）。題材はすべて合成
- perf を走らせていない。R20-08（全件2度読み）の**桁は測っていない**（向きだけ）
- `index_cache.rs` の `encode_all` / `decode_all` 本体、`index_store.rs` / `segment.rs` /
  `query_service.rs` / `node_table.rs` / `file_table.rs` / `fs_scan.rs`
- `%MATTA` / `%CHUDAN` 以外の CSA 特殊手でクレートが読み続けるかは未実測
- TypeScript 側は `reducer.ts` の `index_warn` と `WorkspaceTab.tsx` の描画だけ
- 計測はすべて macOS

## lint / hook で強制できるもの

- **`EVT_INDEX_WARN` に流れる文字列に `{:?}` を書かせない検査。** R20-11 は止まる。
  **今回 `BuildWarn::to_user_message` に括り出した**ので、掛ける先が1箇所になった
- **本番から呼ばれない `pub fn` の検出。** R20-07 は止まる。
  **今回は `#[cfg(test)]` に落として構造で閉じた**
- **doc 内のバッククォート識別子が、その関数の引数／同一 crate に実在するかの走査。**
  R20-04（存在しない引数名 `path`）は止まる。`cargo doc` の intra-doc link は引数名を見ない
- **doc の逐語重複検査は R20-13 を止めない**（言い換えなので）
- **`cargo-mutants`（#332）は R20-01 を見つけない。** 2つのブロックの順序を
  入れ替える変異は生成されない。**17ラウンド連続の提案**だが、この件は拾えない
- 止まらないもの: R20-01 / 02 / 03 / 05 / 12（外部形式の意味・腕の順序・doc の主張）

## 修正計画（r20 → r21）

### 束

| 束                 | 所見                                 | 関係                                                                                                           |
| ------------------ | ------------------------------------ | -------------------------------------------------------------------------------------------------------------- |
| **門と検査の順序** | R20-01 → R20-03 / R20-06             | 順序を戻すと R20-06 の指摘箇所が消える。R20-03 は同じ場所で一緒に決める（1手も読めていないなら局面を入れない） |
| **口の整理**       | R20-07 → R20-14 / R20-15             | `read_path_to_jkf` を落とすと、名前の対応と doc の参照先が同時に決まる                                         |
| **doc の精度**     | R20-02 / 04 / 05 / 08 / 09 / 12 / 16 | 互いに独立                                                                                                     |

### このラウンドで直したもの

| 順  | 所見                            | 結果                                                                                                                                                                                                                               |
| --- | ------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | R20-01 / R20-03 / R20-06        | 検査を `says_nothing` より**前**に戻し、`NothingToIndex` に `warn` を持たせた。1手も読めていないなら局面を入れない。**題材をヘッダあり／なしの2軸 × 壊し方4通りに広げ、順序を r19 に戻す変異で `ヘッダなし` 側が落ちることを確認** |
| 2   | R20-07 / R20-14 / R20-15        | `read_path_to_jkf` を `#[cfg(test)]` に落とし、表と `# Errors` を `read_to_jkf` へ移した。`unreadable_record` の参照先も直した                                                                                                     |
| 3   | R20-11                          | `BuildWarn::to_user_message` に括り出した。呼び手2つの写経も消えた                                                                                                                                                                 |
| 4   | R20-02 / 04 / 05 / 08 / 09 / 12 | doc を実装に合わせた                                                                                                                                                                                                               |
| 5   | R20-10                          | `()` を返すので呼び手に手段が無いこと、早期 return の片方が無音であることを書いた                                                                                                                                                  |

### 直さないもの

| 所見        | 行き先          | 理由                                                                                                                                                                                               |
| ----------- | --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R20-13      | **r21**         | 「断らない理由」の5箇所。1箇所に寄せる作業で、**今回 doc を何度も書き換えたので、落ち着いてから畳む**                                                                                              |
| R20-16      | **見送り**      | `K` は `N+1` で冗長だが、行番号 `M` と手数を両方出すのは利用者が原因に辿り着くのに要る。`read == 0` の日本語は「0 手までしか読めませんでした」で不自然だが誤りではない。**所見に反論を書いて残す** |
| R20-08 の桁 | **r21 で perf** | 全件2度読みの費用。向きは doc に書いたが測っていない                                                                                                                                               |

### 次ラウンドの焦点

1. **順序を戻したことで、`says_nothing` の門が本来の仕事をしているか。**
   アプリが作る「本当に空」の `.csa` / `.kif` / `.ki2` / `.jkf` が、
   いまも**黙って** `NothingToIndex` になるか（警告が出たら退行）
2. **`NothingToIndex { warn }` の呼び手2つ。** `warn` が `None` のとき本当に何も出ないか
3. **1手も読めていない判定**（`no_move_was_read`）が、正当な「0手の棋譜」を
   巻き込んでいないか。終局だけの記録・ヘッダだけの記録
4. `BuildWarn::to_user_message` の文言が、反則手を含む棋譜で意味を成すか
5. **doc を今回また大きく書き換えた。** 実装との突き合わせ

### 検証の見積り

- Rust: 3コミット。増分 40〜60 秒 → 約3分
- `.md`: 上のコミットに同梱

## r19 への訂正

**r19 の報告書「修正結果」に「出口を警告に変えたので沈黙の被害は消えた」と書いたが、
正確ではない。** 検査の位置も一緒に動かしたため、
**ヘッダを持たない CSA については沈黙が復活していた**（R20-01）。
消えたのは「ヘッダを持つ CSA が断られて局面ごと消える」ほうだけ。

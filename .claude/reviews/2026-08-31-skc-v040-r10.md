# レビュー skc-v040 ラウンド10

- 日付: 2026-08-31
- 範囲: ラウンド9の修正ぶん＋この PR の最終状態
- 走らせた reviewer: comment / robustness / rust

## 所見

### [HIGH] R10-01 同点のとき候補は**まだ並び順**で決まる

- reviewer: **robustness / rust が別々に実測**
- `lines > *best` は狭義比較。改行が1つも無い棋譜（1行の KI2、CR だけで区切る古い棋譜）は
  正しい読み方も1行なので**必ず同点**になり、配列の先頭が勝つ
- BOM 無し UTF-16BE の1行 KI2 で `▲パス` ではなく `눥៿浑楫` が出る
- **r9 の「並べ替えは等価変異」は複数行の題材でしか成り立っていなかった**

### [HIGH] R10-02 ISO-2022-JP に 8bit が1つ混ざると、化けた行を名指しする

- reviewer: robustness（実測）
- r9 で足した 7bit の条件が、「別の文字コードが混ざっている」の検出経路を消していた。
  印を失い、クレートの Shift_JIS 読みが勝って**何ともない行を生の ESC 込みで**出す

### [MEDIUM] R10-03 0バイトの棋譜が「平手の初期局面1件」として警告ゼロで索引に入る

- reviewer: robustness（実測）
- **平手の初期局面で検索すると壊れた 0バイトファイルが全部ヒットする**。
  9ラウンドかけて「読めなかったときに何と言うか」を磨いたが、
  **読めてはいけないものが読めている**ほうは一度も見ていなかった

### [MEDIUM] R10-04 警告本文に長さの上限が無い

- reviewer: robustness（実測 200,050 文字）
- クレートのエラーは読めなかった位置から**行末まで**引用する。
  改行の無い大きなファイルでは中身がそのまま文言になり、
  webview の state に200件まで溜まる。生の NUL や ESC も入る

### [MEDIUM] R10-05 `repeat_n` は Rust 1.82 以降。MSRV は 1.77.2

- reviewer: rust（別クレートで再現）
- **`clippy::incompatible_msrv` はテストの中を既定で見ない**ので素通りしていた。
  r9 が同じファイルの `is_none_or` をわざわざ降ろした直後に、別の 1.82 API が残っていた

### [BLOCK] R10-06 消した `looks_like_text` を指す doc が1本残っている

- reviewer: **3人とも**
- R7-03（NUL の並び）・R8-02（`detect_encoding`）と**同じ形で3回目**。
  しかも今回は**その故障を直したコミット自身が新しい1件を作った**

### [MEDIUM] R10-07 `VERSION` の doc は r9 で直っていない

- reviewer: comment（`git show --stat` で発見）
- **r9 の報告書は「修正済み」と書いていたが、コミットは `kifu_reader.rs` 1ファイルだけ**。
  報告書の「修正済み」欄を実体で確かめていなかった

### [MEDIUM] R10-08 `can_be_named` の doc が全称で、ISO-2022-JP の規則と矛盾する

- reviewer: comment

### [MEDIUM] R10-09 「読めなかったファイルは索引に入らない」が全件構築で成り立たない

- reviewer: robustness
- `api.rs` は `ok` フラグを計算しているのに `batch.push` の判断に使っていない。
  差分更新（`project_manager.rs`）は `return None` する

### [MEDIUM] R10-10 「棋譜でないファイルだと言う」とテストの doc が名乗るが、実装は言わない

- reviewer: robustness

## 重複・矛盾した所見

- R10-01 は robustness と rust が別々の題材で実測。
  **直し方も一致**（同点なら決めない）
- R10-06 は3人が挙げた

## 範囲外（issue へ送る）

| 内容                                         | 送り先           |
| -------------------------------------------- | ---------------- |
| 読めなかった棋譜の索引への残り方が経路で違う | **#333（新規）** |
| 「拡張子が中身と合っているか」まで案内するか | #327             |

## レビュー中の事故（2回目）

r9 に続き、rust-reviewer が「レビュー中にワークツリーが書き換わっていた」と報告した。
**robustness-reviewer が変異を当てていたため**（今回はこちらからは変異を走らせていない）。
robustness は毎回 `git checkout` で戻し、最後に clean を確認している。
rust は `git show HEAD:` で取り直して所見を出した。
**次からは変異を当てる reviewer を1人に絞り、他には明示的に「書き換えない」と伝える。**

## lint / hook で強制できるもの

- `clippy.toml` に `check-incompatible-msrv-in-tests = true` → **R10-05 は機械で落ちる**
- doc の識別子をイントラドキュメントリンクにして
  `#![deny(rustdoc::broken_intra_doc_links)]` → **R10-06 は機械で落ちる**。3回目の提案
- `cargo-mutants`（#332）→ R10-01。**6ラウンド連続の提案**
- **報告書の「修正済み」を `git show --stat` で確かめる** → R10-07。
  これは `/review-fix` の手順に足すべきこと

## 次ラウンドの対象

10件すべて直す。範囲外は #333 のみ。

# レビュー 410-cap-sfen-spelling ラウンド8

- 日付: 2026-09-07
- 範囲: `fix/410-cap-sfen-parse-error`（`origin/main` 2273b3a0 との差分、44コミット）
- 走らせた reviewer: robustness / comment
  （rust は r7 で「所見なし」、architecture は r6 で「焦点に新しい所見は無い」）
- 対象コミット: `畳み込み済み`

**r7 の焦点への答え**:

1. join error 3本が基準を満たすか → **満たす**。定型部分は索引側68文字・検索側36文字で、
   案内は全部先頭にあり刈られない（自分でも数えた）
2. `EMBEDDED_VALUE_LIMIT` への改名で振る舞いが変わっていないか → **変わっていない**
3. 新しく黙らせた失敗 → **無い**（robustness が追加行の `let _` / `.ok()` / `map_err` を全部当たった）

## 所見

### [MEDIUM] R8-01 200件を超えると画面の5行が新着ごとにずれる。doc は「先頭5件で固まる」と書いた（robustness）

`slice(-199)` は**新しい200件を残す窓**で、追記だけではない。200件に達した後は
警告が1件届くたびに先頭が落ちるので、`slice(0, 5)` が描く5行が**毎回ひとつずつ入れ替わる**。

**自分でも数えて確認した**（201件目で `[2,3,4,5,6]`、205件目で `[6,7,8,9,10]`）。
読めない棋譜が300個あるフォルダでは、構築中に文字が流れて1件も読み切れず、
終わったときに残るのは**最初でも最後でもない中途半端な5件**。

### [MEDIUM] R8-02 状態遷移表の直しが、前のコミットに入っていなかった（comment）

**私のコミットメッセージが嘘だった。** `畳み込み済み` は本文に「状態遷移表の鍵を5行とも
同じ粒度に」と書いたが、`git show --stat` に `search.md` が無い。置換が静かに空振りしていた。

結果、1行目の鍵（`Ok`、`warns` 空）が4行目を文字通り含んだまま、局面の欄は
「入る」と「入らない」で正反対。**この表が自分の存在理由として書いている用途で誤読させる。**

### [MEDIUM] R8-03 `EMBEDDED_VALUE_LIMIT` という名前が、r7 で直したはずの誤読を作り直していた（comment）

受け皿は書かれた文字を区別せずに数えるので、**案内も引用も同じ枠を食い合う**。
「埋める値1つあたり」と読んだ人が案内を250文字書くと、理由には50文字しか残らない。
R5-02 / R6-01 で2回踏んだ「案内が引用に押し出される」を、**名前が誘発する**形になっていた。

**r7 で `MESSAGE_LIMIT` から改名したのは私の判断**で、r7 の指摘（doc が名前を打ち消している）
自体は正しかったが、**寄せた先の名前が間違っていた。**

### [MEDIUM] R8-04 `parse_failed` の doc が `EVT_INDEX_WARN` の文言を「他の2つ」と数えていた（comment）

いま8つあり、**うち4つはこの差分が書いた**。読者はここで数えて2つ確かめて済ませる。

## 所見なし の申告

- 無し（2人とも所見あり）

## 見ていない範囲

- アプリを起動した実測は無い（2人とも）
- `cargo audit` / `npm audit` は未実行
- `search/cache` / `search/store` / `benches/` の中身
- `JoinError` の `Display` が実際に何文字になるか

## lint / hook で強制できるもの

- **R8-02 の形（コミットメッセージが、実際には入っていない変更を名乗る）は機械で拾える。**
  ただし今回の原因は置換の空振りで、**手順の側で塞ぐのが筋**（置換が当たったことを
  確かめてからコミットする）
- R8-01 / R8-03 は機械で止まらない。前者は Rust の doc が TS 2ファイルの配列操作から
  導かれる結論を主張する形、後者は定数の名前と `write_str` の数え方の一致
- **「doc が指した先の実在」は、このラウンドで0件**（8ラウンド目にして初めて）

## 修正計画（r8 → r9）

### このラウンドで直したもの

| 所見  | 直し方                                                                                               |
| ----- | ---------------------------------------------------------------------------------------------------- |
| R8-01 | doc を現物に合わせた（`畳み込み済み`）。200件までは固定、それ以降は1つずつずれる、と書き分けた       |
| R8-02 | 表を直した。**今度は置換が当たったことを assert してからコミットした**                               |
| R8-03 | `SCREEN_MESSAGE_LIMIT` に直し、doc を「1回通る文言の上限。案内も引用も同じ枠を食い合う」に書き換えた |
| R8-04 | 列挙を落とし、基準は `IndexWarnPayload::message` の doc が持つ、という参照にした                     |

### 次ラウンドの焦点

1. **改名が2回入ったことで、綴りの不整合が残っていないか**（3世代ある）
2. R8-03 の書き直しが、`write_str` の数え方から1文ずつ導けるか
3. **所見が0なら0と書くこと。** r8 は4件すべて doc / 名前で、振る舞いは1行も変えていない

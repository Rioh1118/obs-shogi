# レビュー search-contracts ラウンド4

- 日付: 2026-09-05
- 範囲: `6936b80b~5..HEAD` の5コミット（`d5cbf080` / `0fb738aa` / `bbbe1115` / `f8666d71` / `6936b80b`）
- 走らせた reviewer: rust / comment / robustness（構造は範囲を畳んだぶん rust に統合）
- 前ラウンド: `2026-09-05-search-contracts-r3.md`

**r3 の修正はデータを壊すものを1つも入れていない。** BLOCK 級の新規欠陥は無し。
残っているのは (a) 塞ぎ損ねた穴、(b) 誰にも届かない診断、(c) doc の誤り。

**件数は 29 → 16 → 20 → 11。** 範囲を畳んだ効果が出ている。

## 所見

### HIGH

#### R4-A `node_id` の門番を条件付きにした理由が偽

comment / robustness / rust の3人。`index_cache.rs:628-629`。**`0fb738aa` が書いた。**

```rust
// **節表があるときだけ見る。** 表を持たない `file_id` は
// `is_occ_alive` が落とすので、ここで弾く必要が無い
```

**`is_occ_alive`（`file_table.rs:56-62`）は `FileTable` しか見ず、`NodeTables` を
引数にも取らない。** 「節表を持たない `file_id`」と「生きていない `file_id`」は別の条件。

実際に成り立っている不変条件は別で、**`FileTable` と `NodeTables` を触る本番の経路は
`index_store.rs:267-268` の2行しか無く、そこで必ず対で入る**。

**反例が私自身のテストの中にある。** `a_bucket_that_is_out_of_order_or_in_the_wrong_place_is_refused`
（`:1447`）は `NodeTables::default()`（表ゼロ）で `file_id: 1, gen: 1` の出現を書き、
`is_ok()` を主張している。「表が無く、かつ `is_occ_alive` が真」の blob を、
**門番を足した同じコミットのテストが正常として固定している。**

到達経路: 節表側の `file_id` 欄（`index_cache.rs:559`）が1ビット化けると、その表は
別の `file_id` に載り、元の `file_id` が「表が無い」側へ落ちる。`ft` は無傷なので
`is_occ_alive` は真。**その出現の `node_id` は誰にも検査されず、0手目として並ぶ** —
門番を足した当の故障が素通りする。

#### R4-B `fork_off` / `fork_len` が無検査。R3-C が名指しした3つのうち1つしか入れていない

robustness / rust。`index_cache.rs:562-579` / `node_table.rs:19-27` / `query_service.rs:131-134`。

`forks_len` は `:563` で既に読めているのに、節の欄と突き合わせない。
`cursor_lite` は範囲外で `None` を返し、`query_service.rs:134` が `CursorLite::root()` に
すり替える。**`node_id` を検査して塞いだのと1文字も違わない失敗。**

**同型の失敗が4回目。** r1 M4（7箇所中4箇所）→ r2 R2-9 / R2-15 / R2-3（各5/5/3 中 3/3/2）
→ r3 R3-C / R3-H / R3-I → r4 R4-B。

**そして r3 で入れた機構が止められなかった理由が特定された。**

> r3 が導入した「名指しのパスが差分に入ったか」という手順は、**パスは見るが名指しの項目は見ない**ので、
> この取りこぼしを止められていない（robustness）

`index_cache.rs` は差分に入っているので、パスの突き合わせは通る。
**機構を症状（パス）に合わせて作り、原因（項目）に合わせていなかった。**

#### R4-C 書き側の門番の `Err` が、製品では誰にも届かない

robustness / rust。`index_cache.rs:421-424,153-157,22-26` / `build.rs:222` / `lib.rs:51-52`。

コメントは「**ここで止めれば、壊れた原因の場所で分かる**」と書いた。だが `Err` の行き先は
3つとも塞がっている。

| 段                       | 何をしているか                                               |
| ------------------------ | ------------------------------------------------------------ |
| `index_cache.rs:153-157` | `map_err` で `trace!("encode_all FAILED: {e}")`              |
| `index_cache.rs:22-26`   | `trace!` の本体は `log::debug!`                              |
| `lib.rs:51-52`           | ロガーは `Info`。`Debug` を許すのは `obs_shogi::engine` だけ |
| `build.rs:222`           | `let _ =` で捨て、`JoinHandle` も捨てる                      |

しかも `Err` のとき **tmp を書く前に返るのでキャッシュが一切書かれない**。次の起動は
「ファイルが無い」で全件構築、以後永久に同じ。**コメントが防ぐと言った状態に、別経路で戻る。**

#### R4-D 新しいテストの doc が変更の経緯そのもの

comment。`sfen_position.rs:425-427`。**`d5cbf080` が書いた。**

> 枚数を桁の途中で弾いていた頃は…と言っていた。…`add_n` の1つに畳んだので

`CONTRIBUTING.md:140-144` の禁止事項に正面から当たる。3行のうち2行は現在のコードについて
何も言っていない。`commentHistory` の `HISTORY_WORDS` に `頃は` も `言っていた` も
無いので機械で止まらなかった。

### MEDIUM

#### R4-E 書き側の門番が3つの検査のうち1つだけ

robustness / rust。`index_cache.rs:427-432` vs `:612-619,630-637`。

読み側は桶の所属・並び・`node_id` の3つ。書き側は所属だけ。
**`:421-424` が書き側に門番を置いた理由は、並びが崩れた場合にも `node_id` が
範囲外の場合にも一字も変えずに当てはまる。** 理由が同じなら検査も同じ場所に要る。

並びを崩すのは `compact_bucket` の k-way マージで、桶の割り当てより壊れやすい。

#### R4-F `compact_bucket` の `debug_assert!` は `cargo test` から到達できない

robustness。`index_cache.rs:347-350`。

呼び手は `compact_all_buckets` だけ、そのさらに唯一の呼び手は `save_checkpoint` で
`AppHandle` を要るためテストから呼ばれない。**検証では一度も評価されない。**

評価されうるのは開発中の debug アプリだけで、そこで発火すると detached な
`spawn_blocking` の中で panic して握りつぶされる。同じ条件を20行下の `encode_all` が
`Err` で拾うので、**assert がある方が失敗の見え方が悪い**。

#### R4-G `InvalidHand` の `count` は今も利用者が書いていない数を出す

rust。`sfen_position.rs:40-45,219-220,257-267`。

`... b 18P99999999P 1` は1回目で `before = 18`、2回目の桁が 19 に飽和して
`after = 37`。**`too many pieces in hand: Pawn x37 (max 18)`** が出る。
利用者が書いたのは 18 と 99999999 で、**37 はどこにも無い**。

R3-B の「`count` も嘘」は頻度が下がっただけで構造として残っている。
しかも doc（`:42-43`）は「それより大きい数は `19` として届く」と書いているので、
読み手は `count <= 19` を前提にする。

#### R4-H 「672通り中513件」に付けた注が、その数と無関係な検査を原因にしている

comment。`docs/state-transitions/search.md:249-254`。**`f8666d71` が書いた。**

この数は**zstd の伸長を通った件数**。`decode_all` に門番を足しても、
zstd を通る単ビット反転の件数は1件も変わらない。変わるのは**その下の欄ごとの表**。

規律2で出典を併記して据えた数に、規律6を当てた結果、**据えるべきだった数に誤った因果が付いた。**

#### R4-I ラチェットの `BASELINE` が集計行を含み、`cargo doc` の申告と1ずれる

comment / rust。`scripts/rustdoc-ratchet.sh:15,18-19`。**`bbbe1115` が書いた。**

`^warning` は `warning: \`app\` (lib doc) generated 11 warnings`の集計行も数えるので、`count`は常に「実際の警告数＋1」。失敗時に「警告が増えた: 13」と出るのに`cargo doc` は「generated 12 warnings」と言う。

`BASELINE=12` の由来もスクリプトのどこにも書かれていない。
**規律2（数を書くなら式か出典を併記）を、そのラチェット自身で守っていない。**

#### R4-J テストコメントが、削った仕組みを説明したまま

rust。`sfen_position.rs:374`。**`d5cbf080` が触った同じ関数のテスト。**

> 桁を読み切る前に弾く。読み切ってから弾くと、この綴りで止まる

**2つの文がどちらも偽。** 桁は飽和させるので読み切るし、`add_n` はループの前に返るので
止まらない。R3-D で指摘されたのと同じ形の腐りが、R3-D を直したコミットの中に残った。

#### R4-K `search.md:263` の `fork_off` の説明が誤り。同じ事実の異説が3箇所

rust。`docs/state-transitions/search.md:263` / `index_cache.rs:622-626` / `:1265-1267`。

| 場所                       | 何と書いてあるか                   |
| -------------------------- | ---------------------------------- |
| `index_cache.rs:622-626`   | 0手目として並ぶ（**正**）          |
| `search.md:263`            | 「通る。黙って間違った索引になる」 |
| `index_cache.rs:1265-1267` | 「指し手が別の棋譜のものになる」   |

`fork_len` は表に1行も無い。

## 実測で問題なしと判断したもの

- **鍵の値は変わっていない。** `zobrist.rs` の差分は可視性と定数の移設だけで
  `hand_count` の頭打ちは無傷。`MAX_REPRESENTABLE_HAND_COUNT = 18` で従来と同値
- **`d5cbf080` で `Ok` / `Err` が反転した入力は無い。** `-` / `18P` / `9P9P` / `2P2P` /
  7種すべて上限 / `018P` はすべて `Ok`。飽和は `num <= 18` の範囲に効かない。
  `u32` も溢れない（`num*10+9 <= 199`）
- **`add_n` の `expect` 2つに落ちる経路は無い**（呼び手は `hand_piecekind_from_letter` を通り7種だけ）
- **`decode_all` の `node_id` の門番は正常なキャッシュを弾かない。** 節表が「あるのに空」は
  本番で作られるが、そのとき `by_bucket` も空なので出現が無い。`compact_bucket` が
  `is_occ_alive` で古い世代を落とすので、ずれた `node_id` は blob に載らない
- **blob を壊すテストは狙った欄を壊している**（バイト位置を書き順から追って確認）
- **ラチェットは増減に単調に効く**（2回連続で `12` が再現。0件でも `-lt` 側で落ちる）
- **`cargo test --lib` 229 passed**

## 見ていない範囲

- `read/` 配下の本体、`store/file_table.rs` の `iter_all` の呼び手
- `decode_all` の桶・節表以外の節（`scan` / `path_to_id`）の検査の網羅性
- `build.rs` / `project_manager.rs` の失敗経路（#406 / #407）
- bench の実走（#402）
- TS 側 `features/position-search/` の状態遷移
- `verify:rust` 全体（reviewer は `cargo test --lib` と ratchet のみ実走）

## lint / hook で強制できるもの

- **名指しの「項目」の突き合わせ**（R4-B）: r3 の手順はパスまでしか見ない。
  所見が挙げた**識別子**が `git diff -U0 <base>..HEAD` の追加行に現れるかを見る
- **経緯の語**（R4-D）: `HISTORY_WORDS` に `頃は` / `と言っていた` を足す。今回の1件はこれで止まった
- **`Info` 未満で終わる失敗の記録**（R4-C）: `rg 'trace!\("[a-zA-Z_]+ FAILED'` が
  現在4件。`lib.rs` の `LevelFilter::Info` と突き合わせれば機械的に列挙できる
- **`count` を見ないテスト**（R4-G）: 4箇所の `matches!(.., Err(InvalidHand { .. }))` は
  欄の中身を見ない。`d5cbf080` が足したテストも `kind` しか見ていない
- **復号側に裸で残る欄**（R4-B）: `rg 'fork_off,$' index_cache.rs` が0件であることをラチェットに
- **機械では防げない**: R4-A（理由が偽）/ R4-E（対の検査）/ R4-F（到達不能な assert）/
  R4-H（誤った因果）/ R4-J（削った仕組みの説明）

## 修正計画（r4 → r5）

### 対象そのものを疑ったか

**畳んだ効果は出ている。** 29 → 16 → 20 → **11**。しかも
**r3 の修正はデータを壊すものを1つも入れていない**（BLOCK 級の新規欠陥なし）。
範囲はこのまま。

疑うのは**私が r3 で入れた機構**。

### 4回目の同型失敗を、原因に合わせて塞ぐ

| ラウンド | 所見                | 名指し                                   | 直した    |
| -------- | ------------------- | ---------------------------------------- | --------- |
| r1       | M4                  | 7                                        | 4         |
| r2       | R2-9 / R2-15 / R2-3 | 5 / 5 / 3                                | 3 / 3 / 2 |
| r3       | R3-C / R3-H / R3-I  | （r2 の再掲）                            |           |
| r4       | R4-B                | 3（`node_id` / `fork_off` / `fork_len`） | 1         |

r3 で入れた手順は「所見が挙げた**パス**が `git diff --stat` に現れるか」。
**`index_cache.rs` は差分に入るので通ってしまう。** 症状に合わせて作り、原因に
合わせていなかった。

**手順を項目まで下げる。**

> 結果表に1件書く前に、その所見が挙げた**識別子**（`fork_off` / `HAND_COUNT_SLOTS` /
> `api` など、見出しと本文に出る名前）が
> `git diff -U0 <base>..HEAD` の**追加行**に現れることを確かめる。
> 現れないものは「未完」として書く。閉じない。

r1 の M4（`api`）も r2 の R2-3（`node_id`）も r4 の R4-B（`fork_off`）も、これで止まる。

**併せて、所見の見出しに列挙された名前は「1件」ではなく「N件」として数える。**
R3-C を1件として閉じたのが、そもそもの取りこぼしだった。

### このラウンドで直すもの

| 順  | 所見                                       | なぜこの順か                                                          | この直し方で壊しうるもの                                                                                                                                                                                                              |
| --- | ------------------------------------------ | --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **R4-B** `fork_off` / `fork_len`           | HIGH。4回目の同型失敗の実体。**上の手順の最初の適用対象**             | `forks_len` は既に手元にある。**正常なキャッシュを弾かないか**が焦点で、`an_index_of_a_realistic_size_can_be_read_back`（節表つき300ファイル）が守る                                                                                  |
| 2   | **R4-A** `node_id` の門番の条件と理由      | HIGH。1と同じ関数。**反例が自分のテストにある**ので、そのテストも直す | 条件付きをやめて `nts.get()` が `None` でも `Err` にすると、**表を持たない `file_id` の出現を持つ blob が弾かれる**。本番では `index_store.rs:267-268` が対で入れるので作られないが、**既存のテスト題材が落ちる**ので題材に節表を足す |
| 3   | **R4-E** 書き側の検査を読み側と対にする    | 1・2と同じ関数。理由が同じなら検査も同じ場所に                        | `EncodeCtx` は `nts` を既に持つ。書けなくなる blob が増えるが、それは壊れているものだけ                                                                                                                                               |
| 4   | **R4-C** 届かない診断                      | HIGH。上の3つが作る `Err` の行き先。**先に直さないと3つとも効かない** | `trace!` を `log::error!` に上げる。`build.rs:222` の `let _ =` は #407 の範囲なので触らない。**ログには出るが画面には出ない**ことを doc に書く                                                                                       |
| 5   | **R4-F** 到達しない `debug_assert!`        | 3で `encode_all` に同じ検査が入るので、消すだけ                       | 無し（`Err` の方が強い）                                                                                                                                                                                                              |
| 6   | **R4-G** `count` の嘘                      | MEDIUM だが利用者に出る                                               | 飽和したことを欄に持たせる。`InvalidHand` の形が変わるので `matches!` を書いている4箇所を確かめる                                                                                                                                     |
| 7   | **R4-D + R4-J** 経緯と、削った仕組みの説明 | doc のみ。**`HISTORY_WORDS` を足すのが本体**                          | ラチェットに語を足すと、既存のコメントが引っかかる可能性がある。数えてから足す                                                                                                                                                        |
| 8   | **R4-H + R4-K** doc の誤った因果と異説     | doc のみ                                                              | `search.md` を触るので `verify:rust` も走る                                                                                                                                                                                           |
| 9   | **R4-I** ラチェットの数え方                | 最後。スクリプト自身の話                                              | 集計行を外して `BASELINE=11` にする。**両方向で効くことを確かめ直す**                                                                                                                                                                 |

### 直さないもの

無し。**11件すべてこのラウンドで取る。** 範囲を畳んだので、送る先が無い。

### 次ラウンドの焦点

1. **R4-A で条件付きをやめたことで、正常なキャッシュを弾いていないか。**
   `file_build.rs` の `FileBuild::empty` と `project_manager.rs` の build 失敗腕が作る
   「節表があって空」の形が、本当に出現を持たないか
2. **R4-B / R4-E の検査が、`encode_all` と `decode_all` で対になっているか。**
   片方だけ増えると、書けるのに読めない blob ができる
3. **上の手順（識別子の突き合わせ）が効いたか。** r5 で「名指しの取りこぼし」が0件なら
   機構が効いている。**5回目が出たら手順ではなく私の読み方を疑う**
4. **R4-C で `log::error!` に上げたものが、本当に出荷ビルドのログに出るか**
5. r4 で見なかった範囲: `decode_all` の `scan` / `path_to_id` 節、`store/file_table.rs`

### 検証の見積り

9件 × `verify:rust`（約2分15秒）≈ **21分**。`docs/` と `package.json` を触る回があるので `verify` も。

## 結果（r4 の修正）

| 所見                                                         | コミット   |
| ------------------------------------------------------------ | ---------- |
| **R4-A / R4-B / R4-C / R4-E / R4-F** 門番と診断              | `c93f6a7d` |
| **R4-D / R4-G / R4-H / R4-I / R4-J / R4-K** doc とラチェット | `f99a8d2a` |

**11件すべてこのラウンドで取った。** 送り先は無し。

### 識別子の突き合わせを実際に走らせた

計画で決めた手順を `git diff -U0 6936b80b..HEAD` に当てた結果。

| 識別子                                  | 追加      | 削除 |              |
| --------------------------------------- | --------- | ---- | ------------ |
| `fork_off` / `fork_len`                 | +12 / +13 | 0    | OK           |
| `node_id`                               | +14       | -2   | OK           |
| `is_occ_alive` / `trace` / `bucket_idx` | +1        | -1   | OK           |
| `debug_assert`                          | 0         | -1   | OK           |
| `count`（`InvalidHand` の欄）           | 0         | -4   | OK           |
| `HISTORY_WORDS`                         | 0         | 0    | **拾えない** |

**手順に2つ穴が見つかった。**

1. **「追加行」に限定していたのが誤り。** `debug_assert` も `count` も**消した**ので
   削除行にしか出ない。**追加・削除の両方**を見る形に直した
2. **識別子そのものが動かず、中身だけ変わる修正は拾えない。**
   `HISTORY_WORDS` に語を2つ足した変更は、配列の中身なので識別子が差分に出ない。
   実際には入っている（`git diff` で確認済み）

2つ目は手順の限界。**識別子で拾えないものは、所見の本文が挙げた具体値**
（この場合は `頃は` / `と言っていた`）**で拾う。**

### 修正で新しく分かったこと

**門番を6つに増やしたら、既存の題材3つが本番と違う形だった。**
「出現はあるが節表が無い」形で、本番の口（`insert_file_segments`）は
ファイル表と節表を対でしか受けない。題材を本番に揃えた。

**読み側の門番の変異が、書き側の門番を入れた後は素通りするようになった。**
書き側が先に止めるので、壊れた blob を作れない。**書けた blob を壊す**形の
テストを別に足して、6つすべてが変異で落ちることを確かめた。

**`HISTORY_WORDS` に語を足したら、既存のコメントが1件当たった**
（`src/__tests__/sourceText.ts:33`）。ラチェットが効いた形なので、そちらも直した。

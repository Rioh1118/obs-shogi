# レビュー 404-reveal-item-in-dir ラウンド3

- 日付: 2026-09-07（レビューは 2026-09-06 に走らせた）
- 範囲: `fix/404-reveal-item-in-dir`
- 走らせた reviewer: architecture / react / robustness / comment / oss-hygiene
- レビュー時の HEAD: `畳み込み済み`（`2273b3a0` を基点）／**報告書を書いた時点では `origin/main`（`8d53ea45`）へ rebase 済み**
- 前ラウンド: `2026-09-06-404-reveal-item-in-dir-r2.md`

**r2 の修正が作った所見が9件ある**（R3-02 / R3-04 〜 R3-06 / R3-09 〜 R3-13 / R3-15 / R3-17 / R3-19 / R3-22 / R3-23）。
計画が挙げた焦点7点のうち当たっていたのは3点（`last` の持ち越しすぎ、二重の見せ方、窓の照合）。

## 所見

### R3-01 [HIGH] 基点が古く、`origin/main` の #449（F-32）を巻き戻す形になっていた（oss-hygiene / robustness）

`git merge-base` が `2273b3a0` で、`origin/main` は `8d53ea45`。台帳 §0 の同じ段落を両者が触っていたので、
このまま出すと **F-32（インラインの1件目）の記録が消える**。採番元から番号が消えると
`position-search.md` 側の参照が宙に浮く。

- 結果: **解消済み**（`origin/main` へ rebase し、§0 を「グローバルは F-13 / インラインは F-32」の
  2本立てで組み直した。§2 の F-32 行と main の表の整形はそのまま残した）

### R3-02 [HIGH] `system-dialogs.md` の「F-13 の1件だけ」が、インラインの1件目（F-32）を無かったことにする（oss-hygiene）

`system-dialogs.md` は「通知の土台の書き方はそこが唯一の前例」と宣言する側なので、
グローバルとインラインを混ぜて数えると、次に載せる人が `PositionSearchModal` の実例に辿り着けない。

- 結果: 対応済み（畳み込み済み）グローバル（F-13）とインライン（F-32）を分けて数える形にした

### R3-03 [HIGH] `openerCapability` が「許可を無効化する軸」を見ていない（architecture）

reviewer の実測で2通りとも緑になった。

- `{"local": false, "windows": ["main"], "permissions": [...]}` — `local: false` かつ `remote` 無しなら
  Tauri は**そのコマンドを1つも登録しない**（`acl/resolved.rs`）のに、この検査は許可として数える
- `tauri.conf.json` の `app.security.capabilities` が非空だと、Tauri は**そこに名指しされたファイルだけ**を使う。
  参照されないファイルに書いた許可は捨てられるのに、この検査は数える

`windowLabels()` は型に `security?: { capabilities?: unknown[] }` を書きながら一度も読んでいない。

- 結果: 対応済み（畳み込み済み）`local: false` を効いていない許可として外し、`tauri.conf.json` での名指しと `remote` / `webviews` を赤くする

### R3-04 [MEDIUM] `windows` は glob なのに完全一致で照合している（architecture）

Tauri は `parse_glob_patterns` を通す（doc も "Can be a glob pattern"）。
reviewer の実測で `{"windows": ["*"]}`（`main` に当たる**正しい**書き方）が落ちた。**偽陽性**で、
逃げるには capability の側を検査に合わせて書き換えることになる。

- 結果: 対応済み（畳み込み済み）`*` と `?` を解釈して照合する。文字クラスは字面のままなので、使うと赤くなる

### R3-05 [MEDIUM] capability ファイルの3形のうち1形しか解けない（architecture）

Tauri の `CapabilityFile` は 単体 / 配列 / `{ "capabilities": [...] }` を受ける。
配列で書くと3本が `Cannot read properties of undefined (reading 'map')` で落ちた——
R2-04 で潰したのと同じ「案内の代わりに TypeError」がファイルの形の側に残っている。

- 結果: 対応済み（畳み込み済み。型の直しも同じ塊に入っている）単体・配列・名前付きリストの3形を読む

### R3-06 [MEDIUM] `as Capability` を信じていて、`permissions` を持たない JSON や壊れた JSON でも原因を言わない（robustness）

`capabilities/**` の `.json` を全部読む形にしたので、書きかけの JSON が1枚入るだけで
`grantedIdentifiers()` を通る3本が落ち、**どのファイルが原因かはメッセージに出ない**。

- 結果: 対応済み（畳み込み済み）壊れた JSON と `permissions` の無い JSON を、相対パスを添えて落とす

### R3-07 [MEDIUM] `revealInFileManager` の doc が `default.json` を名指しして許可を書いている（architecture）

検査は「1枚を名指しせず全枚から集める」形へ意図的に変えたのに、コードの doc だけが1枚を指している。
`capabilities/dev.json` に `opener:default` を足すとこの doc は嘘になるが、検査は緑のまま。

- 結果: 対応済み（畳み込み済み）doc から許可の現在値を落とし、出典を capability と検査に寄せた

### R3-08 [HIGH] `CONTRIBUTING.md` の「向きは9つ」が実際の向き（13）と合わない（comment / oss-hygiene / architecture の3名）

`test()` は11本、assert は13箇所。表に無いのは4つ——JSON 以外の書式／口を1つも読めていない／
capability を1枚も読めていない／窓の label を読めていない。
**同じ PR で「手書きの数を写さない」と決めた行の隣で、数を写して3回目の食い違いが出ている。**

- 結果: 対応済み（畳み込み済み）**数を落とした**。3ラウンド続けてずれたので、向きだけを並べる

### R3-09 [HIGH] ルートを選び直した直後の「engines/ を開く」が、**前のルートのフォルダ**を開く（react / robustness の2名、実測あり）

`last` は「どのルートを読んだ索引か」を持たない。ルート A を読み終えてから B を選ぶと、
`localAiRoot` は B、`data` は A のまま。`enginesDirPath` は `data.engines_dir.path`（= `A/engines`）を返し、
`enginesDirRevealable` も true なので**押せて、A のフォルダが開き、失敗もしない**。
ネットワークや外付けのルートはスキャンに時間がかかるので、切り替え直後にこの窓が開く。

- 結果: 対応済み（畳み込み済み）索引に「どのルートを読んだか」を持たせ、別のルートの索引は使わない

### R3-10 [HIGH] ルートごと読めない失敗でも `last` を残すので、`void refresh()` が doc の約束を果たさない（react / robustness）

`scanNow` の catch も `last: lastIndex(s)` を積む。AI ルートが消えている回に reveal が失敗 → 読み直し →
`validate_dir` で失敗 → **古い索引が残るので `enginesDirOk` は true のまま**。
「消えていれば『engines/ を作成』が現れる」は成立せず、「engines/ を開く」も押せたまま同じ失敗を繰り返す。

- 結果: 対応済み（畳み込み済み）ルートを読めなかった回は索引を捨てる。残すのは読み取りと無関係な失敗だけ

### R3-11 [MEDIUM] スキャンに世代の関門が無く、遅れて届いた結果が現在のルートを上書きできる（react）

`scanNow` は `await` の後で無条件に `setScan({status:"ready"})` する。`void refresh()` が
利用者の指示なしに走るようになったぶん、A の結果が B の後に届く並びが現実になった。

- 結果: 対応済み（畳み込み済み。テストの直しも同じ塊に入っている）世代を持たせて追い越された結果を捨てる

### R3-12 [HIGH] `enginesDirExists` という prop に `exists && isDir` を渡していて、「無い」と「同名のファイルが居る」を子が区別できない（react / comment）

`engines` がファイルのとき、`SetupGuide` には「無い」としか伝わらないので、
警告は「フォルダではありません」なのに StatusCard は「なし」、hero と主カラムと Step 2 に
「engines/ を作成」が3つ出て、押すと Rust の英文で落ちる。

- 結果: 対応済み（畳み込み済み）`engines` を3状態にして子へ渡す。ファイルが居る回は作成を出さず、Step 2 を塞ぐ

### R3-13 [MEDIUM] `settings.md` の「2つは排他」が、その3行上の自分の記述と実装の両方に反する（robustness / comment / oss-hygiene）

同名のファイルが在る回は「作成」と「開く」が**同時に出る**。spec を読んで排他を回復しようとすると
R2-10 を再導入することになる。

- 結果: 対応済み（畳み込み済み）3状態の表で書き直した。実装が排他になったので、記述と現物が噛み合う

### R3-14 [HIGH] ADR-0004 に足した注が、実装にも台帳にも無いことを書き、決定3に無い規則を決定3に帰し、そもそも採用済み ADR の書き換えが `OPERATING-MODEL.md` の許す3つに当たらない（oss-hygiene / comment）

- 「『選択…』と『engines/ を作成』が常設」——作成は `!enginesDirOk` のときだけ描かれる。
  **この失敗が出る瞬間の画面には無い**（R2-08 で1度潰した形が場所を変えて復活している）
- 「`autoDismiss` の toast に動作を持たせられないのも決定3のとおり」——決定3は `autoDismiss` に触れていない。
  その制約を持っているのは `NotifyRequest` の型
- `OPERATING-MODEL.md` が許す書き換えは「実測値の更新／誤記の訂正／supersede の印」の3つだけ

- 結果: 対応済み（畳み込み済み）**ADR の注を落とした**。`OPERATING-MODEL.md` が許す書き換えに当たらない。理由はコードの doc へ

### R3-15 [MEDIUM] toast の本文が、6秒という上限の根拠を超えている（robustness）

`AUTO_DISMISS_MS` の doc は「題1行と本文1行（日本語で40字前後）」を根拠にしている。
いまの本文は日本語37字＋パス（30〜60字）で、toast の幅（36rem・`$font-aux`）だと**約3行**。
土台の最初の利用者が、その doc が名指しした「長くする理由」に最初から当たっている。

- 結果: 対応済み（畳み込み済み）どのフォルダかを題で言い、本文を1行に収めた。パスはログに残る

### R3-16 [MEDIUM] `settings.md` の本文の記述が、同じブランチの後のコミットで実装から外れた（oss-hygiene）

spec は「出すのはパスと、場所が変わった可能性」と書くが、実装は R2-09 で2通りに効く形へ変えた。
spec に合わせて直すと、Linux で毎回出る失敗に対して存在しない原因だけを出す画面に戻る。

- 結果: 対応済み（畳み込み済み）通知の書き方を、いまの決まり（原因を決め打ちしない・題で言う）に書き直した

### R3-17 [MEDIUM] `exists` の語が3つの意味を持つようになった（comment）

`AiRootIndex.engines_dir.exists`（生の存在）／`enginesDirRevealable`（生の存在）／
`SetupGuide` の prop `enginesDirExists`（実は `exists && dir`）。

- 結果: 対応済み（畳み込み済み）3状態にしたので `exists` の語が1つの意味に戻った

### R3-18 [MEDIUM] `windowLabels()` の `?? "main"` に根拠が無い（comment）

label を書かない窓を Tauri が `main` と名付けるのは外部仕様。CONTRIBUTING が「必ず書く」と
挙げている筆頭がこれ。

- 結果: 対応済み（畳み込み済み）`main` が既定の label であることを doc に書いた

### R3-19 [MEDIUM] `revealInFileManager.test.ts` のコメントが fixture の出所を取り違えている（comment）

`Path doesn't have a parent:` は**プラグインの Rust 側**の文言で、IPC では文字列で返る。
「`Error` を投げる形」の実例として使うと、読み手は存在しない経路を信じる。

- 結果: 対応済み（畳み込み済み）`Error` の枝の fixture を、実際に来ない文言から外した

### R3-20 [MEDIUM] `void refresh()` に添えた理由が、2つの呼び出し元のうち片方でしか成り立たない（comment / react）

「開けるのは在ると読めているときだけ」が真なのは engines 側だけ。AI ルート側は `canOperate` だけで押せる。
同じ doc が3段落上で「原因は2通り」と書いているのに、`refresh` の理由は片方に決め打ちしている。

- 結果: 対応済み（畳み込み済み）「2つの原因の片方にだけ効く」と書いた

### R3-21 [MEDIUM] テストがボタンを `title` と配列添字で引いていて、区別のための構造が UI に無い（react）

`title` は tooltip の文言であって識別子ではない。`SetupGuide` 側に tooltip を足すという自然な変更で
5件が同時に落ち、落ち方は原因を指さない。`SSection` が `<section>` をアクセシブル名なしで出しているので
`within` で絞る手が塞がっている。

- 結果: 対応済み（畳み込み済み。入れ忘れの直しも同じ塊に入っている）`SSection` の見出しを節の名前にして、名前で絞る形にした

### R3-22 [MEDIUM] `docs/IDEAS.md` の節が、数も「畳める範囲」も現物と合っていない（oss-hygiene）

props は15個ではなく16個。「hero を切り出せばコールバック5本が閉じる」も、閉じるのは1本だけで
残り4本は Step 側でも使う。着手する人が見積りを外す。

- 結果: 対応済み（畳み込み済み）数を落とし、hero に閉じるのは1本だけと書き直した

### R3-23 [MEDIUM] `localAiRoot` が `config.ai_root` の写しで、真実の源が2つある（react。差分の外）

`SInput` は `readOnly` で、`chooseAiRoot` は provider が config を更新してから返るので、
この state は要らない。索引とルートの不変条件を書く場所が2つに割れる原因になっている。

- 結果: 見送り（issue #461）差分の外の既存問題

### R3-24 [MEDIUM] AI ルートごと消えたとき、通知と診断が二重に出て、診断は Rust の英文（robustness）

`scan_ai_root` の失敗はそのまま `role="alert"` と hero に出る（`ai_root does not exist: …`）。
`revealOrNotify` の doc が「例外の本文は出さない」と決めた方針と、同じ画面の別の欄が食い違う。

- 結果: 対応済み（畳み込み済み）読めていないフォルダに対して「開く」を勧めない。Rust の英文の件は #456 に足した

## 重複・矛盾した所見

- **R3-09 / R3-10 / R3-11 は同じ根**——`ScanState` の `last` が「どのルートの、いつの索引か」を持っていない
- **R3-12 / R3-13 / R3-17 も同じ根**——`exists` と `exists && isDir` を割ったのに、
  子へ渡す口が2値のまま
- **R3-14 と R3-02 は同じ形**——docs に書いた「前例」「常設」が現物より広い
- **R3-08 は3ラウンド連続**（5つ→9つ→13）。数を書き続ける限り腐る

## 見ていない範囲

- **実機の挙動は3ラウンドとも誰も見ていない。** 全員がプラグインと Tauri のソースからの判断
- Windows / Linux での `reveal_item_in_dir`
- `NotificationLayer` / `Notice` の実描画（R3-15 の行数は SCSS の値からの計算）
- レビュー中、**このワークツリーに複数の reviewer が同時に probe を書いた**。
  終了後に `git status` が汚れていたので復元した（`src-tauri/capabilities/default.json`）。
  そのため reviewer 側の「全体の `npm test`」の結果は信用していない

## lint / hook で強制できるもの

- R3-03 / R3-04 / R3-05 / R3-06 は**検査自身の穴**。直しがそのまま強制になる
- R3-08 は「表が `向きはNつ` を名乗るなら、対応する検査の `expect(` の数と一致すること」で機械化できる。
  ただし**数を書かない**方が安い
- **`docsIdentifiers` / `docsSourcePaths` は `docs/state-transitions/` しか見ていない。**
  今回の HIGH の多く（R3-02 / R3-13 / R3-14 / R3-16）は `docs/spec/` と `docs/decisions/` で起きている。
  走査を `docs/**` へ広げれば、識別子とパスの腐りだけは同じ形で拾える
- **採用済み ADR の本文追記**は機械で止められる（`OPERATING-MODEL.md` が許す3つ以外の差分を落とす）
- **台帳の F 番号の欠番**も拾える（`docs/` 全体の `F-<数字>` に対して §2 に行があること）
- R3-09 は型で寄せられる——`last` を `{ root: string; index: AiRootIndex }` にすれば、
  ルートを渡さない持ち越しは tsc が落とす
- R3-12 も型で寄せられる——`enginesDirExists: boolean` を3状態の union にすれば、`enginesDirOk` を渡す誤りが落ちる

## 修正計画（r3 → r4）

### 束

- **索引の同一性**: R3-09 → R3-10 → R3-11。`last` にルートを持たせると、
  「読めないルートの索引を残さない」も「世代」も同じ箱で扱える
- **`engines` の3状態**: R3-12 → R3-17 → R3-13。子へ渡す口を3状態にすると、
  名前の混線も spec の「排他」も同時に解ける
- **検査の読み取り**: R3-05 → R3-06 → R3-03 → R3-04。ファイルの形を正規化してから、
  無効化の軸と glob を足す
- **docs の前例**: R3-02 → R3-14 → R3-16

### このラウンドで直すもの

| 順  | 所見  | なぜこの順か                                        | この直し方で壊しうるもの                                                                                             |
| --- | ----- | --------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| 1   | R3-05 | 機械。読む形を正規化してから軸を足す                | 3形を受けると、**単体形しか無い今は分岐が死にコードになる**。テストで3形とも通す                                     |
| 2   | R3-06 | 同上。壊れた JSON で名前を出す                      | `expect.fail` を使うので、走査の途中で止まる（残りのファイルを見ない）                                               |
| 3   | R3-03 | 機械。無効化の軸を足す                              | `local: false` を落とすので、**将来 remote 用の capability を足すと赤くなる**（意図した挙動）                        |
| 4   | R3-04 | 機械。偽陽性を消す                                  | glob を解釈しないので、`*` を含む `windows` は「当たっているとみなす」。**窓の指し違いを1つ見逃す**                  |
| 5   | R3-09 | 索引の同一性の先頭。以降の判定の材料                | ルートが変われば `last` が消えるので、**切り替え直後は「engines/ を開く」が押せない**（それが正しい）                |
| 6   | R3-10 | 同上                                                | 読めないルートでは索引を捨てるので、`SetupGuide` の StatusCard が一斉に「なし」へ落ちる                              |
| 7   | R3-11 | 同上                                                | 世代で捨てるので、**遅れて届いた成功が黙って消える**。ログにも残らない                                               |
| 8   | R3-12 | 3状態の先頭                                         | `SetupGuide` の prop が union になるので、**呼び出し側の書き換えが要る**（型で落ちるので漏れない）                   |
| 9   | R3-20 | 失敗経路のコメントと実装                            | 無し（コメント）                                                                                                     |
| 10  | R3-15 | 本文の長さ。文言を短くする                          | パスを本文から落とすので、**どのフォルダかは題で示す**ことになる。題が長くなりすぎないか                             |
| 11  | R3-21 | テストの引き方。`SSection` に名前を付ける           | `aria-labelledby` を足すので、**支援技術での読み上げが変わる**（改善方向だが、既存のテストが名前で引けるようになる） |
| 12  | R3-19 | fixture の出所                                      | 無し                                                                                                                 |
| 13  | R3-18 | 外部仕様の根拠                                      | 無し                                                                                                                 |
| 14  | R3-07 | doc の出典                                          | 無し                                                                                                                 |
| 15  | R3-08 | 表の数を落とす                                      | 数が無くなるので、**向きを1つ足したときに表を直す合図が減る**。逃げ道の列で補う                                      |
| 16  | R3-02 | docs の前例                                         | 無し                                                                                                                 |
| 17  | R3-14 | ADR の注を落とし、理由をコードの doc へ             | ADR から理由が消えるので、**段を見直す人はコードを読むことになる**。台帳から ADR へ辿れる形は残す                    |
| 18  | R3-16 | spec の本文                                         | 無し                                                                                                                 |
| 19  | R3-13 | spec の排他                                         | 無し                                                                                                                 |
| 20  | R3-22 | IDEAS の数と範囲                                    | 無し                                                                                                                 |
| 21  | R3-24 | hero の副動作（読めないルートで reveal を勧めない） | `SetupGuide` の error 枝の副動作が変わるので、**そこを通る他の失敗でも「選択…」になる**                              |

### 直さないもの

| 所見                                | 行き先 | 理由                                                             |
| ----------------------------------- | ------ | ---------------------------------------------------------------- |
| R3-23（`localAiRoot` の写し）       | issue  | 差分の外の既存問題。`entities/app-config` との関係も見直しが要る |
| `scan_ai_root` の英文がそのまま出る | #456   | 同じ「Rust の英文が画面に出る」なので既存の issue に足す         |

### 次ラウンドの焦点

1. `last` にルートを持たせたことで、**押せるべき瞬間に押せなくなっていないか**（同一ルートの再スキャン中）
2. 世代で捨てる形が、**成功を黙って捨てていないか**
3. `engines` の3状態が、`SetupGuide` の段（`step2` の done/active）と噛み合っているか
4. 題を「AI ルート」「engines/」で分けたことで、**どのフォルダの話か**が伝わるか。題が長すぎないか
5. `SSection` に `aria-labelledby` を足したことで、既存の画面の読み上げが壊れていないか
6. 検査が3形・`local`・glob を扱うようになったことで、**恒真になっている枝**が無いか
7. ADR から注を落としたことで、`info` を選んだ理由に辿れなくなっていないか

### 検証の見積り

21件 ＝ 21コミット。`docs/state-transitions/` を触るのは0件（今回の docs は spec / ADR / IDEAS /
CONTRIBUTING）なので `npm run verify` のみ。およそ 12 分。**次ラウンドへ送ったものは無い。**

## このラウンドで作った退行と、その後始末

- `畳み込み済み` は**型検査を通らない状態で積まれていた**（`畳み込み済み` で修正）。
  検証の出力を `grep` に通していたので、`tsc` の失敗が握り潰されていた。以降は
  `npm run verify > log; echo $?` で exit code を見ている
- `畳み込み済み` で足したテストは**変異を当てても落ちなかった**（`畳み込み済み` で修正）。
  遅れて届いた結果が state に入る前に assert していた
- `畳み込み済み` は**呼び出しだけを入れて定義を入れ忘れ**、3本が ReferenceError で落ちた（`畳み込み済み`）
- 負荷が高い回にテストが1本落ちた（`畳み込み済み`）。スキャンの結果が入る前に押していた

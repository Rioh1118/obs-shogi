# レビュー 502-engine-never-returns ラウンド14

- 日付: 2026-09-09
- 範囲: `1eaa35c3...HEAD`（取り込んだ基底の地点から）
- 走らせた reviewer: architecture / react / robustness / comment / oss-hygiene
- 対象コミット: `8f3d230e`
- 前ラウンド: [r1](2026-09-08-502-engine-never-returns-r1.md) 〜 [r13](2026-09-08-502-engine-never-returns-r13.md)

**変異担当を別のワークツリーへ隔離した**（`isolation: worktree`）。r13 で分かった計測汚染への対処。

## このラウンドで分かった一番大きなこと

**私は「直した」と報告した変更を、同じラウンドで2件とも入れていなかった。**

- HIGH-5（テストの冒頭 doc の「門4つ」）——修正スクリプトが手前の `assert` で止まった
- MEDIUM-6（窓の assert を `toEqual([])` にし、窓の前提を名指しする）——置換に `assert` を
  付けず、**一致しなくても "ok" を出す**書き方をしていた

comment と react がそれぞれ独立に見つけた。**r4/r5 で自分に課した「名指しした変更が
実際に入ったことまで確かめる」を、また守れていない。** r14 では適用後に grep で確かめ、
そのうえで変異を当てている。

**隔離は効いた。** robustness は自分のワークツリーで自由に変異を当て、
**門6つすべてが赤くなり、緑のままの3つは `engine.md` が名指ししている3つと完全に一致する**
ことを実測した。**台帳は正確**。12並列の負荷下で排出の早期離脱ゼロ・25回緑、
解析側も32並列で `advance()` の超過100ms 未満・失敗ゼロ。

## 所見

### HIGH-1 テストの冒頭 doc が、同じファイルの5本目を「観測できない」と否定する（comment / architecture / robustness / oss-hygiene の4本）

r13 で潰したのと同じ形が、**同じ修正コミットの中で再発**。数の食い違いが 4 対 5 から
**4 対 6 に開いた**うえ、「`finally` の同一性判定は観測できない」が、
同じファイルの新設テストによって偽になっていた。

### HIGH-2 窓を見る assert 3本が、緑では空配列に当たる（react / robustness。実測）

追い越された起動は commit を1つも起こさないのが**正しい振る舞い**なので、
`not.toContain` は `[]` に当たる。**窓が開いていなくても同じ形で緑になる。**

### MEDIUM-3 〜 MEDIUM-10

- `IDEAS.md` に書いた「`startingSeqRef` は dispatch 以外に効いていない」が、**門が塞いでいる窓を隠す**（architecture）
- `engine.md` ※2 の根拠「門が開くのは `shutdown` を通ったときだけ」が `finally` と食い違う（comment）
- `QUIET_TURNS = 2` に理由が無い／付けた理由が実測と合わない（comment / react）
- `drain` に固定回数の逃げ口が残り、理由が事実でない（architecture / react）
- 述語にしたのに `settle()` を2回続けて呼ぶ箇所が6つ（architecture）
- `reasonForPhase` の戻り型が広く、`no-engine` を返せる（architecture）
- `engine.md` が「世代の門」で2つの別機構を指す（oss-hygiene）
- `settings.md` が断りの**文言を写して**いる／台帳が**閉じた issue**を追跡先にしている（oss-hygiene）

### 訂正

- oss-hygiene の「このワークツリーで検証を通せない」は **`npx vitest` を使ったため**
  （CLAUDE.md が警告している罠。`npm run test` は通り、私は各コミットで通している）
- architecture の r13 HIGH-1（engine はまだ非決定）は**変異と並走した計測**だった

## 修正の結果（`/review-fix`）

| 所見                  | 結果                                                                      |
| --------------------- | ------------------------------------------------------------------------- |
| HIGH-1                | `36b10ce7`。冒頭 doc から数も内訳も落とし、`engine.md` を唯一の在庫にした |
| HIGH-2                | `68131316`。窓の前提を名指しし、`toEqual([])` で締めた                    |
| MEDIUM-3 〜 MEDIUM-10 | `36b10ce7` / `68131316`。**`reasonForPhase` の戻り型は tsc が守る**       |
| 閉じた issue の追跡先 | `docs/IDEAS.md` へ（差分の外）                                            |

**変異を当てて確かめたもの**——門7つすべてで、名指しした assert が落ちる
（**基底を取り込んだ後にもう一度当て直した**。合成を通したテストはどちらの側にも無かったため）。

## 基底の取り込み（`9c6a703a`）

基底が21コミット進んでいたので取り込んだ。**前回の取り込みでは rerere が過去の解決を
自動で再生し、`analysis.md` の E6 列（この PR の主役の遷移）が黙って消えていた。**
今回は `--no-rerere-autoupdate` で解き、**3点比較を両方向で取って**残差を1件ずつ確かめた。

- 基底側の残差3件はこちらが上位互換（`waitUntil` の打ち切り、S1/P1 の E6、タブの理由）
- こちら側は G-6 と F-9 を**基底の版に合わせた**——基底は同じ結論に独立に到達し、
  #523 まで立てている。欠番にするより行を残して直すほうが良い

## 見ていない範囲

- `perf` / `ui` / `rust` reviewer は14ラウンドとも走らせていない
- **実プロセスでの確認は14ラウンドを通して1件も無い**
- 取り込んだ基底の21コミット分の中身はレビューしていない
- 解析側の `advance()` は実時計のまま（robustness は32並列で再現せず）

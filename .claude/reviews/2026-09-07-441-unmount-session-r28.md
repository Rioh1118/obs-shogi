# レビュー 441-unmount-session ラウンド28

- 日付: 2026-09-09
- 範囲: `git diff origin/main...HEAD`（`fix/441-stop-analysis-on-unmount`）
- 走らせた reviewer: react / robustness / comment / oss-hygiene / architecture（5人とも走り切った）
- 対象コミット: `588cf1ff`
- 前ラウンド: [r26](2026-09-07-441-unmount-session-r26.md) / [r27](2026-09-07-441-unmount-session-r27.md)

## 前ラウンドからの持ち越し

- **r23 の所見16**（自動再開の8本の ref）→ **issue #489**。r27 で追記済み。機構は動かさない
- **r26 の所見11**（`useEngineSeat` のコメント密度）→ 直さないと判断済み。two-strikes 待ち

## このラウンドでいちばん重いもの

**#441 が塞ごうとしている漏れが、1本残っていた**（react が発見、こちらで再現）。
28ラウンド目にして、この PR の主題そのものに未修正の欠陥が見つかった。

## 所見

### [BLOCK] 1. 畳んだ後に戻ってきた席を取り残す——**#441 の漏れそのもの**

reviewer: react（実測）。こちらで再現し、テストを置いた。

`sweepOnUnmount` は畳まれた**時点**で席を読み、空なら降りていた。だが席は
その後に戻ってくる——捨てる停止が落ちると `keepOrForget` が欄へ書き戻す。
書き戻ったときには撃ち直す者が居ない。

```
# 現物                    → 赤（[undefined, "unmount"] が飛ばない）
# 外側の門を落とす        → 緑
```

筋は特別ではない。**解析中に盤を2手進める → 追い越された席が着地して捨てられる →
その停止が飛んでいる間に画面を離れる → そのあとで停止が落ちる。**
残った席は Rust に居続け、以後の解析が全部「Analysis already running」で断られる。

### [HIGH] 2. タブ名を直した結果、画面仕様4本が現在形で嘘になった（**こちらの退行**）

reviewer: robustness（実測）

`f0e21efd` は渡す値だけを直したので、`docs/spec/` の9行が「実在しないタブを渡している」と
言い続けている。**うち2行は「いま満たしていないこと」の欄**なので、次に導線を触る人が
**直っている欠陥を直しに来る**。`CLAUDE.md` が同じ PR で直すと決めている条件に当たる。

### [HIGH] 3. `ENGINE_FAILED_MESSAGE` の案内先が行き止まり。台帳の読み手の数も違う

reviewer: robustness（実測）

`useEngine()` の呼び手は2つだけで、どちらも readiness しか読まない。
**`state.error` を描画する行は `src/` に1つも無い。** 台帳は「1（設定タブを開いて
いる間だけ）」と数えているので、**残っている出口の勘定そのものがずれている。**

利用者に起きること: 起動が落ち続けている回に ▶ → 「設定でオプションを変えて
保存すると起こし直せます」→ 歯車 → **タブには理由が1文字も無い** → 変えて保存 →
また落ちる → 画面は1ドットも変わらない。

### [HIGH] 4. ハーネスは主チェックアウトから読まれる。**r27 の直しは1文字も届いていなかった**

reviewer: architecture / oss-hygiene（独立に実測）

worktree で作業していても、agent と skill は主チェックアウト側から配られる。
`fcc36a16` で書いた「変異は作業ツリーの外で当てる」は、**このラウンドの reviewer には
届いていない**（主チェックアウトは別のブランチに居る）。届いたのは、こちらが task の
本文へ手で書き写した1行だけ。

配られていた版は `CLAUDE.md` が禁じている前提（「Rust の `#[test]` は0個」
「verify は約8秒」）を現在形で書いたままだった。

### [MEDIUM] 5. 隔離コピーの手順そのものに欠陥（**こちらの退行**）

reviewer: oss-hygiene（実測）

`fcc36a16` が書いた宛先が固定なので、並走する5人が取り合う。しかも2回目の
`ln -s` は既に在るリンクを辿って**作業ツリーの `node_modules` の中**にリンクを作る。
`git status` は `node_modules` を見ないので、**同じ節が命じている確認手段では
絶対に見えない。**

### [MEDIUM] 6. 猶予と間引きを成立させている門が、それぞれ無検査

reviewer: react（実測。差を組んで見せた）

- `isRestartScheduled()`（`provider.tsx:606`）——**これが猶予そのもの**。
  落とすと ↓ を押しっぱなしの1手ごとに `stop`+`start` が出て、ペインが毎手空になる
- `ResultFlush.schedule` の重複防止——落とすと `receive` のたびにタイマーが増え、
  `timerRef` は最後の1本しか覚えないのでこのフックの存在理由が消える

どちらも落として既存の全件が緑。

### [MEDIUM] 7. 直した箇所の隣に、戻す根拠になるコメントが残っている

reviewer: comment

`ad355c4d` が `isHeld()` の呼びを外したのに、同じファイルの `:872-875` が
`takeSeatAndGo` を「`isHeld()` を見てよい側」として数え続けている。
`:220` は「**席を見て降りる形にしない**」と書いているので、
**腐ったコメントが、直したばかりの穴の再導入を後押しする。**

### [MEDIUM] 8. `.claude/agents` と `.claude/skills` が識別子の検査の外

reviewer: architecture（実測）

reviewer の定義は識別子を「これを grep しろ」という**材料**として名指すので、
腐ると空振りし、何を探せばよいか分からないまま節を飛ばす——赤くならないので
飛ばしたことも残らない。**現に2件腐っていた**（このブランチの改名で消えた綴り）。

### [MEDIUM] 9. `node:fs` を直に引く走査器は、名乗る義務を素通りする

reviewer: architecture（実測）

r27 で塞いだのは `walk.ts` を引く形だけ。起点を自分の居場所から取る形は
`CONTRIBUTING.md` が文でしか禁じていない。しかも同じ `CONTRIBUTING.md` が
**「`exportsTested` だけは `import.meta.url` から取る」と先例として書いていた**
——現物はそうなっていない（`SRC` から引いている）。

### [MEDIUM] 10. `settingsTabNames` が `updateParams` の形を見ない。挙げた「本筋」も成立しない

reviewer: architecture（実測）

`SettingsPanel` の `goTab` が現に `updateParams({ tab }, …)` を使っている。
また doc が挙げた「`URLParams["tab"]` を `TabKey` にする」は、`tab` を
`create-file` が `"create" | "import"` として共有しているので**実装できない**。

### [MEDIUM] 11. 席を取る口が3つあるのに、帳簿を通るのは1つだけ

reviewer: architecture

`analyzeWithTime` / `analyzeWithDepth` は同じ `take_session` を通るのに
`useEngineSeat` を1つも通らない。呼び手0なのでいま実害は無いが、
**配線した瞬間に #441 と同じ症状が戻る。**

## MEDIUM（doc と記録）

12. **記録した本数が2件とも別の木の値**（oss-hygiene。r23 / r24 に続いて3・4件目）
13. **`useResultFlush` に凍結の規約の写しが残っている**（comment。r27 で直したのは片側だけ）
14. **`CONTRIBUTING.md` の表が同じ規約を機械より狭く書いている**（oss-hygiene。4箇所目）
15. **`waits.ts` の中に寸法の手写しが3つ**（oss-hygiene。うち2つは導出値）
16. **「数字も比も写さない」の適用範囲が無いので、同じ論点が3ラウンド続いている**（oss-hygiene）
17. **`sourceText.test.ts` に変異の実測値が残っている**（comment。コメントを1行足すたびに動く数）
18. **`commentHistory` の doc が、止めていない形を止めていると書いている**（comment）
19. **`CONTRIBUTING.md` の verify の使い分けが分類から5種類抜けている**（oss-hygiene）
20. **`(S6/P1, E6)` がその行の代表的な住人を書いていない**（robustness）

## 反論

**robustness の所見2（`dropPendingForLostSeat` に席を握ったまま来る筋が4本ある）は採らない。**
呼び出し時点の `isHeld` をログで全数観測したところ、`588cf1ff` で 22/22、
`177b6baf` で 23/23 とも `false` だった。**held=true は再現しない。**

**ただし同じ所見の後半（観測法が飲まれる）は正しい。** r27 が使った
「握っているのに呼ばれたら投げる」は、無条件に投げても落ちるのが 22 箇所中 6 だけ
——呼び手の2つの `catch` が飲む。**結論は変わらないが、r27 の根拠は弱かった。**

## 確かめて問題が無かったもの

- **`ad355c4d` は r26 の窓を開けていない**（react。本体を空にすると r26 のテストだけが赤）
- **`6ae8bac3` はタイマーの口を通っている**（react。▶ 側の門を落としても緑、タイマー側で赤）
- **縮めた寸法は検出源を殺していない**（r27 で13変異、r28 でも再確認）
- **依存の方向の違反0件、Tauri コマンドの登録漏れ0件**（architecture）
- **リンクとソースパスは全部解決する**（oss-hygiene が `.md` 401 本を機械で辿った）
- **`bridge.rs` は TS 側の席の扱いと矛盾しない**（architecture）

## 見ていない範囲

- `analyzer.rs` / `protocol.rs` の内部と #463 の窓。**28ラウンド続けて未見。**
- 実プロセスでの検証。誰もしていない。

## 修正計画

順は「振る舞い → こちらの退行 → 機械 → doc → 行き先」。

1. **所見1 → 外側の門を落として内側1枚に寄せ、テストを置く。** 門を2つとも変異で確かめる
2. **所見2 → 画面仕様9行。** 同じ置き去りが起きないよう、綴りの検査に doc も歩かせる
3. **所見6 → 門を2つ、テストで固定する**
4. **所見7・13・14・15・16・17・18・20 → doc の写しと腐り**
5. **所見5・4 → 隔離コピーの手順と、ハーネスの読み込み元**
6. **所見8・9・10 → 機械の穴を3つ**
7. **所見12 → 記録した数字を実測に直す**（測った木のコミットを添える）
8. **所見3・11 → issue。** どちらも設計の選択が要る

**壊しうるもの。** 1 は #463 の窓に触るので、**内側の門を落とすと既存テストが
赤くなること**まで確かめてから動かす。2 の検査は `tab` を共有する
`create-file` を巻き込みうる。

### 次ラウンドの焦点

- 1 の直しが、**#463 の窓**（席を取ってから `go` が線に出るまで）を開けていないか
- 2 の検査が、**settings 以外のタブ名**を誤って赤くしていないか
- 6 で足した2本が、**寸法を動かしたとき**にも成立するか

## 修正の結果

| 所見 | 結果 | コミット |
| ---- | ---- | -------- |
| 1 | 直した。内側の門1枚に寄せ、テストを置いた | `177b6baf`（門を2つとも変異で確認） |
| 2 | 直した。画面仕様9行＋検査に doc を歩かせた | `5e6fe185`（2通りの変異で確認） |
| 6 | 直した。門を2つ、テストで固定 | `189f43b2`（どちらも変異で確認） |
| 7 / 12 | 直した | `4c1c2842` / `321fdf5d` |
| 4 / 5 / 8 | 直した。手順を `mktemp -d` に、`review-round` に手順0、ハーネスを識別子の網に | `c3977c0d`（変異で確認） |
| 9 / 10 | 直した。`node:fs` も材料に、`updateParams` の形も見る | `c3977c0d` / `5e6fe185`（どちらも変異で確認） |
| 13〜20 | 直した | `4c1c2842` / `321fdf5d` / `7e16424e` |
| 3 | **issue #523**（実装は直さない。台帳と仕様の数字だけ直した） | `7e16424e` |
| 11 | **issue #524** | — |

### 所見1 について——28ラウンド目に主題の欠陥が出た

**この PR は「畳まれたときに席を返す」ための変更で、その席が1本残っていた。**
27ラウンドの誰も踏まなかったのは、既存のテストが**畳む時点で席を握っている**筋しか
組んでいなかったため。書き戻しは `await` の向こうで起きるので、
「畳む → 落ちる → 戻る」の順を作らないと見えない。

**外側と内側で同じ述語を2回書いていたことが、そのまま原因になっている。**
外側は #463 に対して何も足していなかった（内側が同じ位置で降ろす）。

### 所見2・5 について——こちらの退行

どちらも前のラウンドの直しが作った。所見2 は「コードだけ直して doc を置き去り」、
所見5 は「並走の汚染を直す手順が、宛先を固定にしたせいで別の汚染を作る」。

**直した対象の周りを、直した後にもう一度読むこと。** r26 → r27 → r28 で
3ラウンド続けてこの形が出ている。

### robustness の preset の所見について

`updatePreset` が `dispatch` を `persist` より前に撃つので、保存が落ちても
エンジンだけが新しい設定で起こし直る（次の起動で設定は消えている）。
**既存の #170 に追記した**（新しい issue は立てない）。

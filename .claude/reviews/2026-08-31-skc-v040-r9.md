# レビュー skc-v040 ラウンド9

- 日付: 2026-08-31
- 範囲: ラウンド8の修正ぶん＋この PR の最終状態
- 走らせた reviewer: comment / robustness / rust
- 前のラウンド: `-r1.md` 〜 `-r8.md`

## 所見

### [MEDIUM] R9-01 `looks_like_text` が改行の無い正当な棋譜を落とす

- 場所: `search/kifu_reader.rs`
- reviewer: robustness（実測）
- `parse_ki2_str("▲７六歩 △３四歩")` は `Ok(3 moves)`。**改行が1つも無い KI2 は正当**。
  CR だけで行を区切る古い棋譜も同じ
- 落ちると「棋譜ではないファイルに拡張子が付いていないか」と出る。
  **R5-07 で塞いだ嘘が別の門で戻っていた**

### [MEDIUM] R9-02 名乗れない候補は**まだ配列の並び順**で決まっている

- 場所: `search/kifu_reader.rs`
- reviewer: robustness（変異を当てた）
- r8 の直しが効いているのは `ENCODINGS_THE_CRATE_SKIPS` の先頭に UTF-16 があるからで、
  構造で決まってはいない。**並べ替える変異も、後勝ちにする変異も生き残る**

### [MEDIUM] R9-03 ISO-2022-JP の印に 7bit の制約が無い

- 場所: `search/kifu_reader.rs` の `declared_encoding`
- reviewer: rust
- ISO-2022-JP は定義上 7bit なのに、印は3バイトの並びしか見ていない。
  `.kif` に改名したバイナリがたまたま `1b 24 42` を含むと
  「途中で切れていないか確かめてください」と出る。
  **棋譜ですらないファイルに切れていないかと言う**

### [MEDIUM] R9-04 `describe` の出口は7つあり、7つ目は doc に無く到達もしない

- 場所: `search/kifu_reader.rs`
- reviewer: **3人とも**
- 3人が別々に到達する入力を探して**作れなかった**。
  9ラウンドかけて磨いた優先順の中に、doc に無く到達もしない文言が混ざっていた

### [MEDIUM] R9-05 `ParseFailed.path` を読む者が1人もいない

- 場所: `search/kifu_reader.rs`
- reviewer: comment / rust
- r8 で Display から外した結果、読む経路が消えた。`pub` なので `dead_code` も鳴らない。
  **R1-06 で消した `read_many_to_jkf` と同じ形の死骸が同じファイルに生えていた**

### [HIGH] R9-06 doc が `looks_like_text` の門を落としている

- 場所: `search/kifu_reader.rs` の `try_other_encodings` / `can_be_named`
- reviewer: comment
- 両方の doc が挙げる条件は `had_errors` だけ。r8 が直したまさにその入力で嘘になる

### [MEDIUM] R9-07 `named` の代入は書かれていない前提に載っている

- 場所: `search/kifu_reader.rs`
- reviewer: rust
- 隣り合う枝で片方は「先に当たったものを採る」と理由付き、
  もう片方は理由なしで代入＝後勝ち。**一意性がどこにも書かれていない**

### [MEDIUM] R9-08 テストの doc に「3度とも」が入っている

- 場所: `search/kifu_reader.rs`
- reviewer: comment
- レビューを回した回数。**R7-06 で同じ形を落としたのと同じファイル**

### [MEDIUM] R9-09 `VERSION` の doc が上げる基準を2つ述べている

- 場所: `search/index_cache.rs`
- reviewer: comment
- 見出しは「読み方が変わったら」、締めは「集合が変わったら」。
  `PositionKey` の作り方が変わった場合、締めの規則では上げなくてよいことになる

## 重複・矛盾した所見

- R9-04 と R9-05 は3人／2人が独立に挙げた
- **R9-01 と R9-02 は同じ直しで消える**（門をやめて行数で比べる）。
  robustness が両方を1つの直し方で示していた

## 範囲外

このラウンドは全件が範囲内。新しい issue は立てていない。

## 見ていない範囲

- 3人とも `index_cache.rs` の符号化・復号本体、`fs_scan.rs`
- TS 側（この PR は未変更）
- 実コーパス609件での文言の出方

## レビュー中の事故

rust-reviewer が「レビュー中にワークツリーが繰り返し書き換えられていた」と報告した。
**こちらが並行して変異テストを走らせていたため。** 本人は clean を確認して回し直し、
49件緑を得たうえで所見を出している。次からは**レビュー中に変異を当てない**こと。

## lint / hook で強制できるもの

- `KifuReadError` を `pub(crate)` に落とせば R9-05 は `dead_code` が拾う
  （`search` の外へ出ていない）。**R1 の同節と同じ提案で2回目**
- `cargo-mutants`（#332）→ R9-02 / R9-04。**5ラウンド連続の提案**
- R9-01 / R9-03 / R9-06 は機械で拾えない

## 次ラウンドの対象

9件すべて直す。

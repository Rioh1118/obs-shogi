# レビュー book-to-main ラウンド28

- 日付: 2026-09-05
- 範囲: r27 の所見3件を直した3コミット（`git diff c7724cf2 1110e34b`）
- 走らせた reviewer: architecture / oss-hygiene
- 対象コミット: `1110e34b`
- 前ラウンド: `.claude/reviews/2026-09-05-book-to-main-r27.md`

所見 **4件**（BLOCK 0 / HIGH 0 / MEDIUM 4）。
推移は 22 → … → 4 → 4 → 3 → **4**。

**4ラウンド続けて HIGH 0。**

このラウンドで**同じ欄が3度目の所見になった**（ヘッダの括弧書き）。
two-strikes を超えているので、**その欄ごとやめた。**

issue へ送るものは無かった。

---

## 所見

### AT-04 (MEDIUM, 1人が実測): `g` フラグを外す変異が2つ生き残っていた

r27 は「意味のある変異はすべてテストが殺す」と結論したが偽。
**コメントと単引用の `g` を外す変異が2つとも705本すべて緑のまま**だった。

しかもこの2つは出力の違いでなく、**唯一の利用者（`missingIdentifiers`）の答えを変える。**

```
orig  : ["CLAUDE_PROJECT_DIR","GIT_DIR","tool_input"]  ← ソースに無い
noG   : ["tool_input"]                                  ← コメントの g を外すと2つが実在に戻る
sqNoG : ["CLAUDE_PROJECT_DIR","GIT_DIR"]                ← 単引用の g を外すと1つが実在に戻る
```

**壊れる向きは「消し足りない」側**なので、`docs/` がこれらを引いたとき壊れた側だけが黙って緑になる
—— まさにこのテストのコメントが名指ししている形。
生き残った理由は、どちらのテストも対象を**1個しか置いていない**こと。

- 結果: 対応済み `639083a5`（2個にした。**両方の変異が落ちることを確認**）

### AT-01 (MEDIUM, 1人が実測): ヘッダの「Rust の差分が無い」が3度目の所見になった

r27 の範囲には `yaneuraou_db.rs` の2行が入っている。
**AQ-03（r24 のヘッダ）→ AR-04（r25 のヘッダ）→ AT-01（r27 のヘッダ）と3度**、
同じ欄が所見になっている。毎回直し方が違い、毎回また外れる。

- 結果: 対応済み `6450d325`（**two-strikes を超えているので括弧書きごとやめた。**
  reviewer の一覧だけ残す）

### AT-02 (MEDIUM, 1人が実測): 「どのファイルに出ても指し先が割れない」が `.claude/reviews/` で割れている

**各報告書は自前で所見番号を採っている。** `book-foundation-r1` の F-13 / F-14 / F-15 は
台帳の同じ綴りと**指し先が別**で、裸で引いている箇所もある。
`docs/` にも1件橋渡ししていた（`book-key-failures.md` の「R1 F-04 の根」）。

書いた量化子が「どのファイルに出ても」で、**`.claude/reviews/` を除外していなかった。**
このループ自身が毎ラウンド報告書を読み書きしていて、r27 の AS-02 はまさに
報告書の中の識別子の帰属を所見にしている。

- 結果: 対応済み `c6e95716`（量化子を実際の範囲へ絞り、別採番であることを書いた）

### AT-03 (MEDIUM, 1人が実測): `0165bc18` の帰属が AP-04 だけになっていた

r24 の所見は `AP-03 / AP-04` の合体見出しで、**引用した規則の文は AP-03 側の成果。**
**AP-03 で引くと「生んだ差分は無い」に着く。**

- 結果: 対応済み `6450d325`

---

## 重複・矛盾した所見

- **4件とも1人ずつ**
- **AT-04 は AS-03 の直しが作った。** 変異を当てて「全部塞いだ」と結論したが、
  **当て漏らしがあった**（8通りのつもりが、`g` フラグの2つを試していない）。
  「網羅を名乗る」形が、変異テストの側で出た
- **AT-01 / AT-03 / AT-02 は報告書と注記の数・帰属・量化子**。5ラウンド連続
- **同じ欄が3度目の所見になったのは、このループで初めて**（連続ではない。r27 には無い）。
  `CLAUDE.md` は「同じ失敗を2回するまでルールを足さない」と決めているが、
  ここは**書くのをやめる**方に倒した ——
  3度とも「書いた数が実測と合わない」で、書き方の問題ではなく書くこと自体の問題
- 所見の水増しは無かった。2人とも「確認して所見が無かったもの」を明記していて、とくに:
  - **`d2b1482c` の例外は `session.rs` / `yaneuraou_db.rs` の両方で成り立っている**
    （記号を全走査し、`F-27` 以外に漏れが無いこと、`yaneuraou_db.rs` が `F-N` を
    1つも使わないことを確認）
  - **`:4` と `:16` の食い違いは解けている**（`F1` の定義元が `verify-gate-decision.md` で、
    別綴り＝別表なので矛盾しない）
  - **AS-03 の 224 / 198 / 26 は実測と一致**（正規表現を実際に当て、
    マッチごとにどの枝だったかを記録した結果が `{caret: 0, nl: 198, ws: 26}`）
  - **`shebang は残す` は load-bearing**（`(?!!)` を外すとこの1本だけが落ちる）

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送った19件（再掲しない前提）
- `origin/main` が元から持っていた実装と doc の中身
- `docs/spec/screens/` の13面が現物の UI と合っているか
- `user_book1.db` の実測3行とピーク確保の実測
- `codeOf` の c-like 枝の分岐網羅
- `.claude/reviews/` の所見番号の採番を全シリーズでは網羅していない
  （反例は `book-foundation-r1` と `yaneuraou-db-r4` の実測のみ）

## lint / hook で強制できるもの

- **AT-04 で `g` フラグの変異が塞がった。** ただし**変異の当て漏らしは人の注意では防げない** ——
  `sourceText.ts` に対する定型変異（各代替枝の除去・`g` フラグ除去）を CI で回す形なら
  機械で拾える。#351 の範囲
- AT-01 は**書くのをやめたので機械化の対象から外れた**
- AT-02 / AT-03（量化子と帰属）は機械で防げない

# レビュー book-to-main ラウンド16

- 日付: 2026-09-04
- 範囲: r15 の所見7件を直した分（`git diff c0bcc3c7 HEAD -- . ':!.claude/reviews'`）
- 走らせた reviewer: comment / robustness / oss-hygiene（Rust の差分が無いので3人）
- 対象コミット: `5a6e6751`
- 前ラウンド: `.claude/reviews/2026-09-04-book-to-main-r15.md`

所見 **3件**（BLOCK 0 / HIGH 0 / MEDIUM 3）。
推移は 22 → 18 → 23 → 10 → 12 → 7 → 5 → 4 → 6 → 8 → 9 → 9 → 6 → 7 → 7 → **3**。

**ゲートの免除の根拠は、robustness が所見なしと返した。** r13 から3ラウンド往復していた系統が
止まった。実測は 5動詞 × 4オプションの20通りを、競合中・clean・汚れたツリーで走らせた形。

**3件はすべて記録側。** 2件は r15 の報告書が名乗る数と系統の内訳、1件は
**5ラウンドかけて剥がした全称の出所**が別シリーズの報告書に無傷で残っていたもの。

issue へ送るものは無かった。

---

## 所見

### AG-03 (MEDIUM, 1人): 5ラウンドかけて剥がした全称の出所が、別シリーズの報告書に残っている

`2026-08-30-book-foundation-r15/r16` は現行の免除（`gate_is_teardown`）を入れた**最初の所見**で、
`verify-gate.sh` の根拠を辿った人が次に開く出典。そこに3つとも断定形で残っていた。

- 「`--skip` はコミットを1つも作らない」→ 偽（r9 の Z-06）
- 「使うのは競合を抱えた状態に限られ」→ 偽（r10 の AA-05）
- 「`npm run verify` が必ず落ちる」→ 偽（r13 の AD-02。r15 の AF-01 で3つ実測されている）

r9 以降、実装・判定表・テストのコメント・このシリーズの報告書から同じ主張を剥がしてきたが、
**出所が手つかずだった。** 次のラウンドがここを引くと、いま縮めた根拠を
「元はこう書いてあった」として復元しうる。

- 結果: 対応済み `8349177f`（別シリーズだが、この PR が5ラウンドかけて潰した主張の出所なので
  同じ PR で直した。根拠の本体は `gate_is_teardown` の上に一本化済みなので書き足さない）

### AG-01 (MEDIUM, 2人): AF-07 の見出しが名乗る「結果欄2つ」は、r14 に1つしかない

`df60dc22` を挙げている結果欄は1つだけ。もう1箇所は「重複・矛盾した所見」の箇条書きで、
結果欄ではない。「2」が付くのは `df60dc22` が偽にした**コード上の箇所**の数。

**r15 は AF-06 で「報告書が名乗る数が、同じ行に書いた範囲の中身と合うか」を所見にしている。**
その同じ報告書の見出しが同種の数え違いをしていた。

- 結果: 対応済み `4bcc0029`（本文は既に単数で正しいので、見出しから数を落とした）

### AG-02 (MEDIUM, 2人): 系統の内訳が AF-05 を取り込み、AF-02 を落としている

AF-05 が指すのは `CLAUDE.md` の「現在値の取り方」の参照（AD-01 → AE-07 → AF-05）で、
`gate_is_teardown` にも `df60dc22` にも掛からない。逆に AF-02 は AF-01 が測り直したのと
**まったく同じ命題**なのに、列から落ちていた。系統の定義（「r13 で削った → r14 で戻した →
r15 で戻し方も偽」）も AF-05 には当たらない。

この2行は「今回の所見の根は1本」という診断そのもので、次のラウンドの読み始めになる。
**AF-05 を免除の系統に入れたまま読むと、`CLAUDE.md` の参照の系統が独立して2ラウンド
続いていることが見えなくなる。**

- 結果: 対応済み `ea923b1e`（波及の件数も名乗らず、番号だけを並べた）

---

## 重複・矛盾した所見

- **AG-01 / AG-02 は2人が指した。AG-03 は1人**
- **robustness が「所見なし」を返した。** `1ca271b2` / `74d08966` が触った4箇所の主張を
  全部実測に掛けて、誤りが無いことを確認している。とくに:
  - 免除を潰した版で、`docs/IDEAS.md` だけが `UU` の状態では **20通りすべてが deny される**
    （「検証が落ちるなら出口が消える」に反例なし）
  - `--skip` は5動詞とも手元の変更を取り込まない（`rebase --skip` は捨て、
    `cherry-pick --skip` は未ステージのまま残して積んである commit だけを入れる）。
    **片方の根拠しか当たらない綴りは出なかった** ——「2つの連言」は実装と合う
  - **「これ以上狭める余地は無い」**とも明記された。残った2文は存在言明と構造の言い換えで、
    対応表が増減しても偽にならない向き
- **3件とも記録側で、実装にも doc にも掛からない。** 合流の中身（Rust / 一次資料の表 /
  ゲート / 索引）に掛かる所見は、このラウンドで初めて0になった

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送った17件（再掲しない前提）
- `src-tauri/src/book/` の振る舞い（3ラウンド続けて Rust の差分が無い）
- `user_book1.db` の実測3行とピーク確保の実測
- ShogiHome v1.29.0 側の引用（現物を取っていない）
- `git am` の競合中の実測（reviewer の手順で patch を積む経路を作れなかった）
- reviewer の人数の申告（個別の出力がリポジトリに残らないため検証できない、と2人が明記）

## lint / hook で強制できるもの

このラウンドで新しく挙がったものは無い。AG-01 / AG-02 の型（報告書が名乗る件数が本文の数と
合うか）は `- 結果:` の行数・`### XX-NN` の個数・推移の項数として機械で数えられるが、
**`.claude/reviews/` は `gate_kinds_for_path` が種類を返さない**ので掛け先は CI 側。
#351 の候補で、このラウンドでは足さない（案A の方針）。

機械で守れないもの: AG-03（過去の報告書の断定が、後のラウンドで偽と確定したかの照合）。

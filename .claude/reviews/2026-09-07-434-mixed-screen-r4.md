# レビュー 434-mixed-screen ラウンド4

- 日付: 2026-09-08
- 範囲: `git diff origin/main...HEAD`。r3 の14件を直した後の状態
- 走らせた reviewer: architecture / react / robustness / comment / oss-hygiene
- 対象コミット: `fdf030f0`
- 前ラウンド: [r1](2026-09-07-434-mixed-screen-r1.md) / [r2](2026-09-07-434-mixed-screen-r2.md) / [r3](2026-09-07-434-mixed-screen-r3.md)

**所見は 21 件。件数は4ラウンド横ばい**（21 → 19 → 24 → 21）。
**うち2件は r3 で入れたコードの欠陥**で、片方は「穴を塞ぐために足した機構」自身の穴。

## 所見

### BLOCK

#### r4-01 `reset_state` が `loadFailedSeq` を 0 に戻す。「単調増加で、戻らない」が3箇所で宣言されているのに破れている（**5人全員**）

`reducer.ts:127-132` が `{ ...initialGameState }` を返し、`initialGameState.loadFailedSeq` は `0`。
`game_loaded` は**わざわざ持ち越して理由まで書いている**（「戻すと、控えた値との比較が別の失敗と一致しうる」）のに、
20行下の `reset_state` がその通りのことを黙ってやる。

宣言は3箇所（`types.ts:60` / `reducer.ts:17-20` / `game.md:172-176`）。**どれも偽。**

react が stub で再現し、robustness と architecture が独立に同じ筋道を出した:

1. 前の回に `/b.kif` が載らず `loadFailedSeq = 1`
2. ヒットから `/b.kif` へ要求（`failedSeq: 1` を控える）
3. 飛行中に `resetGame()`（ワークスペース切替・`kifu_closed`・**ツリー再取得で節が消えた回**）→ `0`
4. `/b.kif` がまた載らない → `1`。`1 !== 1` が偽なので**要求が捨てられない**
5. あとでそのファイルを直して開くと、**誰も頼んでいない局面へ盤が飛ぶ**

**`>` に変えるのは逆効果**（react）——リセット後は `現在値 < 控えた値` になり、落ちた回を取りこぼす。

`provider.test.tsx` の `resetGame` の test は `loadFailedAbsPath` しか見ていないので、**この欄を守る機械が無い**。

### HIGH

- 結果: 対応済み（`71eb2bbd` `reset_state` で持ち越し。**`reset_state` が返す欄を全部並べる test** を置いたので、次に欄が増えたら赤くなる。持ち越しを消す変異で2本落ちることを確認）

#### r4-02 形式の条件が `isOpenedInTree` に残り、doc が禁じている開き直しを本体が起こす（architecture / robustness / react の3人）

r3-01 で落としたのは `FileNode` 側だけ。`selectNodeByAbsPath` は3条件のまま。

**doc と本体が正面から矛盾する**——`isOpenedInTree.ts:14` は「`kifuFormat` のずれを『開き直せば直る』と読むな」と書き、
`:24` の条件が偽になることで**呼び手が実際に開き直す**。

robustness と react が独立に実測（KIF 本文を `csa` として読むと投げずに0手・盤が全マス空）。
`forceReopen` が偽でも形式のずれで開き直すので、**改名した棋譜へ検索ヒットから飛ぶと棋譜が消え、
次の1手で空の棋譜がディスクへ書かれる**。

**`origin/main` と同じ挙動なので、この PR が作った経路ではない**（robustness が BLOCK にしなかった理由）。
問題は、r3-01 で合意した処方が2つの関門のうち1つにしか当たっていないこと、および
`8b020bf3` のコミットメッセージと ※7 が「塞いだ」と読めること。

### MEDIUM

- 結果: 対応済み（`f235630b` もう一方の関門からも形式の条件を落とし、`isOpenedInTree` はファイルごと畳んだ——2条件になり `FileNode` と同じ式で、何も共有しなくなったため。落ちる test を先に書いて確認）

#### r4-03 `gameView.player` を真偽にしか使っていないのに deps に入れ、盤が1手動くたびにフックが作り直される（react）

`view.player` は `buildPlayer` が作る新しいオブジェクト。`usePositionHitNavigation` は
`gameView.player` の**有無**しか見ていないのに、callback と effect の両方の deps に入れている。

`PositionSearchModal.tsx:229-232` は `accept` → `rowProps` → `PositionHitItem` の `memo` の連鎖を
守りたいと書いているのに、**その連鎖を毎手切っている主犯を名指しできていない**（コメントは「ツリーの選択」と書く）。

r3 で足した `loadFailedSeq` は `load_failed` でしか動かないので、作り直しの原因ではない。

- 結果: 対応済み（`619d212e` `hasPlayer` に落としてから依存に入れた）

#### r4-04 §4 の抽出条件が、§4 自身が名指しする行を1つも含まない（oss-hygiene）

r3-09 で母集合を抽出条件へ一本化した結果、**条件が唯一の出典になった**。
その条件を字句どおり当てると、§4 の issue 表の先頭2行（F-7 / F-9）も、
本文が名指しする F-19 / F-27 / F-33〜F-36 も、全部 §4 の外になる。
F-9 は §0 の9経路と §1 の「読み手1」にも載っているので、**§0 / §1 / §4 が同じ行について別のことを言う**。

- 結果: **送る**（§0 / §1 / §4 の3節を同時に見る判断が要る。F-9 の扱いは #277 の範囲に触れる）

#### r4-05 r3 報告書の結果行3件が所見の外にある。**r3-11 が r2 に対して出した所見と同一**（oss-hygiene）

`r3.md:41-43` / `:68-70` / `:245-247`。走査すると r1 / r2 は0件、**r3 だけ3件**。
`fdf030f0` は「24件すべてに結果を並べた」と書いたが、3件は次の見出しの下に落ちた。
r3-02 は BLOCK 節の最後なので、**BLOCK を読んだ人に「対応済み」が見えない**。

- 結果: **機械にしない判断**。`/review` の作り直し（`wt-workflow-overhaul`）が報告書の形を変えるので、いまの形に固定するラチェットは無駄になる。位置は書き戻しで直す

#### r4-06 r3 の計画が、棄却した案を結論として書いたまま（oss-hygiene）

`r3.md:220-221` は「**前者を採る**」（印を試行の開始で落とす）、`:276` の表も同じ。
実際に入ったのは連番。結果欄（`:55`）には棄却の記録があるが、
**判断を読むための節と順序の表は逆の結論を書いている**。

- 結果: 対応済み（この書き戻しで、計画の「前者を採る」を実際に入れた形へ送った）

#### r4-07 `game.md` の ※8 が「`loadGame` の2つ」のまま。このラウンドで5本＋`resetGame` 2本になった（oss-hygiene。r3-14 の再掲＋新しいずれ）

`689f9334` は E16 の行を直したが ※8 の数え上げは直していない。
さらに ※3 に「E2 で消える」と書き足したのに、**E2 行のテスト欄は `✗` のまま**——
台帳が主張する振る舞いを踏む test が同じ PR で入ったのに、表は誰も見ていないと言う。

- 結果: **送る**（E2 のセルを `✗` → `△` にする判断を伴う）

#### r4-08 `position-search.md` が存在しない返り値の語彙（`true` / `false`）で書かれている（oss-hygiene）

現物は `NavigationOutcome`（`"started"` / `"not-in-tree"` / `"tree-unavailable"`）。
`:89` は**この PR が書き換えた行**で、真偽値の語彙をわざわざ引き写している。
同じ文書の `:104-106` は3枝を別々の行として書いているので、1つの画面仕様が2つの返り値の形を主張している。

- 結果: **送る**

#### r4-09 断りの1文目が、台帳が唯一挙げている復帰路（ファイルを直す）を否定している（robustness）

本文は「同じファイルを開き直しても結果は変わりません」と断定するが、
`loadGame` が落ちるのは**その時点の中身**のせいで恒久の性質ではない。
F-31 の復帰欄は「ファイルを直すか、別の棋譜を選ぶ」。**利用者はそのファイルを諦める。**

- 結果: 対応済み（`f5e284af` 1文目の断定に範囲を付け、外で直す道を残した。test で固定）

#### r4-10〜r4-16（comment。多くが持ち越し）

- **r4-10** 「門番が全部止める」の機構説明が持ち主を含めて4箇所（r3-16）
- **r4-11** 細い context の理由が4箇所。`useGame`（29ファイルから呼ばれる主口）に TSDoc が無い（r3-17）
- **r4-12** 橋の `notify({...})` の引数リストに17行。ファイル71行中33行がコメント（r3-18）
- **r4-13** `canSkipReopen` / `canSkipOpen` が2文字違いで別の述語（r3-19）
- **r4-14** `forceReopen` を覆せる理由が5箇所（**新規**）
- **r4-15** `dedupeKey` の採用基準が唯一の実例と合わない（r3-15）
- **r4-16** `LoadedKifuPathContext` / `useLoadedKifuPath` だけが `LoadedKifuPath` 綴り。
  「盤に載っている棋譜のパス」は `loadedAbsPath` として118箇所（**新規**）

- 結果: **送る**（r4-02 / r4-09 / r4-18 で本文と関門が動いたので、縮めるのはその後）

#### r4-17〜r4-21（architecture。持ち越しが中心）

- **r4-17** 対局者名の射影が3箇所。**この PR は同じ1行の直しを2つのファイルに別々に書いた**（r1-10 から**4ラウンド目**）
- **r4-18** `loadGame` の `Err` が自由文字列。読まれない伝達路が2本になった（r1-11 から**4ラウンド目**）
- **r4-19** barrel が公開境界として働いていない。`SelectNodeOptions` は外から0参照（r3-23）
- **r4-20** 「`entities/file-tree` から `entities/game` は見えない」を根拠に API を作ったが、
  **lint は同一レイヤのスライス間を素通しする**（`file-tree` 自身が `app-config` を読んでいる）（**新規**）
- **r4-21** `isOpenedInTree` の呼び手が1つになり、`lib/` への切り出しが重複を1つも消していない（**新規**）

## 重複・矛盾した所見

- **r4-01 は5人全員**。直し方も全員一致（`reset_state` で持ち越す）。react だけが `>` への変更を明確に否定している
- **r4-02 は3人**。robustness は「main と同じなので BLOCK にしない」、react と architecture は「r3-01 の処方を両方に当てるのが一貫」
- **r4-05 / r4-06 は、このループ自身の記録の欠陥。** r4-05 は r3-11 と同型の再発
- **r4-13 / r4-21 は r4-02 の直し方に依存する。** 形式の条件を落とすと `isOpenedInTree` は2条件になり、
  `FileNode` と同じ式になる——そのとき切り出しを残す意味が変わる

## 見ていない範囲

- **Rust / SCSS / 見た目 / 実機は4ラウンドとも誰も見ていない**
- 通知モーダルの重なり順・フォーカストラップ（4ラウンド持ち越し）
- `npm run verify` を通したのは react だけ（94 files / 871 緑）

## このラウンドで止める（停止条件の変更）

**r5 は回さない。**

`review-loop-stopping-condition` のメモ（2026-09-08）が「所見ゼロまで」を廃止し、
**2巡＋機械化**に変えている。成果は直した所見の数ではなく**増えた機械の数**で数える。
根拠は275本の報告書の実測——最長46ラウンド収束せず、r7〜r19 は reviewer 自身が
「新しく挙がったものは無い」と書きながら続き、r10 以降は所見の多数派が
**報告書自身の記述の食い違い**に移っていた。

**このループは4ラウンドで同じ形を再現した。** 件数は 21 / 19 / 24 / 21 で横ばい、
r4 の新規は5件、残り16件は「自分の直しの直し」と持ち越し。r4-05 / r4-06 は
このループ自身の報告書についての所見。

**手で直した類型のうち3つを機械にした**（下の「機械にしたもの」）。
メモの唯一の反例——「機械にすれば所見の類型は消える。個別に直しても消えない」——に従う。

### 機械にしたもの

| 機械               | 止めるもの                                 | このループで手で直した回数              |
| ------------------ | ------------------------------------------ | --------------------------------------- |
| `ownedIdentifiers` | 「`X` の `y`」の指し先が所有者の側に在るか | **3回**（r2-01 / r3-02 / r4-02 の残骸） |
| `tableNotes`       | 定義の無い `※N` を指していないか           | **3回**（r1-17 / r2-12 / r3-13）        |
| `barrelConsumers`  | 呼び出し元の無い公開名が**増える**こと     | 1回（r4-19）＋バックログで3回提案       |

どれも**走査器そのものに合成入力の test**を置き、**0件で緑になる形を下限で止めて**ある
（`mechanization-backlog.md` の「走査を書くときの既知の罠」）。

**`ownedIdentifiers` は最初に書いた形では役に立たなかった。** repo 全体に綴りが在るかで
見ると `isActive` は他の4部品が持っているので素通りする。所有者を引いてから
その中を見る形にして初めて、r3-02 の変異で落ちるようになった。

## 修正計画（r4 → r5）

- 結果:
  - **r4-17**: 対応済み（`ac7bbef9` `entities/kifu` の `playerNames` に寄せた。空白だけの欄で答えが割れていたのを1つにした）
  - **r4-18**: 対応済み（`1a18f975` `KifuLoadFailure` にし、段と「何が起きたか」を code から決める）
  - **r4-19**: 対応済み（`64493029` `SelectNodeOptions` を barrel から下ろした。**機械にもした**）
  - **r4-20**: **送る**（`vite.config.ts` の lint に触る判断）
  - **r4-21**: 対応済み（`f235630b` に含む）

### 束

- **形式の条件**: r4-02 → r4-21 → r4-13。条件を落とすと `isOpenedInTree` は2条件になり
  `FileNode` と同じ式になる。そのとき切り出しを残す意味と名前が決まる。**r4-02 が先**
- **報告書の記録**: r4-05 → r4-06。同じファイルを2度触るので続けて取る
- **4ラウンド持ち越し**: r4-17 / r4-18。**r3 の計画で「r4 で先に取る」と書いた。今回取る**

### このラウンドで直すもの

| 順  | 所見  | なぜこの順か                                                             | この直し方で壊しうるもの                                                                                                                                               |
| --- | ----- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | r4-01 | **5人全員。** 印の機構そのものの穴                                       | `reset_state` が持ち越す欄が2つになる。**全欄を列挙した期待値と比べる test** にしないと、次に欄が増えたとき同じことが起きる                                            |
| 2   | r4-02 | **棋譜を失う。** main と同じ経路だが、r3-01 の処方が片方に当たっていない | 形式のずれが残ったまま開き直さなくなるので、**`.csa` に KIF が書かれる不具合（#507）は残る**。それは発生源で直すもの                                                   |
| 3   | r4-21 | 2の後、`isOpenedInTree` が2条件になってから                              | `lib/` のファイルと `OpenedKifuFields` が消える。barrel からは既に下ろしてある                                                                                         |
| 4   | r4-03 | 再レンダの主犯                                                           | `hasPlayer` に差し替えると、`player` が別インスタンスに変わっただけの回で effect が走らなくなる。**`game_loaded` は `loadedAbsPath` と同時に動く**のでそこは満たされる |
| 5   | r4-09 | 本文。台帳と食い違う                                                     | 「ファイルを直す」を足すと、`danger` の根拠（この画面からは直せない）は保つ必要がある                                                                                  |
| 6   | r4-05 | このループ自身の記録                                                     | 無し                                                                                                                                                                   |
| 7   | r4-06 | 6と同じファイル                                                          | 無し。**計画は履歴なので消さず、反転先へ送る**                                                                                                                         |
| 8   | r4-17 | **4ラウンド持ち越し。** r3 の計画で優先すると書いた                      | `GameView` か `entities/kifu` に射影を足す。**欠けているときの見せ方（`—` と `（不明）`）が2箇所で違う**ので、寄せると片方の見た目が変わる                             |
| 9   | r4-18 | **4ラウンド持ち越し。** 同上                                             | `Err` を code にすると、`state.error` に積む文字列と分かれる。**呼び手が1つのうちに形を決める**のが狙い                                                                |
| 10  | r4-08 | 存在しない語彙                                                           | 無し                                                                                                                                                                   |
| 11  | r4-16 | 綴りの揺れ。参照5ファイル                                                | 改名なので、`docs` と test の参照も同時に張り替える                                                                                                                    |

### 直さないもの

| 所見                                                | 行き先 | 理由                                                                                   |
| --------------------------------------------------- | ------ | -------------------------------------------------------------------------------------- |
| r4-04（§4 の抽出条件）                              | **r5** | §0 / §1 / §4 の3節を同時に見る判断が要る。F-9 をどちら側に置くかは #277 の範囲に触れる |
| r4-07（`game.md` の ※8 と E2 のセル）               | **r5** | E2 を `✗` → `△` にする判断。r4-01 で test が増えるので、その後のほうが正確に書ける     |
| r4-10 / r4-11 / r4-12 / r4-14（コメントの重複と量） | **r5** | r4-02 / r4-03 / r4-09 で `FileNode` / 橋 / フックの本文が動く。縮めるのはその後        |
| r4-15（`dedupeKey` の採用基準）                     | **r5** | `shared/lib/notification` の doc。3ラウンド持ち越し                                    |
| r4-19 / r4-20（barrel と同一レイヤの import）       | **r5** | どちらも `vite.config.ts` の lint に触る判断を伴う                                     |

**10件を r5 へ送る。**

### 対象そのものを疑ったか（手順4・2回目）

**4ラウンド横ばい**（21 → 19 → 24 → 21）。手順4 の「3回続いたら対象を疑う」は
r3 で1度通し、ユーザーが「3つ目の条件だけ戻す」「回し切る」と決めた。

**今回わかったのは、収束していない理由が機構ではなく手順にあること。**

| 何が起きたか                             | 件数                                         |
| ---------------------------------------- | -------------------------------------------- |
| 前ラウンドの**コード修正**が生んだ欠陥   | r4-01 / r4-02（2件）                         |
| 前ラウンドの**doc 修正**が生んだ食い違い | r4-04 / r4-05 / r4-06 / r4-07 / r4-08（5件） |
| **持ち越し**（同じ所見の N ラウンド目）  | r4-10〜r4-15 / r4-17〜r4-19（9件）           |
| 新規                                     | r4-03 / r4-14 / r4-16 / r4-20 / r4-21（5件） |

**新規は5件しか出ていない。** 残り16件は「自分の直しの直し」か「持ち越し」。
つまりループは**新しい欠陥を見つける速度より、自分の修正が欠陥を生む速度のほうが速い**局面ではなく、
**持ち越しを消化していないだけ**。r5 で持ち越し9件を取り切れば、件数は半分になるはず——**それが r5 の観測点。**

### 次ラウンドの焦点

1. **`reset_state` が持ち越す欄**（順1）。全欄を列挙した test になっているか
2. **形式の条件を両方の関門から落とした結果**（順2・3）。#507 の記述が「発生源で直す」で一貫しているか
3. **`hasPlayer` に差し替えた effect**（順4）。載った回で要求が当たるか
4. **射影を1箇所にした結果**（順8）。欠けているときの見せ方が2箇所で揃ったか
5. **`Err` を code にした結果**（順9）。段と文言の決め方が呼び手から離れたか
6. **持ち越し9件のうち、r5 で取り切れなかったものの数**（上の観測点）

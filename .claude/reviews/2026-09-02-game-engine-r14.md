# レビュー game-engine ラウンド14

- 日付: 2026-09-02
- 範囲: 対局側だけ。**`docs/state-transitions/game-session.md` の全セルを名指しで見せた**
- 走らせた reviewer: robustness（表の全セル） / rust（r13 の変更と未読の実装）
- 対象コミット: `e8f9478`

**生の所見14件**（HIGH 5 / MEDIUM 9）。

## 7ラウンド持ち越していた宿題が終わった

`game-session.md` の **57セル（E1〜E18＋E7' × G0/G1/G2）を全部**突き合わせた。
未照合のセルは無い。r7 から毎ラウンド「見ていない範囲」に書かれ続けていたものが消えた。

**セル自体はほぼ一致していた**（不一致は `(G2, E3)` の引用文字列1件）。
腐っていたのは**注と、テスト列の印**のほう。

---

## 実害

### R14-H1 喋り続けるエンジンに上限が1つも残っていない（rust HIGH / **自分が作った退行**）

r13 で沈通条件を **and** で足したので、`info` を出し続けながら `bestmove` を
返さないエンジンに上限が無くなった。`Phase::Thinking` を終わらせる口は
`on_tick` の2つだけで、片方は `enforce_engine_timeout`（既定 false）で塞がり、
もう片方が沈黙を要求する。`run_search` にも締切は無い。

詰み探索のループ、`btime 0` で終わらない実装、`info string` を出しながら
固まった実装——どれでも踏む。フロントには読み筋だけが流れ続け、
`over` も `moveDecided` も来ず、ログにも1行も出ない。
**利用者が中断を押すまで対局が終わらない。**

「持ち時間切れの30秒後に必ず負ける」を、**上限を消して**直していた。
この repo で6回出ている「片方を塞いで反対側を開ける」形。

### R14-H2 表の E18 が r13 以前の条件のまま（両 reviewer が指摘）

`stalled_turn` に条件が増えたのに、表の3箇所（`:89` の定義 / `:122` のセル /
※12）とも「持ち時間＋`SEARCH_GRACE`」だけを書いている。
`(G0, E18)` は「→ G2」と**無条件の遷移**として書かれているが、
喋り続けるエンジンでは発火しない。

**表がいま、R14-H1 の退行を正しい姿として記述している。**

### R14-H3 ※8 が「`info` は2箇所で落ちる」と書くが、片方は r13 で消えている（state-table HIGH）

落とす場所は `on_search_info` の `is_to_move` **1本しかない**。
この注を信じて `search.rs` に間引きを書き戻すと R13-H2 がそのまま再発し、
逆に「`search.rs` が落としているのだから」と `is_to_move` を冗長と読んで消すと、
相手の手番中の先読みが画面に出る。**いま現物を守っているのはその1行だけ。**

### R14-H4 `last_progress` の doc が「動かす口は `begin_turn` の1つ」と断言（rust HIGH / 自分が書いた）

`on_search_info` も動かす。**しかもそれが R13-H1 の修正の本体。**
消すと沈黙条件が意味を失い、r13 が直した症状に戻る。

### R14-H5 「`TurnChanged` は `running: null` で飛ぶ／3 は毎手通る」が偽（state-table HIGH）

`Handover::StartNow` と `PonderHit` は emit より**前に** `begin_turn()` を通るので
`running: Some(...)` が載る。`null` になるのは先読みが外れたときだけで、
`ponder` の既定は false なので**既定設定では一度も起きない**。

境界の型（`rust-types.ts`）はさらに強く「3 は毎手通る」と書いている。
この記述に従って受け手を作ると、毎手 `running` を捨てて次の `ClockUpdated`
（最短500ms）まで期限を持たないコードになる——doc が避けようとした
「相手の手番の頭で毎回止まって見える」を、doc のとおりに作ると起こす。

---

## その他（MEDIUM）

- **テスト列の `✓` が凡例を満たしていない行が5つ**（E7 / E7' / E8 / E12 / E18）。
  固定しているのは `G0` 列だけで、`(G2, ...)` を踏むテストは無い。
  E18 に至っては純関数を呼ぶだけで `on_tick` を駆動していない。
  「埋まっていないセル」の節もそれらを挙げていないので、
  **表と節の両方を読んでも未検証だと分からない**
- **`SearchMessage::Info` に世代（`req`）が無い。** E16「世代違いは捨てる」が
  `Outcome` にしか掛かっていない。先読みが外れた瞬間に届く `info` が
  そのまま転送され、盤に無い局面の評価値が一瞬出る
- **「埋まっていないセル」の E16 の理由「意図的に作る口が無い」が偽。**
  `Runner` を直に組んで `req` をずらせば1関数呼び出しで踏める。
  同じ表の2行上が同じ手法を「踏める」と書いている
- **不変条件4 の「3箇所」が4箇所**（`finish` が抜けている。r13 で足した）
- **※6 の断言が同じ注の3行下で否定されている。** 「`finish` の時点では送らない」と
  書いた後に「`A0` だった側にはその場で送る」。前者に従って loop を消すと、
  人対人の対局でエンジンに `gameover` が永久に届かない
- **秒数を7箇所で書いている。** ※4 は「秒数はここに書かない」と宣言した28行後に
  自分で秒数を書く。**7つとも値は合っている**が、これが7ラウンド腐り続けた機序
- **`CLOSE_TIMEOUT` の doc が内訳から `CLOSE_ABORT_TIMEOUT` を落としている**（r13 で分けた）
- **`EngineOption` が2つの互換性の無い型の名前になった。** 対局側（GUI→エンジン）と
  解析側（エンジン→GUI）で向きも形も違う。r13 が「同じ綴りのバリアントを止める検査」を
  挙げた同じラウンドで、型名で同じ衝突を作った。TS 側にも写した
- **`PlayerSpec` の TSDoc が `EngineOption` に付け替わった。** 宣言を doc と型の
  **間**に差し込んだので、IDE のホバーが別の型の説明を出す。
  `EngineOption` は `index.ts` から輸出されていないので、`options` を組む側は
  スライスの公開 API を素通りするしかない
- **`EngineRegistry::spawn` が `async fn` の中で同期 IO とプロセス生成を直に呼ぶ。**
  `protocol.rs` は**まさにこの理由で** `kill` と書き込みを `spawn_blocking` に
  逃がしてあるのに、同じ根拠がここには適用されていない。
  `canonicalize` と `UsiEngineHandler::spawn` には上限も無い
- `(G2, E3)` の引用文字列が現物と違う（表 `"already over"` / 現物 `"game is already over"`）

---

## 見ていない範囲

- **2人ともエンジンを1本も起動していない。** R14-H1 の「そういう挙動をするエンジンが
  実在する」は**推測**
- `protocol.rs` の `start_listening` / `ensure_ready` の世代管理 / flush の順序 /
  `discard_pending` — rust reviewer が読んだが、到達経路を確定できず所見にしていない
- `registry.rs` の `starting` の出し入れ（終了時にしか起きず害を特定できず）
- `session.rs` の `mod tests` の中身（r13 が足した4本の外）
- `failure-surfacing.md` に r13 が足した F-27 / F-28 の中身
- `game.md` / `engine.md` / `app.md` との相互参照

---

## 修正計画

1. **R14-H1**（退行）を先に。上限を戻す
2. 表と doc の偽（H2 / H3 / H4 / H5、※6、不変条件4、E16 の理由、秒数、`(G2,E3)`）
3. テスト列の `✓` を実体に合わせる。**宛先を trait にしたので、印だけだったセルを実際に埋められる**
4. 実装（`SearchMessage::Info` の `req`、`EngineOption` の改名、`PlayerSpec` の doc と輸出、
   `spawn` の `spawn_blocking`、`CLOSE_TIMEOUT` の doc）

## 結果

6コミット。`npm run verify` / `npm run verify:rust` とも green。

| 所見       | 直した内容                                                                      | コミット  |
| ---------- | ------------------------------------------------------------------------------- | --------- |
| **R14-H1** | `HARD_TURN_LIMIT` を別の腕として足す（沈黙条件と併存）                          | `2b4f469` |
| **R14-H4** | `last_progress` の doc を実態（2箇所で進む）に                                  | `2b4f469` |
| **R14-H2** | E18 の定義・セル・※12 を3つの締切に                                             | `9702ec3` |
| **R14-H3** | ※8 を「落とすのは1本だけ」に                                                    | `9702ec3` |
| **R14-H5** | `TurnChanged` の `running` は載ることもある。既定では 3 は起きない              | `9702ec3` |
| MEDIUM     | ※6 の主語を分ける／不変条件4 を4箇所に／E16 の理由／秒数7箇所／`(G2,E3)` の引用 | `9702ec3` |
| MEDIUM     | テスト列の `✓` を `△` に落とし、何が未検証かを ※10 に書く                       | `9702ec3` |
| MEDIUM     | `SearchMessage::Info` に `req`。`(G2, E7)` と E16 を実際に埋める                | `f1d51ea` |
| MEDIUM     | `EngineOption` → `SetOptionValue`（型名の衝突を解く）                           | `f1d51ea` |
| MEDIUM     | `PlayerSpec` の TSDoc を戻し、`index.ts` から輸出                               | `f1d51ea` |
| MEDIUM     | `spawn` の同期 IO を `spawn_blocking` へ                                        | `f1d51ea` |
| MEDIUM     | `CLOSE_TIMEOUT` の内訳を式で固定                                                | `f1d51ea` |

**ユーザーの指示で入れた構造の変更**（所見ではなく、所見の下地を消すため）:

| 変更                                                               | コミット  |
| ------------------------------------------------------------------ | --------- |
| `engine/` の依存を下向きだけに。環を `engine_layering.rs` で機械に | `032efba` |
| 出来事の宛先を trait にして、具象への依存を逆転                    | `7142731` |
| ADR-0008 責任境界と依存の向き                                      | `1198fb0` |

### 直し方を変えたもの

**R14-H1** は所見が「絶対上限を `MAX_THINK_TIME` に揃える」だったが、
`analyzer` の定数を `game` から参照すると段を跨ぐ（`game` は `analyzer` より下）。
`HARD_TURN_LIMIT` を `session.rs` に置き、doc で「片方だけ動かさない」を書く。

**`(G2, E7)`** は所見が「`gameover` を送ることを見る」だったが、送れたかは
protocol が要るので見られない。**`A0` に戻ること**を見る形にした——
戻らないと `close` の畳み待ちが必ず上限まで走るので、実害としては同じ。

### 変異で確かめたもの

| テスト                                                      | 当てた変異                                                | 落ちた    |
| ----------------------------------------------------------- | --------------------------------------------------------- | --------- |
| `talking_forever_still_hits_the_hard_limit`                 | r13 の状態（絶対上限なし）へ戻す                          | ✓         |
| `ending_the_game_tells_the_app_before_it_tells_the_engines` | `Over` を出さない                                         | ✓         |
| `info_from_a_stopped_search_is_not_shown`                   | 世代を見ない／常に一致                                    | ✓（両方） |
| `the_close_budget_is_deliberately_short`                    | `CLOSE_TIMEOUT` を最悪値より長く                          | ✓         |
| `dependencies_only_point_downwards`                         | （`AppState` を移す前の状態で `game ⇄ state` を検出済み） | ✓         |

### 自分が作った退行

- **R14-H1。** r13 で沈黙条件を **and** で足し、上限を消していた。
  「持ち時間切れの30秒後に必ず負ける」を直すのに、**上限そのものを外していた**。
  この repo で6回出ている「片方を塞いで反対側を開ける」形
- **R14-H2 / H3 / H4。** r13 の3つの変更が、表と doc を偽にした。
  コードだけ直して表を直していない
- **`EngineOption` の衝突と `PlayerSpec` の doc の付け替え。** どちらも r13 で入れた

### 表の宿題が終わった

`game-session.md` の **57セル全部**を突き合わせた。r7 から7ラウンド
「見ていない範囲」に書かれ続けていたものが消えた。

**セル自体はほぼ一致していた**（不一致1件）。腐っていたのは注と印。
そして印が実体を持てなかった理由が**具象への依存**だった——
宛先を trait にしたら、印だけだったセルを実際に埋められるようになった。

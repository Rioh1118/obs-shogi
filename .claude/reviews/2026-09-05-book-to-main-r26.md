# レビュー book-to-main ラウンド26

- 日付: 2026-09-05
- 範囲: r25 の所見4件を直した4コミット（`git diff 81cf3870 e55cee20`）
- 走らせた reviewer: architecture / oss-hygiene
- 対象コミット: `e55cee20`
- 前ラウンド: `.claude/reviews/2026-09-05-book-to-main-r25.md`

所見 **4件**（BLOCK 0 / HIGH 0 / MEDIUM 4）。
推移は 22 → … → 8 → 6 → 4 → **4**。

**2ラウンド続けて HIGH 0。** 4件のうち1件は**規則と手段が正反対を宣言していた**もの、
1件はテストの空白、2件は報告書の数字。

issue へ送るものは無かった。

---

## 所見

### AR-02 (MEDIUM, 1人): 規則が「必ず添える」、手段が「省く」で正反対を宣言していた

`0165bc18`（r24 の AP-03 / AP-04 対応）が規則「どの表かを**必ず**添えること」と
`session.rs` の module doc「**個別の注記では表名を省く**」を**同じコミットで**置き、
`f83767e3`（r25 の AQ-04 対応）が `yaneuraou_db.rs` に同じ形を広げた。

**同じ PR の中で正反対。** r24 の AP-04 と同じ grep を次のラウンドで当てると、
**いま正しいとされている36行が全部「未達」として再浮上する。**

- 結果: 対応済み `d4c4af4e`（規則の側に例外を書いた。委譲そのものは前のラウンドで
  採った判断で、コード36行を書き換える方ではない）

### AR-01 (MEDIUM, 1人が実測): 実 corpus の224行を落としている枝が、どのテストでも固定されていない

`stripShellComments` の3つの開始位置のうち、shell の7本が与えるのは `^` と `[ \t]` だけ。
**`\n` 枝（2行目以降の行頭 `#`）を与える入力が1本も無かった** ——
`|\n` を外しても703本すべて緑で、`verify-gate.sh` を通すと残る非空行が 194 → 333 に増える。

**壊れる向きが悪い。** under-strip 側で、`missingIdentifiers` は「doc が指す綴りが
corpus に**無い**もの」だけを返すので、**corpus が太る破損は黙って緑になる。**
`55dc0608` が塞いだ穴と同じ失敗が、行頭コメント側で戻ってくる形。

- 結果: 対応済み `581637db`（**`|\n` を外すと落ちることを確認**）

### AR-03 (MEDIUM, 1人が実測): 「8文書 / 10文書が別の意味で使う」が、その表自身を数に含んでいる

`yaneuraou-db-parse.md` は `S0`〜`S2` と `E0`〜`E9` を自分で定義しているので、
**この表の `S` / `E` は「別の意味」ではない。** 検証する人は7と9を得る。

**r25 の AQ-02 が r24 に立てた基準（同じ数え方から出ていない）を、その隣で破っていた。**

- 結果: 対応済み `d4c4af4e`（「この表を除く全部が別の意味」へ）

### AR-04 (MEDIUM, 1人が実測): r25 のヘッダと本文が、`.rs` の差分行を別の数え方で書いていた

- ヘッダ「コメント2行だけ」は差し引き（net）でしか出ない数。実測は 5 insertions / 3 deletions。
  **r25 が同じ報告で直させた r24 のヘッダ「コメント4行だけ」は「書き換わった行数」**
- 本文「その2行」→ 4行のうち**3行**。AP-04 の「6件中3件」と一致するのは3

**r24 の AP-06 と r25 の AQ-03 は「ヘッダの数を実測と一致させる」を2ラウンド続けて
所見にした。** その所見を書いた報告書自身が、直させたのとは別の数え方で書いていた。

- 結果: 対応済み `82b42516`

---

## 重複・矛盾した所見

- **4件とも1人ずつ**
- **AR-02 はこのループで初めて出た形** ——「規則」と「規則を満たすために採った手段」が
  同じ PR の中で正反対。どちらも自分が書いたもので、片方ずつ見ている限り両方正しく見える
- **AR-01 は AQ-01 と同じ根**（新しく前提にした分岐にテストが無い）。3ラウンド連続
- **AR-03 / AR-04 は AQ-02 / AQ-03 と同じ根**（報告書の数）。こちらも3ラウンド連続で、
  **直させた次のラウンドで自分が同じ形を踏んでいる**
- 所見の水増しは無かった。2人とも「確認して所見が無かったもの」を明記していて、とくに:
  - **`S` / `E` / `H` の3種で過不足ないことが確認された** ——
    `yaneuraou_db.rs` から `[SEH][0-9]` を除くと該当0件。`H` に文書数が添えられていないのも
    正しく、`H` を定義する文書は1本だけ
  - **AQ-02 が直した3つの数は同じ数え方から出る**（24文書 / `S` 8 / `E` 10）。
    ハイフン形を含めても `S` と `E` は変わらない
  - **順は固定されている** —— 逆順へ戻すと落ちるのは `70f2bb0e` が足した1本だけで、
    703本の中でこのテストだけが押さえている
  - **2つの assert はどちらも load-bearing** —— 片方だけを落とす変異が両方向に存在する
    ことを実測で示し、AP-02（空虚な assert）に当たらないことを確認した

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送った19件（再掲しない前提）
- `origin/main` が元から持っていた実装と doc の中身
- `docs/spec/screens/` の13面が現物の UI と合っているか
- `user_book1.db` の実測3行とピーク確保の実測
- `codeOf` の c-like 枝の分岐網羅（今回の差分が触っていない）

## lint / hook で強制できるもの

- **AR-01 は変異で確かめたテストが入った**（`\n` 枝1本）
- AR-02 の型（記号を裸で書いた注記）は #351 へ送済みの候補と同じだが、
  **免除の形がこのラウンドで決まった**ので、検査を足すなら module doc の表名を見る枝が要る
- AR-03 / AR-04（報告書の数）は機械で防げない

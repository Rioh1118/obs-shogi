# レビュー 362-ruling-timeout

- 日付: 2026-09-16
- 範囲: `git diff origin/main...HEAD`（基準 `928dd523`）。ADR-0011 着手順1
- 走らせた reviewer: architecture / comment / rust / robustness / react / oss-hygiene（6本を1巡）
- 対象コミット: `b80cefc4`〜`05d2c01d`

## 入れた機械

| 機械                                                    | 何を落とせるようになったか                                                                                                                 | 変異                                                                         |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------- |
| `src/__tests__/abortVocabulary.test.ts`                 | 打ち切り（`RULING_TIMEOUT`）を「中断」と呼んだコメント。入れた時点で**12箇所**捕まえた                                                     | 12箇所を直して緑、直す前は12件を名指して赤                                   |
| `src/__tests__/gameOverReasonWire.test.ts`              | Rust の `GameOverReason` が TS の写しに届いていないこと。**欄を持たない enum を `the_typescript_copy_has_every_field` が見ていなかった**穴 | union の member をコメントへ退避する変異で赤（**前の版はこれで緑だった**）   |
| `every_game_over_reason_goes_on_the_wire_as_camel_case` | `#[serde(rename)]` で線の綴りだけが変わる形。写しを引く側は宣言の綴りを引き続けるので、これが無いと丸ごと素通りする                        | `#[serde(rename = "userAbort")]` を1つ付けて赤（**前の版はこれで緑だった**） |
| `commentHistory` の `HISTORY_WORDS` に1語追加           | 「〜す前は」の形の経緯。既存の「る前は」と対になる綴りで、測って現存0件だったので誤爆なしで入る                                            | —（既存の機械の拡張）                                                        |
| `result.test.ts` に `gameResultLabel`                   | 勝敗が付かずに終わった対局を「引き分け」と名乗ること                                                                                       | 条件を外す変異で赤                                                           |

**前の2つは、reviewer が隔離コピーで実際に変異を当てて「緑で通る」ことを示した。**
読みだけの所見ではない。

## 直した所見

- `b80cefc4` — `RulingTimeout` の新設と、TS の写し・理由・帯の文言を同一コミットで
- `85b2519a` — 判定に失敗した終局の説明が「中断しました」を名乗っていた
- `853c43ac` — **`winner: null` を全部「引き分け」と名乗っていた**（HIGH。architecture と robustness が独立に指摘）。
  アプリが故障して畳んだ対局を利用者が引き分けとして記録することになる
- `f36b3d6e` — 理由の doc の一意性の断言が2つとも外れていた
  （`Resign` も両方から入る／`Aborted` は終了時の掃除からも入る）
- `2e7d9bf3` — 「故障を言える欄はここしか無い」の過大（5箇所）。
  帯にしか無いのは**投げた中身**で、故障が起きたこと自体は理由の欄も `detail` で言う。
  `rulingFailure` の doc が指していた経路（「断られた裁定が `endGameByRule` で通った回」）も
  成立しない —— 断られた呼び出しは再試行しないので通らない
- `05d2c01d` — 走査に掛からない「中断」（`rulingAdapter` の文言2つ、`useGameMoveGate`、
  `gameOutcome`、`types.ts` / `PlayView` の「必ず中断される」）と、
  `turnChanged` が落ちた回を「30秒で畳まれる」と書いていた spec

### 提案どおりに直さなかったもの

- **帯を共通部品に抜く**（react）。ADR-0011 着手順2 が `GameOverModal` を結末だけにし、
  V3 をスタッツの本体にするので、**同じ2箇所をまもなく書き換える**。
  いま抜くと着手順2 がその部品ごと組み直すことになる。issue も立てない
  （着手順2 が ADR に載っている＝既に追跡されている）
- **`rulingFailure` の上書き**（react / robustness）。直し方に判断が要る（捨てる／連結する／
  欄を割る）ので #584 へ。robustness は「より近い故障は出ている」として所見にしていない ——
  **2人の重み付けが割れたことも issue に書いた**

## 送ったもの

- #584 判定器が投げた回、根本原因が後続の断りに上書きされて画面から消える
  （`bug` / `area:game` / `priority:low`）

## 機械にできなかったもの

このラウンドの所見は**「一意性の断言が現物と合わない」に集中した**（3件）。
`mechanization-backlog.md` の「機械では止まらないもの」の 4 にそのまま当たる類型で、
今回も走査は書けていない。**書いた側（私）が3件とも出した**ので、
「〜だけ」「〜しか無い」を書いたら反例を1つ探す、を人が守るほかない。

「コメントの主張と実装のずれ」（同 1）も4件。こちらは backlog の筆頭類型。

## 見ていない範囲

- `session.rs` の diff 外（探索・時計）。rust-reviewer も同じ範囲を見ていない
- 実機での動作確認（`npm run tauri dev`）。対局を1局通した確認はしていない
- ADR-0011 の決定2〜6（着手順2以降の範囲）

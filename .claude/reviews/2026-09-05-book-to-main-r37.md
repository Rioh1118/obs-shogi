# レビュー book-to-main ラウンド37

- 日付: 2026-09-05
- 範囲: r36 の所見8件を直した5コミット＋報告書（`git diff 5e153f82 ae57f2e0`）
- 走らせた reviewer: architecture / oss-hygiene
- 対象コミット: `ae57f2e0`
- 前ラウンド: `.claude/reviews/2026-09-05-book-to-main-r36.md`

所見 **8件**（BLOCK 0 / HIGH 0 / MEDIUM 8）。
推移は 22 → … → 6 → 11 → 8 → **8**。

**13ラウンド続けて HIGH 0。**

**このラウンドで床の設計そのものが両側から崩れた** ——
締めすぎ側では11ディレクトリの削除が誤った赤を出し、緩すぎ側では
`//` の3分の1の末尾を捨てる変異が全部通る。**両立しないので、床が狙う範囲を決めた。**

**報告書のハッシュが1つ、どのブランチからも到達できなくなっていた。**

---

## 所見

### BH-02 (MEDIUM, 1人が実測): 下げた床が、下げる根拠にした場合をまだ赤くする

BG-03 は `session.rs` 1本で `line` が 9609 に落ちることを根拠に床を下げたが、
**下げた先（9800）でもその例は赤いまま**（実測 9615）。

ディレクトリ単位で掃くと **11ディレクトリの削除が赤**。
`chars.block` は余裕が11,702文字で、**上位2ファイルを消すだけで割る。**

コメントは偏っていて、最も多い1ディレクトリが
`line` の33%・`block` の41%・`hash` の71%を持つ。
**「実測の1割下」では1ファイルの削除を吸収できない。**

- 結果: 対応済み `9175d4e0`（BH-01 と同じコミット）

### BH-01 (MEDIUM, 1人が実測): 文字数の床は「40文字で切る」以上の切り詰めを取れない

床の根拠は「切り詰める変異は文字数を1桁落とす」と書いてあるが、実測は

| 変異          | `chars.line`          | 判定                    |
| ------------- | --------------------- | ----------------------- |
| `[^\n]{0,40}` | 331,737（**9.8%減**） | 当時の床 330,000 を通る |
| `[^\n]{0,60}` | 365,678（0.6%減）     | 通る                    |

`//` の長さは p50=37 / p90=53 で、**42文字超が3,651本（33.5%）。**
`{0,40}` は本文の3分の1の末尾を捨てながら6つの床を全部通り、
**42桁より後ろに書かれた違反は永久に検出されない**（注入で確認）。

**閉じたのは「40文字未満に切る」変異だけで、切り詰め族そのものは残っていた。**

- 結果: 対応済み `9175d4e0`（**床が狙う範囲を決めた** ——
  取るのは「枝が落ちて欄が0になる」と「本文が数文字に切られる」の2つだけ。
  **最も多いディレクトリを消しても越えない位置**へ置き直し、
  床では取れない族を列挙した。枝の除去・2文字化・行頭限定の6通りが落ちることを確認）

### BH-03 / BI-03 (MEDIUM, 2人が実測): 「3形で9割」が実測 71.7%

被覆は **667/930**。取りこぼしの263種は全て1文字接頭辞＋数字で、
**`book-foundation`（157種）と `yaneuraou-db`（46種）は1つも拾わない** ——
**この PR の親系列と兄弟系列がまるごと網の外。**

**「9割」と読んだ人は「ほぼ全部拾えている」と思って網を広げる判断をしなくなる。**
`a5da6559` の「この2形で採番している」（実測20%）を直したコミットが、
同じ文で別の過大評価に置き換えていた。

`F-N` の25箇所目は `protocol.rs:917` の**文字列リテラル**で、しかも半角括弧。
コメント内は24箇所。**「全て全角括弧か裸」と「25箇所」は同時に成り立たない。**

- 結果: 対応済み `a13c8f96`

### BI-02 (MEDIUM, 1人が実測): r36 の対象コミットが、どのブランチからも到達できない

`e7ee403f` は r35 の報告書コミットを amend した残骸で、`5e153f82` が実体。
**36本のうち到達できないのはこの1つだけ。**

`git diff d0e24021 e7ee403f` は `unknown revision` で落ちる ——
**35本が守ってきた「報告書のハッシュは履歴から引ける」がここで切れていた。**
しかも r36 が読んだのは r35 の本文が1文違う古い版。

- 結果: 対応済み `2dbc8ea7`（差も1行添えた）

### BI-01 (MEDIUM, 1人が実測): BF-03 の直しが、同じ宣言を3箇所落としている

`dad17fe7` は r35 の1文だけを直したが、同じ宣言が r34 に2箇所・r33 に1箇所ある。
**`dad17fe7` は r34 を実際に開いて別の行を書き換えていて、
同じファイルの2箇所を素通ししている。**

実測では、ヘッダと**見出しの数**が割れるのは r9 / r22 / r23 / r24 / r25 の5本。

**AZ-05 → BB-01 → BE-01 → BG-03 → BI-01 で「instance だけ塞ぐ」形の6度目。**

- 結果: 対応済み `2dbc8ea7`

### BI-04 (MEDIUM, 1人が実測): r36 の実測値4つが再現しない

| 書いた値           | 実測                                                                         |
| ------------------ | ---------------------------------------------------------------------------- |
| 938種・9形         | **930種**（同じ規則で `476` は完全一致するのに総数だけずれる）               |
| `line 10704`       | **10703**                                                                    |
| `hash 196`         | **194** —— この変異は**本数を1件も動かさない**（動くのは文字数 7709 → 3979） |
| 現存31本のうち19本 | **34本**のうち19本（19 は合うが母数の取り方が違う）                          |

`hash 196` の訂正には意味がある —— **この変異は文字数の床でしか取れない**ので、
そう書くほうが「対で床を置いた」理由と繋がる。

- 結果: 対応済み `935236e8`

### BI-05 (MEDIUM, 1人が実測): 「6,808 のうち 6,498 が移動、残る 229」の引き算が閉じない

6,808 − 6,498 = **310**。229 ではない。

6,498 と 229 は **`//` 10,881件の内訳**（桁0 4,154 / 字下げ 6,498 / 行末 229）で、
6,808 は変異後の減少幅。**母数が違うので引き算では繋がらない。**

**BF-01 は「2文で自分と矛盾していた」を直す所見だった。**
直した文が、今度は引き算の合わない3つ組になっていた。

- 結果: 対応済み `935236e8`（母数を 10,881 に統一し、「残る」を使わない形へ）

---

## 重複・矛盾した所見

- **8件のうち1件は2人が別々に到達した**（BH-03/BI-03）。単独は6件
- **BH-01 / BH-02 で、床の設計が両側から崩れた。**
  締めすぎ（11ディレクトリの削除が誤った赤）と緩すぎ（3分の1の末尾を捨てる変異が通る）が
  **同時に成り立っていて、「実測の1割下」では両方を満たせない。**
  **床が狙う範囲を決める**ことで初めて解けた ——
  BA-01 → BC-01 → BG-02 と3度、床を締める方向で直してきたが、
  **締める方向に解が無いことが分かったのは今回**
- **BI-01 は「instance だけ塞ぐ」形の6度目。**
  しかも**直しの対象ファイルを開いていながら、同じファイルの2箇所を見ていない**
- **BI-04 / BI-05 / BH-03 は数え方**。11ラウンド連続。
  **BI-05 は BF-01 の直しが作り、BH-03 は BE-05 の直しが作った**
- **BI-02 はこのループで初めて出た形** —— 自分の amend が、
  自分が書いた報告書の参照を宙に浮かせた。**36本で1本だけ**
- 所見の水増しは無かった。2人とも「確認して所見が無かったもの」を明記していて、とくに:
  - **BF-02 の落とし残しは無い** —— 「別のファイルの件数を引いている箇所」を
    `.claude/reviews/` 全体で走査。`前ラウンド` 行6箇所・
    book-to-main の `範囲` 行31本・推移の列、**すべて指し先と一致**
  - **`1bc33b00` の書き換えは正しい** —— `yaneuraou-db` 9本で
    **ヘッダの件数＝ID の異なり数が全部成立**（21/9/12/12/7/21/16/15/13）。
    `E-03` 欠番・`E-04` 2行・r7 の `H-01` 重複も現物どおり
  - **BF-04 は正しい**（3組＝6件、単独5件）
  - **「8件とも1人ずつ」「12ラウンド続けて HIGH 0」「推移」は正しい**
  - **`11,708件` / `568ファイル` / `476種` は完全一致**
  - **`chars` の数え方そのものの変異5通りは全部赤**
    （`+= 1` / `+= 100` / `=` / キーの取り違え）
  - **`BRANCH_NAME` の誤検出は0** —— `git ls-files` の1,003パスと
    204のディレクトリ接頭辞に当てて、`src/` `src-tauri/` `docs/` からの当たり0
  - **`\bfeature\/` を `\bfeature` に緩めると6件の誤検出**（`src/features/` を巻き込む）——
    いまの綴りが load-bearing

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送ったもの（再掲しない前提）
- `origin/main` が元から持っていた60本と、実装・doc の中身
- **`64件`（文字列リテラルの内側で始まる一致）** —— 別の走査規則では69になった。
  r36 が判定規則を書いていないので突き合わせられない
- **`263 と 128`（1文字／2文字の識別子）** —— 抽出規則が書かれておらず再現できない
- `HISTORY_WORDS` の26語が1語ずつ load-bearing か（26通り）
- `yaneuraou-db` r1〜r9 と `book-foundation` 16本の**本文の主張**
- 3重以上の変異、`walk.ts` の `sourceFiles` / `ROOTS` への変異
- `codeOf` を使う他のラチェット
- `docs/spec/screens/` の13面が現物の UI と合っているか
- `user_book1.db` の実測3行とピーク確保の実測
- Rust 側（このラウンドの差分に `.rs` は無い）

## lint / hook で強制できるもの

- **BI-02 は機械で見られる** —— 報告書のヘッダから hex を拾って
  `git merge-base --is-ancestor <h> HEAD` が偽なら落とす。
  **36本のうち35本は現に守っている**ので規則の新設ではなく既存実態の固定。
  **今回の1本は人の目でしか見つかっていない**
- **BI-01 は BF-02 の検査を散文の宣言まで広げないと拾えない** ——
  「前ラウンド」行だけを見る実装では、r33:146 / r34:80 / r34:139 は素通りする
- **BH-01 は床では機械化できない**（床は文字数の1割しか見ないので、
  平均長付近で切る変異は原理的に通る）。**注入を回す仕組みが要る** —— #351 の (d)
- **BH-02 は機械化できる** —— 床を `床 < 実測 − 最大寄与ファイル` として assert すれば、
  1ファイルの削除で赤くなる置き方が入った時点で落ちる。
  **「実測の1割下」は寄与の偏りを見ていないので、`session.rs` のようなファイルがある限り
  毎回この形で破れる**
- **BH-03 は機械化できる** —— 見出しの ID を集めて網に食わせ、
  **被覆率を数として assert する**（除外する形は allowlist に明示）。
  doc に率を散文で書く限り、採番が増えるたびに腐る
- **数を書くときは、それを再現するコマンドか規則を同じ行に書く** ——
  このラウンドの5件中4件は、それが無いために
  「数え方が違うのか、値が違うのか」の切り分けに時間が要った

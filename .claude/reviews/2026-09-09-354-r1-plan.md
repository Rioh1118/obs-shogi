# r1 の所見を束ねた計画（#354）

出典: `2026-09-09-354-r1-{architecture,comment,perf,robustness}.md`。
所見は 4観点で 29件。**同じ根から出たものを束ねて 10 の修正にする。**

## 直す

| #   | 根                                                                   | 出どころ                                     |
| --- | -------------------------------------------------------------------- | -------------------------------------------- |
| P1  | 詰み判定が持ち歩で爆発する（実測 66 秒）                             | perf BLOCK                                   |
| P2  | doc が実装と食い違う（Rust 側 module doc / `#354` の指し先）         | comment BLOCK, comment MEDIUM                |
| P3  | トライルールが直前の手を見ていない                                   | arch MEDIUM, robustness HIGH, comment MEDIUM |
| P4  | 勝者が `Color`（`Color.Black === 0` が falsy）で呼び出し側が誤読する | robustness HIGH, arch MEDIUM                 |
| P5  | 壊れた入力を黙って通す（USI 書式のごみ / `maxMoves` の異常値）       | robustness HIGH, robustness MEDIUM           |
| P6  | 裁定できなかったときの失敗が、意味も情報も足りない                   | robustness HIGH+MEDIUM, arch HIGH            |
| P7  | `JishogiRule` の網羅が tsc に見えていない                            | arch MEDIUM                                  |
| P8  | 「合法手」の綴りが2つ（`PossibleMoves` / `LegalMoves`）              | comment MEDIUM                               |
| P9  | doc が主張しているのにテストが固定していない分岐（6点）              | robustness MEDIUM                            |
| P10 | 持将棋の書き分け・理由の向き・`JISHOGI` の意味の二重化               | comment HIGH, comment MEDIUM ×2              |

**順序の理由。** P1 が最優先（勝敗が消える）。P4 は公開する型を変えるので、
テストを足す P9 より前に置く。P3 は P9 のテスト3本と同じ局面を使うので隣に置く。
P10 は P3・P4 で実装が動いた後に書かないと、また嘘になる。

**この修正が壊しうるもの。** P4 は `GameOutcome` の公開面を変える（呼び出し側は
まだ居ないので波及は tests のみ）。P1 は `moveValidation.ts` の条件の順を入れ替える
——積の条件なので結果は変わらないが、`getAllPossibleMoves` は盤の表示が使っている
経路なので、返る集合が同じことをテストで確かめる。P8 の改名は3ファイルに及ぶ。

## 直さない（理由を残す）

| 所見                                                         | なぜ                                                                                                                                                                             |
| ------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| arch HIGH「置き場が `entities/game` で良いか」               | issue #354 の本文と `docs/spec/features/game-play.md` が `entities/game/lib/` を名指ししている。スライスを割り直すのは設計の選択なので、勝手に決めずに残す（`/implement` 手順7） |
| arch HIGH「合法手の権威が2つに割れている」                   | doc は直す（P6）。**権威をどちらにするかは判断が要る**ので issue にしてユーザーに選ばせる                                                                                        |
| perf HIGH「毎手 `Record` を作り直すので合計が2乗」           | 直し方（`Record` を持ち回る判定器）は**呼ぶ側と一緒でないと形が決まらない**。実測値を関数の doc と `game-play.md` に残し、画面を作る人が読める場所に置く                         |
| perf MEDIUM「王手放置の検査を `move`/`unmove` にする」       | 呼び出し側の生きた盤を書き換えて戻す形になる。いまは写しなので失敗しても盤が壊れない。**#354 の範囲外で、壊したときの被害が盤全体**なので issue に送る                           |
| arch MEDIUM「`GameProgress` を `GameSettings` から組ませる」 | 同上。入口の形は呼ぶ側と一緒に決まる                                                                                                                                             |
| arch/comment MEDIUM「barrel に公開面を載せる」               | 呼ぶ側が居ないうちに載せると、最初の呼び出し元が現れる前に公開面が固まる。代わりに `game-session/index.ts` に「まだ barrel に無い」と書く                                        |
| robustness MEDIUM「玉の無い SFEN を終局として通す」          | `Position.newBySFEN` が通す盤で、根の SFEN を作るのは呼び出し側。いま弾くと駒落ちや詰将棋の局面から対局を始める道を先に塞ぐ。判断が要るので issue の材料としてだけ残す           |
| robustness MEDIUM「`initializeFromSFENString` が投げうる」   | 渡しているのは tsshogi 自身が直前に書き出した `record.position.sfen`。落ちる入力を示せていないとレビュアー自身が書いている                                                       |
| arch MEDIUM「`Color → Side` の持ち主が無い」                 | P4 で解消する（`winner` を `Side` で返すので変換が要らなくなる）                                                                                                                 |
| arch MEDIUM「同層の横断 import を lint で禁じる」            | 機械化の提案。`.claude/knowledge/mechanization-backlog.md` へ送る                                                                                                                |

## 機械にするもの

- **閾値付きの性能テスト**（perf の提案）。P1 の 66 秒と、`Record` 作り直しの合計を
  両方とも赤くできる。`EXPECTED_MIN` と同じくラチェットとして置く

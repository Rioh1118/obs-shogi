# レビュー book-to-main ラウンド15

- 日付: 2026-09-04
- 範囲: r14 の所見7件を直した分（`git diff cfaa8251 HEAD -- . ':!.claude/reviews'`）
- 走らせた reviewer: comment / robustness / oss-hygiene（Rust の差分が無いので3人）
- 対象コミット: `c0bcc3c7`
- 前ラウンド: `.claude/reviews/2026-09-04-book-to-main-r14.md`

所見 **7件**（BLOCK 0 / HIGH 0 / MEDIUM 7）。うち1件は自分で見つけた。
推移は 22 → 18 → 23 → 10 → 12 → 7 → 5 → 4 → 6 → 8 → 9 → 9 → 6 → 7 → **7**。

**実装と doc に掛かるのは AF-01 / AF-03 の2件で、どちらも `df60dc22` の直しが偽だった。**
残る5件は報告書の記述。AF-02 / AF-04 / AF-07 は AF-01 / AF-03 の波及で、
AF-05 / AF-06 は別系統。

**このラウンドで初めて、条件を主張することそのものをやめた。** AF-01 は
「全称が偽 → 条件付きに狭める → その条件も偽」を3ラウンド繰り返した末に、
**種類と検査の対応をコメントに書かない**ことで決着した。写した時点で腐る種類のもの。

issue へ送るものは無かった。

---

## 所見

### AF-01 (MEDIUM, 3人。1人が実測): 書き戻した条件節も偽だった

`df60dc22` は全称を条件付きに狭めたが、**その条件がまだ偽。**
robustness が本物の競合を作って計測した。

| 競合したファイル                                 | 種類      | `npm run verify`                  |
| ------------------------------------------------ | --------- | --------------------------------- |
| `docs/IDEAS.md`                                  | `ts`      | **rc=0**                          |
| `docs/state-transitions/verify-gate-decision.md` | `ts rust` | **rc=0**（`verify:rust` も rc=0） |
| `package-lock.json`                              | `ts`      | **rc=0**                          |
| `src/shared/ui/ConfirmDialog.tsx`                | `ts`      | rc=2                              |
| `src-tauri/src/book/api.rs`                      | `ts rust` | rc=0 / `verify:rust` rc=1         |

免除を潰した版に `git rebase --abort` を食わせる end-to-end でも、`docs/IDEAS.md` だけの
競合は標準出力0バイト・rc=0。**種類に当たっていても出口は消えない。**

`docs/` だけが競合する rebase は、このループのような doc 中心の作業では普通に起きる。

- 結果: 対応済み `1ca271b2`（**どのファイルなら落ちるかを書くのをやめた。**
  種類と検査の対応は増減するので、写した時点で腐る。存在言明だけ残す）

### AF-03 (MEDIUM, 1人が実測): `--continue` を外す基準を当てると、免除している10通りも外れる

「deny されても免除済みの `--abort` で降りられるので2つ目も当たらない」が示した基準を
そのまま当てると、**`--quit`（5）/ `--skip`（4）/ `--edit-todo`（1）の10通りも
`--abort` で降りられる。** 実測 —— 5動詞すべてで競合中の `--abort` は rc=0 で成功し、
`gate_is_teardown` が YES を返す。

つまりこの10通りは1つ目だけで免除されていることになり、直前で「1つ目だけでは切り出せない」と
言って退けた `git merge topic` と見分けが付かない。**この基準で数え直した人は
`--skip` と `--edit-todo` を「根拠が無い」として外せてしまう**（外すと `expect_teardown` が落ちる）。

- 結果: 対応済み `74d08966`（免除は2つの連言なので、`--continue` は1つ目だけで外れる。
  その句を落とした）

### AF-02 (MEDIUM, 3人): r14 が「実測した」と書いた対照が、このツリーでは再現しない

「対照として `docs/IDEAS.md` だけを `UU` にすると同じ版が deny する」は偽（実測で0バイト・rc=0）。
**同じ報告書の「見ていない範囲」が「この対照は再測していない」と書いていて**、
r13 の記録とも正面から逆。`package.json` の無い合成リポジトリの観測を写したとみられる。

- 結果: 対応済み `2a53d6ef`（対照の1文を削った）

### AF-07 (MEDIUM, 自分で見つけた): r14 が、偽と判明した直しを正しいものとして記録している

AF-01 / AF-03 で `df60dc22` の2箇所が偽と分かったが、それを「対応済み」として記録している
r14 の結果欄は古いまま。**次のラウンドが、すでに撤回した条件と基準を確定した判断として受け取る。**

- 結果: 対応済み `0b7c95e2`（割れた判断の記録も直した。**comment の側が正しかった**）

### AF-04 (MEDIUM, 2人): 「免除の結論は根拠1つ目だけで立つ」が2つの報告書に残っている

**この命題こそが `daef0e7b` に2つ目を削らせ、AE-02 になった。** 実装は戻したのに、
その判断の出典として次のラウンドが読む2箇所は命題を肯定したまま残っていた。

- 結果: 対応済み `e6677015`

### AF-05 (MEDIUM, 1人): AD-01 の直しが、同じ所見の中の「空を指している」を残していた

`1168313a` は見出しと根拠の段落を直したが、4行上は手つかず。**同じ所見の中で
「空を指している」と「節ごと空ではない」が真逆を言っていた。** r14 の診断文も再掲していた。

- 結果: 対応済み `cbb6a2e9`

### AF-06 (MEDIUM, 1人): r10 のヘッダの「5コミット」が、自分で書いた範囲の中身と合わない

同じ行の diff 式が示す範囲は6コミット。5 は `結果:` の行数で、コミット数ではない。
r11〜r14 は同じ数え方で一致しているので、ずれているのは r10 だけ。

- 結果: 対応済み `16a65839`（数を削った）

---

## 重複・矛盾した所見

- **AF-01 / AF-02 は3人、AF-04 は2人が指した**
- **AF-01 / AF-03 / AF-02 / AF-04 / AF-07 は1本の系統。** r13 で削った → r14 で戻した →
  r15 で戻し方も偽、という3ラウンドの往復で、その各段の判断が報告書に残っていた。
  **決着したのは、条件を狭め続けるのをやめて「書かない」に倒したとき**（AF-01）
- **r14 で判断が割れた1件は、comment が正しかった。** r14 は robustness の実測を根拠に
  oss-hygiene 側へ倒したが、その実測は「`--continue` と免除の20通りを分けるか」しか見ておらず、
  **同じ基準が免除の側の10通りにも当たること**を見ていなかった。
  AF-03 はそこを数えて割れを解いた
- 所見の水増しは無かった。3人とも「確認して所見が無かったもの」を明記していて、とくに
  **oss-hygiene が r10〜r14 の「対象コミット」5本を全て解決して規約との一致を確認し、
  推移14個を r1〜r14 のヘッダと1つずつ突き合わせている。** robustness は
  オプション順の変種（`git merge --quiet --abort` など）が git 自身に拒否されることまで確かめ、
  「2つとも当たるのに免除していない綴り」が無いことを確認した

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送った17件（再掲しない前提）
- `src-tauri/src/book/` の振る舞い（このラウンドも Rust の差分が無い）
- `user_book1.db` の実測3行とピーク確保の実測
- ShogiHome v1.29.0 側の引用（現物を取っていない）
- 種類に当たる残りのファイル型での競合（`package.json` / `Cargo.toml` / `vite.config.ts` など）。
  AF-01 は測った3件で成立するので、追加は範囲の確定にしか効かない
- `git am` の競合中の `status`（reviewer の手順では patch が clean に当たる経路が混ざった）

## lint / hook で強制できるもの

このラウンドで新しく挙がったものは無い。AF-06 の型（報告書が名乗るコミット数が、同じ行に
書いた範囲の中身と合うか）は、r14 が挙げた「`対象コミット` を `git cat-file -t` に通す」と
同じ仕掛けで拾えるが、**`.claude/reviews/` は `gate_kinds_for_path` が種類を返さない**ので
掛ける先は CI 側。#351 の候補で、このラウンドでは足さない（案A の方針）。

**AF-01 の直し方そのものが、機械化の代わりになっている** —— 種類と検査の対応を
コメントで主張しなければ、腐りようがない。

機械で守れないもの: AF-03（コメントが示した基準を、免除している綴り全部に当てたときに
矛盾しないか）、AF-02 / AF-04 / AF-05 / AF-07（報告書の記述と、実装・他の報告書の食い違い）。

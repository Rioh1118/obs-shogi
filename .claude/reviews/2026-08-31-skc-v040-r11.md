# レビュー skc-v040 ラウンド11

- 日付: 2026-08-31
- 範囲: ラウンド10の修正ぶん＋この PR の最終状態
- 走らせた reviewer: robustness（変異可）/ comment（読むだけ）/ rust（読むだけ）
- **変異を当てる reviewer を1人に絞った**（r9 / r10 の衝突事故を受けて）

## 所見

### [HIGH] R11-01 Shift_JIS の棋譜が1バイト壊れると読めなくなっていた（`main` では読めていた）

- 場所: `search/kifu_reader.rs`
- reviewer: rust（実測）。**自分でも再現した**
- コミット `0840291` で「UTF-8 と Shift_JIS はクレートが自分で読む」として
  総当たりから落としたが、**クレートは `!had_errors` のときしか復号を採らない**。
  誤りが1つでもあれば捨てて `Decode` を返す

  ```
  クレート単体: Err(Decode Error)
  Shift_JIS:    had_errors=true parse_ok=true   ← 誤りを落とせば読める
  UTF-8 lossy:  parse_ok=false
  ```

- **KIF の既定は Shift_JIS。一番よく通る道**
- 文言も「BOM を付けて保存し直すか、UTF-8 に変換してください」で合っていなかった
- **この PR で入れた退行。11ラウンド目に初めて見つかった**

### [HIGH] R11-02 `tied` は死んでいて、届く入力には合わない案内を出す

- 場所: `search/kifu_reader.rs`
- reviewer: robustness（変異で確認）
- `tied` の腕を `unreachable!()` にしても52件緑。`Equal => tied = true` を消しても緑
- **r10 が根拠に挙げた入力（BOM 無し UTF-16BE の1行 KI2）では同点が起きない** —
  逆バイト順の読みが孤立サロゲートで `had_errors` になり候補に入らない
- 実際に届くのは棋譜ですらないバイナリ（`[0xA0; 12]` など）

### [MEDIUM] R11-03 文言の長さのテストが恒真

- 場所: `search/kifu_reader.rs`
- reviewer: robustness（変異で確認）
- `assert!(count <= MESSAGE_LIMIT + 1)` は**固定したい定数そのもの**と比べている。
  `MESSAGE_LIMIT` を 1,000,000 にすると 200,050 文字の文言が通る

### [MEDIUM] R11-04 空白だけのファイルが 0バイトの門を通り抜ける

- 場所: `search/kifu_reader.rs`
- reviewer: robustness（実測）
- `"\n\n   \n"` は `Ok(moves=1)`。R10-03 の被害が改行1つで丸ごと再現する

### [BLOCK] R11-05 `declared_encoding` の doc の表が、実装・コメント・テストの3つと逆

- 場所: `search/kifu_reader.rs`
- reviewer: comment
- r10 で印を `ESC $ B` だけにしたのに、表はまだ3つ挙げている。
  **消した機能へ戻る導線が doc に残るのは4回目**（R7-03 / R8-02 / R10-06 に続く）

### [HIGH] R11-06 0バイトを弾く根拠「どの形式のパーサも `Ok`」が4形式のうち2つで偽

- 場所: `search/kifu_reader.rs`
- reviewer: comment（実物で確認）
- `.jkf` は serde が、`.csa` は `csa` の手番行の要求が先に弾く。
  `Ok` になるのは KIF / KI2 だけ

### [HIGH] R11-07 `initial_position.rs` の「索引に入らない」が隣の doc と正反対

- 場所: `search/initial_position.rs`
- reviewer: comment
- 同じ PR の2つの doc が同じ振る舞いについて逆を言い、しかも `index_builder` を
  犯人に名指ししている（その判断は `index_builder` に無い）

### [MEDIUM] R11-08 「CR だけで行を区切る古い棋譜も正当な入力」が偽

- 場所: `search/kifu_reader.rs`
- reviewer: comment（nom 7 の doctest で確認）
- nom の `line_ending` は裸の CR を受けない。文字コードに関係なくクレートが読めない。
  **門を置かない判断を支える2つの例のうち片方が偽**だった

### [MEDIUM] R11-09 `describe` の doc は6段だが出口は7つ

- 場所: `search/kifu_reader.rs`
- reviewer: comment。R9-04 と同型で2回目

## 範囲外（issue へ送る）

| 内容                                              | 送り先           |
| ------------------------------------------------- | ---------------- |
| 壊れた索引キャッシュで確保要求が暴走し abort する | **#334（新規）** |

## 見ていない範囲

- **11ラウンドすべてが `index_cache.rs` の符号化・復号本体を「見ていない範囲」に
  挙げ続けている。** #334 でその一部を拾った
- TS 側（この PR は未変更）
- `fs_scan.rs` の走査本体

## lint / hook で強制できるもの

- **テストの assert に、固定したい定数そのものを書くことを禁じる走査** → R11-03。初出
- `cargo-mutants`（#332）→ R11-02 / R11-03。**7ラウンド連続の提案**
- R11-01 は機械で拾えない。「1つの題材で測った結果を一般則にして別の題材で崩れる」の
  11件目で、**今回はそれが実害（読めていた棋譜が読めなくなる）に届いた**

---

## 修正結果（`/review-fix`）

9件すべて直した。1コミット（`fix: Shift_JIS の棋譜が1バイト壊れると読めなくなっていた`）。
`npm run verify:rust` / `npm run verify` を通してある。テストは51件。

| 所見   | 結果                                                                                     |
| ------ | ---------------------------------------------------------------------------------------- |
| R11-01 | 最終手段を2段（UTF-8 lossy → Shift_JIS lossy）に。**文字コードの表で回すテスト**に直した |
| R11-02 | `tied` を消した。同点は先に試したほうを採る（どちらでも行番号は1行目）                   |
| R11-03 | 定数でなくリテラルで上限を置いた                                                         |
| R11-04 | 判定を「バイト列が空白だけか」に広げた                                                   |
| R11-05 | 表を `ESC $ B` だけに直した                                                              |
| R11-06 | KIF / KI2 に限った                                                                       |
| R11-07 | `read_path_to_jkf` の `# Errors` を指すだけにした                                        |
| R11-08 | CR の例を落とした                                                                        |
| R11-09 | `tied` を消したので解消                                                                  |

### 自分でテストを1本消していた

範囲の広い置換で `describe_prefers_the_more_certain_reason`（r8 の成果物）を
巻き添えにしていた。**コンパイラの未使用 import が教えてくれた**。戻した。
`python` の範囲置換は、間に挟まった関数を黙って消す。

### コーパスで測り直した

11ラウンドで読み取り経路を大きく書き換えたので、実コーパス609件を通し直した。

```
parsed OK: 608, ERR: 1
total JKF nodes: 67600
```

**版を上げた直後と同じ**（`bug_big.kif` の1件だけが読めない）。
11ラウンドの書き換えでコーパスの読み取り結果は動いていない。

### 分類ごとの推移

| 分類                           | r1  | r2  | r3  | r4  | r5  | r6  | r7  | r8  | r9  | r10 | r11   |
| ------------------------------ | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | ----- |
| コメントと実装の食い違い       | 3   | 5   | 4   | 4   | 4   | 4   | 3   | 4   | 4   | 4   | 5     |
| 空振り / 題材の選び違い        | 1   | 1   | 1   | 1   | 2   | 2   | 2   | 2   | 1   | 1   | 3     |
| 自分が前のラウンドで入れた退行 | 2   | 11  | 6   | 7   | 8   | 6   | 5   | 5   | 6   | 5   | 5     |
| 読み取りそのものの穴           | 0   | 0   | 1   | 0   | 0   | 0   | 0   | 0   | 0   | 2   | **2** |

**R11-01 は、11ラウンドで初めて「利用者が読めていた棋譜を読めなくした」所見。**
文言の精度を10ラウンド磨いた一方で、**総当たりから外した文字コードが
本当に不要かを測っていなかった**。

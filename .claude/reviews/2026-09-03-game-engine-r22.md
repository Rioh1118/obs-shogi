# 対局エンジン レビュー ラウンド22

- 日付: 2026-09-03
- 範囲: ラウンド21と同じ
- 範囲外: `analyzer.rs` / `bridge.rs` の中身（#371）
- 観点: rust / comment の2本。**変異の並行実行による汚染を避けるため3本目は出していない**

---

## doc・コメント

### R22-H1 doc の引き剥がしが、そのラチェットを足したファイルで、足した18分後に再発している

`comment_identifiers.rs`。`no_doc_block_repeats_a_line` の doc の途中へ
`a_url_in_a_string_is_not_a_comment` を挿入している。

いま `cargo doc` で `a_url_in_a_string_is_not_a_comment` を開くと、
**先頭9行は別のテスト（doc ブロックの重複検査）の説明**で、
`no_doc_block_repeats_a_line` は doc を1行も持たない。

**ラチェットが止めるはずだった形そのもの。** しかも `no_doc_block_repeats_a_line` は
**同じ行の重複**しか見ないので、別の言い回しで足したこの形では緑のまま通る。
5回目。

### R22-H2 「`manager.rs` にはテストが無い」が現物と食い違う

`game-session.md:461`。`manager.rs` には3本ある（ラウンド21で足した）。

この文は「埋まっていないセル」節の**前置き**で、次に着手する人が
「どこが素通りか」を決める根拠になる。閉じる経路を「未検証」と読んだ人は
既にあるテストを二重に書くか、逆に「元から見ていない」と判断して
`close_all` の戻りを潰す変更を通す。

**ラウンド21で `(G1, E15)` を直した理由と同じ形が、同じ文書の別の行に残っている。**

### R22-H3 「埋まっていないセル」の `(G0, E1)` 行が、同じ文書の ※10 と正反対

※10 は「E1 / E4 の `is_engine` の枝は踏んである」、表は「`(G0, E1)` は未検証」。
**ラウンド21が `(G1, E15)` について直したのと同じ食い違いを、その修正で新しく作った。**

### R22-H4 `scanning` の「免除を置かない」が成り立っていない

`mod scanning;` を持つファイルは**丸ごと**検査から外れる。
`root_guard.rs` は現に手で数えている（`.find("\n}")` / `rest.find('<')` /
`chunk[head..].find('(')` / `rest.find('(')` / `rest.find('{')` / `.find(')')` の6箇所）。

doc は「`tests/` に手書きの数えは残らない」という保証を主張しているが、
実際に保証されるのは `mod scanning;` を書いていない3ファイルだけ。
**次に走査を書く人は `mod scanning;` を1行足せば（それは推奨されている行為でもある）、
以後どれだけ手で数えても緑になる。**

### R22-M1 `Command` の doc「判定は `calls` だけを通す」が、順序検査には掛かっていない

`guarded_variables` は `body`（文字列を残した写し）に `match_indices(GUARD)` を掛ける。
**ラウンド21-B1 が主検査について直した穴が、順序検査の側に残っている。**

筋道: `path.exists()` より手前にその綴りを含む文字列（ログ行、エラーメッセージ）が
あるコマンドは、実際に関門を後ろへ動かしても違反として出ない。
doc は「`calls` だけを通す」と書いているので、読んだ人はもう塞がっていると判断する。

### R22-M2 ponderhit 行の「`A3` は踏んである」が、振り分けの側では踏めていない

`Activity::Stopping` を渡して `hand_turn_to` を呼ぶテストは無い。
`stopping_a_search_cancels_it_and_sets_the_right_restart` は `stop_then_start` を
直に呼んでいるので、※2 の表の `A3` 行の**振り分けそのもの**は通らない（reviewer は推測と明示）。

### R22-M3 ラウンド21が表から外した数が、Rust doc と TS の4箇所に残っている

`commands/game.rs` の「4つ挙げてある」「3つあり」、`tauri.ts` の同じ2つ、
`manager.rs` の「3つあり」、`failure-surfacing.md` の「3つで」。

**指し先を持ちながら数だけ繰り返す**形。5つ目の理由が増えたときに嘘になる。

### R22-M4 「台帳は1つ」の理由が2箇所に本文ごとある

`registry.rs` と `state.rs`。所有の話は `manager.rs` にも三度目の説明がある。

### R22-M5 `CLAUDE.md` の「`src-tauri/tests/` は走査そのものを見ているだけ」が `engine_timeouts.rs` と食い違う

`engine_timeouts.rs` はソースを読まず、上限どうしの関係を式で固定している。
`lib.rs` の `CLOSE_TIMEOUT` の doc は**まさにその保証を根拠にしている**。

これを読んだ人は、定数を動かして赤くなったときに「走査の都合」と読んで式を緩める
——ADR-0008 決定4 の「弱いほうに揃えない」と逆向きに働く。

---

## Rust

### R22-H5 doc の引き剥がし（comment 側の H1 と同じ）と、**ラチェットの主張が過大**

`no_doc_block_repeats_a_line` の doc は「拾えるのは、同じ要約を書き足した形だけ」と
書いているが、**実際に拾えるのは完全一致の重複だけ**。要約の書き足しは1文字でも
違えば素通りする。現に自分自身のファイルの引き剥がしを緑で通している。

### R22-M6 門番が、手で数えている4ファイルを全部免除し、数えていない2ファイルにだけ掛かっている

実測:

```
root_guard.rs           免除=true   手書きの数え=11
production_unwrap.rs    免除=true   手書きの数え= 0
comment_identifiers.rs  免除=true   手書きの数え= 2
engine_layering.rs      免除=true   手書きの数え= 3
serde_naming.rs         免除=true   手書きの数え= 4
engine_timeouts.rs      免除=false  手書きの数え= 0
timeout_result.rs       免除=false  手書きの数え= 0
```

**字句解析を書くファイルは必ず `mod scanning;` を書くので、手書きの数えを
書きうる全ファイルが構造的に免除される。**

述語にも抜けがある。免除の無いファイルで実証:

- `const OPEN: char = '{';`（**単一の `=`** の直後）
- `s.split(['{'])`（要素が1つの**配列**）
- `s.find('\u{7b}')`（エスケープ）
- 腕が1つだけの `match ch { '{' => .., _ => {} }`

拾える形（`find('{')` / `contains('{')` / 折り返した引数 / `c == '{'` /
`b != b'{'` / `matches!` / `trim_start_matches(['{','}'])`）は doc どおり動いている。

### 変異試験（baseline 差分、14件中14件 killed）

**ラウンド21で足した 9+3+1 本は、すべて名前が主張する分岐を見ている。**

`matching_angle` と `blank_out` のバイト長は**単体テストだけ**が落ちた——
いま `src/` にその形（`->` を含むジェネリクスを持つコマンド）が無いので現物では踏めない。
**単体で押さえたのは正しい判断**、と reviewer が明記している。

---

## 修正計画

**所見11件（重複を除くと9件）。数は 39 → 27 → 21 → 24 → 31 → 11 と落ちた。**

内訳が変わっている。**実装の穴は0件**で、残っているのは
「ラチェット自身の穴」と「doc が現物と食い違う」の2種類だけ。

しかもその2種類が**同じ形**をしている——**自分が足した機構の主張が、その機構の
現物より強い。** 「免除を置かない」（実際はファイル丸ごと免除）、
「拾えるのは同じ要約を書き足した形」（実際は完全一致だけ）、
「`calls` だけを通す」（順序検査は `body` を見ている）。

### 順

1. **主張を現物に合わせるか、現物を主張に合わせるか、どちらかに倒す**（R22-H4 / H5 / M1 / M6）
2. **doc の引き剥がしを直し、ラチェットを `#[test]` の直前に絞って広げる**（R22-H1）
3. **表とテストの食い違い**（R22-H2 / H3 / M2）
4. **数の重複と `CLAUDE.md`**（R22-M3 / M4 / M5）

## 結果

（`/review-fix` で書き戻す）

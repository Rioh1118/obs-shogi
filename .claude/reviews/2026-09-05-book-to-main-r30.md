# レビュー book-to-main ラウンド30

- 日付: 2026-09-05
- 範囲: r29 の所見4件を直した4コミット＋報告書（`git diff e157700c f1a569e6`）
- 走らせた reviewer: architecture / oss-hygiene
- 対象コミット: `f1a569e6`
- 前ラウンド: `.claude/reviews/2026-09-05-book-to-main-r29.md`

所見 **5件**（BLOCK 0 / HIGH 0 / MEDIUM 5）。
推移は 22 → … → 3 → 4 → 4 → **5**。

**6ラウンド続けて HIGH 0。** 増えたのは、変異を**59通り**当てて
r25 以降ずっと積み残していた `codeOf` の c-like 枝まで踏んだため。

issue へ送るものは無かった。

---

## 所見

### AV-04 (MEDIUM, 1人が実測): 行コメントの `g` を外す変異が生き残っていた

`stripLineComments` の `g` を外すと**2本目以降の `//` が残る。**
当てた時点の706本すべてが緑のままだった。

corpus は 1,573,727 → 1,866,272 バイトに太り、**識別子の形をした綴りが33件入り込む**
（`root_guard` / `timeout_marker` / `engine_layering` など）。

**ただし、いまの検査の答えは変わらない。** `docsIdentifiers.test.ts` が見るのは
`docs/state-transitions/` が引く195件で、原型でも `g` 除去でも
`missingIdentifiers` は `[]` を返す。**corpus が太る向きの破損なので、
`[]` は `[]` のままにしかならない。**

```
入力   : "let x = 1; // 説明\nlet y = 2; // DEAD_NAME のこと\n"
orig   : "let x = 1; \nlet y = 2; \n"
g 除去 : "let x = 1; \nlet y = 2; // DEAD_NAME のこと\n"
```

既存の3本はどれも**行コメントが1本しか無い入力**なので、最初の1本が落ちれば緑になる。
**シェル側は AT-04 で同じ罠に気づいて「2つ置く」形にしてあるのに、
c-like の行コメントだけ抜けていた。**

**塞ぐ理由は「いま赤くなる」ではない。** `stripLineComments` が何をする関数かは
doc が宣言していて（「行コメントを落とす」）、その宣言を固定するものが1本も無かった。
`docs/state-transitions/` が33件のどれかを引いた瞬間、
**壊れた側だけが黙って緑になる。**

- 結果: 対応済み `298a5509`（**この1本だけが落ちることを確認**）

### AV-01 (MEDIUM, 1人が実測): 同じ「r27」が2つの別のものを指すようになった

`58ad1581` が入れた鎖は**対象のヘッダ**で番号を振り（r24 / r25 / r27）、
36行下の「r27 には無い」は**所見を書いたラウンド**で振っている（r25 / r26 / r28）。
**どちらも正しいが、同じページで `r27` が「入っている」と「無い」の両方になる。**

直す前の `AP-06 → AQ-03 → AR-04 → AT-01` は所見番号だけを並べていて、
この衝突は無かった —— **「数え方の根拠を正す」ための直しが、新しい二重索引を持ち込んだ。**

- 結果: 対応済み `dafa3116`（鎖の側に両方を書いて索引を一意にした）

### AV-02 (MEDIUM, 1人が実測): 「10箇所」と内訳「r2（5箇所）」が別の数え方から出ている

内訳を足すと9にしかならない。10 は**行**で数えた値で、r2 の 5 は**綴りの異なり数**。
r2 は6行に7出現あり、`:56` が `F-05` と `F-10` を1行に持つ。

**同じ数え方から2つの数が出ていない形は、r25 の AQ-02 が r24 に立てた基準そのもの。**

- 結果: 対応済み `9db0711d`（行に揃え、他の数え方での値も添えた。AU-02 の結論は数に依らない）

### AV-05 (MEDIUM, 1人が実測): 「706本すべて緑のままだった」は、当てた時点では705本

`88dd1666` はテストを1本足すコミットなので、その直前の木（`e157700c`）に戻して
`*`→`+` を当てた実測は **705 passed**。706 は**直しを入れた後**の値。

「その時点の suite 全体が見逃した」の根拠として書いてあるので、
追試する人が `git checkout e157700c` すると数が合わない。
**r29 が AU-01 で所見にしたのと同じ形の数え違いが、その直しの側にも入っていた。**

- 結果: 対応済み `660b9421`（報告書を705へ。コミットメッセージ側は訂正を報告書に残した）

### AV-03 (MEDIUM, 1人が実測): 反証済みの数え方が2つのコミットメッセージに残っている

`58ad1581` が r28 の本体を「3度・非連続」に直したが、
その直しの対象になった `6450d325` と、報告書を積んだ `e157700c` のメッセージは
**「AP-06 → AQ-03 → AR-04 → AT-01 と4ラウンド連続」のまま。**

`CLAUDE.md` は「経緯は git log と PR に残る」と決めているので、
`git log --grep=AP-06` で辿る人は反証済みの鎖に着く。
r28 の「結果: 対応済み `6450d325`」がそこへ送るため、
**報告書を正しく読むほど誤った記述に着く。**

- 結果: 対応済み `be6bda97`（**reword はしない** —— 以降のハッシュが全て変わり、
  r24〜r29 が本文に持つ「対応済み `<hash>`」が全部空を指す。訂正は報告書側に置いた）

---

## 重複・矛盾した所見

- **5件とも1人ずつ**
- **5件すべてが r29 の直しから出た。** r23 / r24 / r29 に続いて4度目
- **AV-04 は「変異を当てて塞いだ」の4度目の取りこぼし**
  （r27 が8通り → r28 が `g` の2つ → r29 が `*`→`+` → r30 が `stripLineComments` の `g`）。
  r29 は29変異を当てたが、**当てた先が `stripShellStrings` に寄っていて、
  両方の枝が通る `stripLineComments` に届いていなかった。**
  今回は**59通り**当て、`stripShellStrings` の2重変異10通りと、
  r25 以降ずっと積み残していた **c-like 枝22通り**まで踏んだ
- **AV-01 / AV-02 / AV-05 は同じ根**（数え方が2つ混ざる）。6ラウンド連続。
  **今回はうち2件が「直した側が持ち込んだ」形**で、AU-01 の直しと AU-02 の内訳
- **AV-03 はこのループで初めて出た形** —— 報告書は直したが、
  その根拠を持つコミットメッセージが訂正前のまま残る。**reword できないので構造的に残る**
- 所見の水増しは無かった。2人とも「確認して所見が無かったもの」を明記していて、とくに:
  - **`stripShellStrings` は尽きた** —— 単一15通り＋2重10通りを全て当て、
    生存7件はどれも corpus が1バイトも変わらない。
    答えを変える生存は `[^"\n]`→`.` の1件だけで、向きは red（見える壊れ方）
  - **c-like 枝の生存9件は、実 corpus に対して出力が1バイトも変わらない**
    （`missingIdentifiers` の145件も完全に一致）。
    「行頭でないブロック開始」「`*/` の重複」が走査対象に実在しないため
  - **`88dd1666` の新テストは `*`→`+` の唯一の殺し手**（落ちるのはこの1本だけ）で、
    2つの assert は片側だけでは塞げない（食い過ぎ側は `toContain` が拾う）
  - **r29 の `orig` / `plus` の2行と行番号 `210` は現物と完全に一致**。
    戻る名前もちょうど3件（`GIT_DIR` / `GIT_INDEX_FILE` / `GIT_WORK_TREE`）
  - **`33b38cac` の引っ込めは完了している** —— 同じ規則の写しを
    `docs/` / `CLAUDE.md` / `AGENTS.md` / `CONTRIBUTING.md` / `.claude/skills/` /
    `.claude/agents/` / `src/` / `src-tauri/src/` に走査して0件。
    残るのは事実の記述だけ
  - **「全11本」は正しい** —— `git rev-list` を全走査して括弧書きを持ったことがある版を
    集めると r14〜r20 と r24〜r27 のちょうど11本。r21〜r23 は元から持っていない
  - **他シリーズに同じ形は無い** —— 括弧書き自体は他にもあるが、
    どれも「外した観点の理由」か「役割注記」で、
    **人数を差分の中身で正当化する形は book-to-main 以外に1件も無い**
  - **r28:81 の「このループで初めて」は反証できなかった** ——
    ヘッダ関連の所見7件を欄ごとに分けると、`対象コミット` 欄と `範囲` 欄は各2度で、
    3度に達した欄は括弧書きが初めて

## 見ていない範囲

- #386 / #342 / #290 / #351 へ送った19件（再掲しない前提）
- `origin/main` が元から持っていた実装と doc の中身
- `docs/spec/screens/` の13面が現物の UI と合っているか
- `user_book1.db` の実測3行とピーク確保の実測
- `docsIdentifiers.ts` の `IDENTIFIER` / `EXEMPT` / `missingIn` 側の変異
- `walk.ts`（`sourceFiles` の拡張子・`includeTests` の既定）の変異
- 3重以上の変異
- **`stripLineComments` の `g` 除去を、`codeOf` を使う他のラチェットが見逃す量**
  （`commentHistory` / `asyncResultUse` / `escapeReceivers` など）。
  測ったのは `missingIdentifiers` の答えだけ
- `book-foundation` 以外のシリーズの所見番号の採番全体

## この PR の外で観測したこと

**ゲートがまたメインのチェックアウトを検証した**（#342）。今回は別セッションが
`src/__tests__/commentHistory.test.ts` と `sourceText.ts` を編集中で、
**そちらの `sourceText.ts:22` が `commentHistory` に引っ掛かっていた。**
このワークツリーの同じ検査は緑。**あちらが畳むまで待って通した。**

## lint / hook で強制できるもの

- **AV-04 で `stripLineComments` の `g` 変異が塞がった。** ただし**取りこぼしは4度続いている。**
  今回の実測で、#351 で回すべき定型変異の対象が
  **`stripShellStrings` だけでなく `stripLineComments` / `stripShellComments` /
  c-like 枝の全体**であることが分かった。「生存**かつ** `sourceCorpus()` が変わる」を
  落とす形で、59通りのうち1件を機械的に拾えた
- AV-01 / AV-02 / AV-05（数え方）と AV-03（reword できないコミットメッセージ）は
  機械で防げない

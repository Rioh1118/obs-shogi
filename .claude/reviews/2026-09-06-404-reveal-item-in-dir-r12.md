# レビュー 404-reveal-item-in-dir ラウンド12

- 日付: 2026-09-07
- 範囲: `fix/404-reveal-item-in-dir`（r11 の修正まで）
- 走らせた reviewer: architecture / react / robustness / comment / oss-hygiene
- 対象コミット: 畳み込み済み
- 前ラウンド: `2026-09-06-404-reveal-item-in-dir-r11.md`

所見は **14件**（18 → 21 → 24 → 20 → 13 → 14 → 6 → 11 → 9 → 16 → 15 → 14）。
重複を畳む前は27件。**r11 の修正が作った所見が6件**（R12-01〜R12-05 と R12-14）。

r11 の反省（同じツリーを5本が共有して変異が混じった）を受けて、今回は
「作業ツリーを1文字も変えない。変異は scratchpad のコピーで」と指示した。
5本とも守り、`git status --short` は最後まで空。react は7ファイルがバイト一致であることを
確認したうえでコピーに変異を当て、`changed` の4箇所を1つずつ潰す表を作っている。

## 所見

### R12-01 [HIGH] F-37 の「作れた」と「作れなかった」が、段も色も同じで出る（robustness）

記号も面も `role` も段が決めるので、成功と失敗を同じ `info` にすると、6秒で消える2枚の差が
**題の末尾の1語**（「しました」／「できませんでした」）だけになる。作れていないのに
「作れた」と読んで先へ進むと、どのルートにもそのフォルダが無い。
台帳の F-37 は「次の操作: 要る」と書いているのに、失敗側の段が「何もしなくてよい」を名乗る。

- 結果: 対応済み。作れた回は `info`、作れなかった回は `warning`（同じ名前でもう一度で通る
  見込みがある）。本文も割り、失敗側は次の一手を書く

### R12-02 [MEDIUM] F-37 の失敗側の題を固定しているテストが1本も無い（robustness）

`notify` への assertion は3箇所しか無く、宛先を失った作成を扱う2本はどちらも
`success: true` で終わる。題の三項を潰す変異も、`dedupeKey` を落とす変異も通る。

- 結果: 対応済み。失敗側の段と題を見る1本を足し、成功側の `dedupeKey` も見るようにした

### R12-03 [MEDIUM] 「文面の決まりは `settings.md`」が空振りしている（robustness / oss-hygiene の2名）

台帳とコードの2箇所がそこを指しているのに、指し先には F-13 のぶんしか無い。
同じ節の「『フォルダを開けなかった』**だけ**は通知へ出す」も、5行上の表が2件目を載せた時点で偽。

- 結果: 対応済み。F-13 と F-37 で節を割り、F-37 の文面の決まり（題で成否を言う／段も割る／
  旧ルートのパスを書かない／本文は1行）を書いた

### R12-04 [MEDIUM] `engines/` が無い回にも「engines/ 配下に置いてください」と続けて言う（robustness）

「検出された問題」の2行目に門番が無いので、`other` と `missing` では2行が必ず並ぶ。
1行目が「フォルダではない」と言った当のものの中へ置け、と指示することになる。

- 結果: 対応済み。`enginesDirUsable` で門番した（述語は R11-07 で用意済み）

### R12-05 [MEDIUM] 帯の再スキャンを塞いだことで、走査が返らない環境の出口が消えた（robustness。r11 の退行）

画面にある再試行の口は帯と基本の節の2つで、r11 でどちらも同じ条件で死ぬようにしてしまった。
走査が返らない環境では、残るのは「AI_ROOT を選択…」だけ——同じフォルダを選び直しても
何も起きないので、閉じて開き直すしかなく、編集中の下書きが消える。

- 結果: 対応済み。塞ぐのは宛先が食い違う「作成」だけにし、再スキャンは `busy`
  （押せなくしない）で走っていることだけ示す

### R12-06 [HIGH] ダイアログの「engines/ を作成」だけ関門が無い（react / robustness の2名、実測）

ルート A で押し、返る前に B へ切り替えると、**もう指していない A の失敗**が B の画面に
赤字で出る。`indexStatus` が error に落ちるので、B の索引は正常に読めているのに
候補が全部 disabled になり、placeholder が「スキャン結果なし」になる（スキャンは通っている）。

- 結果: **関門は対応済み**（この枝で5箇所目の同型のうち、唯一残っていた口）。
  変異を当てて落ちることを確かめた。
  **失敗の置き場を `indexStatus` から分けるのは #495 へ**——新しい失敗の面を1つ作ることになり、
  台帳にも行が要るので #404 の範囲の外

### R12-07 [MEDIUM] `EnginesDir` の doc が「UI に `===` を散らさない」と約束しているのに、hero に残っている（comment）

doc は「散らさなければ、状態が増えた日に赤くなるのは表だけ」と読者に約束している。

- 結果: 対応済み。約束のほうを実装に合わせた（1つの状態を名指しする分岐は残してよいが、
  **述語で先に受ける**。hero は R11-07 でその形にしてある）

### R12-08 [MEDIUM] テストの doc が、直す前の実装の実測を記録している（comment）

「実測では `Maximum update depth exceeded` が出たうえ、103 回描いても収束しなかった」は
消したコードの説明。`CONTRIBUTING.md` の「『元は〜だった』も書きません」に当たる。

- 結果: 対応済み。理由は `autofillPreset` の doc を指すだけにした（経緯は r11 の報告書に在る）

### R12-09 [MEDIUM] `CONTRIBUTING.md` の表の見出しが検算すると偽（comment）

「なぜ `walk.ts` を通せないか」と書いたが、`ratchetIndex` は `sourceFiles` で歩ける
（自前で歩く理由は親ディレクトリ名が要ること）。R11-14 が直した「例外は2つ」と同じ形。

- 結果: 対応済み。見出しを「なぜ自前で歩くか」にした。
  **oss-hygiene は同じ表を現物と突き合わせて「漏れも余りも無い」と報告している**ので、
  行の中身はそのまま

### R12-10 [MEDIUM] `canCreateProfile` の doc が自分と矛盾して見える（comment）

「1度だけ組んで3箇所が引く」と「条件そのものを1箇所に寄せ切るのは #475」が並んでいて、
何が残っているのか本文から特定できない。

- 結果: 対応済み。#475 に残っているのが何か（`enginesCount > 0` を条件にするのが正しいかは未決）を書いた

### R12-11 [MEDIUM] ダイアログのテストの helper が「帯が出る唯一の分類」と書いている（comment）

帯は `missing` と `other` の2分類で出る。読んだ人は「`other` では帯が出ない」と受け取る。

- 結果: 対応済み。2分類であることと、**`other` の帯はまだどのテストも通っていない**ことを書いた

### R12-12 [MEDIUM] `autofillPreset` の `changed` は4箇所あるのに、テストは1箇所しか殺せない（react、変異で確認）

値を検査している唯一のテストが**全欄が空の下書き**を渡すので、3つの枝が同時に立つ。
どれか1つが `changed` を立て損ねても残りが `next` を運ぶ。
落ちたときの見え方は「AI 名だけ永久に自動で入らない」で、警告も出ない。

- 結果: 対応済み。欄ごとに1本ずつ足した。4つの変異が1つずつ落ちることを確かめた

### R12-13 [MEDIUM] r11 の「赤字を消すのは `await` の後」を元に戻す変異が通る（react、変異で確認）

r11 が所見として立てて直した振る舞いそのものが、無防備で残っていた。

- 結果: 対応済み。1本足した（変異で落ちる）

### R12-14 [MEDIUM] 画面の仕様の「すでに入っている値は上書きしない」が、定跡の扱いと食い違う（comment）

`autofillPreset` の doc は自分の権威として画面の仕様を名指ししているのに、
その仕様には「定跡を無効にした回は入っていても落とす」の例外が書かれていない。

- 結果: 対応済み。例外と、「1つも埋めなかったら同じ下書きを返す」を仕様に足した

## 見送り（issue / IDEAS へ）

| 所見                                                                                                            | 送り先          |
| --------------------------------------------------------------------------------------------------------------- | --------------- |
| ダイアログの作成の失敗が `indexStatus` を殺す（R12-06 の後半）                                                  | #495            |
| 定跡の既定値の規則がチェックボックスにも手書きで残っている                                                      | #496            |
| 走査の状態の持ち方が2つに割れている                                                                             | #497            |
| `cx` が7ファイルに同じ定義で散っている                                                                          | #498            |
| `import type` だけの循環が14本                                                                                  | #499            |
| SCSS の持ち主が別ディレクトリ／「作成を出す理由」の重複／本文のコメントが多い3関数／画面の型の置き場の前例が2つ | `docs/IDEAS.md` |

## 重複・矛盾した所見

- **R12-06 は react と robustness が独立に、別の再現で挙げた**（前者は赤字と候補の disabled、
  後者は成功側の二重走査も）。直し方は一致
- **`CONTRIBUTING.md` の表について comment と oss-hygiene が逆の結論を出した。**
  comment は「`ratchetIndex` の理由が偽」、oss-hygiene は現物と突き合わせて「漏れも余りも無い」。
  見ているものが違う（前者は見出しの主張、後者は行の対応）ので、**見出しだけを直した**

## 見ていない範囲

- **実機を誰も動かしていない。** r1 から12ラウンド続けて同じ
- `other` の帯（ダイアログ）はどのテストも通っていない
- `steps/` の個別部品、SCSS、Rust 側の振る舞い
- r1〜r11 の所見が全て塞がっているかの通読

## lint / hook で強制できるもの

- R12-06 は**型で強制できる**（#497 で状態の持ち主を1つにすれば、
  「索引は新しいルート・ステータスは古い失敗」という組が作れなくなる）
- R12-12 / R12-13 は機械では取れない。**変異を当てるしか判定手段が無い**ので、テストで固定した
- 「同じ事実を2つの文書が別々に数える」（R12-03 と、oss-hygiene が挙げた `system-dialogs.md`）は
  2回目。**数を1つの文書に閉じ、他は参照に縮める**ことで解決した（検査は足していない）

## 検証

- `npm run verify` … 通る
- `npm run verify:rust` … 通る

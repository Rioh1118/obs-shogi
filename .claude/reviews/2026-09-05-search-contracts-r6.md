# レビュー search-contracts ラウンド6

- 日付: 2026-09-05
- 範囲: `84241c87` の1コミット（`index_cache.rs` / `docs/state-transitions/search.md` / `scripts/rustdoc-ratchet.sh`）
- 走らせた reviewer: rust / comment
- 前ラウンド: `2026-09-05-search-contracts-r5.md`

**件数は 29 → 16 → 20 → 11 → 8 → 9。** 2つの reviewer が別々に同じものを2件挙げたので、
畳んで **9件**。製品のデータを壊す新規欠陥は r3 以降ずっとゼロで、r6 も無い。

**増えたのは、r5 の修正が新しく作った退行が3件あるため**（R6-A / R6-D / R6-I）。
「直したことで別の場所が壊れた」が初めて主因になったラウンド。

## 所見

### BLOCK

#### R6-A `error!` に上げた7件のうち1件が、書く側でなく読む側にある

rust / comment（両方が独立に挙げた）。`index_cache.rs:258`。

`read_decode`（`:244-261`）は `try_restore` からしか呼ばれない**読む側**で、
ここに来た時点で書き込みは1バイトも起きていない。にもかかわらず文言が
`save_checkpoint` の6件と一字一句同じ「チェックポイントを書けない」。

**実測**: `rg 'チェックポイントを書けない'` は8件。うち7件は `save_checkpoint`（`:112-207`）の
中、**1件（`:258`）は `read_decode` の中**。

さらに正常な経路で出る。`decode_all` の最初の門番は `bad magic` / `bad version` /
`root hash mismatch`（`:526-542`）で、`try_restore` は `open_project` のたびに走る
（`commands.rs:82`）。**`VERSION` を上げると、開いた全プロジェクトで `error!` が1本
（`.bak` が残っていれば2本）出て、しかも嘘をつく。**
`search.md:242` はこの経路を「利用者に出るものは無い」と正規の経路として書いている。

**`84241c87` のコミット本文は「`save_checkpoint` の失敗7件」「残る2件は復元の失敗」と
書いた。実際は書き側6件・読み側3件で、そのうち1件を書き側の文言で上げた。**
手順9（本文の主張と差分の一致）は識別子の側では守られたが、**件数と性質の側で破れた。**

### HIGH

#### R6-B 門番の根拠が、その門番の出力を受け取る関数を指している（同型の3件目）

comment。`index_cache.rs:664-666` / `:1609-1610` / `index_store.rs:197-203` /
`commands.rs:107`。

「出現を持つ `file_id` は必ず節表を持つ。入れるのは `insert_many_file_segments` と
`install_restored` で、**どちらもファイル表と節表を対で受ける**」と書いた。

`install_restored` は対で受けていない。表を**丸ごと独立の引数**で受ける
（`file_table: FileTable, node_tables: NodeTables, ...`）ので、`file_id` ごとの
対応は型でも本体でも強制されていない。三つ組で受けて対を担保するのは
`insert_many_file_segments` だけ。

しかも `install_restored` の唯一の呼び手（`commands.rs:107`）が渡すのは
`decode_all` が返した `RestoredCache` — **いま説明しようとしている門番が検査した結果そのもの。**
根拠が循環していて、読み手が確かめに行くと確かめられない。

**r4-A → r5-C → R6-B で3件目。** どれも「他モジュールの振る舞いを断定した」形（規律1）。

### MEDIUM

#### R6-C 表に足した「節表の `file_id`」の行が、無条件に **B** と言い切っている

rust / comment（両方）。`docs/state-transitions/search.md:262`。

反例がある。**「節表はあるが出現ゼロ」の `file_id` は普通に入っている**:

- 削除された棋譜 — `tombstone_file`（`index_store.rs:297-310`）は `file_table` だけを
  差し替え、`node_tables` は `old.node_tables.clone()` でそのまま持ち越す
- 読めなかった棋譜 — `build.rs` が `NodeTable::empty()` と空の桶を入れる

`encode_all:391-401` は `by_id_iter` の `Some` を全部書くので、出現ゼロでも節表は blob に載る。
`decode_all` が `Err` を出すのは**出現側の突き合わせ**（`:669-672`）なので、
出現ゼロのファイルの節表 `file_id` が化けても通る。

同じ表の他の行（`node_id` / `fork_off` / `z0`）は全部「〜なら通る」を併記しているのに、
**この行だけ両枝とも B。** `checksum` を入れるかの判断材料を狂わせる。

#### R6-D 重複を6→2に減らした結果、因果が「実際に起きる場所」から消えた

comment。`query_service.rs:131-134`（コメント無し）。

「壊れた索引のヒットが 0手目として画面に並ぶ」というすり替えを実際に行っているのは
`nts.get(...).and_then(...).unwrap_or_else(CursorLite::root)` の3行だけで、**そこに一行も無い。**

r5-F の後、残った2箇所は `index_cache.rs:1931-1933` / `:2034-2036` の
**どちらも `#[cfg(test)]` の doc**。`cargo doc` にも出ず、`query_service.rs` を
読む人の視野にも入らない。**「1箇所に集める」は満たしたが、集めた先が持ち主ではない。**

#### R6-E R5-A で直した形が、隣のテストにそのまま残っている

rust。`index_cache.rs:2110-2119` / `:2020-2029`（同種: `:1578` / `:1594` / `:1602`）。

R5-A の修正は名指しされた1本にだけ当たった。**同じ関数群の隣は `rposition` +
`is_err()` のまま。**

**実測**: `decode_all`/`encode_all` に続く `is_err()` だけの assert は **5箇所**
（`:1578` `:1594` `:1602` `:2027` `:2117`）。一致数を確かめない byte poke は **2箇所**
（`:2022` `:2112`）。

`:2112` が探す `01 00 00 00` は blob に6箇所以上出る。いま正解の欄に当たるのは、
題材の鍵が桶51に落ちて桶52..255 の長さ欄がすべてゼロだから — **その根拠はテストのどこにも
書いていない。** 鍵を桶255寄りに変えるか出現を1件足すだけで `rposition` は `gen` を指し、
**別の門番を踏んだまま緑になる**（R5-A と同一の隠れ方）。

#### R6-F 書き側の新しい門番が依存する不変条件が、doc にもテストにも無い

rust。`index_cache.rs:402-413` / `store/node_table.rs:72-93`。

`encode_all` は `fork_off + fork_len > forks.len()` ならチェックポイント全体を書かない。
この門番が正常な索引を弾かない根拠は「`push_node` が `fork_off` を追加直前の
`forks.len()` に置き、その直後に `fork_len` 個だけ push する」という**別モジュールの
実装詳細ひとつだけ**。

**実測**: `node_table.rs` に `mod tests` は無い（`store/` で持つのは `segment.rs` だけ）。
`an_index_of_a_realistic_size_can_be_read_back`（`:1239-1253`）は `NodeCursor` を手で
組んでいて `push_node` を一度も通らない。

破れたとき壊れるのは索引の中身ではなく**保存**で、症状は「起動が毎回遅い」だけ。

#### R6-G `[index_cache] ` の接頭辞を、マクロと同じ仕事で7箇所手書きしている

comment。`index_cache.rs:130,157,170,176,180,191,198`（マクロは `:22-26`）。

このファイルは接頭辞を1箇所で持つために `trace!` マクロを置いているのに、
`error!` に上げた7箇所は同じ文字列を手で複製した。接頭辞を変えると `trace!` 経由だけ
追随し、**`error!` の7箇所が古い綴りで残る。**

「なぜ `trace!` でなく `error!` か」の根拠（`:153-155`）も `encode_all` の腕にだけ付いていて、
`:191` や `:198` を読む人には見えない。`:157` だけ工程名の括弧が無く揃っていない。

#### R6-H 同じ doc の「`src-tauri/tests/` にあるのは `root_guard.rs` だけ」が偽

comment。`docs/state-transitions/search.md:283-284`。

**実測**: `src-tauri/tests/` の `.rs` は **9本**（`comment_identifiers` / `engine_timeouts` /
`layering` / `production_unwrap` / `root_guard` / `serde_naming` / `state_transition_cells` /
`timeout_marker` / `timeout_result`）＋ `fixtures/` `roots/` `scanning/`。

後半（状態機械を回すテストは無い）は今も正しい。前半の「だけ」が10件を1件と言っている。
これを根拠に「Rust の検査はほぼ無い」と読むと、**`state_transition_cells.rs` が
まさにこの表のセルとテストを突き合わせていること**を見落とす。

`84241c87` はこの行を触っていないが、**同じコミットが同じファイルの表を実装に
合わせた回**で、規律3の「だけ」がそのまま残っている。

#### R6-I 書き側の門番を名乗る `_is_not_written` が、5つのうち3つにしか付いていない

comment。`index_cache.rs:1730,1797,1864` / `:1615`。

書き側の門番は5つ（fork範囲 `:407` / 桶の取り違え `:439` / 並び `:444` /
節表なし `:452` / `node_id` `:459`）。読み側は `_is_refused`、書き側は `_is_not_written` と
綴り分けているのに、`_is_not_written` は **3本**。

**実測**: `refusing to write: key belongs to bucket` の文言を見るテストは**0本**
（`belongs to bucket` は `:439` と `:653` の定義2箇所のみ）。節表なしの書き側は
`occurrences_without_a_node_table_are_refused`（`:1615`）の中に同居している。

コミット本文は「門番10個すべてに変異を当てた」と主張していて**それ自体は本当**だが、
綴りで数えると3。次に門番を足す人は綴りを信じて数える。

## 実測で問題なしと判断したもの

- **10個の門番は正常なキャッシュを1つも弾かない。** 本番で `NodeCursor` を作る口は
  `index_builder.rs:78` → `push_node` と `decode_all`（検査済み）の2つだけで、
  `push_node` は `fork_off = forks.len()` の直後に `fork_len` 個 push するので
  `fork_off + fork_len == forks.len()` を常に満たす。**弾く形は作れない**（根拠が細いのは R6-F）
- **`error!` に上げた7件のうち6件（`save_checkpoint` の中）は正常な経路で出ない**
- **`compact_bucket` の引数を落としても呼び手の意味は変わっていない。** `compact_all_buckets`
  （`:268`）は `from_fn` の添字を `&snap.buckets[b]` に使い続けている。
  `index_store.rs:315` の同名関数は別物で今回の差分は触れていない
- **鍵の値は変わっていない**（差分3ファイルに `position_key.rs` / `zobrist.rs` を含まない）
- **ラチェットは実走で `rustdoc warnings: 11 (baseline 11)`。集計行は1本だけ数えている**
- **手順5・9（識別子の突き合わせ）は守られていた。** R5-A〜H の識別子はすべて `git diff` に現れる
- **`cargo test --lib search::cache` 18 passed**

## 見ていない範囲

- `verify:rust` の `cargo fmt` / `clippy`（どちらの reviewer も未実走）
- `decode_all` の `scan` / `path_to_id` 節（r5 から持ち越し）
- `read/` 配下、`query_service.rs` の本体（r5 から持ち越し）
- `search.md` の `:230-268` 以外
- TS 側 `features/position-search/`、bench

## 修正計画（r6 → r7）

### 対象そのものを疑ったか

**疑うべき局面に入った。**

|                      | r1  | r2  | r3  | r4  | r5  | r6    |
| -------------------- | --- | --- | --- | --- | --- | ----- |
| 所見                 | 29  | 16  | 20  | 11  | 8   | **9** |
| BLOCK                | 1   | 2   | 1   | 0   | 1   | 1     |
| 新規のデータ破壊     | —   | 1   | 0   | 0   | 0   | **0** |
| **修正が作った退行** | —   | —   | —   | —   | —   | **3** |

r5 の計画は「6回目が出たら、直し方ではなく私がレビューを読む粒度を疑う」と書いた。
**6回目（R6-B）が出た。**

### 6回目の同型失敗。今度は粒度ではなく「部分適用」

| ラウンド | 何が起きたか                                                         |
| -------- | -------------------------------------------------------------------- |
| r1〜r3   | 名指しされた箇所の一部だけ直して閉じた（3回）                        |
| r4       | パスは見るが項目を見ない → 識別子まで下げた                          |
| r5       | `s.replace()` に assert が無く黙って何もしなかった → assert 追加     |
| **r6**   | **assert は効いた。効いたが「名指しされた1箇所」にしか当てなかった** |

R6-E がそれをそのまま示している。R5-A が名指した1本は直り、**同じ形の隣4本は残った。**
R6-B も同じで、r4-A / r5-C で2回名指された「他モジュールの断定」を、
名指された文だけ直して隣の文に同じ断定を書いた。

**手順を1つ足す。所見が「Xという形」を指摘したら、その形を `rg` で数え、
全件直すか、直さない分を報告書に理由付きで書く。** r5 の手順8（場所ごとに数える）は
修正の**後**の確認だった。これは修正の**前**に数える。

R6-A / R6-D / R6-I は別の形で、**「直したことで隣が壊れた」**。
r5 は `trace!` → `error!` を7件、重複コメントを6→2、テスト名を綴り分けと、
いずれも**一括で形を変える**修正だった。一括で変えるときは、変えた集合の境界を
1回数える必要がある（`error!` に上げた7件のうち1件が別の関数、
残した2箇所がどちらもテスト、綴りが3/5）。

### 直す順と、その修正が壊しうるもの

| 順  | 所見     | 直し方                                                                                                               | 壊しうるもの                                                                              |
| --- | -------- | -------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| 1   | **R6-A** | `:258` を `trace!("decode_all FAILED: {e}")` へ戻す                                                                  | r4-C の「見えない失敗」に戻る形。**戻らない**: `try_restore` の結末は全件構築で画面に出る |
| 2   | **R6-G** | `err!` マクロを足し7箇所を寄せる。根拠コメントは定義へ                                                               | R6-A で1件減って6件。**R6-A の後に**やる                                                  |
| 3   | **R6-B** | 断定を落とし、`install_restored` は「表を丸ごと受けるので、対を保証するのはこの門番自身」へ。`:1609-1610` は指すだけ | 門番の存在理由が薄く読める。**逆**: 循環でない根拠になる                                  |
| 4   | **R6-D** | `query_service.rs:130` の直上に1行。`index_cache.rs` の2箇所はそこを指すだけ                                         | r5-F の「6箇所」に戻る。**戻らない**: 実装1＋テスト2が指す形                              |
| 5   | **R6-C** | `:262` に出現ゼロの反例を書き足す                                                                                    | 表が長くなる。他の行と同じ形なので許容                                                    |
| 6   | **R6-H** | 2行目を削る（規律4: 書き足すより削る）                                                                               | 「Rust に検査はある」と読めてしまう。後半を残すので誤読しない                             |
| 7   | **R6-E** | `:2020` `:2110` に一致数の assert、`:1578` `:1594` `:1602` `:2027` `:2117` に文言の assert                           | **テストが落ちる可能性がある。落ちたら R6-E は BLOCK に昇格する**                         |
| 8   | **R6-F** | `push_node` に不変条件の doc、`node_table.rs` に `mod tests` を新設して1本                                           | 新規テストなので既存を壊さない                                                            |
| 9   | **R6-I** | `:1615` から書き側を切り出し、桶取り違えの書き側テストを足す                                                         | テスト名が変わる。`comment_identifiers.rs` が名前を見ていないか確認する                   |

**7 が最大の山。** R6-E の assert を足したときに落ちるなら、その門番は
「別の理由で緑だった」ことになる。**落ちたら報告書に書いて、順を組み直す。**

### r7 の焦点

1. **新しい手順（形を `rg` で数えて全件直す）が効いたか。** r7 で「部分適用」が0件なら効いている
2. **一括で形を変える修正が、境界を数えたか**（R6-A / R6-I が示した失敗）
3. **7 で assert を足したテストが、狙った門番の文言で落ちるか**
4. **`err!` マクロが `trace!` と同じ接頭辞を持つか**
5. **7回目の同型が出たら、範囲でなく「私がレビューを直すときの単位」を変える** ——
   1所見1コミットをやめ、**1つの「形」1コミット**にする

## 結果（r6 の修正）

**9件すべて直した。7コミット。** 計画の順どおり。

| 所見                | コミット   | 直し方                                                                            |
| ------------------- | ---------- | --------------------------------------------------------------------------------- |
| **R6-A** + **R6-G** | `82f12ebf` | `err!` マクロを足して7箇所を寄せ、読む側の `:258` は `trace!` へ戻す              |
| **R6-B**            | `09b01bd7` | 循環した一文を落とし、事実（対を担保するのは `insert_many_file_segments` だけ）に |
| **R6-D**            | `f4a59e08` | 因果を `query_service.rs` の実装へ。テスト2箇所はそこを指すだけ                   |
| **R6-C** + **R6-H** | `ef80cf47` | 表の行に反例を書き足し、「`root_guard.rs` だけ」を削る                            |
| **R6-E**            | `868aa13f` | 5箇所を `is_err()` から文言の検査へ。題材も一意に                                 |
| **R6-F**            | `a4c6e248` | `push_node` に不変条件の doc、`node_table.rs` に `mod tests` を新設               |
| **R6-I**            | `1de13e84` | 両側を見る2本を `_is_neither_written_nor_read` に。綴りの規約を1箇所に            |

### R6-E は計画どおり「山」だった

計画に「assert を足したときに落ちるなら、その門番は別の理由で緑だった」と書いた。**落ちた。**

`a_node_id_outside_the_table_is_refused` の byte poke が探す12バイトは、
**blob に3箇所あった**（`file_id` / `gen` / `node_id` が全部 `1` だったため）。
旧実装は4バイトの `1` を `rposition` で探していたので、なお外していた。
`gen` を `7` にして題材を一意にし、一致数を assert してから壊すようにした。

**文言の検査に実害があることも確かめた。** 桶の所属の検査を殺すと、テストは
`別の門番で落ちている: bucket 17 is not sorted` で落ちる。
**`is_err()` のままなら緑だった。** R5-A が隠れていた仕組みそのもの。

### 変異の結果（読み書き10個 ＋ 新規2本）

| 対象                                          | 変異                          | 結果              |
| --------------------------------------------- | ----------------------------- | ----------------- |
| 読み: fork 範囲 / 桶の所属 / 並び / `node_id` | 条件を `false` に             | **4/4 落ちる**    |
| 書き: fork 範囲 / 桶の所属 / 並び / `node_id` | 条件を `false` に             | **4/4 落ちる**    |
| 書き: 節表なし                                | `None` の腕を `usize::MAX` に | **落ちる**        |
| 読み: 節表なし                                | 上と同じテストが両側を見る    | **落ちる**        |
| `push_node`                                   | `fork_off` を +1              | **2本とも落ちる** |
| `push_node`                                   | `node_id` を常に `0`          | **1本落ちる**     |

### 手順の効き

| 手順                                            | 結果                                                        |
| ----------------------------------------------- | ----------------------------------------------------------- | ---------------- |
| 7. `s.replace()` に `assert`                    | **全置換に付けた。1回止まった**（表の行に段落を差し込んで ` | ` を壊した箇所） |
| 8. 場所ごとに識別子を数える                     | **15項目すべて着地。★ゼロ件**（r5 は1件の偽陽性が出た）     |
| 9. コミット本文と差分の一致                     | 通った                                                      |
| **10. 直す前に形を `rg` で数える**（r6 で追加） | **効いた。下記**                                            |

**手順10が3回効いた。**

1. R6-G で `log::error!("[index_cache]` を数え、寄せた後に**1件残った**のを見つけた
   → マクロ定義自身と分かった。ついでに `:1208` の「呼び手は `log::warn!` する」を
   確かめ、`commands.rs:171` が実際にそうしていることを裏取りした
   （**復元の失敗は既に `warn` で記録されている** = R6-A の直し方の裏付け）
2. R6-B で `install_restored` を根拠に挙げるコメントを数え、名指しの2箇所のほかに
   **`:644` を見つけた**。こちらは実物と一致していたので**残した**
   （`Segment::new_sorted` が昇順を前提に受けることも確認）
3. R6-E で `is_err()` を数え、5箇所すべてに当てた

**「名指しされた1箇所だけ直す」は今回は起きていない。**

## 検証

- `npm run verify:rust` **通った**（fmt / clippy / test）
- `cargo test` 全体 **green**。`search` は 92 passed（r5 から +2、`node_table` の新規分）
- `scripts/rustdoc-ratchet.sh` **11 / baseline 11**

## r7 へ

**r7 が0件なら打ち切る。** 焦点は r6 の計画に書いた5つで、
とくに **3（assert を足したテストが狙った門番で落ちるか）は上で実測済み**なので、
r7 では**新しく入れた `err!` マクロと `node_table.rs` の2本**が主対象になる。

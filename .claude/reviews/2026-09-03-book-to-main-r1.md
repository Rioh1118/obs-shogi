# レビュー book-to-main ラウンド1

- 日付: 2026-09-03
- 範囲: `feature/book` へ `main` を取り込んだマージ `b35f9c5f` で**手で書いた5ファイル**だけ。
  `src-tauri/src/book/` 本体と `main` から来たコードは範囲外
- 走らせた reviewer: architecture / comment / rust / robustness / oss-hygiene
- 対象コミット: `b35f9c5f`

手で書いた変更:

| ファイル                                       | 何をしたか                                                     |
| ---------------------------------------------- | -------------------------------------------------------------- |
| `docs/state-transitions/yaneuraou-db-parse.md` | ShogiHome のパス2本を参照リンクへ、`v1.29.0` に固定            |
| `docs/state-transitions/README.md`             | 表の衝突解決（両側の行を残す）＋「他リポジトリのパスの書き方」 |
| `src/__tests__/docsSourcePaths.ts`             | `tracked()` の doc コメントの訂正                              |
| `src-tauri/tests/state_table_terms.rs`         | `TABLES` / `NOT_RUST` / `NOT_IDENTIFIERS` への登録             |
| `src-tauri/tests/test_count_ratchet.rs`        | `EXPECTED_MIN` 190 → 259                                       |

所見 **17件**（BLOCK 0 / HIGH 3 / MEDIUM 14）。

---

## 所見

### R-01 (HIGH, comment / oss-hygiene / architecture): 新設した規約の理由が事実に反し、その規約を書いた表自身が破っている

`README.md:67-70` と `docsSourcePaths.ts:46-50`（同じ主張が2箇所）:

> ShogiHome も**やねうら王も** `src/` から始まるので、綴りだけでは自リポジトリと区別が付かない

**やねうら王のリポジトリに `src/` は無い。** 直下は `.github` / `docker` / `docs` / `script` / `source`。
`ROOTED = /^(src|src-tauri|docs)\//` は `source/` に一致しないので、
`yaneuraou-db-parse.md:13,17,21` の `` `source/book/book.cpp:709-716` `` などは
**そもそも追われず、囲んでも落ちない**（判定器を同条件で再現して確認）。

結果、規約は「他リポジトリのパスは外部リンク」と絶対形で書いてあるのに、
**規約が掛かる当の表が4箇所で破っている**ように見える。次に一次資料を1行足す人は、
`source/...` をリンクへ直しに来て**行番号という一次資料の要点を落とす**か、
「規約は守られていない」と判断して全体を無視するかのどちらかになる。

3人の reviewer が独立に同じ場所を指した。

- 結果: 対応済み `1c8e0cda`（述語を検査の条件へ置き換え、ShogiHome は当たる／
  やねうら王は当たらない、を分けて書いた。`docsSourcePaths.ts` のコメントも直し、
  **追うかどうかを決めているのが `LAYER` ではなく `ROOTED`** であることを名指した
  —— comment reviewer の「どの条件式か指していない」もここで解消）

### R-02 (HIGH, architecture): この規約を守らせる検査が、規約を破るコミットでは走らない

`.claude/hooks/verify-gate.sh:213-221` の `gate_kinds_for_path`:

```sh
*.ts|*.tsx|*.scss|*.rs|...) kinds="ts" ;;
...
docs/state-transitions/*.md) kinds="$kinds rust" ;;
```

`.md` は `ts` の腕に入らない。表だけを直したコミットで走るのは **`rust` だけ**。
ところが新設した規約を強制しているのは `docsSourcePaths.test.ts`（vitest ＝ `ts` 側）。

**表だけを直すコミットは、まさにこの規約を破りうる唯一の形。** そこで vitest が1本も走らない。
`yaneuraou-db-parse.md` に `` `src/background/book/yaneuraou.ts` `` と書き戻す docs-only コミットは
ゲートを緑で通る。今回の合流で落ちたのはマージが `.rs` を含んでいたからで、doc だけなら落ちなかった。

- 結果: 対応済み `d3fe1a4d`（`docs/state-transitions/*.md` を `ts rust` の両方へ。
  判定表 S4 の行と `verify-gate.test.sh` の期待値も揃えた）

### R-03 (HIGH, rust): `declared` をソースの全トークンから作るので、棋譜の語が偶然で通ったり落ちたりする

`state_table_terms.rs:126-128`:

```rust
let declared: BTreeSet<&str> = code
    .split(|c: char| !(c.is_ascii_alphanumeric() || c == '_'))
    .collect();
```

`without_comments` はコメントしか落とさないので、**文字列リテラルと `#[cfg(test)]` の中身が
そのまま `declared` に入る。** sources 6本から拾える大文字トークン87個の中に
`TORYO` / `CHUDAN` / `3334FU` / `2726FU` / `PI82HI22KA` / `ZZZZ` が混ざっている
（すべて `kifu_reader.rs:2910` の fixture 文字列由来）。

2つの向きで壊れている。

1. **今回 `NOT_IDENTIFIERS` に足した2件は「fixture に無かった綴り」でしかない。**
   `search.md` が `%TORYO` を書いていたら、fixture に居るので**緑で通り、
   読み手は「実装に `TORYO` という定数がある」と読める**
2. **本来の目的（消した定数が表に残っていないか）も文字列リテラルで抜けられる。**
   `index_cache.rs:804` の `assert!` の第2引数が ``"`VERSION` と ..."`` を持つので、
   `const VERSION` を消しても `search.md:242` の `` `VERSION` `` は緑のまま

- 結果: 対応済み `ccbc2852`（`const` / `static` の宣言行からだけ名前を取る
  `declared_constants` を新設。`search.md` に `%TORYO` と書くと実際に落ちること、
  既存の定数がどれも `missing` に転ばないことを確認済み ——
  計画の「次ラウンドの焦点2（R-03 の縮み）」への回答）

### R-04 (MEDIUM, rust): 表の登録漏れ検査が `ends_with` なので、新しい表が黙って登録済みになる

`state_table_terms.rs:159`:

```rust
let checked = TABLES.iter().any(|(path, _)| path.ends_with(&name));
```

`"docs/state-transitions/search.md".ends_with("arch.md")` は **true**。
`arch.md` を置くと `TABLES` にも `NOT_RUST` にも無いのに登録済みと判定される。
同じ形で `db-parse.md` / `key-failures.md` も通る。
**この検査は自身の doc で「これが無いと表を1つ足すだけで検査を抜けられる」と宣言している当のもの。**
短く一般的な `search.md` を `TABLES` に足したことで面が広がった。

- 結果: 対応済み `43a7aebe`（ファイル名で厳密に比較。判定を `is_registered` に切り出し、
  実在するファイルでは置けない形をテストで踏めるようにした。`ends_with` に戻すと落ちる）

### R-05 (MEDIUM, rust / architecture ×2): `search.md` の sources に、その表の主題のモジュールが1つも入っていない

登録した6本は `api.rs` / `index_builder.rs` / `index_cache.rs` / `kifu_reader.rs` /
`project_manager.rs` / `types.rs`。ところが `search.md` が状態と遷移として名指しているのは
`IndexStore` の `snap`、`start_restoring` / `install_restored` / `insert_many_file_segments`
（`index_store.rs:172-253`）、`query_service`（`query_service.rs:86`）、
`scan_kifu_files`（`fs_scan.rs:89`）、`FileTable::default()`（`file_table.rs:7`）。**どれも無い。**

いま緑なのは、表が引く大文字定数がたまたま `EVT_INDEX_WARN` と `VERSION` の2つだけだから。
選び方が「表が指す実装」ではなく「**いまの定数がどのファイルにあるか**」で決まっている。
`index_store.rs:18` の `COMPACT_THRESHOLD` を表に書いた瞬間、
検査は実在する定数を `missing` として**誤って落とし**、メッセージは
「消した定数なら表も直すこと」なので直す人を実在する行を消す方向へ誘導する。

- 結果: 対応済み `ba34f216`（`index_store.rs` / `query_service.rs` / `fs_scan.rs` /
  `file_table.rs` を足し、選び方を `TABLES` の doc に書いた。`COMPACT_THRESHOLD` を
  表に書いても通ることを確認済み。**sources は表ごとに独立**なので、足した4本の定数が
  他の表の検査を緩めることはない —— 計画の「次ラウンドの焦点3」への回答）

### R-06 (MEDIUM, rust): 空振り検査が `TABLES[0]` しか見ない

`the_check_actually_finds_constants` は `TABLES[0]`（`yaneuraou-db-parse.md`）だけを見る。
**`search.md` を足しても、その表の検査が空振りしていないことを見るものは増えていない。**
拾える定数は表ごとに 7 / 2 / 2 件。`search.md` の2箇所を日本語に書き換えれば
`constants_in` は0件を返し、その周回は**何を書いても通る no-op** になる。

- 結果: 対応済み `c3a137ce`（表ごとに「1件も拾えていないなら落ちる」を掛けた。
  下限は件数ではなく非空。件数で見る検査は綴りの規則の回帰として1つ残した。
  `search.md` の定数2箇所を日本語に書き換えると新しい検査だけが落ち、
  件数の検査は緑のままであることを確認済み）

### R-07 (MEDIUM, rust): `3334XX` は綴りの規則で落とすべきで、除外リストに足すものではない

Rust の識別子は数字で始まれない。`3334XX` は**どう転んでも定数ではありえない**。
`constants_in` に `word.starts_with(|c| c.is_ascii_uppercase() || c == '_')` を足せば
CSA の指し手（`7776FU` / `2726FU` …）ごと落ちる。
3つの表を全走査して、数字始まりは `3334XX` の1件だけ＝取りこぼしは無いことを確認済み。

- 結果: 対応済み `08f86399`（`constants_in` に「大文字か `_` で始まる」を足し、
  `3334XX` を `NOT_IDENTIFIERS` から外した。綴りの規則の境界を見るテストを新設。
  規則を外すと `the_spelling_rule_separates_constants_from_kifu` と
  `every_constant_named_in_a_table_exists_in_the_source` の2本が落ちることを確認済み）

### R-08 (MEDIUM, robustness / rust): `NOT_IDENTIFIERS` が全表に効くので、1つの表の都合が他の表の目を粗くする

`constants_in` は表を引数に取らないので、除外は `TABLES` 全体に一律で掛かる。
**理由は表ごとにローカルなのに、効き目はグローバル。**

具体的な破れ方: `search.md` に `BOM` と書く人が出る（文字コードの用語）→ 落ちる →
前例に倣って `NOT_IDENTIFIERS` へ足す → しかし `BOM` は
`src/book/yaneuraou_db.rs:357` の**本物の定数**でもあり、`yaneuraou-db-parse.md:107` は
既に BOM を扱っている → 以後 `const BOM` を消しても検査は緑。
同じ衝突候補が `HIRATE` / `VERSION` / `FORK` / `NODE` にもある。

### R-09 (MEDIUM, rust): 本数のラチェットが `src/` と `tests/` を合算する

内訳は `src/` = 244、`tests/` = 15。合算した1つの数しか見ていないので、
**`root_guard.rs` の9本を全部消しても、同じ変更で `src/` に9本足せば 259 のまま緑。**
このファイルの冒頭 doc が「同じコミットで別のテストを足していれば気づけない」を
*この検査が塞ぐ問題として*書いているのに、塞げていない。
`main` の取り込みで `src/` が 175 → 244 に増えたぶん、相殺は前より起こりやすい。

なお `EXPECTED_MIN = 259` の**値そのものは正しい**（base 30 + 160 + 69 = 259、
両親のどのファイルでも本数は減っていない）。上げ幅の問題ではない。

### R-10 (MEDIUM, comment / robustness ×2): `NOT_RUST` が表まるごとの免除なので、TS 側の表が名指す定数は誰も見ない

`("game.md", "棋譜のカーソルと分岐計画。TS 側")`。しかし `game.md:218` は
`` `buildPlayer(nextJkf, ROOT_CURSOR)` `` と書き、`ROOT_CURSOR` は
`src/entities/kifu/model/cursor.ts:187` の実在の定数。
**CLAUDE.md が「`KifuCursor` を作る口は2つだけ」と名指すほど重要な定数**が、
表に書かれているのに腐っても誰も気づかない。

`constants_in` / `declared` はソースを**ただの文字列として読んでいる**だけで、
Rust であることに依存していない。言語の壁は本物ではなく、テストの置き場が
`src-tauri/tests/` である事故に過ぎない。
`game.md:94,192` は Rust の `write_kifu_to_file` も名指しているので、
「Rust を指していない」という理由づけ自体も検証すると止まる。

### R-11 (MEDIUM, robustness): 「バッククォートを外せば通る」抜け道が機械で塞がれていない

`docsSourcePaths` が拾うのはバッククォートの中だけ。`expect(broken).toEqual([])` に
メッセージが無いので、**失敗しても「外して良いのは他リポジトリのパスに限る」がどこにも出ない。**

筋道: `src/book/yaneuraou_db.rs` を分割 → 表の綴りが落ちる →
失敗文言は死んだパスの名前しか言わない → バッククォートを外して緑 →
以後この表は存在しないファイルを指したまま、**機械が「守られている」と言い続ける**。

reviewer が現物14ファイルで試作した「裸のパス禁止」検査は検出0件だった
（フェンス・行内コード・外部 URL リンクを落とした後に残る `src|src-tauri|docs/….ext` を落とす）。

### R-12 (MEDIUM, oss-hygiene): 参照リンク `[…][ref]` を repo のリンク検査が一切見ていない

`stateTransitionIndex.ts:114` が拾うのは `[…](…)` だけ。
`[…][ref]` と `[ref]: url` はどちらも一致しない。

参照名を間違えるか定義を消すと、markdown は**角括弧つきの地の文としてそのまま描画する**。
エラーも赤も出ない。**今回の規約はこの2部構成を「標準の書き方」にした**ので、
今後この表に外部パスが増えるたび、機械が見ない結合が1本ずつ増える。
しかも定義は使用箇所から **170行下**にある。

- 結果: 対応済み `ca817ac8`（`brokenReferencesInBody` を新設し、定義の無い参照と
  使われていない定義の両方を返す。行内コードを落とすのは、命名パターン
  `ObsShogi-v[version]-[arch][setup][ext]` が `[…][…]` の形を踏むため —— これは
  `docs/RELEASE.md` で実際に当たった。参照名を1文字変えると docs 全体の検査が落ちることを確認済み）

### R-13 (MEDIUM, oss-hygiene): タグ固定を、行番号を持たない引用にだけ掛けている

固定した2本はどちらもファイル全体を指し、行番号を持たない。
固定されなかった側（`source/book/book.cpp:709-716` / `:118-119` / `:750-756` / `:823`）は
**全部が行番号つき**で、このファイルにやねうら王の版を書いた行は1つも無い。

README が名指した危険（「引いた行が別のものを指すようになる」）が最も高いのは
行番号つきの方で、そこに版が記録されていない。**判断の適用先が逆になっている。**

- 結果: 対応済み `3506724a`。**計画からは外れた。** 計画は「表の冒頭に版を1行に留め、
  行ごとには書かない」だったが、版を書き足すために v9.40 の現物と突き合わせたところ
  **5件とも行がずれていた**（`book.cpp:709-716` は実際には Apery 定跡の ponder 処理）。
  「行番号は v9.40 時点」とだけ書くと**嘘を固定する**ので、行番号を貼り直した。
  主張している振る舞いは5件とも v9.40 で確認済み。規約側にも
  「行番号を引いたら版を残す」を書いた

### R-14 (MEDIUM, comment / robustness ×2): 隣接する2ファイルが、規約の存否について逆のことを言っている

`docsSourcePaths.test.ts:12-16` は今回触っていないので
「他リポジトリのパスを外部リンクの形で書く**規約にすれば** `docs/` 全体へ広げられる」
（＝まだ無い）のまま。`docsSourcePaths.ts:49-50` は「README の規約」（＝ある）と書いた。

さらに件数「3件」が合わない。判定を `docs/` 全体へ掛けると**7ファイル**が落ち、
うち `decisions/` と `proposals/` は文中に挙がっていない。
質的にも違う: `docs/spec/` が落ちるのは他リポジトリではなく
**まだ存在しない自リポジトリの置き場**（`src/entities/game-session/` など）を予告として書くため。
新設した規約はこの2つ目の原因に一切効かない。

- 結果: 対応済み `58974a5e`（原因を2つに書き分け、件数を消した）

### R-15 (MEDIUM, oss-hygiene): 規約の適用範囲が書かれていない

節見出しは「他リポジトリのパスの書き方」という無限定、理由の文だけが
「これらの表が指すパスは」と表に限定している。どちらとも読める。
`docs/` の他の場所には守っていない綴りが 4ファイル・5綴り残っている
（`IDEAS.md` / `PREMISES.md` / `decisions/0002` / `proposals/naming-and-module-layout.md`）。

- 結果: 対応済み `4be33750`（見出しを「（この置き場の中だけ）」にし、
  広げるときの順 —— 先に検査の範囲、次に文面 —— を1行置いた）

### R-16 (MEDIUM, oss-hygiene): 「表の階層」の図が、同じファイルの在庫表とも `ls` とも合っていない

`ls` は14本。図が挙げるのは13本で **`inline-name-editor.md` が無い**（在庫表には有る）。
図の「判定」は3行なのに、直後の本文（`README.md:39`）は「『判定』の**2つ**は」と書いている。
この2行はどちらも `main` 側の README には無く、**このマージで `main` へ持ち込まれる新規の不整合**。

索引の検査（`stateTransitionIndex.test.ts:29`）は在庫表しか見ないので、図が何本欠けても緑。

- 結果: 対応済み `7fa2a876`（図に `inline-name-editor.md` を戻し、「判定の2つ」を
  数を書かない形へ。**在庫表だけを見ていた検査を図にも掛けた**（`insideFences`）。
  図から1本消すと落ちることを確認済み。計画が心配した `staleUncreatedInBody` の
  誤検出は起きない —— 足した行は「未作成」と書いていないため（焦点6への回答））

### R-17 (MEDIUM, rust / comment): `search.md` が `src-tauri/tests/` の現状と食い違う

`search.md:274-276`:

> `src-tauri/tests/` にあるのは `root_guard.rs`（パスの検証）だけで

`main` 側では正しかった。マージで `state_table_terms.rs` / `temp_dir_names.rs` /
`test_count_ratchet.rs` / `common/` が入り、いまは4本15テスト。
**このマージは `search.md` をその `state_table_terms.rs` へ登録した変更そのもの**なので、
登録した相手の中身が古いまま残っている。後半の「状態機械そのものを回すテストは無い」は今も正しい。

- 結果: 対応済み `27644896`（ファイル名も本数も書かない形へ）

---

## 追記した所見（最初の集約で落としたもの）

reviewer は挙げていたのに、上の R-01〜R-17 にまとめ損ねた3件。**落としたのは
集約した側の漏れ**なので、番号を継いで残す。R-22 は修正の最中に自分で踏んだもの。

### R-18 (MEDIUM, comment): リンク参照名 `sh-` から出典が読めない

`sh` は shogi / shell / ShogiHome のどれとも読める。**この表は ShogiHome と
やねうら王の両方を引いている**ので、次に本家へ張る人が `sh-book-cpp` と名付けると、
2つの別リポジトリが同じ接頭辞に同居する。`sh-no-header` はリポジトリ名も
対象（fixture である `.db`）も名前に残っていない。

- 結果: 対応済み `da3d3b65`（`<リポジトリ名>-<対象>` へ改名し、規約の節にも1行置いた）

### R-19 (MEDIUM, rust): 対応表の長さを手で書いている

`[T; N]` の `N`。数え違いはコンパイルが落とすので**黙って壊れはしない**が、
足すたびに2箇所を直させる（この合流だけで3つとも直した）。しかも `NOT_RUST` の doc が
「足し忘れは必ず起きる」と書いているせいで、長さが足し忘れを見張っているように読める。

- 結果: 対応済み `8b72b4a9`（スライスへ）

### R-20 (MEDIUM, rust): 登録の逆向きを誰も見ていない

登録漏れの検査は実在するファイル側からしか見ない。表やモジュールを消したあとに
残った登録の行は誰も落とさない。残った行は「その表は見られている」と読める。

- 結果: 対応済み `c95a9816`（`TABLES` の表とソース、`NOT_RUST` の表の実在を要求）

### R-21 (MEDIUM, comment): README の「画面の説明を持たない」と L2 の「局面検索の**画面**」

`README.md:6-7` が「この表は網羅を担い、画面の説明を持たない」と書く一方、
`:25` は `position-search-view.md` を「局面検索の**画面**」と説明している。
「画面でホバーと選択がずれる」を書きに来た人が `docs/spec/` へ送られうる。

- 結果: **見送り。** この2行はどちらも `main` 由来で、この合流が作った食い違いではない
  （`/implement` 手順7「範囲の外」）。加えて「画面の説明」は文脈上
  「画面でできることの列挙」と読むのが自然で、画面が持つ状態そのものを L2 に置くことと
  矛盾しない。**反論として残す。** 言い換えを入れるなら #290（docs が現物とずれている）で

### R-22 (MEDIUM, robustness): ゲートが、コミットするツリーとは別のツリーを検証した

修正の最中に実際に踏んだ。ワークツリー `wt-book-main` でのコミットが、
**このワークツリーに存在しないファイル**（`src-tauri/src/engine/registry.rs`。
`main` の作業ツリー側に別セッションが編集中のものがある）の `cargo fmt` 差分で
2回止まった。同じ内容のコミットが数分後には通ったので、**別セッションの
未整形の作業ツリーを見ていた**ことになる。

`verify-gate-decision.md` の**不変条件2「検証するツリーは、コミットされるツリーと同じ」**を
破っている。判定は `.cwd` を信じているので（`verify-gate.sh:281,307`）、
そこがワークツリーを指さない経路がある。

- 結果: **見送り（issue へ）。** ゲート自身の設計の話で、この合流の範囲を超える。
  再現の条件（どういうときに `.cwd` がずれるか）を特定できていない

## 重複・矛盾した所見

- **R-01 は3人が独立に指した**（comment / oss-hygiene / architecture）。
  直し方の提案が2つに割れている:
  (a) 規約の述語を実際の判定条件（`ROOTED` / `LAYER`）に合わせ、`source/` は囲んでよいと書く
  (b) やねうら王側も外部リンク＋タグ固定に揃えて、規約と現物を一致させる
  **(a) と (b) は排他ではない。** ただし (b) だけをやると「なぜ `source/` も？」の理由が
  R-13（行番号に版が無い）でしか説明できない。両方要る
- **R-05 は rust と architecture が同じ抜けを別の理由で指した。**
  architecture は「sources をディレクトリ単位にして手選びをやめる」まで提案しているが、
  rust は「表が名前を挙げたファイルだけ」という基準を doc に書く案。
  前者は R-03（`declared` の作り方）を直さないと誤検出が増える側へ倒れる
- **R-08 と R-10 は同じ根**（除外の粒度が「表ごと」でなく「全体」）。R-10 のほうが失うものが大きい
- architecture は R-04 / R-05 / R-10 をまとめて
  「この検査を `src/__tests__/` へ移して `docsSourcePaths` と同じ場所に置く」を提案している。
  R-02（gate の穴）も同時に閉じるが、**このマージの範囲を大きく超える**

## 見ていない範囲

- `src-tauri/src/book/` 本体、`main` から来たコード（範囲外の指示）
- `docs/state-transitions/search.md` / `game.md` / `position-search-view.md` の**内容の正しさ**
  （表が実装と合っているか）。見たのは登録の形と、そこから拾える定数の実在だけ
- ShogiHome v1.29.0 のリンク先が**表に書いた内容と合っているか**（URL が 200 を返すことのみ確認）
- `.claude/skills/state-transition-table/SKILL.md` が今回の規約と矛盾しないか
- `docs/spec/` 一式（`main` 由来）。ただし `docs/` 全体へ判定を掛けた結果は観測した

## lint / hook で強制できるもの

| 何を                                             | どこで                                                    |
| ------------------------------------------------ | --------------------------------------------------------- |
| 表だけのコミットで vitest を走らせる             | `verify-gate.sh` の `gate_kinds_for_path` に `ts`         |
| 数字始まりの語を定数と見なさない                 | `constants_in` の綴り規則（除外リストが伸びるのを止める） |
| 参照リンクの定義漏れ・未使用定義                 | `brokenLinksInBody` に正規表現2本                         |
| 外部リンクが `blob/(main\|master\|HEAD)/` でない | `stateTransitionIndex` に短い検査                         |
| 裸のパスの禁止（バッククォートを外して逃げる）   | `docsSourcePaths` に検査を1本                             |
| 表ごとの sources の選び忘れ                      | 表が名指す `snake_case` が sources に無ければ落とす       |
| 階層図に名前が出ていない表                       | `stateTransitionIndex.test.ts:29` を図にも広げる          |
| 領域ごとのテスト本数の床                         | `test_count_ratchet.rs` を `src` / `tests` に割る         |

機械で守れないもの: R-01 の (b)（既存の外部リンク化は作業であって検査ではない）、
R-14 / R-15（規約の文面の正確さ）、R-16 の「判定の2つ」（数を書かない書き方にするしかない）。

## 修正計画（r1 → r2）

### 束（同じ根から出ている所見）

- **綴りの規則**: R-07 → R-03 → R-08。R-07（数字始まりを規則で落とす）を先に入れると
  `3334XX` が除外リストから消える。R-03（`declared` を宣言の位置から作る）を入れると、
  R-08 の「除外リストに載るかどうかが fixture 次第」という**根が消える**（`MATTA` は
  規則でも `declared` でも通らなくなり、除外リストに載っている理由が明示的になる）。
  R-08 の残り（除外が全表にグローバルに効く）は別物になるので、R-03 の後に読み直す
- **規約の文面**: R-01 → R-13 → R-15 → R-14。R-01（理由が事実に反する）を直すと
  R-13（タグ固定の適用先が逆）と R-15（適用範囲が無い）は**同じ節の書き直しの一部**になるが、
  指摘箇所は消えない（別の段落）。R-14（隣接ファイルが逆のことを言う）は
  R-15 で範囲が決まらないと書けない
- **登録の形**: R-04 → R-06 → R-05。R-04（`ends_with` の部分一致）を直しても
  R-06 / R-05 の指摘箇所は残る。順序だけの束

### このラウンドで直すもの

| 順  | 所見                                  | なぜこの順か                                                                                     | この直し方で壊しうるもの                                                                                                                                                                   |
| --- | ------------------------------------- | ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | R-02 gate が `.md` に `ts` を掛けない | 機械で強制できるもの。これが入らない限り、以降の文面の直しは docs-only コミットで検証されない    | 表だけのコミットの検証時間が `rust` のみ（約2分15秒）から `ts`（約8秒）ぶん増える。`docs/state-transitions/` を触る全コミットが vitest を待つ                                              |
| 2   | R-07 数字始まりを規則で落とす         | 機械で強制。除外リストが伸びるのを止める。R-03 より先に入れないと `3334XX` の扱いが二重になる    | 大文字始まり以外の**本物の定数**があれば拾われなくなる。Rust の識別子は数字始まりになれないので実害は無いが、`_MAX` のような `_` 始まりを許す条件を落とすと拾えなくなる                    |
| 3   | R-04 `ends_with` → ファイル名の一致   | 機械で強制。面が広がったのは今回 `search.md` を足したから                                        | `TABLES` のパスの綴りが `docs/state-transitions/` 以外を指していた場合、いままで通っていたものが「未登録」で落ちる（現状3件はすべて同じディレクトリなので影響なし）                        |
| 4   | R-06 空振り検査を全表へ               | 機械で強制。`TABLES` を増やした今回の変更が直接の原因                                            | **`book-key-failures.md` と `search.md` は定数が2件しかない。** 閾値を `>= 5` のまま全表へ掛けると即座に落ちる。`!is_empty()` へ変えること                                                 |
| 5   | R-12 参照リンクの検査                 | 機械で強制。今回この書き方を「標準」にしたので、盲点も今回作った                                 | `[…][ref]` を**意図的に**地の文として書いている箇所があれば落ちる。`docs/state-transitions/` 全14ファイルを走査してから入れる                                                              |
| 6   | R-03 `declared` を宣言の位置から      | R-08 の根を消す。上の機械化を先に入れてから触る                                                  | **`declared` が縮むので、いま緑の定数が `missing` に転ぶ。** とくに `#[cfg(test)]` の中の `const`（`LATEST_RETIRED_VERSION` / `BOARD_ONLY_CSA`）を落とすと、それを名指す表があれば赤くなる |
| 7   | R-05 `search.md` の sources           | R-03 の後。`declared` の作り方を変えた後でないと、足したファイルが何を持ち込むか読めない         | `index_store.rs` / `query_service.rs` / `fs_scan.rs` / `file_table.rs` の `const` が `declared` に入る。`COMPACT_THRESHOLD` などが**別の表の**検査を緩める（R-08 の形）                    |
| 8   | R-01 規約の理由を判定に合わせる       | ここから文面。機械の側が固まってから書く                                                         | 「`source/` は囲んでよい」と書くと、やねうら王が将来 `src/` へ移ったときに規約が黙って誤りになる。**判定の出典を `ROOTED` / `LAYER` に一本化して逃げる**                                   |
| 9   | R-13 行番号の引用に版を残す           | R-01 で「なぜ `source/` も外部リンクか」の理由が要るので、その直後                               | やねうら王の版を1つ固定すると、表の他の行（版を書いていない実測値など）との整合が問われる。**表の冒頭に版を1行**に留め、行ごとには書かない                                                 |
| 10  | R-15 規約の適用範囲                   | R-01 / R-13 で節の中身が決まってから、その節の効力範囲を書く                                     | 「この表の中だけ」と書くと、`docs/` 全体へ広げる将来の判断が「規約を書き直す」作業になる。**広げる条件を同じ節に1行**書いて逃げる                                                          |
| 11  | R-14 隣接ファイルの doc               | R-15 で範囲が決まらないと書けない                                                                | 件数を消すと「なぜ絞っているか」が薄くなる。**原因を2種類（他リポジトリ／未来の置き場）として書き分ける**こと。件数は書かない                                                              |
| 12  | R-16 階層図の欠落と「2つ」            | 文面。独立                                                                                       | 図に1行足すと `stateTransitionIndex` の `staleUncreatedInBody`（フェンス内の裸の名前を拾う）が `inline-name-editor.md` を「未作成」と誤認しうる。**要確認**                                |
| 13  | R-17 `search.md` の腐った1文          | 文面。独立。マージが作った腐り                                                                   | 本数を書かない形に直すので、次に `src-tauri/tests/` が増えても腐らない。壊しうるものは無い                                                                                                 |
| 14  | R-18 参照名 `sh-`                     | **計画の後に足した。** R-12 で参照リンクを機械が見るようになったので、名前の付け方もここで決める | 改名は使用と定義の両方を同時に直す必要がある。片方だけだと `brokenReferencesInBody` が落ちる（＝機械が守る）                                                                               |
| 15  | R-19 配列長の手書き                   | **計画の後に足した。** この合流で3回直させられた摩擦                                             | `[T; N]` → `&[T]` で `for (a, b) in TABLES` の要素型が参照になる。`for &(a, b)` へ直さないと型が合わない（＝コンパイルが落とす）                                                           |
| 16  | R-20 登録の逆向き                     | **計画の後に足した。** R-04 で登録の判定に触ったので、逆向きの穴もここで塞ぐ                     | `TABLES` の sources に実在しないパスを書けなくなる。**いま緑**なので、足した4本（R-05）が全部実在することの裏取りにもなる                                                                  |

### 直さないもの

| 所見                                      | 行き先   | 理由                                                                                                                                         |
| ----------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| R-08 除外が全表にグローバルに効く         | **#386** | R-03 を直すと形が変わる。**変わった後の形を見てから**表ローカル化を設計する。いま直すと二度手間                                              |
| R-09 ラチェットが `src` と `tests` を合算 | **#387** | 値そのものは正しい（検算済み）。置き場ごとの床に割るのは `test_count_ratchet.rs` の設計変更で、このマージの範囲を超える                      |
| R-10 `NOT_RUST` の TS 定数を誰も見ない    | **#386** | TS 側に対の検査を作る話。**このマージが作った欠陥ではない**（`game.md` は `main` から来た）。範囲を広げると PR が読めなくなる                |
| R-11 裸のパスを禁止する検査               | **#386** | 新規の検査1本。R-02（gate）が入れば「規約を破ったコミットで検査が走る」までは担保される。検査そのものは次の PR                               |
| R-21 README の「画面の説明を持たない」    | 見送り   | どちらの行も `main` 由来。**反論を所見に書いた**                                                                                             |
| R-22 ゲートが別のツリーを検証した         | **#342** | close 済みだったものを、再現の記録を添えて再オープンした                                                                                     |
| R-01 の (b)（既存4箇所の外部リンク化）    | 見送り   | R-13 で「版を表の冒頭に残す」を採るので、行番号つき引用をリンクへ変える必要が消える。**リンクへ変えると行番号が URL に埋もれて読めなくなる** |

R-08 / R-10 / R-11 は #386 に1本でまとめた。3件とも同じ機構（doc と実装の綴りを
突き合わせる検査が2つに割れている）から出ていて、**別々に直すと二度手間になる**。

### 対象そのものを疑ったか

**17件のうち7件（R-03〜R-08、R-10）が `src-tauri/tests/state_table_terms.rs` の1ファイルに集まっている。**
残る4件（R-01 / R-11 / R-14 / R-15）は `docsSourcePaths` と規約の対。
**所見の 11/17 が「doc と実装の綴りを突き合わせる機構」に集中している。**

architecture reviewer は落とす案を出している ——
**`state_table_terms.rs` を `src/__tests__/` へ移し、`docsSourcePaths` と同じ場所に置く。**
手書きの対応表が1つになり、8秒の `npm run verify` で回り、R-02（gate の穴）と
R-10（TS の定数を誰も見ない）が同時に閉じる。`constants_in` / `declared` は
ソースを**ただの文字列として読んでいる**だけで Rust であることに依存していないので、
移せる。

**このラウンドでは採らない。** マージの範囲を大きく超え、`feature/book` が
9ラウンド掛けて作った検査を移設することになる。**issue に出して、次に所見が
この機構へ再び集まったら実行する**（`/review-plan` 手順4 の「3ラウンド続いたら」の観測点）。

### 次ラウンドの焦点

次の `/review-round` は、これを reviewer へ渡す。

1. **R-06 の閾値**: 全表へ広げるとき `>= 5` を残していないか。`book-key-failures.md` と
   `search.md` は定数2件しか無い
2. **R-03 の縮み**: `declared` を宣言の位置から作った結果、**いままで緑だった定数が
   `missing` に転んでいないか**。`#[cfg(test)]` の中の `const` の扱いを決めたか
3. **R-05 の持ち込み**: sources に4本足したことで、`COMPACT_THRESHOLD` などが
   **他の表の**検査を緩めていないか（R-08 と同じ形の穴が増えていないか）
4. **R-02 の代価**: 表だけのコミットで vitest が走るようになった。
   `gate` 自身のテスト（`verify-gate.test.sh`）が新しい対応を固定しているか
5. **R-01 の逃げ方**: 「判定の出典は `ROOTED` / `LAYER`」と書いたとき、
   その2つの綴りが実装から消えたら規約が指す先が消える。**綴りを doc に書く以上、
   `docsSourcePaths.ts` の定数名が腐る経路が増えていないか**
6. **R-16 の副作用**: 階層図に `inline-name-editor.md` を足したことで
   `staleUncreatedInBody` が誤検出していないか
7. **文面の整合**: R-01 / R-13 / R-14 / R-15 は同じ規約を4箇所（README の節、
   `docsSourcePaths.ts`、`docsSourcePaths.test.ts`、`yaneuraou-db-parse.md`）で
   書き直す。**書き直した後に4つが同じことを言っているか**

### 検証の見積り

13件 × `verify-gate.sh` の判定。R-02 を入れた後は `docs/state-transitions/*.md` が
`rust` と `ts` の両方を呼ぶので、文面の直し（順8〜13）も Rust の検証を待つ。

| 直すもの                                         | 走る検証  | 件数 |
| ------------------------------------------------ | --------- | ---- |
| `.sh`（順1）                                     | gate      | 1    |
| `.rs`（順2・3・4・6・7）                         | rust      | 5    |
| `.ts`（順5・11）                                 | ts        | 2    |
| `docs/state-transitions/*.md`（順8〜10・12・13） | rust + ts | 5    |

Rust の検証は**初回のフルビルド後は差分ビルドで済む**（このワークツリーは既に
`target/` が温まっている）。実測で調整し、**20分を超えるようなら順12・13 を r2 へ送る。**

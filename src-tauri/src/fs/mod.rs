//! ファイルを壊さずに扱うための原始操作。
//!
//! **ここは設定も root も知らない。** どこが作業場所かを決めるのは上のスライスで、
//! ここが持つのは「この名前は使えるか」「この書き込みは途中で壊れないか」だけ。
//! 知ると、設定を読む側とファイルを触る側が互いを使い合う形（環）になる。

pub mod error;
pub mod path;
pub mod write;

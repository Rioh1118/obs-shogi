//! Tauri コマンドの入口。
//!
//! **ここだけが `AppState` を受け取る。** 解析（`analysis`）も対局（`game`）も、
//! 自分のモジュールの中にコマンドを置かない。置くと、そのモジュールが
//! `AppState` を知り、`AppState` はそのモジュールを持つので**環になる**。
//!
//! ここがやるのは `AppState` から持ち物を取り出して渡すことだけ。
//! **判断を書かない**——書くと、同じ判断が下の層にもできる。

pub mod analysis;
pub mod game;

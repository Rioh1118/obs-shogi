//! アプリの設定ディレクトリに置く JSON。
//!
//! **どれも同じ形をしている** — `app_config_dir` の下にファイルを1つ置き、
//! 無ければ既定値、書くときは原子的に置き換える。別のスライスに分けていると、
//! 片方だけ直した違いが「どの設定がどう壊れるか」の差になって出る。
//!
//! **1つだけ既定への倒れ方が違う。** `updater` は壊れていても `Err` を返さず
//! 既定値を返す（理由はそちらのモジュール doc）。他の3つは `Err` を返し、
//! 画面が「設定を初期化」などの導線を出す。

pub mod app;
pub mod commands;
pub mod presets;
pub mod study;
pub mod updater;

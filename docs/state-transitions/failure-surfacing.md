# 失敗がどこへ出るか（横断表）

**どの失敗がどこへ出るかの台帳。** L0〜L2 のどの表からも参照される。
F 番号はここで採番し、各表と ADR から参照する。

分類そのものは **ADR-0004 で決着済み**（段は「復帰に何が要るか」で4つ）。
ここが持つのは分類ではなく、**個々の失敗の現在地**。

**推測でなく現物から。** 初回の棚卸しは 2026-08-29、`main` `bb29884` の実測。
以降は失敗を直すたびに該当行を書き換える。**直したのに行が古いままなら、
この表は台帳として嘘をつく。**

**行を足したときも既存の行を書き換えたときも**、その記述の末尾に
`（測定日 / 測定したブランチかコミット）` を添える。

## 0. 先に、いま出口がいくつあるか

利用者に届く経路は **8つ**。

|                  | 場所                                                         | 出るもの                                 | 消えるか                   |
| ---------------- | ------------------------------------------------------------ | ---------------------------------------- | -------------------------- |
| モーダル         | `features/kifu-read-error/ui/KifuReadErrorDialog.tsx`        | 棋譜が開けなかった                       | 閉じるまで残る             |
| モーダル         | `features/file-conflict/ui/FileConflictDialog.tsx`           | 同名ファイル。**リネームして続行できる** | 閉じるまで残る             |
| モーダル         | `widgets/file-tree/ui/FileTree.tsx` の `FileTreeErrorNotice` | ファイル操作の失敗（ツリーは残る）       | 閉じるまで残る             |
| その場           | 同上（ツリーが1本も無いとき）                                | ツリーを開けなかった                     | 直るまで残る               |
| フォーム内       | `create-file` の3フォームの `FsErrorView`                    | 作成・取り込みの失敗                     | 打ち直すまで               |
| 入力欄の直下     | `widgets/file-tree/ui/InlineNameEditor.tsx`                  | 名前の訂正で直る失敗                     | 打ち直すまで               |
| 境界の差し替え   | `shared/ui/AppErrorBoundary.tsx`                             | React が落ちた。**囲った範囲だけ**※      | 残る                       |
| 画面内インライン | 設定タブ・保存モーダル・検索モーダル                         | 各機能ごとに手書き                       | その画面を開いている間だけ |

ファイル系の4つは `entities/file-tree/ui/FsErrorView` を共有する（ADR-0005 決定5）。
`position-navigation` の「この手目を盤上で再現できない」はモーダル内の手書き。

※ 全画面ではない。`pages/AppLayout.tsx` で**モーダル層と棋譜ペインをそれぞれ囲っている**。
囲われていない場所で throw すると、いまも root ごと unmount する。

**トーストは無い**（`shared/ui/toast` は存在しない）。

## 1. `error` の置き場は6つあり、読み手はほとんど居ない

**これが最大の発見。** 分類の前に、そもそも出す場所が無い。

| スライス          | フィールド        | `set_error` 相当の発火元           | **UI の読み手**                     |
| ----------------- | ----------------- | ---------------------------------- | ----------------------------------- |
| `game`            | `state.error`     | **9箇所**（`provider.tsx` の各所） | **0**                               |
| `analysis`        | `state.error`     | 4箇所                              | **0**（issue #157）                 |
| `study-positions` | `state.error`     | `load_error` / `save_error`        | **0**                               |
| `search`          | `state.openError` | `open_error`                       | **0**                               |
| `file-tree`       | `state.error`     | 4種類の行き先※                     | 1（ツリーは消さない → F-3）         |
| `file-tree`       | `state.kifuError` | `kifu_error`                       | 1（`AppModalLayer` → モーダル）     |
| `engine`          | `state.error`     | `initialize_error`                 | 1（**設定タブを開いている間だけ**） |

※ `failWithNotice`（移動）/ `failToNameInput`（リネーム・フォルダ作成。
名前を直せば通る失敗＝`isNameInputError` は積まない）/ `pushError` の直呼び
（削除、衝突の解決の中断）/ `loadFileTree` の `reload_failed`。
`failToCaller`（作成・取り込み）は**一度も積まない**。
**呼び出し箇所の数は書かない。**（数えると、経路を1つ足したときに嘘になる）
→ `docs/state-transitions/file-tree.md` の ※2

さらに **`clearError` は6スライスすべてが context に公開しているが、呼び出し元は
`file-tree` の1つだけ**（#169 で `FileTree.tsx` が呼ぶようになった。残り5つは0）。

> **置き場だけが6つ作られ、表示する場所と消す導線がどちらも作られていない。**
> 分類が決まっただけでは埋まらない。1つずつ出口を作ることになる（ADR-0004 決定6）。

## 2. 個別の失敗（F 番号は各表から参照される）

見る軸は **次の操作** / **復帰導線** / **残るか**。段そのものは ADR-0004 が決める。

| #         | 失敗                                                     | 場所                                     | いま起きること                                                                                                      | 次の操作               | 復帰導線                                         | 残るか                  |
| --------- | -------------------------------------------------------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ---------------------- | ------------------------------------------------ | ----------------------- |
| **F-1**   | 設定の読み込み失敗                                       | `RequireRootDir`                         | **`/` に戻すだけ**。理由は出ない                                                                                    | 要る                   | 無い                                             | —                       |
| **F-2**   | 解析エラー／同期タイムアウト                             | `analysis` `set_error`                   | state に載るが**読み手0**                                                                                           | 要る                   | 無い                                             | ダメ                    |
| **F-3**   | ファイル操作の失敗（作成/リネーム/削除/移動）            | `file-tree` → `FileTreeErrorNotice`      | ツリーは残したままモーダルで知らせる                                                                                | 要る                   | 読み直し／ワークスペース選び直し                 | 閉じるまで              |
| **F-4**   | 解析／検索イベントの購読失敗                             | `analysis:153` `search:75`               | `console.error` のみ。**機能が丸ごと死ぬ**                                                                          | 要る                   | 再起動のみ                                       | ダメ                    |
| **F-5**   | エンジンプリセットの保存失敗                             | `engine-presets:59` → `EngineTab:64`     | **unhandled rejection**。画面に何も出ない                                                                           | 要る                   | 無い                                             | ダメ                    |
| **F-6**   | 解析の開始／停止が失敗                                   | `AnalysisPaneHeader:84`                  | `console.error` のみ。**押しても何も起きない**                                                                      | 要る                   | 再試行                                           | ダメ                    |
| **F-7**   | `stop_analysis` の失敗                                   | `analysis` `stopAnalysis` の `finally`   | フロントは停止、**Rust にセッションが残る**。以降の開始が全部弾かれる                                               | 要る                   | エンジン再起動                                   | ダメ（issue #120 の形） |
| **F-8**   | エンジン停止の失敗                                       | `engine:86` `shutdown().catch(() => {})` | フロントは未起動、**プロセスは残りうる**                                                                            | 要る                   | 無い                                             | ダメ                    |
| **F-9**   | エンジン初期化の失敗                                     | `engine` `initialize_error`              | 設定タブでのみ見える。**同じ設定では再トライしない**                                                                | 要る（設定の修正）     | `clearError` は呼ばれていない                    | 残るべき                |
| **F-10**  | 研究局面の削除失敗                                       | `PositionDetail:51`                      | `console.error` のみ。**消えたように見えて消えていない**                                                            | 要る                   | 再試行                                           | ダメ                    |
| **F-11**  | 研究局面の読み込み失敗                                   | `study-positions:103`                    | 静かに空リストになる                                                                                                | 要る                   | 無い                                             | ダメ                    |
| **F-12a** | 棋譜の**保存**失敗（`persistIfPossible`）                | `game` `set_error` ×1 ＋ **戻り値**      | `state.error` の読み手は依然0。ただし**コメントの保存と分岐の削除**は出る※5                                         | 要る                   | コメントは書き足せば再試行、削除は閉じて選び直す | それ以外はダメ          |
| **F-12b** | 棋譜の**操作**失敗（移動・分岐入替・分岐削除・盤面選択） | `game` `set_error` ×8                    | state に載るが**読み手0**。分岐メニューは**閉じるだけで盤が動かない**（2026-08-30 / `fix/225-fork-menu-main-line`） | 要る                   | 無い（同じ入力で同じ結果）                       | ダメ                    |
| **F-13**  | Finder で開けない                                        | `revealInFileManager:10`                 | `console.error` のみ                                                                                                | 要らない               | 不要                                             | **消えてよい**          |
| **F-14**  | リネーム名が不正                                         | `InlineNameEditor`                       | 入力欄の直下に出す。入力は残る                                                                                      | 要る（**入力の訂正**） | 入力を直す                                       | 打ち直すまで            |
| **F-15**  | SFEN のパース失敗（3件）                                 | `sfenConverter:19` ほか                  | `console.warn`。空表示 / `null`                                                                                     | 要らない               | —                                                | **消えてよい**          |
| **F-16**  | `cancelSearch` の失敗                                    | `search` の `cancelSearch`               | `console.error`。**意図的にベストエフォート**                                                                       | 要らない               | 不要                                             | **出してはいけない**    |
| **F-17**  | 検索プロジェクトを開く失敗                               | `search` `open_error`                    | state に載るが**読み手0**                                                                                           | 要る                   | 再試行                                           | ダメ                    |
| **F-18**  | エディタ内部エラー                                       | `LiveMarkdownNote:55`                    | `console.error` のみ                                                                                                | 不明                   | —                                                | 不明                    |

### すでに届いているもの（分類の参考）

| #   | 失敗                    | 出方                                       | 次の操作             | 残るか                     |
| --- | ----------------------- | ------------------------------------------ | -------------------- | -------------------------- |
| G-1 | 棋譜が開けない          | **モーダル**。閉じるだけで再試行導線は無い | 要らない（読めない） | 閉じるまで                 |
| G-2 | 同名ファイルの衝突      | **モーダル**。リネームして続行できる       | **要る（入力）**     | 閉じるまで                 |
| G-3 | React が落ちた          | **囲った範囲だけ差し替わる**（2箇所）      | リロード             | 残る                       |
| G-4 | 局面の保存／削除        | モーダル内インライン                       | 直して再送信         | モーダルを閉じるまで       |
| G-5 | 検索の起動              | モーダル内インライン                       | 閉じる／やり直す     | 同上                       |
| G-6 | エンジン初期化（= F-9） | 設定タブ内                                 | 設定を直す           | **タブを開いている間だけ** |
| G-7 | AI ライブラリのスキャン | タブ内                                     | 再スキャン           | 同上                       |
| G-8 | 更新の取得              | 専用画面。**再試行ボタンあり**             | 再試行               | 残る                       |
| G-9 | インデックスの警告      | 設定タブに件数と先頭5件                    | 要らない             | 蓄積される                 |

## 3. 段への割り当て

段は ADR-0004 が決めた4つ（`info` / `warning` / `danger` / `fatal`）。
どの F がどの段かは **ADR-0004 の「割り当て」節**が持つ。ここで二重に持たない
（2箇所に置くと、片方だけ直る）。

**例外は F-3。** ファイル操作の失敗は1つの段に収まらず、`code` ごとに
`warning` と `danger` に割れる。対応を持っているのは
`fsErrorTier`（`src/entities/file-tree/api/error.ts`）。ADR-0004 の表は
F-3 を1行で書いているので、そこだけでは `danger` の存在に気づけない。

ADR-0004 が使う `F-12a` / `F-12b` は、**採番元がここ**。段が分かれる
（保存は `warning`、操作は `danger`）ので §2 でも2行にしてある。

※5 F-12a のうち出口があるのは**2つ**。`entities/game` の書き換え操作
（`makeMove` / `swapBranches` / `deleteBranch` / `setCommentsByCursor` / `setCurrentComments`）が
`AsyncResult` を返すようになり、それを読んでいる2箇所が出す。

- **コメントの保存** — `KifuCommentNote` がノートの中に出す。書けていないので
  `baseText` を進めず、下書きは画面に残る。**閉じると失われる**。
  面をまたいで預かる形は → #314

  **ただし出るのは、ノート自身の閉じる操作（× / Escape / Cmd+Enter）を通ったときだけ。**
  分岐メニュー・手のメニュー・別の手のコメント・棋譜の差し替え・一覧ごとの unmount では、
  `KifuStreamList` が閉じる手続きを通らずにノートを外すので、そのあとに返った失敗は
  **本文もろとも無通知で消える**（5経路のうち塞がっているのは1つ）→ #315

- **分岐の削除** — `KifuStreamList` の `confirmDelete` が確認ダイアログの中に出す。
  **失敗したあとは実行ボタンを押せなくする**（押した時点の指定を撃ち直すので、
  失敗の間にメモリ側の棋譜が変わっていると別のものを指す）。閉じて選び直せば
  そのときの棋譜に対して指定が組み直される

**盤で1手指したときと分岐の入れ替えの保存の失敗は、依然としてどこにも出ない**
（`async-result-ignored` の印がある）。出口を作るのは #277。

## 4. まだ出口が無いもの

**抽出条件: §2 の「いま起きること」が `console.error` だけ / state に載るが読み手0 /
何も出ない、のいずれか。** 「復帰導線」の列ではない（そこに何か書いてあっても、
出口が無ければ利用者はその導線に辿り着けない）。

issue になっているもの:

| #   | issue | 内容                                             |
| --- | ----- | ------------------------------------------------ |
| F-5 | #170  | プリセット保存の失敗が黙ってデータを捨てる       |
| F-9 | #171  | エンジン初期化に失敗すると復帰できない           |
| F-7 | #172  | 解析の停止に失敗すると Rust にセッションが残る   |
| —   | #179  | 読み取れないサブフォルダがツリーから黙って消える |

まだ issue になっていないもの:

**F-1 / F-2 / F-4 / F-6 / F-8 / F-10 / F-11 / F-12b / F-17 / F-18**。

F-12a は**コメントの保存と分岐の削除**に出口がある（§2 の ※5）。
盤で1手指したときと分岐の入れ替えの保存の失敗は残る。
issue は #227 / #200 と、出口をまとめて作る #277。

F-8（エンジン停止の失敗＝プロセスが残りうる）は ADR-0004 で `fatal` に
割り当てられている。F-6 と F-8 は「復帰導線」の列に何か書いてあるので、
条件を列で引くと落ちる。

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code
# settings.json / agents / skills / hooks / rules / reviews は追跡する。
# 機械固有の設定とセッション作業用の計画メモだけ除外する。
.claude/settings.local.json
.claude/plans/
# セッション間の引き継ぎ指示書。実体はメインのチェックアウトに置き、
# worktree からは絶対パスで読む。腐るものなので履歴に残さない。
.claude/handoffs/

# Debug logs
.debug/

# 決定を詰めるあいだ、案をアプリの配色のまま見比べるための置き場。
# 決まった内容は ADR に書くので、ここは残さない。
.preview/

# セキュリティポリシー

## 脆弱性の報告

**公開の Issue で報告しないでください。**

GitHub の非公開報告機能を使ってください。

1. このリポジトリの [Security タブ](https://github.com/Rioh1118/obs-shogi/security/advisories/new) を開く
2. "Report a vulnerability" から報告する

報告には、可能な範囲で次を含めてください。

- 影響を受けるバージョンと OS
- 再現手順
- 想定される影響
- 発見に使った設定やファイル（棋譜ファイルなど、共有できるもの）

個人開発のため、応答は速くありません。**7日以内に一次返信**を目標にしていますが、
遅れる場合があります。急ぎの場合は報告の中でその旨を書いてください。

## 対象バージョン

現在サポートしているのは、[Releases](https://github.com/Rioh1118/obs-shogi/releases/latest) の最新版のみです。
古いバージョンへの修正の後方移植は行っていません。

## 脅威モデル

ObsShogi はローカルで動作するデスクトップアプリケーションであり、
サーバーを持たず、利用者のデータを外部に送信しません。
そのうえで、**設計上そうなっている**ことと**脆弱性**を区別するために、前提を明示します。

### 設計上そうなっていること（脆弱性ではありません）

- **任意の実行ファイルを起動します。** USI エンジンは利用者が自分でパスを指定します。
  指定された実行ファイルは利用者の権限で起動されます。これは将棋 GUI として必要な機能であり、
  ObsShogi は「利用者が指定した実行ファイルは利用者が信頼している」という前提で動作します
- **ローカルのファイルを読み書きします。** 棋譜ファイル、設定ファイル、定跡ファイルを
  利用者が指定した場所で扱います
- 設定ファイルにはエンジンの絶対パスが平文で保存されます

### 脆弱性として扱うもの

次に該当するものは報告してください。

- **信頼していないファイルを開くだけで**任意のコードが実行される、
  またはアプリの管理範囲外のファイルが読み書きされる
  （悪意のある棋譜ファイル、定跡ファイル、設定ファイルによるもの）
- エンジンが返した出力によって、アプリが意図しない動作をする
- 利用者が指定していないパスへの書き込み、または削除
- 利用者が指定していないネットワーク通信
- 更新機構（アップデータ）の検証を回避して、任意のバイナリを配布できる
- 設定ファイルやログに、利用者が意図しない情報が出力される

### 対象外

- 利用者が自分で悪意のあるエンジンを指定した場合の挙動
- 利用者が自分の権限で行える操作
- 依存ライブラリの脆弱性のうち、ObsShogi の使い方では到達しないもの
  （報告は歓迎しますが、優先度は低くなります）

## 依存関係

Dependabot による依存の更新監視を有効にしています。
セキュリティ更新は通常の変更より優先して取り込みます。
